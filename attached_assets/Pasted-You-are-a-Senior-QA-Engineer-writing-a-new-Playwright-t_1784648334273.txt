You are a Senior QA Engineer writing a new Playwright test 
file for the redesigned /discover page.

CONTEXT — confirmed live structure:
- 4 tabs in a single TabsList: 
  tab-travelpulse (By Location, default), 
  tab-packages (Ready Made Trips), 
  tab-events (By Date), 
  tab-services (Browse Services)
- Clicking a tab does NOT change the URL (client-side state only)
- URL CAN set the initial tab via ?tab=packages etc. (falls 
  back to travelpulse if invalid)
- All 4 tabs' data loads on page mount regardless of which 
  tab is active
- Tab 1 (travelpulse) content = just <CityGrid />, nothing else
- Tab 2 (packages) = template cards, testid card-template-${id}
- Tab 3 (events) = <GlobalCalendar />
- Tab 4 (services) = filter bar + service cards, 
  testid card-service-${id}, button-add-to-cart-${id}

WRITE a new Playwright spec file: discover-tabs.spec.ts

Cover these cases:

1. PAGE LOAD
   - /discover loads, hero band renders (text-page-title, 
     input-search, tab-travelpulse, tab-packages, tab-events, 
     tab-services all visible)
   - Default active tab is travelpulse (CityGrid visible)

2. TAB SWITCHING (client-side only)
   - Click tab-packages → template cards become visible, 
     CityGrid is hidden/unmounted from view
   - Click tab-events → GlobalCalendar becomes visible
   - Click tab-services → filter bar + service cards visible
   - After clicking each tab, assert the URL has NOT changed 
     (this is expected behavior, not a bug — write the 
     assertion to confirm it, not to fail on it)

3. DEEP LINKING VIA URL
   - Visit /discover?tab=packages directly → packages tab 
     is active on load
   - Visit /discover?tab=events directly → events tab is 
     active on load
   - Visit /discover?tab=invalidvalue → falls back to 
     travelpulse (default)

4. BROWSE SERVICES TAB — filters
   - Click tab-services
   - Test category quick-filter chips (button-quick-cat-all, 
     button-quick-cat-${slug})
   - Test filter bar: input-location, select-category, 
     input-min-price, input-max-price, select-rating, select-sort
   - Confirm filtering updates the visible card-service-${id} results

5. ADD TO CART FROM DISCOVER
   - On services tab, click button-add-to-cart-${service.id} 
     on a visible card
   - Confirm cart updates (check cart count/indicator, or 
     cart page reflects the item)

6. EXPERT HANDOFF BANNER (conditional)
   - Visit /discover?source=quick-start&showExperts=true
   - Confirm handoff banner + matched experts section render
   - Visit /discover WITHOUT those params → confirm banner 
     does NOT render

7. FEE-CONFIG REDIRECT (folding in ticket #818)
   - Visit /admin/fee-config directly
   - Confirm it redirects to /admin/fee-bands and does NOT 
     show the old deprecated form

Use data-testid selectors exclusively — do not use text 
matchers, since mobile view hides label text on some tabs.

After writing the file, run it:
npx playwright test discover-tabs.spec.ts

REPORT FORMAT:
- File created: yes/no, path
- Test run result: X passed / X failed (list any failures 
  with the exact assertion that failed)
- Any testid mismatches discovered while writing (i.e. if 
  a testid documented in the audit doesn't actually exist 
  in the DOM)