Triaged. **This is the render-fail-with-logic-green case you pre-classified — stale/un-seeded deploy. I touched no assertions.**

## What failed
All 4 DOM tests failed at the **same point**: `getByTestId('selection-controls-panel')` not found after navigating to `/experiences/wedding?destination=Kyoto` and clicking `tab-vendors`.

## Why it's deploy-currency, not test-plumbing or a code bug
The log shows the failure is at the **panel** assertion (line 84) — **`page.getByTestId('tab-vendors').click()` on line 83 succeeded** (it didn't error). So:
- The page **loaded**, the template rendered, and the **tab bar rendered and was clickable** → route is right, **no auth wall**, selectors for navigation are correct. **No plumbing fix is warranted** (and the logic gate + build gate are both green, so the code is sound).
- Only the **new `SelectionControlsPanel` is absent** on the deployed app.

That points squarely at **`BASE_URL` serving a pre-reconcile build with an un-seeded `control_config`** — which is exactly what you'd expect right now: **`main` is broken (the sort-block syntax error), so Replit is serving the last-good build from before the reconcile.** The panel code and its startup seed aren't live there yet. Tabs render (they predate the reconcile); the panel can't, because nothing on that deploy knows about `selectionControls`.

So the gate is behaving **correctly** — it refuses to go green against a stale deploy. This is the deploy-currency guarantee you asked for, enforcing itself.

## Path to green (no code/test change)
1. **Merge #23** → `main` boots again.
2. Replit redeploys current `main` → ships the reconcile UI **and** re-runs the startup seed (`backfillSelectionControls`) on the deployed DB.
3. The post-merge DOM run finds the panel and goes green — that run closes DOM verification on live `main`.

I changed nothing in the spec (softening would have hidden exactly this signal). Net state: **#23 build ✅ / logic ✅, DOM ❌-because-stale-deploy** — merging the hotfix is the unblock, in the order already recommended (#23 → #24).

I'm still subscribed; once #23 lands and Replit redeploys, I'll confirm the DOM gate flips green (or re-triage if it surfaces a real render/narrow/parity bug at that point — which I'd report, not soften).