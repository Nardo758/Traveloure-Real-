Dashboard = command center: row-selector + single full card, aligned to mockup v3 Stage B.
Branch claude/plancard-redesign (PR #80). Platform/brand tokens — NOT the mockup's palette/fonts.

STRUCTURE (the model):
- Saved-trips row = the selector/triage (Stage A's job). Tapping a trip in the row sets which
  trip the full card below renders. Confirm it's wired as a selector; if it only links away,
  wire it to drive the full-card selection.
- ONE full card (stage="full") below the row = the selected/active trip. DROP the
  stack-of-all-plans-as-full-cards from the prior change — one full control center, not N
  (kills the multiple-maps / N-query-fanout load).
- Default the shown trip to the soonest/active plan; the row switches it.

ALIGN TO MOCKUP v3 STAGE B — STRUCTURE & LAYOUT ONLY, on platform tokens:
Match the mockup's section presence, order, hierarchy, and spacing for the full card:
  gradient hero (status pill + title + dates + metric strip) → Concierge module (front-and-center)
  → Card/Map view toggle → Map Control Center → Transport + per-leg booking → Upsell slot (gated)
  → Escalation CTA → bottom bar.
- The hero STAYS the C-gradient hero from Phase 3 — that IS the mockup's dark header, just with
  the gradient/photo treatment. Do NOT revert it to a flat dark slab or import the mockup's navy.
- Fix the current deviations from the mockup's polish:
  · Concierge tagline truncation → shorten or wrap, no clip.
  · "SOON" pill off-palette purple → neutral on-dark (or brand) token, kill the purple.
  · any broken/two-tone CTA button → clean single button.
  · spacing/hierarchy to the mockup's rhythm.

GUARDRAILS:
- Platform/brand tokens ONLY. NO mockup navy/teal/gold, NO Bricolage/Hanken, NO inline hex, NO new
  fonts. "Align to mockup" means STRUCTURE, not aesthetic — its colors/fonts were illustrative.
- No affordance regression (plancard-surface-manifest.md). Gated items stay gated. MetricStrip
  stays the one shared component.
- Quick coordination check on PlanCard.tsx / dashboard.tsx contention before editing.

GATE: tsc clean; grep (no mockup palette/fonts/hex/new tokens); manifest affordances render; ONE
full card (not a stack); row drives selection. Commit, then STOP — visual, I re-eyeball the
rendered dashboard (one card + row, section alignment, the fixes) before merge.