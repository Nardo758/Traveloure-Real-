You are a Senior QA Engineer at Netflix.
Test ONLY navbar links and routes on Traveloure.

TASK: Verify every navbar link works correctly.

STEP 1 — List every navbar link:
Find client/src/components/layout/ or 
client/src/components/nav/
List every link, dropdown, and menu item
with its target URL.

STEP 2 — For each link, verify:
A) Does the route exist in App.tsx?
B) Does the page component exist?
C) Does it require auth? If yes, is 
   ProtectedRoute wrapping it?
D) Does it show correct page title?
E) Is URL correct after navigation?

STEP 3 — Test these specific scenarios:

Scenario 1 — Normal click:
Click each link as logged-in user.
Expected: Correct page loads, no 404.

Scenario 2 — Rapid clicking (10x fast):
Click same link 10 times rapidly.
Expected: No duplicate API calls,
no broken state, page loads once.

Scenario 3 — Double click:
Double click each navbar link.
Expected: Navigate once, not twice.

Scenario 4 — Right click → Open new tab:
Open every navbar link in new tab.
Expected: Page loads correctly in 
isolation without parent context.

Scenario 5 — Direct URL access:
Type each URL directly in browser bar.
Expected: Page loads, not redirect to 
404 or homepage unexpectedly.

Scenario 6 — Back/Forward browser buttons:
Navigate A→B→C then press back twice.
Expected: Returns to A correctly,
no blank page, no infinite loop.

Scenario 7 — Refresh on each page:
Land on each navbar page, press F5.
Expected: Page reloads correctly,
user stays logged in, data persists.

Scenario 8 — Access while logged out:
Log out, try every protected navbar link.
Expected: Redirect to login, then 
redirect back after login.

Scenario 9 — Role mismatch:
Log in as traveler, try accessing:
/expert/dashboard
/provider/dashboard
/admin/dashboard
Expected: Access denied or redirect,
NOT the actual dashboard.

Scenario 10 — Mobile navbar links:
At 375px width, open hamburger menu.
Click every link in mobile menu.
Expected: Menu closes, page navigates,
no overlap or broken layout.

REPORT FORMAT for each link:
Link name | Target URL | Scenarios 1-10 
PASS/FAIL | Root cause if FAIL | Fix applied