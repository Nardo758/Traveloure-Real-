RE: Code Pull Assessment — all 3 FAILs are static-read misreads; re-verify at RUNTIME, not in plan mode

Your three FAILs and the minor note are each contradicted at specific lines. Exit plan mode and re-verify in the running app:

Q6: You searched ServiceForm — the property editor lives on Workstation by design (property/room edits route there; the ServiceForm.tsx:1898-1940 redirect you found IS that design). See client/src/pages/provider/workstation.tsx:1972 (input-edit-property-min-stay) and :2040 (cancellation band options with enforced-window copy). Runtime check: open a property listing's editor on Workstation.
R3: admin.routes.ts:882–1030 is the ready-made trips rail, not the listing rail. The provider-services decision rail calls notifyListingDecision (dedupeKey guard, 23505 swallowed) from all four arms: admin.routes.ts:2837 (edit-approved), :2863 (approved), :2930 (edit-rejected), :2948 (rejected); helper at :2766. Runtime check: approve any submitted listing as admin, then open the provider's Inbox → Notifications tab.
Item 11: ServiceForm.tsx:2761 — the outer ternary {role === "provider" && formData.serviceOfferingTypeId && !offeringCategoryUnresolved ? null : (…)} renders the ENTIRE Category block (including the button-change-offering branch you cited at 2757-2800) as null for a resolved provider offering. That branch reaches the DOM only when the offering's category cannot resolve (the FP-1/A1 honest-failure case the dispatch preserves) or on non-offering paths. Runtime check: pick any normal offering in the wizard — no Category row, exactly one Change button.
Minor note: data-testid="card-listing-languages" exists — client/src/components/service-languages-card.tsx:180, on the Card wrapper.
tsc: run npx tsc --noEmit 2>&1 | grep -c 'error TS' outside plan mode — expected exactly 170 at 93128dc.
Your Q5 note (client toast doesn't distinguish 400 vs 409 wording) is accepted as a valid polish observation — noted, not a blocker.

Please re-run items Q6, R3, and 11 in the running app and report the runtime result for each.