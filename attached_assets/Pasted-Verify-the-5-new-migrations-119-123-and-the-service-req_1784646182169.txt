Verify the 5 new migrations (119-123) and the service_requests 
feature landed correctly after restart/redeploy.

STEP 1 — Confirm migrations ran clean:
Check the server boot logs for runMigrations() output.
Report: did all 5 migrations (119, 120, 121, 122, 123) run 
successfully with no errors? Show the relevant log lines.

STEP 2 — Verify each migration's actual effect in the DB:
1. Confirm provider_services table has a transport_provided column
2. Confirm itinerary_items table has a provider_service_id 
   column with a foreign key to provider_services
3. Confirm the affiliate-partner approval columns exist 
   (list their names)
4. Confirm fee_bands table has coordination_floor and 
   coordination_percent columns, and that they're seeded 
   with values (not null/empty)
5. Confirm service_requests table exists — show its full 
   schema (columns, types, constraints)

STEP 3 — Check the status CHECK constraint situation:
Query the DB directly to see if service_requests.status has 
a CHECK constraint at the DB level, or if it was skipped 
(per the known drizzle-kit push vs migration race). Report 
which one happened. Then confirm the zod schema validation 
on the API write path for service_requests still enforces 
valid status values regardless.

STEP 4 — UI verification:
1. Visit /admin/service-requests — does it render? Is it 
   now in the sidebar? Under which group?
2. Go to /admin/providers — does the Reject button work 
   (click it on a test provider, confirm status updates)?
3. Visit any discover-location page — does the "Request a 
   service" card appear and render correctly?

STEP 5 — Regression check:
Run the existing Playwright suite to confirm nothing else 
broke:
npx playwright test --list
Then run the admin and provider-related spec files specifically.

REPORT FORMAT:
- Migrations: PASS/FAIL (with log excerpt)
- DB schema checks 1-5: PASS/FAIL each
- CHECK constraint: DB-level present or absent + zod 
  validation confirmed working
- UI checks: PASS/FAIL for each of the 3 items
- Playwright regression: PASS/FAIL + test count