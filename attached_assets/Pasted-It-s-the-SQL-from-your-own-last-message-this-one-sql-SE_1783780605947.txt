It's the SQL from your own last message — this one:

```sql
SELECT COALESCE(delivery_method,'(NULL)') AS value, COUNT(*)
FROM provider_services GROUP BY 1 ORDER BY 2 DESC;
```

That returns every distinct `delivery_method` value in the live `provider_services` table with its row count. The row I need is `document` — that count is the one number that decides flatten-to-`pdf` vs add-`document`-to-the-enum. Send me the whole output, not just that row, because if there's any value in there we *didn't* anticipate (a stray `video-call`, a null cluster, some legacy string), the refusal guard would RAISE on it and I'd rather see it now than have 109 abort mid-apply.

The catch is running it **against prod**, not the local seeded DB — and you said you have no prod `DATABASE_URL` in this environment, which is the actual blocker. Your prod is Neon, so you have two ways to get the number:

The direct route is the **Neon console** — open your project at console.neon.tech, go to the SQL Editor, make sure you're pointed at the production branch/database, paste the query, run. That's the fastest and it sidesteps needing the connection string anywhere near your agent environment.

If you'd rather run it from a terminal, pull the pooled connection string from the Neon dashboard (Connection Details) and run it read-only:

```
psql "postgresql://<user>:<pass>@<host>.neon.tech/<db>?sslmode=require" \
  -c "SELECT COALESCE(delivery_method,'(NULL)') AS value, COUNT(*) FROM provider_services GROUP BY 1 ORDER BY 2 DESC;"
```

One caution since this touches prod credentials: it's a pure read, so it's safe to run, but don't paste the connection string into the agent session or anywhere it gets logged — run it yourself in the Neon console or a local shell. The number is all I need back.

While you're in there, if you want to save a round trip, run the two-table version from the 1d brief so we have the `service_templates` counts from prod too rather than from your local seed — though as you argued, those two template rows are seed-constructed and already decided, so it's optional:

```sql
SELECT 'provider_services' AS tbl, COALESCE(delivery_method,'(NULL)') AS value, COUNT(*) AS rows
  FROM provider_services GROUP BY 1,2
UNION ALL
SELECT 'service_templates', COALESCE(delivery_method,'(NULL)'), COUNT(*)
  FROM service_templates GROUP BY 1,2
ORDER BY 1,3 DESC;
```

Paste whatever comes back and I'll either green-light the full 109 or tell you to cut 109a and hold the `document` half.