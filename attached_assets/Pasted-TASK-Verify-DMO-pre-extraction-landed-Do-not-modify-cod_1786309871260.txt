TASK: Verify DMO pre-extraction landed. Do not modify code.

Step 0 (mandatory): pkill -f "tsx server/index", press Run, wait for curl -s localhost:5000/api/version to return JSON. Prove process start (ps -o lstart= -p $(pgrep -f "tsx server/index" | head -1)) is later than git log -1 --format=%cd.

1 — Boot sweep (wait ~90 seconds after startup). Grep the server log for dmo-extraction-warmup. Expect a sweep-complete summary line with real counts (scanned/extracted/emptied/failed). If the library has un-extracted guides, SELECT count(*) FROM dmo_extracted_places; should rise versus before the restart. Report the summary line verbatim and the before/after count. A skippedCap warning is fine — note it (the next boot continues the backlog).

2 — Reader is now instant. As an expert, open a guide in the Research Reader that you have never opened before (one the sweep just warmed). The places list must appear immediately — no multi-second extract spinner. Report the guide and the load feel.

3 — Approve-time extraction (the live end-to-end). As an admin, approve a guide-shaped intake item into the expert library (if none is pending, pick any guide-shaped dmo_raw_content row with expert_workspace_visible=false, flip it via the admin intake approve endpoint — not psql). The approve response must return fast (<1s). Within ~30s, the log should show the pre-extraction hook outcome for that id, and dmo_extracted_places should hold rows for it. Then open it in the Reader as an expert — instant places. Report the hook log line and row count.

4 — Regression. POST extract-places on an already-extracted guide → cached: true instantly with the same places as before. PATCH a ticketing URL on one place → 200, and it persists. Invalid (http://) URL → 400.

5 — Cost visibility. Check the AI cost tracking (admin cost view or ai_cost_tracking table): warmup/approve-triggered extractions should be attributable (trigger source in the metadata/label) versus on-demand ones. Report what you see.

Report PASS/FAIL per item with actual log lines and counts. Any API returning text/html = stale process — redo step 0 before calling anything a failure.