This is a critical, launch-blocking bug — fix immediately.

CONFIRMED POLICY DECISION:
- Traveler-initiated cancellation (within cancellation policy 
  window): FULL refund, including the platform fee. Traveler 
  gets back 100% of what they paid (service price + platform 
  fee), subject only to whatever cancellation-window percentage 
  policy applies (e.g. if outside a "free cancellation" window, 
  apply that policy's percentage to the FULL amount paid, fee 
  included — not just the service price).
- Provider-initiated cancellation: 100% refund including the 
  platform fee, regardless of any window/policy (provider's 
  fault, already agreed).

FIX REQUIRED:

1. Wire the actual Cancel action (both traveler-initiated and 
   provider-initiated) to the SAME refund logic that already 
   works correctly in the standalone refund endpoint. Cancelling 
   a paid booking must trigger a real Stripe refund automatically — 
   no more silent non-refunded cancellations.

2. Refund amount = full amount paid (service price + platform 
   fee) × applicable cancellation policy percentage. No portion 
   of the platform fee is kept in any traveler-cancellation scenario 
   within policy.

3. If cancelling outside the free-cancellation window, the UI 
   must clearly show the traveler what refund amount they'll 
   receive BEFORE they confirm cancellation — no surprises.

4. Confirm the traveler is NOTIFIED (email/in-app) when the 
   cancellation and refund complete — no silent state changes.

5. Confirm provider earnings/payout records are correctly reversed 
   when a booking is cancelled+refunded (no lingering earnings 
   for a refunded booking).

RE-TEST after the fix, with real Stripe evidence (not just DB 
status flags):
- Traveler cancels a paid booking within free-cancellation window 
  → confirm 100% refund (service + fee) actually occurs in Stripe 
  test mode, verified via Stripe's refund records
- Traveler cancels outside the window (if a partial-refund policy 
  applies) → confirm correct percentage applied to the FULL amount, 
  and that the UI showed this before confirmation
- Provider cancels a paid booking → confirm 100% refund including 
  fee
- Double-refund protection → re-confirm still holds after this 
  change (real concurrent test, not code review)
- Provider earnings correctly reversed post-refund

Do not mark this fixed until you show real Stripe refund records 
(refund IDs, amounts) alongside the corresponding DB state — 
same evidence standard as the payout reconciliation work earlier.

RESULT: one line — "cancellation now refunds correctly per policy, 
verified with real Stripe evidence" or list what's still broken.