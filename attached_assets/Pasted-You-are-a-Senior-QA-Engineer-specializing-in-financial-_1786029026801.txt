You are a Senior QA Engineer specializing in financial systems.
Test /admin/revenue, /admin/payouts, /admin/fee-bands, and any 
other money-related admin pages. This is the highest-risk area 
in the entire admin panel.

TEST 1 — Revenue dashboard accuracy:
1. Confirm displayed revenue numbers match actual data (spot-
   check: pick 3 confirmed bookings, manually calculate expected 
   platform fee + provider payout, compare to what's shown)
2. Confirm date-range filters work correctly (this week/month/
   custom range all show accurate, non-overlapping totals)
3. Confirm no double-counting if a booking was refunded/cancelled

TEST 2 — Payouts:
1. Confirm payout list shows correct provider, amount, status
2. If admin can manually trigger/approve a payout — test with 
   a real test transaction, confirm the amount matches earnings 
   records exactly
3. Confirm a payout cannot be approved twice (duplicate-payout 
   protection — same class of bug as the booking race condition 
   fixed earlier)
4. Test what happens if admin tries to pay out an amount that 
   doesn't match the ledger — should be blocked or flagged

TEST 3 — Fee bands editor:
1. Confirm current fee-bands data displays correctly 
   (coordination_floor, coordination_percent from the earlier 
   migration)
2. Edit a fee value, save, confirm it actually takes effect on 
   the NEXT booking calculation (not just saved to DB but unused)
3. Confirm editing fees doesn't retroactively change ALREADY 
   confirmed/paid bookings (historical records should be immutable)
4. Test invalid inputs (negative percentage, over 100%, non-
   numeric) — confirm validation blocks bad data

TEST 4 — Reconciliation page:
If /admin/reconciliation exists, confirm it accurately flags 
any mismatches between expected and actual payment records.

Report: Test | Expected | Actual | Pass/Fail | Financial impact if Fail

RESULT: one line — "X/Y PASS — safe/NOT safe. Any financial 
discrepancy found must be listed explicitly, not summarized."