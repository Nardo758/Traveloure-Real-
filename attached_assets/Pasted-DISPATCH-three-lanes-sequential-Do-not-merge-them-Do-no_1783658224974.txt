DISPATCH — three lanes, sequential. Do not merge them. Do not skip a gate.

═══════════════════════════════════════════════════════════
LANE 1 — global-calendar semantic fix   (branch: fix/global-calendar-grouped)
═══════════════════════════════════════════════════════════

Your eventsOnly filter is probably dead. You wrote
  eventsOnly: c.seasonalRating === "events-only"
but routes.ts:11029 comments  // null ratings go to "events-only"
and implements  eventsOnly: !c.seasonalRating.
Meanwhile content.routes.ts:5057 has  average: ... || !c.seasonalRating
already absorbing the nulls. If "events-only" is not a real column value,
eventsOnly matches zero rows and the tab renders EMPTY — no crash, no error.

STEP 1 — read-only. Settle the semantic with data, not reasoning.
  Identify the table backing seasonalRating (cite file:line of the query).
  Run:
    SELECT seasonal_rating, count(*) FROM <table> GROUP BY 1 ORDER BY 2 DESC;
  Paste the full result. Report whether 'events-only' appears at all,
  and how many rows are NULL.
  STOP. Report before editing.

STEP 2 — only after approval. Fix BOTH lines, not one:
    average:    c.seasonalRating === "average"
    eventsOnly: !c.seasonalRating
  Changing eventsOnly without changing average double-buckets every
  null-rated city. Say explicitly which lines you changed and why.

STEP 3 — verification. Curl is not sufficient.
    curl the endpoint, paste grouped.eventsOnly.length and grouped.average.length
  Then state plainly: you cannot screenshot an authenticated, below-the-fold
  view. Do not claim the tab renders. Hand that check to Leon.

DO NOT: delete the dead routes.ts copy. Touch router mounts. Widen scope.


═══════════════════════════════════════════════════════════
LANE 2 — duplicate route census   (branch: audit/duplicate-routes)
═══════════════════════════════════════════════════════════
READ-ONLY. No deletions. No mount changes. Report only.

Established: content.routes.ts:4935 serves /api/travelpulse/global-calendar
because app.use(contentRoutes) at routes.ts:461 registers ~10,400 lines
before the inline duplicate at routes.ts:10891. The DEAD copy held the
CORRECT implementation.

Established separately: trips.routes.ts is imported, never mounted, and
shadowed by inline handlers at routes.ts:476/484/505/555/583.

Both shadowing directions exist in the same file. Nobody knows the count.

Enumerate EVERY duplicate route registration under server/:
  method + path | site A file:line | site B file:line | which registers first
               | which Express serves | do the handlers diverge?

For each divergent pair, paste both handler bodies.
Flag any case where the DEAD copy looks more correct than the live one.

Also resolve, with citations, the three unverified orphans:
  experts.routes.ts, cross-sell.routes.ts, saved-items.routes.ts
  — imported at routes.ts:104/107/111, never mounted.
  Is each shadowed by an inline handler, or genuinely unreachable?
  You previously called these "dead code" without checking. Retract or confirm.

DELIVERABLE: the table. Nothing else. Deletion is a separate lane.


═══════════════════════════════════════════════════════════
LANE 3 — E2E_TEST_PASSWORD   (branch: fix/e2e-password-coalesce)
═══════════════════════════════════════════════════════════

E2E_TEST_PASSWORD is set to an EMPTY STRING. e2e-test-accounts.seed.ts:18
uses ?? which does not catch "". Seed and login currently agree only because
both fell through to the same literal. That is coincidence.

  - Change ?? to || , or unset the variable. State which and why.
  - Cite every other read of E2E_TEST_PASSWORD. Same bug?
  - Grep for ?? on any env var that could be "" . Report all hits.
  - Re-seed, then log in as test-traveler-kyoto@traveloure.test. Paste status.


═══════════════════════════════════════════════════════════
STANDING PROHIBITIONS — all lanes
═══════════════════════════════════════════════════════════
- A read-only probe uses GET, or a request you EXPECT to fail. Do not confirm
  a write vulnerability with a successful write. This has happened twice.
- Trip 7f82873e-49e4-4e60-9372-4ddf02fadc4f has title "probe-recheck" from a
  prior diagnostic PATCH. Restore it. Report the original value first.
- Do not restart workflows during a read-only phase.
- Do not report curl 200 or an above-the-fold screenshot as evidence a page
  renders.
- Do not explain an anomaly as "transient", "stale", "warm-up", or "a quirk".
  Produce a mechanism or say "cannot explain".
- STRIPE_SECRET_KEY must begin sk_test_. If it does not, stop the lane.
- Do not touch: server/routes.ts mounts, migrations, components/plancard/,
  fee_bands. No fee/commission/margin literals.