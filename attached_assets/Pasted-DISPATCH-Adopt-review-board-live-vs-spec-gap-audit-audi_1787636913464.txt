DISPATCH — Adopt review board: live-vs-spec gap audit

audited@4e1f8314 · lane: adopt-gap-audit · agent: Replit · read-only, no edits, no commits

Locked
Spec of record: docs/design/ADOPT_OPTIMIZATION_SPEC.md. Feature inventory: docs/design/ADOPT_OPTIMIZATION_FEATURES.md with its reading-rules header (instrument mode; §2 fonts/colours superseded; adopt ticks / tray / Finalize / "saved as its own trip" out of scope). Visual: docs/design/adopt-optimization-mock.html.
Instrument mode (2026-08-25-two-modes): earn tokens; Fraunces page title only; Inter body; Geist Mono for prices, times, medians, slip id; hairlines; no photos; semantic colour only; coral only on the paid Generate.
No /generate, no payment calls, no comparison creation during this audit.
Phase 0 — capture what's live

Restart Start application. On the root preview, using existing dev data:

Slip page /plans/<tripId> for a trip that has a comparison — capture the action row (Share · Preview Trip Card · Add all to checkout · Optimize this plan · Finalize plan).
Click Optimize this plan → capture the dialog (Auto state, then Hotel tile selected with its list). Cancel. Do not confirm.
Review page /itinerary-comparison/<id> for the 3-variant comparison (26933c18…), the 2-variant (0924f013…), and the 0-variant (c24e6aaf…) — full-page captures at 1280.
Save to /tmp/adopt-audit/. Report dimensions only.
Phase 1 — element-by-element comparison

Walk the feature inventory §3.2 → §3.11 and §4.1, in order. For each numbered element, one row:

| Inventory § | Element | Mock says | Live shows | Match? | Source (file:line) | Note |

Rules for the "Live shows" column: what actually renders, quoted (copy, font in use, colour token in use, testid present). Rules for "Match?": ✅ exact · ≈ present but styled/worded differently · ❌ absent · ⛔ present but ruled out of scope (must be absent). For every ≈ or ❌, the note names the smallest change that would close it, or says "DTO field missing: <name>" if the data isn't on the wire.

Specific things to check because they're the likely "still off":

Fonts: is the page title actually Fraunces, body actually Inter, numbers actually Geist Mono? Report the computed font-family from devtools for the h1, a card name, a price, and the slip line.
Colours: any remaining --accent-*, purple, or non---earn-* hex in itinerary-comparison.tsx, ProposalColumn.tsx, PlanCard proposal path — grep -nE "#[0-9A-Fa-f]{6}|--accent|--surface-2|--hairline" <files>.
Card anatomy §3.7: tag · name · tagline · anchor row · total + per-person · items grid 46px 1fr auto · day separators · pinned stops with —. Which are present.
Chips §3.7: money / drive-time / caution; baseline strip empty and aria-hidden.
Anchor row §3.8 colour by type.
Review header §3.5: slip line and exclusions — present, and honest about omitted fragments.
Legend §3.11 and footnote §3.10 (without the "saved as its own trip" clause).
Dialog §4.1: every listed element and testid; button-generate label updates with the chosen anchor; custom location submits name only.
Out-of-scope check: no + ticks, no adopt tray, no Finalize modal — confirm absent.
Testids §8: list which exist, which don't.
Phase 2 — report

The table, the five computed font-family readings, the grep output, the ⛔ confirmations, and a ranked gap list (biggest visual impact first) with the estimated diff size of each fix. HARD STOP. No edits.

What Not To Do
No edits, no commits, no attached_assets writes.
No confirming the dialog, no comparison creation, no payment calls.
No opinions beyond the table; the ranked list is the deliverable.

The ranked list becomes the next lane's phase list, one gap per commit.