4.2b — Spotlight (R33) · Trend lock (R34) · Gap pairing (R35), LANDED (7b539587)
Three self-unlocking blocks, compute + render + tests. All ship dark for Kyoto today and activate automatically when their data exists — exactly the readiness-tiered design the dispatch called for.

Two mapping findings that shaped the build (both surfaced honestly, not worked around):

R33 source: the dispatch's "R21 / trend-engine forward half / attention deviation" vocabulary doesn't exist in the repo. The only real dated-event source is travel_pulse_calendar_events; the trend engine stores aggregate metrics (its composite score is avoided). Its seed has zero Kyoto rows → spotlight dark, self-unlocking.
R35 kind mismatch: the only coverage read (resolveCoverageGaps) counts provider_services (service supply), not bookable properties. So "zero bookable properties" has no honest source for the property-led (stay) hero — pairing stay-demand with a service-coverage number is the §13 grain/kind error R35 itself guards against. So it's kind-gated: only a service-led hero pairs with a service gap; property-led → null. The property-coverage read is the real followup (now the fourth item waiting on neighborhood-grain data).
Subset invariant (R33): the spotlight aggregates the hero's own floor-cleared cells over the event range — a strict subset, never a second total (asserted in tests).

Gates: 26/26 tests (+9: spotlight floor/subset, trend 9→null/10→renders, gap kind-gating), tsc 164, demand-rollup guard (onepager module incl. service) + decision-guards lint green. R33/R34/R35 recorded (date-slug).

⚑ 4.2b.0 — the Replit checks (I can't run the DB from the sandbox — please paste-run)
The build already proceeded (self-unlocking), so these just confirm dark-today / tell you when things unlock:

If A returns rows, tell me — R33 would then render for Kyoto and I'd want it in the 4.4 draft.

Replit pull:

Next: resume 4.3 (admin control: generate-draft per qualifying market, R32 approve flag audit-logged under the §2 guard, monthly regen job) → 4.4 (real Kyoto draft → your HARD STOP; it'll be the full template with honest absences where Kyoto's data is dark). Proceeding to 4.3.