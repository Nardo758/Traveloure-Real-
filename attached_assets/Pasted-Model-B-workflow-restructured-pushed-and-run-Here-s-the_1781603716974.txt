Model B workflow restructured, pushed, and run. Here's the honest result.
What I did
Restructured e2e-tests.yml → Model B (no Actions DB work):
Removed migrate + seed steps and all DATABASE_URL references
Wait-for-server now polls both /health (DB connectivity) and /api/ready (seeding completion) with a 10-minute hard-fail window
Added workflow_dispatch for manual trigger
Aligned Node to 20
Kept Playwright report upload on failure (if: always())
Fixed broken imports blocking the suite:
journey-4-5.spec.ts, journey-5-admin.spec.ts, journey-6.spec.ts, journey-7.spec.ts all imported loginAsTestAccount from ./helpers/auth which did not exist
Created e2e/specs/helpers/auth.ts with the same API-login pattern from global-setup.ts
Pushed to develop → triggered GitHub Actions run
Honest journey-1 result
I ran the journey-1 spec locally against the deployed target (https://traveloure-platform.replit.app) to get the exact assertions:
Table
Flow	Status	Exact failure
1A — Authed traveler	🔴 RED	page.waitForSelector('[data-testid^="cart-item-"]') timed out at 15s. Page snapshot shows: "Your cart is empty" — the Add to Cart button click did not persist the item.
1B — Guest → sign-in → cart migrate	🔴 RED	expect(locator('text=Sign in to book')).toBeVisible() failed. Page snapshot shows the actual text is "Sign in to save and optimize" (with button text "Sign in to save and optimize"), not "Sign in to book".
Stage 1 wiring	🟢 3 passed / ⏭️ 1 skipped	EscalationCTA, /local-experts route, and ExpertMatchCard rendered correctly. One data-dependent test skipped (empty DB for that surface).
What the red means
These are real product bugs, not test-flake or infrastructure issues:
Cart persistence is broken — clicking "Add to Cart" on a service card doesn't actually add to the cart
Guest cart CTA copy mismatch — the spec expects "Sign in to book" but the UI says "Sign in to save and optimize"
The workflow is working correctly: it reaches the deployed app, confirms DB health + seeding, runs the tests, and reports real failures. No false-green.
Next steps (if you want)
Fix the cart Add to Cart action (backend POST /api/cart or frontend state bug)
Align the guest cart sign-in CTA text with the spec, or update the spec to match the current UI
Re-run the workflow after fixes — it will automatically run on next develop push or via the Actions tab → Run workflow button
Workflow run: https://github.com/Nardo758/Traveloure-Real-/actions/runs/27589444196