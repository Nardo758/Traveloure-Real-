Let me first try running the script in this environment to see if a usable DB is wired:

Can't run the script from this environment — no staging DB credentials wired here. Surfacing that clearly and proceeding with 1.4 (the script is independent of 1.4's code).

Can't run the staging script from here — Claude environment has no `DATABASE_URL`. **You/Replit need to run it manually before merging** with the one-liner I committed. Proceeding to 1.4.

Let me check the existing API patterns for the new endpoint:

Adding the public `GET /api/fee-bands/:bandKey` endpoint right next to the existing booking-fee-config one:

Now wire optimize.tsx to read the fee. Let me look at the broader structure to understand how to integrate cleanly:

Now I understand. Let me rewrite `pricingTiers` to fetch the AI+Expert-Review price dynamically:

Now update the rendering to use the dynamic price:

Now update the price-display to show "from" for the full-service tier:

Type-check + grep gate:

Clean. Those other "price:" literals are mock activity prices (sample data, not fees). Committing Phase 1.4:

**Phase 1.4 shipped (`f4b35d2`).** Phase 1 is complete on the branch.

---

## Phase 1 — Done. Status & open items.

### Shipped this sprint (6 commits)

| Commit | Phase | Content |
|---|---|---|
| `98b3b42` | 0 | Audit (read-only gap report) |
| `a024048` | 1.1 | Scaffold migrations + Drizzle types — purely additive |
| `bab5ed6` | 1.2 | Recreated seeds after a parallel session overwrote them; activity_provider correction folded in |
| `f5183a2` | 1.3 | Resolver flip: `commission.ts` + `pricing.service.ts` read `fee_bands`; 16/16 pure unit tests pass with all-zero neutrality diffs; staging script at `server/scripts/phase1-neutrality-check.ts` |
| `f4b35d2` | 1.4 | `optimize.tsx` $49.99 → `fee_bands.optimize_expert_review` via new public `GET /api/fee-bands/:bandKey` endpoint |

### What's wired now

- `fee_bands` is the single source for rate lookups (`commission.ts`, `pricing.service.ts`, `optimize.tsx`).
- `platform_settings.active_provider_commission_policy = 'beta_flat'` → flipping to `'tiered'` activates the seeded-dormant tiered bands without code change.
- `service_categories.category_key` is the brief's join key; 15 UPSERTs + 9 INSERTs = 24 brief categoryKeys; 14 legacy rows untouched.
- `template_category_matrix` seeded with REQ/REC/OPT for the 6 explicit templates + programmatic `custom` row.

### Mandatory before any production flip — you/staging team must run

```bash
# On staging (with DATABASE_URL pointing at staging DB):
npx tsx server/scripts/phase1-neutrality-check.ts
```

Expected output: every booking_fee_configs row prints `old % → new % (diff = 0.0000) ✓ zero`. Skip list (deposit + transport rows) prints `(skipped) ⊘ N/A`. Exit code 0 = ship-safe.

If any row shows a non-zero diff, **stop and surface it** — that means a category in production has been admin-edited away from defaults and the band lookup returns a different rate.

### Outstanding items (deferred, not blocking 1.4 completion)

1. **Phase 5 source-spec uploads** — the four specs (`UPSELL_ENGINE_…`, `NEIGHBORHOOD_SPINE_…`, `WAYS_TO_EARN_…`, `EXPERT_OFFERING_CATALOG`) need to land in `/docs/specs/` before Phase 5 work starts.
2. **Phase 2 (offering types)** — executable now; greenfield, no missing specs. Awaiting your go.
3. **Phase 3 (neighborhood spine)** — executable now; extends `city_neighborhoods` + 3 join tables (per your earlier direction).
4. **Phase 7 `/earn`** — depends on Phase 2.
5. **Phase 8 admin CRUD for `fee_bands` / `platform_settings`** — would replace the per-category surfacing in `/admin/fee-config.tsx`. Not blocking launch.

### Items I want to flag

1. **`booking_fee_configs` is now dormant** as a resolver source but still exists in the DB. The admin UI (`/admin/fee-config`) still writes to it. So **admin edits via that page won't take effect** until Phase 8 swaps the admin UI to `fee_bands`. If admins are likely to want to edit rates between now and Phase 8, this is a gap. Two mitigations:
   - Short-term: add a banner to `/admin/fee-config` saying "Editing here is currently a no-op — use the DB directly until Phase 8"
   - Or: bring `fee_bands` admin UI forward to Phase 1.5 / Phase 2 prerequisite
2. **`optimize.tsx` AI-only $19.99 and Full-service $199** are NOT data-driven. They're documented as display estimates (the real AI-only fee comes from `optimization_fees`; Full-service is an expert-set "starting from" price). Worth confirming this matches your product intent before launch UX is signed off.

Awaiting direction on:
- Run the staging neutrality script and confirm zero diffs? (You/Replit)
- Phase 2 go signal?
- Or address the admin-UI gap (#1 above) first?