DISPATCH — Adopt the Optimization, continue (Phase 3 → Phase 4)

audited@<current main> · lane: adopt-optimization-client (continuing) · agent: Replit · commit only on "commit"

Step 0 — land the reference file

Copy the attached adoptoptimizationmock.md to docs/design/ADOPT_OPTIMIZATION_FEATURES.md verbatim, then add this header block at the top:

Reading rules (binding). This file is the feature inventory of the mock. It is not a styling authority. Where it conflicts with ADOPT_OPTIMIZATION_SPEC.md or a ledger ruling, the spec/ruling wins:

§2 Design system → superseded by 2026-08-25-marketplace-earn-grammar. Fonts: Fraunces / Inter / Geist Mono. Tokens: --earn-*. Semantic mapping keeps its meaning with earn colours: save/positive → --earn-green-ink + wash; caution/costs-more → --earn-gold-ink + wash; recommended/brand → --earn-teal-ink + wash. No purple, no --accent-*.
§3.9 adopt tray + + ticks, §3.10 "saved as its own trip", §4.2 Finalize modal, §3.12 "keep-original + saved-as-new" → out of scope pending rulings R-A / R-B / R-C (SPEC §4). Do not build; do not render the copy.
§4.1 custom location "geocoded … used as you type" → client never geocodes; submits {type, name}; server resolves or omits (SPEC §2.3.5).
§5 "Candidate data is illustrative" → candidates come only from GET /api/trips/:id/anchor-candidates (pre-comparison) or …/itinerary-comparisons/:id/anchor-candidates (rerun).
§8 testids are targets for new elements; existing production testids are never renamed.

One commit: docs(design): adopt-optimization feature inventory + reading rules. Also update SESSION_MOCKS_INDEX.md row. Push on "commit".

Locked (carry forward)

Everything in the previous dispatch + SPEC §0. Server work already approved and landed: create-handler pin threading, shared resolver, trip-scoped candidate read. No further server/ edits this lane.

Phase 3 — "Build around a location" dialog (resume)

Per SPEC §2.3 and features §4.1 with the reading rules applied. Testids from §8: optimize-scrim, optimize-modal, opt-auto, opt-hotel, opt-neighborhood, opt-activity, anchor-list, as-candidate-N, anchor-custom, button-generate. Dialog sits in front of both runComparison entry paths; choice held across the Stripe sheet; one createComparison call with pinnedAnchor. Dismiss: ✕ / Cancel / backdrop / Escape; role="dialog" aria-modal; prefers-reduced-motion respected.

Proof: Auto run and one pinned-hotel run on real dev data; network log shows one create each, zero /generate, zero payment calls beyond the existing gate; review page shows the pinned hotel on all three anchor lines; empty type shows "No {type}s scored near your stops yet." Captures to /tmp/adopt/phase3-*. HARD STOP.

Phase 4 — review board parity (features §3.5–3.8, §3.11)

Bring the existing review page to the inventory, earn-tokened:

Review header (§3.5): mono slip line Slip {id} · {n} proposals for your remaining {m} items; exclusions right — {k} purchased items pinned in all, {j} item excluded (with {expert}). Each fragment omitted when its count is 0 or the source field is absent. Report which fields exist on the comparison DTO; anything missing is a FOLLOWUP, not a fabrication.
Context strip (§3.6): already shipped; confirm testids slip-optimize-preview-context, preview-trending-now, preview-seasonal match; keep the "From AI destination intel" provenance line.
Chip strip (§3.7): confirm proposal-preview-money / -drivetime semantics; add .chip.caution gold treatment for "Costs $N more"; baseline column renders an empty aria-hidden strip.
Card anatomy (§3.7): tag (Recommended teal / Your current plan muted outline), display name, tagline, anchor row, total mono + per-person, items grid 46px 1fr auto, day separators mono uppercase, pinned/purchased stops with pin glyph and — price, newly-introduced stops marked (.new teal) without the + tick (R-A pending).
Anchor row colour (§3.8): hotel → gold, neighbourhood → teal, activity → green; on the Phase 2 line, no new logic.
Honesty legend (§3.11): six cells, earn-tokened dots, copy verbatim.
Footnote (§3.10): verbatim minus "every version saved as its own trip" — use "your original is preserved; nothing is purchased by applying."
Column testids proposal-column-baseline, proposal-column-v1..v3 added alongside existing card-variant-*.

Proof: 3-, 2-, 0-variant comparisons re-captured; all existing comparison tests green; tsc 0 new; testid delta reported by name. HARD STOP.

What Not To Do
No §2 fonts/colours from the inventory; no purple.
No adopt tray, no + ticks, no drag, no Finalize modal, no "saved as its own trip" or "hands them a copy" copy.
No client geocoding; no fabricated candidates.
No server/ edits; no /generate or payment calls in proof.
No renaming of existing testids.
Close-out

Ledger: 2026-08-25-optimizer-pin-on-create (create threading + trip-scoped read), 2026-08-25-adopt-review-board-parity (Phase 4). FOLLOWUPS: SPEC §1 rail correction; R-A/B/C awaiting ruling; any DTO fields missing for §3.5.