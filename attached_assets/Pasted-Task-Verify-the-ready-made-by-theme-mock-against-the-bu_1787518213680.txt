Task: Verify the ready-made-by-theme mock against the build.

Context. docs/design/ready-made-by-theme-mock.html is the ratified design for the Ready-Made shelf, reorganized so the theme/experience is the organizing idea (was "Trips by Locals" / "Trips by Trip Planners" — author type now demotes to a card badge). Ledgers: 2026-08-22-ready-made-themes and 2026-08-22-expert-minted-themes. Shipped surface: Discover Packages tab (client/src/pages/discover.tsx), server GET /api/ready-made (server/routes/ready-made.routes.ts), shared label helper shared/ready-made-plan-types.ts. Confirm the mock renders and the live shelf matches; report divergence. Do not write feature code.

A. Render the mock. Open docs/design/ready-made-by-theme-mock.html; confirm light + dark. Confirm it shows: a theme chip rail ("All experiences" + per-theme chips), per-theme shelves, cards with the author as a badge/eyebrow (not a section header), and the note that chips render only for themes with live stock (real counts, never the full 20-theme vocabulary). Screenshot both themes.

B. Verify the live surface (shipped on main):

Shelf layout — go to Discover → Packages tab. Confirm it renders a theme chip rail + per-theme shelves, NOT the old "Trips by Locals / Trips by Trip Planners" author-type sections. Author type appears as a card badge.
Real stock only (§13) — confirm chips/shelves appear only for themes that have at least one live (approved+active) listing, with real counts — no empty aisles, and never the whole vocabulary as decoration.
Server filter — GET /api/ready-made?planType=<key>: a valid READY_MADE_PLAN_TYPE_KEYS key narrows the feed; an unknown key returns 400 (never a silently-empty shelf). The approved+active read-gate holds (filters only narrow, never widen). ?authorId= still works.
Expert-minted / custom themes — a listing authored with a custom label gets its own chip (Sparkles mark) and shelf; GET /api/ready-made?customLabel=<label> matches the stored author label server-side; the label renders as the card eyebrow via the shared planTypeDisplay, identical on shelf and detail page. Confirm free text never enters the planType filter key (validated vocabulary stays closed).
Card author link — the author row is a real un-nested storefront anchor; an author with no handle → plain text, no broken link.
C. Report the delta. List anything the mock shows that the live shelf doesn't (or vice-versa) — chip rail behavior, shelf grouping, badge placement, empty/loading states, custom-theme rendering — naming the file each fix attaches to.

Output: mock renders (Y/N + both-theme screenshots); B.1–B.5 pass/fail with observed values (a theme with its real count, a 400 on a bad key, one custom-theme chip); the C delta list.

After this, the remaining unverified session mocks are storefront-discovery and the concierge-revision pair. Want me to pre-check the ready-made mock-vs-code delta on my side now, or hold for the agent's report?