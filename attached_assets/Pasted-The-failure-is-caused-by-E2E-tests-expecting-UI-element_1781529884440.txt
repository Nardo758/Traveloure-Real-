The failure is caused by E2E tests expecting UI elements/routes that are not present or not stable in the deployed app for commit aceee9fd88977dffcbd0bd064e463a480a9b7114.

Primary fixes
1. Fix /local-experts page to render the expected switcher
The clearest hard failure is in e2e/specs/journey-1.spec.ts:226-230:

Test: /local-experts route renders without 404
Failing selector: [data-testid="role-switcher"]
File: https://github.com/Nardo758/Traveloure-Real-/blob/aceee9fd88977dffcbd0bd064e463a480a9b7114/e2e/specs/journey-1.spec.ts
The test assumes:

ts
await page.goto('/local-experts');
await expect(page).not.toHaveURL(/404/);
await expect(page.locator('[data-testid="role-switcher"]')).toBeVisible({ timeout: 10_000 });
So the app must ensure that /local-experts renders a visible element with:

TSX
data-testid="role-switcher"
If the route exists but the component was renamed/removed, restore compatibility by adding the test id back:

TSX
<RoleSwitcher data-testid="role-switcher" />
Or:

TSX
<div data-testid="role-switcher">
  <RoleSwitcher />
</div>
If the route itself was changed, add an alias so /local-experts still resolves instead of redirecting to a page that lacks that control.

2. Restore the landing page title
journey-2.spec.ts:36-38 fails immediately on:

ts
await page.goto('/');
await expect(page).toHaveTitle(/traveloure/i);
File: https://github.com/Nardo758/Traveloure-Real-/blob/aceee9fd88977dffcbd0bd064e463a480a9b7114/e2e/specs/journey-2.spec.ts

The same title expectation exists in journey-1.spec.ts:84-85.

This means the deployed homepage title likely no longer contains “Traveloure”. Fix the document title on the landing page, e.g.:

TSX
useEffect(() => {
  document.title = 'Traveloure';
}, []);
Or update the page metadata so the final rendered title still matches /traveloure/i.

3. Make expert/discover wiring match the test contracts
The failing tests indicate missing Stage 1/Stage 3 UI contracts:

journey-1.spec.ts:220-223 expects SmartServiceRecommendations on /discover
journey-2.spec.ts:62-65 expects expert cards on /discover?showExperts=true&destination=Tokyo
journey-2.spec.ts:72-86 expects the expert tab to show either:
[data-testid="advisor-card"], or
[data-testid="button-find-expert"], or
[data-testid^="button-request-expert-"]
Files:

https://github.com/Nardo758/Traveloure-Real-/blob/aceee9fd88977dffcbd0bd064e463a480a9b7114/e2e/specs/journey-1.spec.ts
https://github.com/Nardo758/Traveloure-Real-/blob/aceee9fd88977dffcbd0bd064e463a480a9b7114/e2e/specs/journey-2.spec.ts
You should make sure the rendered components preserve these data-testids:

TSX
data-testid={`service-rec-${id}`}
data-testid={`card-expert-match-${id}`}
data-testid="tab-expert"
data-testid="advisor-card"
data-testid="button-find-expert"
data-testid={`button-request-expert-${id}`}
data-testid="plancard-escalation-cta"
If the UI was refactored, reintroducing these attributes is the lowest-risk fix.

Why this job failed
The workflow itself is fine; it simply runs Playwright against the deployed site:

Workflow: https://github.com/Nardo758/Traveloure-Real-/blob/aceee9fd88977dffcbd0bd064e463a480a9b7114/.github/workflows/e2e-tests.yml
The failing code is in:

e2e/specs/journey-1.spec.ts
e2e/specs/journey-2.spec.ts
The failures point to app/test contract drift, not CI configuration:

missing role-switcher on /local-experts
homepage title not matching Traveloure
expert/discover components not rendering expected test ids or not rendering at all
Recommended code-level solution
If your recent changes renamed components/selectors, add compatibility attributes rather than rewriting tests first.

Example pattern:

TSX
<button data-testid="button-find-expert">Find Expert</button>
TSX
<div data-testid="advisor-card">
  <AdvisorCard advisor={advisor} />
</div>
TSX
<div data-testid="role-switcher">
  <RoleSwitcher />
</div>
TSX
<div data-testid={`card-expert-match-${expert.id}`}>
  <ExpertMatchCard expert={expert} />
</div>
TSX
<div data-testid={`service-rec-${service.id}`}>
  <SmartServiceRecommendation service={service} />
</div>
That is the most targeted fix for this failing job.