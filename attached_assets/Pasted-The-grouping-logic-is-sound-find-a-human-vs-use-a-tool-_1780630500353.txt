The grouping logic is sound — "find a human vs use a tool" is the right split, and pulling all three "people/services you can hire" items into one dropdown fixes a real discoverability problem. Planning Tools becoming genuinely tool-only (AI Planner, Visa, Live Intel, Deals) is clean. I'd build the restructure.
Two flags before you bundle it, one critical:
Critical — the deep-link is now load-bearing. Service Providers → /discover?tab=services is no longer a stopgap; it's a primary nav path. If the Discover tab doesn't read its initial tab from the URL query param, that link drops users on Discover's default view (the TravelPulse city-discovery panel), not the service catalog — a broken core nav route on day one. This has to be verified/fixed before the nav is wired to depend on it. Make it the gate: grep how the Discover tabs initialize state; if it's local component state and not URL-driven, that's a prerequisite task, not a nice-to-have.
Minor — the label. "Work With Experts" mislabels one of the three children. Your own positioning strictly separates consultative Experts (advisors) from Service Providers (delivery — a photographer or transport isn't an "expert" in that sense). A group label that calls all three "Experts" blurs a distinction you deliberately built. "Find Help" is accurate but a little generic; I'd consider "Experts & Services" or "Get Help" — something that explicitly spans both halves of the model.
Also worth noting: with Local Experts and Travel Advisors now sitting side-by-side in the dropdown but sharing one page via the role param, the in-page role switcher you deferred gets more valuable — users will now think of them as siblings and expect to toggle without reopening the nav. Not blocking, just flagging the deferred item's value went up.
On bundling: yes, one brief — they touch different files (nav/router, experts.tsx, the /vendors gate) with no hard coupling except the deep-link dependency above. Phase it so the agent can't get blindsided:

Phase 0 (gate): verify /discover?tab=services deep-link works; if not, make the tab URL-driven first.
Nav restructure: new grouped dropdown, absorb the old top-level Local Experts slot, three children wired.
Experts page fixes: role-aware CTA, Load More handler, hide neighbourhood filter on advisor view.
/vendors gate: role-gate the "Add Vendor"/manage controls, public title → "Service Providers." Confirm /vendors is fully out of the public nav now (Service Providers points at Discover), so the gate is protecting direct-URL access, not a nav entry.

Lock the label and confirm that /vendors disposition, and I'll write it up with the grep/TS verification steps and a "What Not To Do" section, the way you hand them to the agent.