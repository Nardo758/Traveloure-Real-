DISPATCH — Trailhead Kyoto Ignition Sitting (Replit, live)
Run this on Replit (needs the DB, Tavily key, and running app — it can't run in the build sandbox). Authorization: approved — this spends ~$0.32 of Tavily and flips the Kyoto ingest flag. Full detail: docs/findings/trailhead-t2-ignition-dispatch.md (§0–7), trailhead-t4-walkthrough.md, trailhead-t2-batch-review.md.

Preconditions: on main, latest pulled, app boots clean, migrations 245/246/247 recorded, DMO_INGEST_ENABLED currently OFF.

Step 0 — Baseline (BEFORE anything else)
Screenshot the empty Kyoto discover feed → save docs/findings/assets/kyoto-discover-baseline-preignition.png. The empty state never returns once lit.

Step 1 — Confirm Tavily price
Check the live Tavily dashboard: per-search should be $0.008. Receipt of record: 40 credits × $0.008 = $0.32 vs the $150 cap. If the live price differs, recompute in the dispatch doc before proceeding; if the batch would approach $50, STOP.

Step 2 — Registry seed (idempotent, no scrape)
Run server/seeds/dmo-anchor-registry-sync.seed.ts. Confirm Kyoto dmo_sources rows exist (live), the other six inert/unverified.

Step 3 — Confirm plan + cost tracking (read-only)
Per dispatch §2–3: the Kyoto slot-derived plan, and that extraction cost meters to ai_cost_tracking.

Step 4 — Ignite
Set env DMO_INGEST_ENABLED=1 (Kyoto-scoped by code), reboot. Expect the scheduler-enabled log line.

Step 5 — Run the batch
POST /api/admin/dmo/ingest-kyoto. Capture the run log + the actual $ receipt.

Step 6 — PROVE zero traveler-visible (the hard gate)
Run the §6 SQL (discover_page_visible=true count for Kyoto stubs) → must be 0. Paste this result back.

Step 7 — Single-stub path test (walkthrough)
Pick one ingested stub: hidden → POST /api/admin/dmo/publish/:id → traveler render shows the "From the web · not bookable here" card → click-out fires /api/affiliates/track → flip it back (or note it). Confirms the whole T4 rail on live data.

Step 8 — Fill the T2.4 pack (docs/findings/trailhead-t2-batch-review.md)
Objective fields — fill from the run: per-category scraped-vs-target, 10-stub fidelity sample (name/coords/facts/link vs source), duplicate/junk rate, cost actuals, the Kyoto plan diff.

Step 9 — HARD STOP → the verdict is human
The pack's VERDICT is not the agent's to invent: the gut read (directory vs destination) and the Edinburgh promote/hold call are Leon's judgment. Present the filled pack + the §6 proof and ask Leon to rule. Record his verdict on the pack.

Do NOT bulk-flip the batch or run POST /api/admin/dmo/resolve (T3) here — those are the next gate, after the verdict.

Send back to me: the §6 zero-visible result, the filled T2.4 pack, and Leon's verdict. I'll confirm the born-hidden invariant held, then queue B1 (threshold calibration, against the real match table you'll produce at the next gate), B2 (affiliate minting, once T0 confirms a program), and B4's one-wire fix.