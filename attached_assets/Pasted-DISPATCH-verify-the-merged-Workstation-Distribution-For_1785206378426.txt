DISPATCH — verify the merged Workstation + Distribution Formats program (PRs #315–#318) on the live deploy, and hunt for bugs.

Ground rules: You are verifying and bug-hunting, not redesigning. Do not modify money-path code (payments, earnings, payouts, refunds) — report money-path findings, don't fix them. Never accept Replit's "copy dev database over production" prompt. Report every finding with the page/endpoint, what you expected, what you saw, and console/network evidence.

0 — Deploy freshness (do this first). Confirm the running deploy is at merge commit b4a0bd78 (or later). Then POST /api/auth/login with a valid test account must return JSON, not 404 "Cannot POST" — the previous deploy was in a broken state where the API wasn't served at all. If the API still 404s after a clean republish, capture the boot logs and stop: that's the priority finding.

1 — Workstation entry (as expert). /expert/workspace shows ONE "New build" action with a destination input above it, and ONE "Your builds" list mixing assigned client trips and authored builds, each badged (Client — name / Store — status / "Draft — not distributed"). There must be no "New store trip" or "Create for your store" second lane anywhere.

2 — Location-aware create. Create a build with destination "Osaka" — it must succeed (builds are not Kyoto-locked). Create one with the field blank — destination defaults to Kyoto. In the builder, the destination chip must be editable (pencil); change it and confirm the Add panel's platform search, the map, and the itinerary structure all reload against the new location without a page refresh.

3 — Builder conformance. Two columns under one slim header (editable title, destination chip, fact chips, distribution chips). Right panel tabs exactly Add · AI Gaps · Distribute — no emoji, no Schedule Check/Earnings/Event Coord tabs (Schedule Check lives inside AI Gaps; Event Coord and Partner Bookings are cards under Distribute → Client; Build Notes is a collapsible panel card that still autosaves). Palette: warm console tones only — flag ANY pink/traveler-red (#FF385C-family) element on this page as a bug. No "FOR SALE" badge or "· for sale" breadcrumb anywhere.

4 — Ship to store (§12 gate placement). On a Kyoto authored build: Distribute → Store shows a "Ship to store" button; clicking it creates a draft listing and the listing editor appears in place. Click it twice fast — must NOT create two listings. On the Osaka build: ship-to-store must be refused with the Kyoto launch message (the build itself stays fine). On a client-assigned trip: the Store card shows honest clone-to-build copy, no fake button.

5 — Distribution formats. A Kyoto travel build's canvas renders grouped by real neighborhoods (Higashiyama/Arashiyama/Gion…) with day ribbons and category badges, unmatched items under "Elsewhere in Kyoto", plus a quiet "Day list" toggle back to the classic editable card. A wedding/proposal build renders the venue-timeline (Timeline / Ceremony & Venues / Vendors & Services / Guest Logistics / Other). A non-Kyoto travel build renders the classic day list unchanged. Seasonal notes must only appear when the listing genuinely has a Best Season value.

6 — Social kit. Distribute → Social shows the story-frame rail built from the build's real items (zero items → honest empty state, no invented frames). CTA frame carries the expert's real /p/handle link, or the honest "Claim your handle" state — never a fabricated URL. Captions/share image: no listing → gated with "Ship to store…" copy; listing not yet approved → share image honestly locked.

7 — Store page + link preview. For an admin-approved listing: /ready-made/:id renders (Kyoto cultural → neighborhood name strip lead; wedding → venue-led hero with only real facts — no party size, since the DTO has none), the teaser gate still hides itinerary content pre-purchase, and view-source shows og:title/og:description for the listing (crawl preview). For a draft listing: no listing data in the page source at all. For a garbage id: the normal app shell, not an error page.

8 — The #315 P0 fix. As a traveler, "Book via agent" on partner content must create the request (previously 500). As an expert, the Inbox's agent-request queue must list it (previously 403 for everyone).

9 — Free bug hunt. With the browser console open throughout: any uncaught JS errors, any 4xx/5xx on the flows above, any route rendering 404/blank, and any §13 honesty violation — a rating, count, date, or status that isn't backed by real data (empty must render as honest empty, never a placeholder number).

Deliverable: a pass/fail table for sections 0–8, then the bug list ranked by severity with reproduction steps. Clean up any test builds/listings you created.