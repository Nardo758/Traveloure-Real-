Phase 0.6 — three unresolved items. Read-only except step 2. Report only.

1. PORT AUTHORITY (this decides the fix)
   server/index.ts:352-370 hardcodes port: 23636. The artifact router
   injects PORT into the sandbox at launch. Two sources of truth for one
   value.

   Establish which is authoritative:
   - Is 23636 assigned by the artifact router at launch, or pinned by
     .replit [[ports]] and guaranteed stable across launches?
   - Cite where the router gets the number. If it reads .replit, quote it.
   - Restart the sandbox workflow twice. Report PORT from /proc/<pid>/environ
     after each. Same value both times?
   - Is there ANY path by which the sandbox binds a port != 23636 while
     Express still dials 23636? Fork, new environment, Reserved VM,
     concurrent sandbox. Answer yes or no with evidence.

   Why this matters: if the router assigns the port, a reassignment brings
   the sandbox up HEALTHY on a new port while every /__mockup/* returns 502
   via proxy.on("error"). That is indistinguishable from "sandbox not
   running" — the exact wrong conclusion two prior sessions reached.

2. THE 502, ACTUALLY OBSERVED
   You cited server/index.ts:367 for the 502 body. State plainly: did you
   curl the external /__mockup/ URL during the down window, or read the
   response off the source?

   If read off source: stop the sandbox once, curl the EXTERNAL Replit URL
   (not localhost:23636), and paste verbatim status line, all response
   headers, and body. Then restart. localhost:23636 exit 7 is a different
   surface than the canvas hits — you said so yourself, then reported it
   as the failure capture.

   Also: load the canvas with the sandbox down and paste the browser
   console output and the Network entry for the iframe. If the console is
   empty, say "console empty, 502 body rendered as document text." That
   sentence is the deliverable.

3. TIMELINE — it does not currently parse
   You wrote: "PIDs appeared at 00:02; session log capture was at 00:07 but
   the initial check was before startup settled."

   If PIDs existed at 00:02 and capture ran at 00:07, the sandbox was up at
   capture. Either a timestamp is wrong or the conclusion is.

   Give the ordered timeline with timestamps: sandbox process start, prior
   agent's screenshot, prior agent's "workflow not running" observation,
   your Phase 0 capture. Reconcile the prior agent seeing Current.tsx render
   against the claim the workflow was down.

   If you cannot reconcile them, say "cannot reconcile" and state what
   evidence would.

DO NOT: fix anything. Change the hardcoded port. Add auto-restart. Add a
BASE_PATH fallback. Touch the WebSocket upgrade. Create a main-app route.
Widen scope.