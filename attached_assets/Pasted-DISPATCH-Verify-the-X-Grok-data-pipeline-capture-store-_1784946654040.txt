DISPATCH — Verify the X (Grok) data pipeline: capture → store → push
Paste to the Replit agent. Read-only + one safe trigger. Report PASS/FAIL per check with evidence.

Context: "X data" enters via Grok (xAI) — prompt-based real-time intelligence, no direct X API. Verify each stage of the pipeline is capturing, storing, and pushing correctly.

A. CAPTURE — is Grok actually being called and succeeding?
Confirm the key is configured: XAI_API_KEY (or the key server/services/grok.service.ts reads) is set. Report set/unset — if unset, everything downstream is stale/seed data and that's the finding.
Check the scheduler is live: server logs should show [TravelPulse Scheduler] start lines; report the last scheduled/completed refresh timestamp if logged.
Safe trigger: if an admin/manual refresh endpoint exists for TravelPulse (check server/routes for a travelpulse refresh/admin trigger), fire it for one city (Kyoto) and capture the log output. If only the scheduler exists, skip — don't force anything.
B. STORE — is the data landing, and is it fresh?
C. PUSH — is stored data reaching the surfaces?
GET /api/travelpulse/cities → 200 + Kyoto present with a real pulseScore (not null/0-across-the-board).
GET /api/travelpulse/cities/Kyoto/hidden-gems → 200 + rows (or honestly empty — report which).
Discover page (Playwright or browser): city cards show pulse/season chips with real values; TravelPulse ticker renders.
By-Date calendar: GET /api/.../global-calendar?month=<current> returns only approved events — cross-check one AI-sourced pending event id from B does NOT appear in the response.
Demand chain: SELECT count(*), max(created_at) FROM service_demand_signals; — nonzero + recent means the trending→demand generator is running; then GET /api/services/demand?city=Kyoto returns real counts (empty = empty is honest, but pair it with the table count to distinguish "no data" from "broken pipe").
D. Known governance items (verify, don't fix)
Confirm POST /api/discovery/scan is gated only by isAuthenticated (content.routes.ts:~6795) — the flagged AI-spend/injection risk. Just confirm current state.
Confirm no Grok-written rows appear in content_registry (SELECT count(*) FROM content_registry WHERE content_type IN ('dmo_content') is DMO-only; gems/trending are outside the central system) — expected today, documents the asymmetry we filed.
Report
Table: stage (capture/store/push) → check # → PASS/FAIL/STALE → evidence. The key question to answer: is data flowing end-to-end right now, or is any stage running on stale/seed data?