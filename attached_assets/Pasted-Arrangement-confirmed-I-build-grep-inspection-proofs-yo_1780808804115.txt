Arrangement confirmed (I build + grep/inspection proofs; you run `npm run check`). Starting **Phase 1**.

**Server change 1 — let the trip owner read the trip-level notes blob** (currently assigned-expert-only). Let me read the exact current handler region and the ownership helper.

**Server change 2 — resolve-on-write for pin coordinates.** Let me check the plancard route's imports and the items query so I can insert a bounded coordinate-backfill that persists to `itinerary_items` (reusing the same Google geocode call as `/api/geocode`).

I'll create one shared server geocode util (single source), route the existing `/api/geocode` through it, and use it for the resolve-on-write backfill.

Now route the existing `/api/geocode` GET through the util (single source):

Add the import to content.routes.ts. Let me check the existing import style at the top.

**Add resolve-on-write to the plancard GET.** Import the util and add a bounded backfill helper:

Now invoke it right after the items query in the GET:

Now the client: remove the second geocode path in `MapControlCenter`. First let me confirm how activities carry coordinates into the component (`lat/lng` vs `latitude/longitude`).

No `latitude→lat` mapping exists — so the GET's coordinates are currently dropped and the client *always* geocodes. That's the root cause. Let me find where `trip-details.tsx` builds the PlanCard data from the GET.

The map data comes from `itinerary`, not the plancard GET (which is only used for `optimizationDelta`). Let me see what `itinerary` is.

Important: `trip-details` builds the PlanCard from `generatedItinerary.itineraryData` (raw AI JSON, `a.lat/a.lng`), and `realLegsMap` is an empty TODO (574-575) — so the carefully-built plancard GET (coords, real legs, badges) may not be what renders here. Let me check whether `PlanCard.tsx` self-fetches the GET, which would be the real data path.

There are two paths: `PlanCardSummary` consumes the real `plancardData.days` (GET), while `trip-details` passes generated-JSON `days`. I need to see how the exported `PlanCard` chooses, and which `days` feed `MapControlCenter`.

Now it's clear — and it's good news. `PlanCard` (PlanCard.tsx:860) **self-fetches** the plancard GET (886) and uses `days = daysProp ?? plancardData?.days` (907); trip-details calls `<PlanCard>` **without** a `days` prop (646-650), so the map IS fed by the GET. The `planCardDays` block in trip-details is dead code. 

The real bug: the GET emits `latitude`/`longitude` but `PlanCardActivity`/`MapControlCenter` read `lat`/`lng` — so coords never arrive and the geocoder always fires. Clean fix: align the GET to emit `lat`/`lng`. Let me view the two mapping spots.

Lines 292-293 (main) and 392-393 (fallback) feed the client map. Let me read both with context and align them to `lat`/`lng`.

Now remove the client geocoder in `MapControlCenter`. Let me read the component's imports and the `MapContent` signature so I replace the geocode logic cleanly.

Replacing the entire client geocoder block (per-item Geocoder + synthetic-offset fallback + center Geocoder) with coordinate reads + the server `/api/geocode` path:

Add the `useQuery` import to MapControlCenter:

Now add the trip-level expert-notes panel (the "both" decision). First the query in the outer `MapControlCenter`:

Now the panel itself, after the route-summary block:

Let me find and clean the literal hex in MapControlCenter (so the gate grep is clean for this file):

Now update the imports: drop now-unused `MODE_COLORS`, add `getModeColor`:

Now let me run the Phase 1 gate greps across the plancard surface.

All Phase 1 gates pass by grep. Let me do a final type-level self-review of the touched files before committing (since I can't run tsc here).

Phase 1 is complete and self-review is clean. Committing.

Phase 1 committed (`f3eba7a`). Pushing so you can run the tsc gate in your workspace:

## Phase 1 complete — committed `f3eba7a`, pushed to `claude/plancard-piece3`

**Approach:** reconcile, not rebuild. The three layers already existed; I fixed what was actually broken and added the approved pieces.

### What changed
**Server**
- `server/utils/geocode.ts` (new) — the single geocode path. `GET /api/geocode` now routes through it (`content.routes.ts:5205`).
- **Resolve-on-write** (`plancard.routes.ts:31-58`): the plancard GET fills + persists any missing `itinerary_items` lat/lng once (bounded to 12/req, best-effort), so the client never geocodes.
- **Field-name fix** (`plancard.routes.ts:293-294, 392-393`): GET now emits `lat`/`lng` to match the `PlanCardActivity` contract — previously it emitted `latitude`/`longitude`, which silently starved every pin and *forced* the client geocoder. This was the root cause.
- **Notes owner-read** (`trips.routes.ts:3663`): `GET /expert-notes` now readable by the trip owner too (authoring stays assigned-expert-only).

**Client**
- `MapControlCenter.tsx`: removed the in-browser `google.maps.Geocoder` + synthetic-offset fallback; pins read coords directly; day-center fallback uses `GET /api/geocode` (same path the Workspace uses). Added the **trip-level expert-notes panel** under the notes toggle (the "both" model — per-item pins *and* trip panel). Stray inline hex → platform tokens; badge color via `getModeColor`.

### Gate — grep proofs
| Check | Result |
|---|---|
| No client `google.maps.Geocoder` in plancard | **none ✓** |
| No literal inline hex in `MapControlCenter` | **none ✓** |
| Single mode taxonomy (only `transport-modes.ts` defines ids) | **✓** |
| No new fonts/CDNs in plancard | **none ✓** |
| Single server geocode path (util used by both routes) | **✓** |

**`tsc --noEmit`: yours to run** — registry is blocked here so I can't install `typescript`/`@types`. Please run `npm run check` on `claude/plancard-piece3` (tip `f3eba7a`). I self-reviewed the touched files type-by-type; I expect it clean.

### One thing I found (noting, not acting)
`trip-details.tsx:589-625` builds a `planCardDays` object from the generated-itinerary JSON but **never passes it** (`<PlanCard>` is called without a `days` prop, `:646-650`), so `PlanCard` self-fetches the real GET. That block is **dead code**. I left it untouched — flag for a later cleanup, out of Phase 1 scope.

---

Per our arrangement, I'll **pause here for your tsc confirmation** before starting **Phase 2** (verify the multi-stop deep-link builder: Apple per-leg fallback + Google escape hatch + `stop.mapsUrl` precedence + unknown-mode degrade — most of which already exists in `lib/navigate`/`lib/maps`). Give me the green and I'll proceed.