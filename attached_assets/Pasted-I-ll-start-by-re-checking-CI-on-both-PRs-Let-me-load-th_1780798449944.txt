I'll start by re-checking CI on both PRs. Let me load the tools I need.

CI status (just now):

**#26** — `build` ✅, `verify-selection-controls` ✅, `lockfile-purity` ✅, `report` ✅; **`e2e-selection-controls` ❌**; `Playwright E2E Tests` ⏳ in_progress.
**#27** — `build` ✅, `verify-selection-controls` ✅; `e2e-selection-controls` ⏳, `Playwright E2E Tests` ⏳ in_progress.

Neither is *fully* green yet. The DOM-gate red on #26 is almost certainly the known stale-deploy condition — but I won't assume; let me confirm from the failure log, and re-poll the in-progress checks.

Confirmed: #26's `e2e-selection-controls` failure is **all 4 DOM tests failing** (render/narrow/parity/tab-isolation) — the classic **stale-deploy** signature. #26 contains *zero* app code (workflow-only) and its logic gate is green, so this is purely the deployed app predating the feature — not a regression. Expected, and only resolvable post-publish.

Before deciding on the merge, I need to know whether the other in-progress check — **"Playwright E2E Tests"** — tests the live deploy (so it's also stale-noise) or builds locally (a real signal worth waiting for). Let me check that workflow.

Decisive finding: **"Playwright E2E Tests"** (`e2e-tests.yml`) *also* targets the live deploy (`BASE_URL: …replit.app`). So **both** deploy-targeted suites are the same stale-deploy noise — by construction they cannot pass until `main` is published, and they say nothing about #26/#27's correctness. The deploy-independent gates that actually matter are all green:

| Check | #26 | #27 |
|---|---|---|
| `build` | ✅ | ✅ |
| `verify-selection-controls` | ✅ | ✅ (37/37) |
| `lockfile-purity` | ✅ | lands via #26 |
| `e2e-selection-controls` (deploy) | ❌ stale | ⏳ stale |
| `Playwright E2E Tests` (deploy) | ⏳ stale | ⏳ stale |

By your own plan, the deterministic set is the required set (DOM promoted only after a green post-publish run). Both PRs are green on that set, and waiting on the deploy suites is circular. Proceeding with the authorized merge — **#26 first**:

#26 merged ✅ (`56b1a4d`) — `lockfile-purity` + `report` + `workflow_dispatch` now on `main`. Now **#27** (disjoint files from #26, so clean):

Both merged:
- **#26 → `56b1a4d`** (squash) — `lockfile-purity` + `report` + `workflow_dispatch` on `main`
- **#27 → `dd9687d`** (squash) — filter-doubling fix + working sort + e2e absence assertion

`main` now carries the full deterministic check set: `build`, `verify-selection-controls`, `lockfile-purity`. Let me confirm the merges on `main` and tidy up the (now-merged) subscriptions.

Both squash-merges confirmed on `main` (`dd9687d` ← `56b1a4d` ← `46adef5`). Unsubscribing from the now-merged PRs:

Both merged, confirmed on `main`, and unsubscribed (they're done). Now the honest part on the remaining steps.

## ⚠️ Two steps I cannot execute — they're yours

**1. Branch protection.** I have **no `gh` CLI, no GitHub API access, and no MCP branch-protection tool** in this environment — the GitHub MCP toolset here is read/PR/merge only, with nothing that writes branch-protection settings. So I can't run the `gh api` block. This also resolves the contradiction in your message ("Hold publish and branch protection for me" vs "Run the branch-protection gh api block now") in the only way it can: **branch protection is yours to run.** Here's the exact block, ready to paste — now is indeed the moment (full deterministic set is on `main`):

```bash
gh api -X PUT repos/Nardo758/Traveloure-Real-/branches/main/protection --input - <<'JSON'
{
  "required_status_checks": {
    "strict": true,
    "contexts": [
      "build (vite + esbuild bundle)",
      "verify-selection-controls (logic gate)",
      "lockfile-purity (no replit.local)"
    ]
  },
  "enforce_admins": true,
  "required_pull_request_reviews": { "required_approving_review_count": 1, "dismiss_stale_reviews": true },
  "restrictions": null
}
JSON
```

The three contexts are the **exact job `name:` strings** as they appear as status checks. **Deliberately excluded:** `e2e-selection-controls (DOM gate)` and `Playwright E2E Tests` — both target the live deploy and can't pass pre-publish; promote `e2e-selection-controls` to required only after its first green post-publish run.

**2. Publish `main`.** That's a Replit deploy action — outside my reach. Run it after branch protection is live, per your sequence.

## What I *can* do next — on your word
Once you've published `main`, tell me and I'll **`workflow_dispatch` the `Selection Controls Gate`** (the `workflow_dispatch` trigger landed via #26) against the fresh deploy. I'll then:
- confirm #27's **`button-toggle-filters` absence assertion** plus the **render / narrow / parity / tab-isolation** checks flip green, and
- re-triage any genuine render/narrow/parity failure (report it, never soften an assertion to force green).

## Recap of your sequence
1. ✅ #26 merged → `lockfile-purity` + `workflow_dispatch` + `report` on `main`
2. ✅ #27 merged → doubling fix + working sort + e2e absence gate on `main`
3. ⏳ **You:** run the branch-protection `gh api` block above (full check set present now)
4. ⏳ **You:** publish `main` → deploy stops being stale
5. ⏳ **Me, on your go:** `workflow_dispatch` the DOM gate against the live deploy and confirm it's green

Steps 1–2 done. Ball's in your court for 3 and 4; ping me for 5.