The commission work is the exception, and it has three problems — verified, not guessed:

The changelog overstates what's pushed. The sophisticated version it describes — named constants EXPERT_SHARE_RATE/PLATFORM_FEE_RATE, a resolveCommissionRates() helper reading booking_fee_configs, cart/checkout unified — is not in the repo. Zero matches for either constant or the helper. What actually landed (commit fe60c80) is narrower: it set the commission-endpoint fallback to 0.75 and added a proper NaN guard (safeParseRate — that part is correct and closes flag #5a). So the consolidation is either sitting unpushed on Replit or the summary got ahead of the commits. Worth checking what's actually committed.
The landmine is still live. The three platform-fee sites are still bare * 0.30 (verified at 3289, 5528, 5654, comment still says "platform takes 30%"). So checkout pays experts 70% while the commission endpoint now pays 75% — the exact cross-surface inconsistency I flagged, still in the code, now 5 points apart. What the cart shows and what gets charged can disagree.
It's the wrong number for your latest call — and ironically, the "fix" made it worse. You just said 30% to platform (= expert 70%). The commission endpoint was moved to 75% (your earlier call), so it now contradicts your current instruction — while checkout, the part that wasn't touched, still does 30% platform and happens to match what you now want. The fix moved the wrong surface.

And the admin-editable requirement isn't built. booking_fee_configs exists as a table, but no read-from-config path is wired into the fee calc — checkout still hardcodes. So "change it in the admin panel" doesn't actually work yet for the surfaces that charge money.
My recommendation: let everything else ride, but freeze the commission/checkout path from real charges until it's reconciled — right now it's inconsistent in a way that will mischarge.
And I still need the answer that unblocks a correct fix, now sharper given you said 30%: is 30%-to-platform the single global rate for everything (expert services and bookings), replacing the 75%? Or is 30% the booking fee while expert services stay at 75% (two separate admin-editable rates)? I've held off updating the docs' commission numbers until you call this, since they still say 0.75.