TASK: Verify the extracted-places, Add-market, fundamentals, and Expert Notes builds landed. Do not modify any code.

Step 0 — mandatory before any testing. The Node process keeps boot-time routes and migrations, so nothing below is valid on a stale process. Run pkill -f "tsx server/index", press Run, wait until curl -s localhost:5000/api/version returns JSON. Prove freshness: ps -o lstart= -p $(pgrep -f "tsx server/index" | head -1) must be LATER than git log -1 --format=%cd. Boot log (or migration ledger) must show migrations 185_dmo_extracted_places, 186_market_geography, 187_expert_notes_two_level applied. Report all three timestamps/confirmations before anything else.

1 — Extracted places are child rows. SELECT count(*) FROM dmo_extracted_places; — should be > 0 if any guide extraction ever ran (the backfill promotes old JSON blobs). As an expert, open a guide in the Research Reader that has extracted places: places list renders as before (byte-compatible API). Add a ticketing link to one place, then re-run extraction if the UI offers it — the link must survive. Report counts.

2 — Add market end-to-end (REAL Overpass — this is the check my sandbox couldn't do). As an admin, open /admin/markets. Click Add market: slug osaka, display name Osaka, country Japan, bbox west 135.40 south 34.60 east 135.55 north 34.72, seed neighborhoods ON. Expect a success response with real way counts (~water 20 / parks 25 / roads 35 kept) and neighborhoods inserted > 0. If Overpass 502s ("Overpass unavailable…"), wait 60s and retry up to 3× — it throttles. Then: curl -s "localhost:5000/api/markets/geography?destination=Osaka" → 200 JSON with non-empty water/parks/roads arrays. Confirm the Osaka row shows in the markets table with checklist badges.

3 — Advisor Fundamentals. Open a build's Workstation → Advisor tab. card-advisor-fundamentals renders below the narration card with real tiered rows (warn rows with CTA buttons, "Worth a look", "Polish"). Click a "Fix in editor" CTA — it must open/focus the items editor. If the trip has no lodging item, the "no lodging" row must show with a View stays button. Report which rows fired.

4 — Expert Notes (trip-level) + §21 privacy matrix. In the Workstation, find the "Expert Notes" card (below "Build notes (private)"). Write a distinctive note, e.g. "TEST-TRAVELER-NOTE-123", confirm autosave. Write a different string in Build notes (private), e.g. "TEST-PRIVATE-456". Then, as the trip's TRAVELER (or via an owner session): fetch /api/trips/<tripId>/plancard — the payload must contain TEST-TRAVELER-NOTE-123 (in trip.expertTravelerNote and tripNote) and must NOT contain TEST-PRIVATE-456 anywhere (grep the raw response). Also as the traveler: curl /api/trips/<tripId>/expert-notes must return 403. As the expert it must return 200 with the private note. If the trip has a generated itinerary, load the traveler's plan page and confirm a "From your expert" block shows the traveler note (text-plancard-expert-note) and the private string appears nowhere on the page.

5 — Regression spot-checks. Teaser SVG still serves: curl -sI localhost:5000/api/ready-made/<an approved listing id>/teaser-map.svg → 200 image/svg+xml. Advisor route-efficiency still 200 JSON. Clean up the test notes if the trip is a real one.

Report PASS/FAIL per item with actual outputs (truncated), plus the step-0 proof at the top. If any API returns Content-Type text/html, that is the stale-process signature — redo step 0 before reporting a failure.