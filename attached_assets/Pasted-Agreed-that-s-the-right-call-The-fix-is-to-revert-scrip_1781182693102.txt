Agreed — that's the right call. The fix is to revert `scripts/post-merge.sh` to its original two-line form; the `.gitattributes` guard is optional so I'll leave it out. Let me look at the file and its history to restore the exact original.

The version on my current branch (`claude/plancard-redesign`) is already the clean two-line form — no sed block here. The sed change must live on a different branch. Let me find where it actually exists.

Good news: there's nothing to revert in the repo I can see. I checked the current branch, `origin/main`, and scanned every remote branch — `scripts/post-merge.sh` is already in its clean original form everywhere:

```bash
#!/bin/bash
set -e
npm install
npx tsx scripts/run-migrations.ts
```

No sed block exists on any pushed branch. So the `post-merge.sh` sed change you added is **local to your Replit workspace and was never pushed** (or has already been undone there) — it isn't in the GitHub repo, which means I can't revert it from here; that one needs to happen in your Replit workspace where the uncommitted change lives. Once you remove the sed block there, the file will match what's already on `main` and nothing further is needed.

To close the loop on all three:

1. **`post-merge.sh`** — already clean in the repo (your local edit is the only copy; revert it in Replit). ✅ nothing for me to push.
2. **`migration-files.ts` tail conflicts** — agreed, normal append conflicts; resolve by keeping both sides' new entries in numbered order. No guard, no automation. ✅
3. **`.gitattributes`** — leaving it untouched (you confirmed `merge=union` is optional, and I'd rather not add a merge driver to the canonical migration registry without need). ✅

Nothing to commit on my side. PR #82 is unaffected by any of this — its real gates were green last I checked, and I'm still subscribed and watching it through to merge. If you push the branch that actually carries the sed edit and want me to confirm the revert, point me at it and I'll verify.