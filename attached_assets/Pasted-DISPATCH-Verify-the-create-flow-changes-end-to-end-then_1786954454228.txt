DISPATCH — Verify the create-flow changes end-to-end, then land them on GitHub
Context: Workspace main is at 1c02871d and carries the create-flow mock-fidelity work (Basics layout, Capacity + Seating/migration 239, Logistics map-first). Migration 239 is confirmed applied (provider_services.seating exists). Two things remain: prove the Seating write→read→render path end-to-end, and land the commits on GitHub main through CI. The branch claude/resume-artifact-work-vmvy96 (GitHub 9126a576) holds every commit.

Part A — Verify the Seating round-trip (task #1552)
Create /tmp/seat-check.mjs and run it with node /tmp/seat-check.mjs. Swap PROVIDER_EMAIL/PASSWORD if that seeded account doesn't exist in this DB; set BASE_URL to the running app's origin if not localhost:5000.

Expected: PATCH 200; owner seating shared PASS; after-clear null PASS. Report the output. If login fails, that only means the seed account isn't in this DB — rerun with a provider account you own.

Traveler-page render (§13 honesty) — do this on an approved, active listing (a draft isn't publicly visible), because the "Seating: …" line is rendered client-side:

Pick an approved in-person listing, set its Seating → Shared in the Capacity step, save.
Open its public page (/services/<id>) → the "Good to know" card should read "Seating: Shared — several parties may be seated together".
Set Seating back to "Not specified", save → that line should disappear (never a guessed default).
Restore the listing's original Seating value if it had one.
Part B — Visual create-flow walk (task #1551)
Signed in as a provider, confirm against the mock:

/provider/services/new → Basics: offering + "Name it" side by side; Price + "One line about it" side by side; Save draft button present; counter reads "…asks for 140+…".
…/edit?step=capacity → Party size [min] to [max] inline + a Seating dropdown beside it.
…/edit?step=logistics → leads with the framing notes, then the map canvas (map-first), Meeting Location form below.
Part C — Land on GitHub through CI (recommended over pushing merged main)
Open a PR from the branch (it already holds every commit on GitHub) so the full gate suite runs before merge:

Base: main · Head: claude/resume-artifact-work-vmvy96
Title: Create-flow: Basics/Capacity/Logistics match the mock + Seating field
Body: summarize — Basics two-column layout + 140-char checklist row + Save-draft restore (D-12 amended); Capacity inline party-size; Seating field (migration 239, additive-nullable, no CHECK, declared in shared/schema.ts) + traveler render; Logistics map-first reorder (Bring/Access stay on Logistics per ledger 2026-08-16-bring-access); ledger row 2026-08-17-capacity-seating-logistics. Note this is follow-up work after #496 merged.
Then watch CI (tsc ratchet = 168, fee-gate, provider-console e2e, money-endpoints, lockfile-purity, decision-guards, chain-integrity) and fix anything red before merging.
Do NOT force-push the branch or push workspace main over it — the branch is the source of truth; just open the PR from it.