Resolve the commission surface conflict 
in Traveloure.

PROBLEM:
Two commission models exist and conflict:
- routes.ts checkout code: flat 30% platform fee
- Business plan: 75/25 expert split, 
  4-12% provider commission

TASK:
1. Create server/utils/commissionCalculator.ts:

export enum BookingType {
  EXPERT_SESSION    = "EXPERT_SESSION",
  PROVIDER_BOOKING  = "PROVIDER_BOOKING",
  EXPERIENCE_CART   = "EXPERIENCE_CART",
  CREDIT_PURCHASE   = "CREDIT_PURCHASE"
}

export interface CommissionResult {
  platformFee: number;
  expertPayout?: number;
  providerPayout?: number;
  bookingType: BookingType;
  commissionRate: number;
}

export function calculateCommission(
  subtotal: number,
  bookingType: BookingType,
  options?: {
    isNewExpert?: boolean;
    providerTier?: 1 | 2 | 3 | 4;
  }
): CommissionResult {

  switch (bookingType) {

    case BookingType.EXPERT_SESSION: {
      const expertRate = options?.isNewExpert
        ? 0.85 : 0.75;
      const platformRate = 1 - expertRate;
      return {
        platformFee: subtotal * platformRate,
        expertPayout: subtotal * expertRate,
        bookingType,
        commissionRate: platformRate
      };
    }

    case BookingType.PROVIDER_BOOKING: {
      const tierRates = {
        1: 0.12,
        2: 0.08,
        3: 0.06,
        4: 0.04
      };
      const tier = options?.providerTier || 1;
      const commission = tierRates[tier];
      return {
        platformFee: subtotal * commission,
        providerPayout: subtotal * (1 - commission),
        bookingType,
        commissionRate: commission
      };
    }

    case BookingType.EXPERIENCE_CART: {
      return {
        platformFee: subtotal * 0.30,
        bookingType,
        commissionRate: 0.30
      };
    }

    case BookingType.CREDIT_PURCHASE: {
      return {
        platformFee: subtotal,
        bookingType,
        commissionRate: 1.0
      };
    }

    default: {
      return {
        platformFee: subtotal * 0.30,
        bookingType,
        commissionRate: 0.30
      };
    }
  }
}

2. Update checkout route in routes/payments.ts:

import { 
  calculateCommission, 
  BookingType 
} from "../utils/commissionCalculator";

Find: platformFee = subtotal * 0.30
Replace with:
const { platformFee, commissionRate } = 
  calculateCommission(
    subtotal,
    BookingType.EXPERIENCE_CART
  );

3. Update expert booking route:
Find where expert session payment is processed.
Replace hardcoded commission with:

const { platformFee, expertPayout } =
  calculateCommission(
    subtotal,
    BookingType.EXPERT_SESSION,
    { isNewExpert: expert.isNewExpert || false }
  );

4. Update provider booking route:
Find where provider booking payment is processed.
Replace with:

const { platformFee, providerPayout } =
  calculateCommission(
    subtotal,
    BookingType.PROVIDER_BOOKING,
    { providerTier: provider.tier || 1 }
  );

5. Add commission breakdown to every 
   booking confirmation response:
{
  subtotal,
  platformFee,
  expertPayout,      // if expert session
  providerPayout,    // if provider booking
  bookingType,
  commissionRate
}

6. Add a simple test — call this in a 
   temporary test route to verify:

GET /api/admin/commission-test
Returns:
{
  expertNew: calculateCommission(
    100, BookingType.EXPERT_SESSION, 
    {isNewExpert: true}),
  expertEstablished: calculateCommission(
    100, BookingType.EXPERT_SESSION,
    {isNewExpert: false}),
  providerTier1: calculateCommission(
    100, BookingType.PROVIDER_BOOKING,
    {providerTier: 1}),
  providerTier4: calculateCommission(
    100, BookingType.PROVIDER_BOOKING,
    {providerTier: 4}),
  experienceCart: calculateCommission(
    100, BookingType.EXPERIENCE_CART),
  creditPurchase: calculateCommission(
    100, BookingType.CREDIT_PURCHASE)
}

Expected results on $100:
expertNew:          platform=$15, expert=$85
expertEstablished:  platform=$25, expert=$75
providerTier1:      platform=$12, provider=$88
providerTier4:      platform=$4,  provider=$96
experienceCart:     platform=$30
creditPurchase:     platform=$100

Do NOT change Stripe Connect configuration.
Do NOT touch frontend files.
Do NOT modify vite.config.ts.
Show every file created or changed.
Show me the commission-test endpoint results.