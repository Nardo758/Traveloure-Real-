Executing as the LB lane: two independent gates, each off current main, applied from the drafted diffs (no redesign), not bundled. Starting with **#49 (click endpoint)** — reading the exact insertion point + imports first.

Imports all present; insert before `export default router;` (line 629). Creating the #49 branch off current main and applying the diff.

#49 applied. Type-checking, committing, pushing, and opening its PR.

#49 in (branch `claude/upsell-click-endpoint`, commit `8850f50`, tsc clean). Now **#51 — independent, off current main**. Creating its branch and reading the exact edit sites.

Note: I'm on the `#51` branch now, so `upsell.routes.ts` is back to main's version here — my `#49` change is safely on its own branch (`8850f50`). Keeping `#51` independent. Reading the exact discover.tsx lines to edit.

#51 applied. Type-check, commit, push, PR.

Both gate branches pushed (`#49` → `8850f50`, `#51` → `9bec76a`), tsc clean, independent off current main. Opening both PRs.

Both gates landed as independent PRs off current main, drafts applied verbatim (no redesign), not bundled.

| Gate | Branch | Commit | PR | tsc | Runtime check |
|---|---|---|---|---|---|
| **#49** click endpoint | `claude/upsell-click-endpoint` | `8850f50` | #52 | ✅ clean | ⏳ live env |
| **#51** discover resolver | `claude/discover-categorykey-resolver` | `9bec76a` | #53 | ✅ clean | ⏳ live env |

**Discipline held:** applied the issue diffs unchanged; branched off current `main` (`baeb020`); two independent PRs (either merges first); **did not touch `plancard-piece3`**; `#49` UPDATE targets the **single most-recent** impression (no inflation); `#51` resolves `categoryKey→id` via the **already-loaded categories array** (one source, no map); `visa-help` left for backlog.

**One honest gap on "check passed" + "hash on main":**
- The **static** half passed — `tsc` clean on both touched files.
- The **runtime** halves can't run from the build env (same limit as the strand gate — no DB/auth/browser here): `#49` needs a real click → `clicked=true` on the correct single row; `#51` needs an Explore tap → discover lands **filtered**, not "all." Those must run in the live env before merge. I flagged both in the PR bodies.
- And the hashes above are **branch** commits, not `main` — I'm **not merging** (merge + runtime verification are the orchestrator/live-env's call; merging on tsc-alone is exactly the "code-level ≠ verified" trap we've held against all session). The **main merge hashes** for the architect lane come after you run the runtime checks and merge.

**Sequence from here:** run each runtime check in the live env → merge #52 and #53 to `main` (independently) → both of PR #50's gates clear → merge #50 → I flip the manifest against merged reality. When #52/#53 land on `main`, paste the merge hashes and I'll **verify the landed code on `main`** (not just that the diff applied) per the table, then take PR #50 + the manifest.

Parked until you run the two runtime checks. The build half of both gates is done and clean.