Acknowledged. I can't run the script here (no DB credentials). Doing both: banner first (small), then Phase 2.

Force-push happened again. Let me abort the rebase, see what survived, and recover:

Going to reset to origin (lose local commits' git log) and re-apply my Phase 1.2/1.3/1.4 work as one clean recovery commit on top of origin's latest:

Re-adding `/api/fee-bands` endpoint and updating migration list:

Now sync Drizzle schema with origin's `platformSettingsTable` rename, then add `categoryKey` to serviceCategories:

**Phase 1 fully recovered (`05fbe7f`).** Now starting **Phase 2 — Offering Types.**

Now the seed migration for service_offering_types per SEED_DATA §4:

Now expert_offering_types per SEED_DATA §5:

Now the completeness gate migration:

Now add Drizzle types and register the migrations:

All 39 expert_offering_types rows present. Committing Phase 2:

**Phase 2 shipped (`f6dda24`).** Banner shipped (`05fbe7f` recovery).

---

## Summary

### Phase 1 recovery (`05fbe7f`)

Phase 1.2 / 1.3 / 1.4 / banner were force-pushed away by a parallel session. Reapplied as one clean commit on top of origin's HEAD. Kept origin's concurrent work:
- `platformSettings` → `platformSettingsTable` (Copilot rename)
- CJS-safe `dirname` wrapper in `run-migrations.ts`
- transport_commerce migration at slot 036

### Phase 2 — offering types catalogs (`f6dda24`)

**Tables (migration 037):**
- `service_offering_types` — provider menu (75 rows after seeds)
- `expert_offering_types` — expert menu (39 rows after seeds)

**Seeds:**
- 038 — 75 provider offerings = 63 universal + 12 market-scoped (Kyoto/Edinburgh/Bogotá/Cartagena/Porto/Mumbai/Goa/Jaipur)
- 039 — 39 expert offerings = 12 advisory / 8 planning / 5 coordination / 6 live_support / 8 specialized. Includes `ai_plan_polish` matching `fee_bands.optimize_expert_review` ($49.99 AI+Expert-Review tier).

**Completeness gate (migration 040):**
- Every `service_offering_types.category_key` resolves to exactly one `service_categories` row.
- `service_tier` ∈ the 5-tier enum.
- `delivery_formats` ⊆ {chat, written, video, live_text, done_for_you}.
- Migration aborts on any failure → server fail-fasts on startup.

**Drizzle types added with naming clarification header:**
- `expertOfferingTypes` (catalog — Phase 2) — vocabulary, read-only
- `expertSelectedServices` (instances — existing) — expert's chosen templates
- `expertServiceOfferings` (legacy — kept) — back-compat per CLAUDE.md / migration 013

Forward direction noted (NOT built): `expertSelectedServices` → reference `expertOfferingTypes` so the catalog becomes the selection vocabulary.

### Gates passing

- `npm run check` → **96** errors (origin baseline)
- Row counts match SEED_DATA §4 / §5 exactly
- Pure-function commission unit tests still 16/16 pass (Phase 1.3 logic untouched)

### Reminder for you/Replit

Before flipping `platform_settings.active_provider_commission_policy` in production, run on staging:

```bash
npx tsx server/scripts/phase1-neutrality-check.ts
```

Expect every active `booking_fee_configs` row to show `diff = 0.0000`. Exit code 0 = ship-safe.

---

## Open next steps

1. **Phase 3 — Neighborhood spine** (executable now). Extend `city_neighborhoods` per your earlier direction; add `expert_neighborhoods`, `provider_neighborhood_coverage`, `neighborhood_coverage_target` with the partial-unique `isLead` index.
2. **Phase 7 `/earn`** — now executable since Phase 2 is done. Greenfield page reading from `service_offering_types` + `expert_offering_types`.
3. **Phase 5/6 still blocked** on the 4 source-spec uploads (`UPSELL_ENGINE_…`, etc.).
4. **Phase 8 admin CRUD** — would let you remove the banner.

Recommend Phase 3 next (greenfield, no missing deps). Want me to proceed?