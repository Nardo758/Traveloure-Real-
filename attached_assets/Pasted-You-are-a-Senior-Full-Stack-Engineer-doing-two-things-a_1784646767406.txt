You are a Senior Full-Stack Engineer doing two things: 
auditing orphaned admin routes, and confirming the current 
Discover page structure so tests can be rewritten accurately.

═══════════════════════════════════════
PART 1 — ORPHANED ADMIN ROUTES AUDIT
═══════════════════════════════════════

During the 32→27 admin sidebar consolidation, these 6 routes 
lost their sidebar link but the routes themselves may still exist:

1. Tourism Analytics
2. Neighborhood Backfill
3. Content Mapping
4. Gem Photo Backfill
5. Template Approvals
6. Fee Config

For EACH route:
A) Confirm the exact route path (grep App.tsx / admin routes file)
B) Does the page still render correctly if visited directly by URL?
C) Is this page still functionally needed by admins, or is it 
   dead/deprecated functionality?
D) If still needed — where should it live in the current sidebar 
   groups (Overview / People / Catalog / Money / System)?
E) If NOT needed — should the route be deleted entirely 
   (not just unlinked), to avoid leaving an unprotected or 
   forgotten admin page live in production?

Report as a table: Route | Still exists? | Renders OK? | Needed? | 
Action (Add back to sidebar under X / Delete route / Leave as-is)

═══════════════════════════════════════
PART 2 — DISCOVER PAGE STRUCTURE CONFIRMATION
═══════════════════════════════════════

Confirm the current live structure of /discover for rewriting 
Playwright tests. For each tab, list:
- Tab name and its data-testid (if any)
- Every component rendered inside it
- Any data-testid on key interactive elements (search input, 
  filters, sort dropdown, cards) needed for test selectors

Structure to verify:
1. Hero band (always visible) — SEOHead, search input, 
   instructional ad button, TabsList (4 triggers)
2. Tab: By Location — TravelPulseCard, CityGrid, 
   AIMatchedExpertsSection
3. Tab: Ready Made Trips — expert template Card grid, Badge chips
4. Tab: By Date — GlobalCalendar
5. Tab: Browse Services — CuratedContentSection, ServiceCard grid, 
   category/price/rating/sort filters
6. Contextual: Expert Handoff Banner (only when arriving from 
   AI quick-start)

Also confirm:
- What data-testid or aria-label is used for each of the 4 tab 
  triggers (needed to click between tabs in tests)?
- Does switching tabs change the URL (e.g. ?tab=services) or 
  is it purely client-side state?
- Does each tab lazy-load its content on first click, or is 
  everything mounted upfront?

REPORT FORMAT:
Part 1: Table as specified above
Part 2: Tab-by-tab breakdown with exact testid/selector for 
each tab trigger and its key child elements