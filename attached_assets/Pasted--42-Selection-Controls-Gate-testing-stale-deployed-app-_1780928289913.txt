#42 — Selection Controls Gate testing stale deployed app

This one does need your action. The CI gate runs Playwright against https://traveloure-platform.replit.app/ (the published app), but that app is out of date with main's code, so the filter-bar tests always fail even when the code is correct. Our recent workflow fixes help with the DB connection, but not this core problem.

Your two options:

Republish the app now so the deployed URL matches current main — gate should pass after that
Remove the gate from required PR checks in GitHub (Settings → Branches → Branch protection rules) until it's reworked to test against a fresh build
Recommended: republish first, re-run manually, and see if it passes. If it still fails, remove it from required checks.

#43 — Transport "Book" button is unwired (P0 strand)

The backend and Stripe checkout are fully built, but the green Book button in the UI has no onClick. Since your app isn't currently published, this is latent (not live for real users yet), but it blocks you from publishing. No user decision needed here — the issue comments already confirm "hotfix forward, don't revert." I can build this fix as a task.

#29 — platform_settings is unread dead config

No action needed yet. The issue is self-contained: platform_settings is orphaned (no code reads it), fees come from hardcoded fallbacks, and it's safe to leave alone until after the migration catch-up (#28). The one thing worth knowing before you publish: when migrations run, provider bookings will shift from 25% → 10% platform fee (per the issue comment's warning). That's a business decision you should be aware of — does a 10% platform cut on provider bookings match your intent?

#28 — Prod migrations 006–030 never applied

No action needed manually — the catch-up runs automatically when you publish (it's self-sequencing and all migrations are idempotent). The comment flagged 3 gaps to verify first, but the code audit confirms there's no hard-failure risk. The one step to do after publishing: confirm the 12 expert_custom_services rows landed in provider_services by running a quick query against the prod DB.

#35 — Flights/hotels filters not yet on CompactFilterBar

Pure build work, no decision needed. I can handle this as a task.

Summary of what you need to decide/do:

#	Your action
#42	Republish the app (or remove gate from required checks)
#43	None — I can build the Book button wire
#29	Confirm: 10% platform fee on provider bookings — is that intentional?
#28	None before publish; query prod after to confirm data migration
#35	None — I can build the filter convergence
Want me to queue up #43 (Book button) and #35 (filter convergence) as build tasks while you sort out #42 and the fee question?