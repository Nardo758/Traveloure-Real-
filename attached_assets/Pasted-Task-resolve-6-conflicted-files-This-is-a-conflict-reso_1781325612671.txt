Task: resolve 6 conflicted files. This is a conflict resolution, not a rewrite — touch only what's inside conflict markers. Preserve HEAD's features; only accept GitHub where HEAD is broken/stale. Work in this order.
1. server/migrations/migration-files.ts (mechanical)
Keep HEAD's 067–074, then append GitHub's 075_seed_feed_composition_settings.sql after them. Before committing, confirm all nine .sql files (067…075) physically exist on disk — no ghost entries. The chain-integrity test must pass.
2. client/src/components/UpsellSlot.tsx (mechanical)
Take GitHub's useUpsellSlot hook. Delete HEAD's dead lastSlotDataRef / onSlotData lines (they reference identifiers out of scope). The rest of the file is identical on both sides — leave it.
3. client/src/lib/feed-stream.ts (mechanical)
Both sides have the same three fields (isRequest, offeringTypeKey, displayName). Take GitHub's version — it closes the MatchSuggestion interface and includes the full matchedServiceSuggestion function. Comment wording is cosmetic.
4. client/src/lib/feed-composition.ts (two blocks)

Block B (~line 154): take GitHub. It implements high-demand injection using the already-declared out / slotQueue / counters. HEAD re-declares those with wrong names (wantedSlots vs opts.wantedSlots, cfg.recCadence vs cadence, FeedItemKind un-imported) — that's the stale draft. Drop it.
Block A (~line 112): this is a comment, but the wording is a real semantic claim about what requestCount measures. Check the actual data source feeding that count: if it's per-neighborhood, use GitHub's wording; if per-city/date-window, use HEAD's. Match the comment to the behavior — don't coin-flip.

5. client/src/lib/guestSession.ts + guest-session.ts (CAREFUL — preserve HEAD safety)
Collapse to one canonical module. Use guestSession.ts as the source of truth and make guest-session.ts re-export from it. The canonical module MUST end up with ALL of:

try/catch around every localStorage access (HEAD's crash-safety — do not drop this)
sessionStorage for impression IDs (HEAD)
getGuestSessionId alias (GitHub)
clearGuestSessionId (GitHub)

If guestSession.ts is missing the try/catch or impression-ID logic, add it before wiring the re-export. End state: no three competing copies — one implementation, one re-export shim.
6. client/src/pages/discover-location.tsx (CAREFUL — read the whole file first)
Read all conflict blocks across the full 1805 lines before resolving anything. Decision rules:

RecommendationCard vs GitHub's city-feed-card-recommendation.tsx: only adopt GitHub's split-out file if it renders the same design as HEAD's inline card — same fields, same LeadExpertCard treatment, same brand colors (#1E3A5F, #2E8B8B, #E8B339). Diff the actual rendered markup. If GitHub's file is thinner or visually different, keep HEAD's inline version and don't import the GitHub file. Compiling is not the bar — matching the design is.
WantedSlotCard neighborhoodId: keep the version that destructures and uses it. If HEAD uses neighborhoodId downstream (neighborhood spine) and GitHub drops it, keep HEAD.
Imports block: resolve the body first, then make the import set match exactly what the resolved body uses. Don't pick an import side blind.

Verification gates before declaring done:

grep -rn '<<<<<<<\|=======\|>>>>>>>' client server returns nothing
tsc --noEmit error count ≤ checked-in baseline (no new errors)
Discover page and feed render correctly in the browser — card design, lead expert styling, and high-demand injection all visibly working

What NOT to do:

Do not accept either side wholesale on files 5 and 6.
Do not drop HEAD's try/catch or impression-ID logic to take GitHub's alias.
Do not leave three guest-session files in place.
Do not call it done on a green tsc alone — confirm the discover feed actually renders.