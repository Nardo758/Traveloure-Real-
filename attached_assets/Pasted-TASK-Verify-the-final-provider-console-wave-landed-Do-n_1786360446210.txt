TASK: Verify the final provider-console wave landed. Do not modify code.

Step 0 (mandatory): pkill -f "tsx server/index", press Run, wait until curl -s localhost:5000/api/version returns JSON. Prove: ps -o lstart= -p $(pgrep -f "tsx server/index" | head -1) is LATER than git log -1 --format=%cd; tip is e6a5a1f3 or newer; boot log (or schema_migrations) shows 191_dmo_extraction_runs applied. Report all three before anything else.

1 — Dashboard aesthetics (commit 8cd150c5). As a provider, hard-refresh /provider/dashboard. Expect the dense mockup look: KPI tiles with small uppercase labels and no icon boxes; Quick Actions as one row of slim bordered pills (not stacked boxes); compact one-line Performance and Availability rows. Screenshot it.

2 — Phase-3 wiring (f9539a37). Settings: NO "Account Settings" card (Change Password/2FA gone). Profile tab: zero Edit/Manage/Change-logo buttons in the DOM. /provider-status (or expert-status): Contact Support navigates to /contact. Also curl -s localhost:5000/api/provider/dashboard should no longer be a JSON route (the orphan was removed — expect the HTML catch-all, which is correct here since the route is intentionally gone).

3 — Content Ops (e4ee207b). As admin, open /admin/content-ops (also in the sidebar under Catalog). Four cards render. GET /api/admin/dmo/extraction-status → real per-guide placeCount/enrichedCount with states extracted/concluded_empty/not_extracted. GET /api/admin/dmo/ingest-youtube/last-run → after ~90s uptime the warmup sweep should have written a run row (SELECT count(*) FROM dmo_extraction_runs; > 0). Click Run on the YouTube card: with no YOUTUBE_API_KEY expect the verbatim 503 message inline; with a key expect real counts.

4 — Link landing + §16 filter (a37425ab/e6a5a1f3). Logged OUT, mobile viewport (375px): open a /services/:id page — photo, price, rating, and booking CTAs all above the fold, no login wall. Then as a provider, set Vacation mode ON: their listings disappear from /api/search/experiences?sources=platform&destination=<their city> and /api/provider-services; their storefront and service pages show the amber "Away — back <date>" chip with booking CTAs disabled. Flip OFF → everything returns. Report the before/after counts.

5 — Regression spot-check. /provider/money still renders Link Performance + Statements cards; Performance still has Reviews and Demand tabs; the offering picker still shows "Don't see your offering?" on a nonsense search.

Report PASS/FAIL per item with actual outputs (truncated) and the Dashboard screenshot. Any API returning text/html where JSON is expected (except the intentionally-removed route in check 2) = stale process — redo Step 0 before reporting failures.