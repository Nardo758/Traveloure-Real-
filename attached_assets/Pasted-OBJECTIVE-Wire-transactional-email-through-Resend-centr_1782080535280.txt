OBJECTIVE
Wire transactional email through Resend, centralized in one reusable module, sending from the no-reply identity with a human reply-to. The Resend domain is already verified — do NOT touch DNS or domain setup.

CONTEXT
- Express + TypeScript backend on Replit.
- Resend domain traveloure.com is verified and live.
- Three secrets are already set in Replit Secrets and exposed via process.env:
  RESEND_API_KEY, EMAIL_FROM_NOREPLY (e.g. "Traveloure <no-reply@traveloure.com>"), EMAIL_REPLY_TO (e.g. "admin@traveloure.com").
- Treat these as the single source of truth for sender identity. No email address may be hardcoded anywhere.

PHASE 0 — AUDIT (read-only, no writes, hard stop after)
- Grep the codebase for existing email/Resend/nodemailer/sendgrid usage. Report every hit as file:line.
- If a mail utility already exists, plan to extend it — do NOT create a parallel one.
- Identify how env vars are accessed elsewhere (dotenv vs raw process.env) and match that pattern.
- Report findings and STOP before writing any code.

PHASE 1 — IMPLEMENT
- Add the `resend` dependency.
- Create server/lib/email.ts (or extend the existing mail module if Phase 0 found one) exporting a single function:
    sendEmail({ to, subject, html, text?, replyTo? }): Promise<{ ok: boolean; id?: string; error?: string }>
- The module reads RESEND_API_KEY, EMAIL_FROM_NOREPLY, EMAIL_REPLY_TO from process.env.
- Validate all three are present at module load; throw a clear, descriptive error if any is missing.
- Default `from` = EMAIL_FROM_NOREPLY. Default `replyTo` = EMAIL_REPLY_TO unless overridden per call.
- Wrap the send in try/catch: on failure, log a structured error and RETURN { ok:false, error } — never throw into the request handler, never crash the caller.

PHASE 2 — TEST (no new routes)
- Add scripts/send-test-email.ts, runnable via tsx, that imports sendEmail and sends one message to a TEST_EMAIL env address.
- This script is the verification path. Do NOT add any HTTP route for testing.

CONSTRAINTS — WHAT NOT TO DO
- Do NOT add any route to server/routes.ts.
- Do NOT hardcode any email address, API key, from, or reply-to literal — all come from env.
- Do NOT perform DNS or domain verification — already done.
- Do NOT install a second mail library (no nodemailer/sendgrid).
- Do NOT send to real users during this task — only the test script to TEST_EMAIL.
- Do NOT swallow errors silently — log them with context.

VERIFY BEFORE DECLARING DONE
- `tsc --noEmit` passes clean.
- grep confirms zero hardcoded "@traveloure.com" literals in the new code.
- Running scripts/send-test-email.ts returns ok:true with a message id.
- Report the message id and the final file list.