This changes the picture substantially. The branch is **not simply "pre-split"** — I found the split files *already present* alongside the monolith, with the endpoint **duplicated**:

## Definitive local findings

**1. My genuinely-unique work is tiny — exactly 3 commits, all client/docs:**
```
b5871f6  Phase 1        → client/src/pages/experience-template.tsx only
da541fa  docs           → docs/planning/
aa9d74f  docs           → docs/planning/
```
None touch `server/routes.ts` or `server/routes/`. (The "76 commits ahead" is an artifact of my stale `origin/main` predating even PR #18 — those 73 other commits are the laughing-bardeen base, presumably already on real main.)

**2. The branch is mid-split, not pre-split.** Both layouts coexist here:
- `server/routes.ts` — monolith, **20,717 lines**, endpoint at `routes.ts:2316`
- `server/routes/` — split dir **exists** (`content.routes.ts`, `plancard.routes.ts`, `optimization.routes.ts`, … 20 files), and the **same endpoint is duplicated** at `content.routes.ts:871`

So `/api/catalog/templates/:slug` → `getExperienceTypeWithTabs` lives in **two places** on this branch. Replit's "main's routes.ts contributes ~2 errors vs 54 here" means **real main finished the split (trimmed the monolith); my branch left both in place**. That endpoint duplication is almost certainly what your fix-up brief targets.

## What this means for your decision rule

Your rule was: *small + needs-fixing-anyway → rebranch off current main and replay; substantial unique work → merge main in.*

My unique work **is** small (3 commits) **and** client/docs-only — it doesn't touch any diverged server file. That points hard at **rebranch off current main + replay my 3 commits**: Phase 1 is a clean cherry-pick onto a tree where `experience-template.tsx` should be identical to my pre-Phase-1 base, and the docs apply trivially. You skip the entire monolith-vs-split merge.

**The one thing I can't confirm from here** (stale `origin/main`): that the laughing-bardeen base (PR #18 + the 73 commits) is actually *on* real main. If it is, rebranch-and-replay is near-zero-conflict. If somehow it isn't, that's the "substantial unique work → merge" case.

## Run this in the Replit shell to close the gap

```bash
git fetch origin main
# Is the laughing-bardeen base already on main? (expect: a commit hash)
git branch -r --contains 7ee4827 | grep origin/main && echo "BASE IS ON MAIN → rebranch+replay" || echo "BASE NOT ON MAIN → merge"
# Confirm main finished the split (expect: small number, and NO monolith hit)
git show origin/main:server/routes.ts | wc -l
git ls-tree origin/main server/routes/content.routes.ts
# Where the endpoint lives on main (expect: only content.routes.ts, not routes.ts)
git grep -n "catalog/templates/:slug" origin/main -- server/
```

- If it prints **BASE IS ON MAIN**: 
  ```bash
  git checkout origin/main -b claude/nifty-dijkstra-v2
  git cherry-pick aa9d74f da541fa b5871f6   # 2 docs + Phase 1
  npx tsc --noEmit | grep -c "error TS"      # re-baseline the floor on the canonical tree
  ```
  Then I do **Phase 2 against `content.routes.ts` (the split location), not `routes.ts`**, and we run the fix-up brief against this canonical tree.

- If it prints **BASE NOT ON MAIN**: send me the conflict surface and I'll switch to the merge-main-in path.

## Knock-ons (your notes, confirmed)
- **Fix-up brief → run against the synced tree.** On this branch the endpoint is duplicated (`routes.ts:2316` *and* `content.routes.ts:871`); fixing the monolith copy would be thrown away. Run it after the rebranch, against `content.routes.ts`.
- **Phase 2 target is `content.routes.ts` + `experience-catalog.service.ts` + `shared/schema.ts` + the seed** — and per the handoff, no additions to `routes.ts` (which is being retired anyway).

I'm blocked on the network-dependent confirmation. Run the Replit block above; once you tell me **BASE IS ON MAIN** (or paste the output), I'll proceed with Phase 2 on the correct tree.