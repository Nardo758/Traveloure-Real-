Fix the dead-end lead problem in Traveloure's lead 
routing service.

PROBLEM:
In server/services/lead-routing.service.ts, the 
routeLead() function around line 150 returns 
assignedExpertId: null in two cases:
1. No approved experts exist for that destination
2. Top routing score = 0 (no destination match)

When this happens, the expert_requests row stays 
as status=pending with no assignment — the traveler 
lead goes nowhere and no one is notified.

FIX REQUIRED:

1. After null-assign detection in routeLead():
   Update expert_requests status to "unassigned" 
   (add this as a valid status value if not present)
   
2. Create a new utility function notifyNullAssign():
   - Log a clear warning:
     [LEAD UNASSIGNED] tripId=X destination=Y 
     reason="no_approved_experts"|"zero_score"
     timestamp=Z
   - In the same function, create an admin 
     notification record in the DB:
     table: admin_notifications
     fields: id, type, message, tripId, 
             destination, reason, isRead, createdAt

3. Create admin_notifications table migration:
   Using Drizzle ORM schema syntax, add:
   export const adminNotifications = pgTable(
     "admin_notifications", {
     id: serial("id").primaryKey(),
     type: text("type").notNull(),
     message: text("message").notNull(),
     tripId: integer("trip_id"),
     destination: text("destination"),
     reason: text("reason"),
     isRead: boolean("is_read").default(false),
     createdAt: timestamp("created_at").defaultNow()
   });

4. Add new admin endpoint in routes.ts:
   GET /api/admin/notifications
   - Returns all unread admin_notifications
   - Requires admin role
   - Ordered by createdAt desc

5. Add traveler-facing fallback:
   When null-assign occurs, update the 
   expert_requests row with a new field:
   fallbackMessage: "We are finding the best expert 
   for your destination. You will be notified when 
   one is assigned."
   
   Add GET /api/trips/:tripId/expert-request-status
   So the traveler dashboard can poll this and show
   the fallback message instead of a blank state.

6. Do NOT change:
   - The scoring logic in scoreExperts()
   - The 100-point scoring model
   - Any existing expert_requests fields
   - lead_routing_logs structure

Do not touch vite.config.ts or frontend files.
Do not install new packages.
Show me every file changed and every line added.