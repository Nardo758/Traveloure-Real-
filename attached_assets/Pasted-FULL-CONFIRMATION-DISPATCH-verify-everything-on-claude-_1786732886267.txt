FULL CONFIRMATION DISPATCH — verify everything on claude/provider-console-batch-exercise-viater landed (rulings 112–116, through commit 93128dc)

Pull and restart:

Restart the server and confirm boot logs show migrations 214, 215, and 216 applied (or already recorded): 214_property_min_stay.sql, 215_edit_split_pending_changes.sql, 216_source_locale.sql. All three are additive-nullable, no CHECK — no publish trap.

Work through the sections below in the real environment as a provider with at least one approved listing. Report PASS/FAIL per numbered item. If something fails, describe exactly what you saw — do not patch around it.

A. Ruling 112 — the ten console rulings (commits before d2a3c57; you confirmed these once — quick regression sweep):

Q1 Logistics: the wizard's Logistics step has ONE searchable neighborhood single-select (option-neighborhood-*) — no checkbox wall, no separate free-text "Service area" input.
Q4 Autosave: start a new listing, fill a few fields, close the tab, reopen /provider/services/new → restore banner (banner-autosave-restored) offers the checkpoint; Discard works.
Q5 Availability: on Catalog, open a listing's availability — weekly pattern saves without a spurious 409; duplicate windows in one save return a clear 400; the "Next available" chip (chip-next-available) renders where slots exist; /provider/calendar links to availability editing (link-calendar-edit-availability).
Q6 Property: a property listing's editor has min-stay (input-edit-property-min-stay, ≥1 enforced) and the cancellation band select with enforced-window copy.
Q7 Listing Home rail: an existing listing's edit page shows the settings cards — Pricing & fees, Availability, Photos & media (card-listing-availability, card-listing-photos), plus Languages (see section D).
Q8 Edit-split: edit an APPROVED listing's price → applies immediately, listing stays live. Change its service name → the live row is unchanged, an "Edit in review" pill (pill-edit-review-*) appears on Catalog, and /admin service approvals shows the staged diff (panel-pending-changes-*); admin Approve applies it, Reject discards it, listing stays live either way.
Q9 Distribute: the storefront header there has Edit (button-edit-handle-bio) → handle claim + bio editing inline; saving the bio round-trips.
Q3 Surcharge zones: a listing with travel-surcharge tiers renders dashed amber rings + fee popups on the Catalog map view (display only — no invented distances).
R3 Notifications: approve or reject any submitted listing as admin → the provider gets an in-app notification (Inbox → Notifications tab, tab-inbox-notifications); deciding the same listing twice does not duplicate it.
B. Ruling 113 — Workstation vs Catalog (you implemented the breadcrumb):
10. Every "Add New Service" affordance routes to /provider/workstation first; the wizard breadcrumb reads "Workstation › New service".

C. Ruling 114 — create-flow refinements (2ea127b):
11. /provider/services/new → pick an offering → the Basics card header IS the offering (name + category chip + tagline + one Change button); no separate offering card, no locked Category row; verification banners at the top of the step.
12. Workstation "Ideas for your business" rail (section-ideas-for-business): ≤6 tiles, only unlisted offerings from the provider's registered categories; clicking a tile pre-seeds the wizard — even when a stale autosave checkpoint exists (deep link wins).
13. A listing in review shows the "more ideas" nudge (nudge-more-ideas) on Listing Home; an approved listing does not.

D. Ruling 115 — provider content language (71b8cba):
14. Listing Home → Languages card (card-listing-languages): chip "Not translated" for Japanese on an untranslated listing.
15. Editor drawer: four fields with originals beside them. Save a Japanese name → chip flips Live; GET /api/services/:id?locale=ja returns it with translation.status: "approved".
16. "Draft with AI": with ANTHROPIC_API_KEY set → labeled amber draft + "Approve draft as-is"; without → honest inline "AI draft unavailable" banner. Report which you saw.
17. Wizard Basics has "I'm writing this in" (English / 日本語). A 日本語-source test listing offers an English translation slot, PUT .../translations/ja returns 400, and its public page viewed in English shows "Shown in Japanese (original)". Clean up the test listing after.
18. Existing listings unchanged everywhere (NULL source = English; shownInEnglish still on the wire).

E. Ruling 116 — receiver-side language switch on every distribution landing (93128dc):
19. /p/<handle> logged out: 🌐 menu (button-language-menu) in the storefront header. Switch to 日本語 → refetch with ?locale=ja; translated cards show translated names, untranslated cards keep originals, and the lane note (text-storefront-original-language-note) appears. Switch back → note gone.
20. OG tags unchanged: curl -A "facebookexternalhit" https://<host>/p/<handle> | grep og: returns canonical source-language tags.
21. /ready-made/<id>: 🌐 menu in the branded header, switches UI language.
22. /trips/shared/<token> and /itinerary-view/<token>: 🌐 menu renders on both (white-on-dark over the shared-trip hero), switches UI language; trip content stays in its authored language by design.
23. Guest persistence: switch language logged out, reload → choice sticks.

Gates (all must pass):

Boot logs: migrations through 216 applied; ledger shows 217/217.
npx tsc --noEmit error count ≤ 170 (the pinned baseline — do not let it rise).
node scripts/check-decision-guards.cjs → OK (DECISIONS.md rows 112–116 present).
node scripts/check-money-endpoints.cjs and node scripts/check-unmounted-routers.cjs → exit 0.
grep -c replit.local package-lock.json → 0 (lockfile purity; if you ran npm install, the postinstall scrub must have kept it clean).
One known-context note so you don't re-flag it: CLAUDE.md "§23" = Locked Decisions item 23 (the edit-split policy), not a ### § header — already resolved in 75d68e3.