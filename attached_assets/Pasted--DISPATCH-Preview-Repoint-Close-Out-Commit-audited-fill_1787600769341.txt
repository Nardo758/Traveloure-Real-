# DISPATCH — Preview Repoint Close-Out & Commit

`audited@<fill main-sha>` · lane: `preview-repoint` · agent: Replit · one lane, one branch

## Locked rulings (do not relitigate)

- **R42** — `client/src` is the sole production client. `artifacts/traveloure` is a design reference only: never a lane write target, never a deploy target, no `client/src` imports from `artifacts/`.
- **R43** — Worktree base must equal `main` HEAD at dispatch time or hard stop. `[guarded]`
- Canonical ledger is `docs/DECISIONS.md`. Root `DECISIONS.md` is not a ledger.
- Code is ground truth over documentation.
- Behavioral proof with `file:line` or exact command output is the only pass condition. "Looks right" and "compile-green" are not pass conditions.

## Scope

Close out the preview-repoint change set and commit it. Nothing else.

## Phase 0 — Report (read-only, no writes)

Produce these five outputs **verbatim**, not summarized:

1. **Ledger diff.** List every ruling ID present in root `DECISIONS.md` and absent from `docs/DECISIONS.md`. Output the list even if empty. State whether root `DECISIONS.md` existed before this lane (cite git log).
2. **Numbering.** Last ruling line of `docs/DECISIONS.md` before your append, and the number you assigned.
3. **Guard.** Exact command run for the config-only entrypoint check, and its exit code.
4. **Dropdown.** The four item labels as rendered in the root preview (port 5000), captured to `docs/design/nav-experts-services/<name>-ROOTPREVIEW.png`.
5. **Diff stat.** `git diff --stat` output.

**HARD STOP.** Do not proceed to Phase 1 until Leon replies "commit".

## Phase 1 — Commit (on "commit" only)

- Single commit. Message: `chore(preview): repoint dev preview to client/src; R42/R43 ledgered`
- Push to lane branch. Do not merge to main.
- Report the commit sha.

## Pass conditions

- Ledger diff list is empty, OR every unique root ruling was union-merged into `docs/DECISIONS.md` with its original text and Leon reviewed each before the move.
- Root `DECISIONS.md` appears in diff stat as deleted or absent — never modified.
- Diff stat contains only: preview helper, `replit.md`, `docs/DECISIONS.md`, the guard script, the ROOTPREVIEW png. Anything else → stop and explain.
- Guard exit code 0 on current tree.
- Dropdown shows exactly: Service Providers, Local Experts, Trip Planners, Event Planners.
- `/ready-made` in root preview shows `client/src/pages/ready-made-detail.tsx` layout.

## What Not To Do

- Do not touch any file under `artifacts/` or `client/src/`.
- Do not modify `.replit` deploy/build commands — they are already correct (`npm run build` → `dist/index.cjs`, Vite rooted at `client/`).
- Do not renumber, edit, or reorder existing rulings.
- Do not commit generated screenshots other than the one ROOTPREVIEW capture.
- Do not "tidy" adjacent config while in there.
- Do not merge to main.

## Open decisions requiring Leon

- If ledger diff is non-empty: which stranded rulings survive the union merge. Lane halts until ruled.

## FOLLOWUPS.md (file, do not absorb)

- Grep root for other docs duplicating `docs/` canonicals.
- Remove `artifacts/traveloure` after transcription of all reference pages completes (attach page list).
- R43 CI check: worktree-base-equals-main guard in lane template.