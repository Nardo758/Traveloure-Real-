Here's a read-only audit prompt to map the full content picture of the Discover feeds:

---

**Read-only audit of the Discover feeds' content sources and types. No code changes. Output an inventory table with `file:line` evidence and a real-vs-seed status per source. Designated branch off main.**

Map every distinct kind of content that can appear in the Discover surfaces, where it comes from, how it's typed, and whether it's actually populated in production.

**1. Surfaces.** Confirm which Discover surfaces exist and their components/routes — discover-by-location and discover-by-date (or others). For each, cite the page component and the data calls it makes.

**2. Organic content (gems).** What table backs the gem cards? How is a gem *typed/categorized* (the filter chips: Eat, Do, Stay, Services, Experts, Events, Photo spots, Vibe — which are content-type tags on gems vs. separate sources)? Where does gem data originate — seeded, TravelPulse-generated, trending-scored? Report the row count in real seed and whether the "Trending" badge is a real signal or static.

**3. The filter chips.** For each chip, report what it actually filters or sources. Flag any chip that points at a *different* source than the gems (e.g. "Services" → `provider_services`, "Experts" → experts, "Events" → `destination_events`) versus a tag-filter over the same gem set.

**4. Injected elements** (from the composition layer). For each, the source and `file:line`: recommendations (engine `/api/upsell/discover-location` → offering-types/inventory), "wanted" recruitment slots (coverage-target gaps), lead expert (`expert_neighborhoods`), and the matched-provider line on gem cards ("matched: local guide ¥6,000" → `provider_services` + coverage).

**5. Other content types.** Affiliate offerings (`aff_*`), date-bound events (`destination_events` for discover-by-date), and any source I haven't named that the survey turns up.

**6. Real-vs-seed status — the production lens.** For every source above, state whether it has *real data in production seed*, is *seed/test-only*, or is *empty* (like the affiliate-was-vacuous and adjacency-0/52 findings). This is the most important column: it tells us what a real user actually sees today versus what's stubbed or empty.

**Output:** one inventory table — `Feed element | Source (table/API + file:line) | Content type(s) | Surface(s) | Data status (real / seed-only / empty)` — followed by a one-paragraph "what a production user in a live market actually sees in the feed today, and what's missing." Make no changes. Stop.

---

The column that'll earn its keep is #6 — the real-vs-seed status. We've now hit "this source is empty in production" twice (affiliate offering-types, adjacency data), and both times it changed what the feed actually does versus what the code implies. This audit gets you the whole map at once: every content type, its source, and whether it's genuinely populated — so you can see the real feed a user gets, not the one the components suggest.