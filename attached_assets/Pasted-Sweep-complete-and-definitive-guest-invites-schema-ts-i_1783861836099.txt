Sweep complete and definitive: **`guest-invites-schema.ts` is the *only* orphan** (4 tables not in push's graph). `231 (schema.ts) + 4 (models/auth) + 2 (models/chat) = 237` = exactly what push created; the guest 4 are the delta. Throwaway DBs cleaned up.

# Bootstrap-range verification — deliverable

## Proxy + confidence
I used the **actual `drizzle-kit push`** command against a throwaway DB (not a guess about push — the real materialization of `shared/schema.ts`). **High confidence** on "what push creates." The one thing I can't see is prod's live DB; the finding assumes prod's schema came from push (as the dispatch states: "prod is push-canonical").

## Inventory result: 000–051's objects vs push
- **000 baseline** (unstamped, idempotent — 231 `CREATE TABLE IF NOT EXISTS`, zero unguarded): runs harmlessly on first deploy as a safety net.
- **All tables 000–051 create are present via push** — **except 4.**
- Token tables (`password_reset_tokens`, `email_verification_tokens`) that *looked* absent are actually **push-created** via `schema.ts: export * from "./models/auth"`. ✓ No gap.

## 🔴 GAP FOUND — 4 tables stamped-but-never-created

| Table | Created by | In push? | Runtime dep |
|---|---|---|---|
| `event_invites` | `001_guest_invite_system` | ❌ **MISSING** | `server/storage.ts` |
| `guest_travel_plans` | `001` | ❌ **MISSING** | `storage.ts`, `serp-api-integration.ts` |
| `invite_templates` | `001` | ❌ **MISSING** | `storage.ts` |
| `invite_send_log` | `001` | ❌ **MISSING** | (no live refs found) |

**Why they vanish:** they're defined in **`shared/guest-invites-schema.ts`**, which `shared/schema.ts` does **not** re-export — and the drizzle config is `schema: "./shared/schema.ts"` (single entry). So `drizzle-kit push` never sees them (empirically: push made 237 tables, these 4 absent). And migration **001 is bootstrap-stamped** (in the 001–050 set) → it **never runs** on first deploy. Push doesn't make them, 000 doesn't make them, 001 is skipped → **on a fresh push+bootstrap prod, these 4 don't exist**, and guest-invite code in `storage.ts` will throw `relation "event_invites" does not exist` at runtime — the exact "stamped-but-not-really-applied" bug.

## Fix (report only — not applied)
Narrow the range **or** un-orphan the schema. Recommended, cleanest and lowest-risk:

1. **Make push complete (root fix):** drizzle config `schema: ["./shared/schema.ts", "./shared/guest-invites-schema.ts"]` (drizzle-kit accepts an array) → push materializes all 4. No circular-import risk. *(A plain `export * from "./guest-invites-schema"` in schema.ts also works but risks a circular import, since that file imports from schema.ts — the config array avoids that.)*
2. **Or narrow the bootstrap:** don't stamp `001_guest_invite_system.sql` → it runs on first deploy and creates the 4 tables. It's idempotent (4× `CREATE TABLE IF NOT EXISTS`), so safe on fresh *and* existing prod.

Either closes the gap; **#1 is the better systemic fix** (it makes the push-canonical schema honest, so no fresh deploy is ever missing them).

## Bottom line
**One gap, precisely bounded.** The 001–051 stamp is honest for everything *except* the 4 guest-invite tables, which push can't create and the stamp skips. The rest of the deploy-safety story is clean (tail 052–109 re-run-safe ✓; token/auth/all-other tables present via push ✓). Close this one — via the config-array fix or un-stamping 001 — and the first deploy is fully known-clean end to end.

Per the dispatch: read-only, nothing changed on `main`, nothing committed. Want me to apply fix #1 (the one-line drizzle-config change) on a branch, or leave it for your call?