Step 5 unblocked — here's q9.sql. It was never committed (same as R7's script — only ever a chat artifact), so I'm sending it as a file. It's read-only and self-contained for psql -f.

Created
q9.sql
+55
-0
Let me send it as a file:

Sent
q9.sql — read-only destination×test-account cross-tab. Run: psql "<DEV_DATABASE_URL>" -f q9.sql

SQL
q9.sql

3.6KB

q9.sql sent — 6/7 verifications green and this is the last gate.

What it produces (three queries):

Q9a — market_slug × (real / test) cross-tab over all trips, using the persisted trips.market_slug (migration 241's backfill). Shows the whole picture, including the (unmapped) R13 bucket.
Q9b — the Kyoto verdict, computed from the raw destination so it doesn't depend on the backfill being correct. Columns: kyoto_total, test_acct, real_acct, authoring_trips, real_traveler_trips, and a boolean clears_10_floor. The headline is real_traveler_trips — real account and not a speculative authoring listing (author_id IS NULL) — since ready-made authoring trips are expert inventory, not traveler demand.
Q9c — a cross-check: backfill_kyoto vs raw_kyoto. If they match (both should be R7's 72), migration 241's resolver/backfill is validated against the raw text; a mismatch is a backfill bug worth flagging.
Two design notes worth your eye before the step-7 commit:

Verdict keys on real_traveler_trips, not real_acct — I split out authoring trips because a Kyoto count inflated by expert-authored listings would overstate demand. If you'd rather the verdict match R7 Q6b's raw framing exactly (real vs test only, authoring included), use real_acct instead — your call; the columns are all there either way.
The test predicate is ILIKE '%@traveloure.test' with NULL-email-is-real, matching R9's shipped isRealAccountSql — so the exclusion is identical to what every rollup read will use.
Run it, paste the output, and append it + the verdict row ("Kyoto clears 10-floor with real trips: YES/NO (n=<real_traveler_trips>)") to docs/findings/R7_DB_PASS.md in the step-7 commit. As you noted, R7 already bounds it (72 raw − a Kyoto-concentrated share of the 74 test rows), so this resolves whether it lands above or below 10.