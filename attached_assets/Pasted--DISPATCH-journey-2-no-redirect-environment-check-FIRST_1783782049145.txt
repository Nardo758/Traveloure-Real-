# DISPATCH — journey-2 no-redirect: environment-check FIRST (READ-ONLY until proven)

**Symptom:** `e2e/specs/journey-2.spec.ts:97` — after clicking generate, `page.waitForURL(/\/itinerary-comparison\/|\/trips\//)` times out at 60s on two attempts. Journey 2C (ExpertMatchCard, no AI call) passes.
**Do not open `EnhancedPlanningModal` or any product code yet.** This signature is identical to a known CI-environment gap already flagged (journey-2A: "no redirect within 60s — no AI keys in CI"). The generate path calls AI itinerary generation; with no provider keys, that call hangs/errors and the redirect never fires — producing exactly this timeout. **Prove it's a product bug before treating it as one.** A modal "fix" against a ghost would mask the real cause and burn a cycle.

---

## Phase 1 — Environment check (read-only; minutes; settles everything)

Answer one question: **does the CI environment that runs this gate have working AI provider keys?**

1. Identify which AI provider the generate path calls (grep the generate handler / itinerary-generation service for the client + env var it reads — e.g. `ANTHROPIC_API_KEY`/`OPENAI_API_KEY`/whatever it actually uses). Report the exact env var name.
2. Check whether that var is set in the gate's environment: the workflow file (`.github/workflows/…` for the Selection Controls Gate / journey-2 job), repo/environment secrets referenced there, and how Model B's deploy target is provisioned. Report set / not-set / can't-determine with evidence.
3. Pull the **server-side** log for the failing generate call, not just the Playwright client timeout. When the E2E clicks generate, what does the API return — 200 with a body, 500, a hang, an auth error from the AI provider? This is the tell: an AI-provider 401/timeout in the server log = keys gap; a clean 200 that simply doesn't trigger a redirect = product bug.

**Report Phase 1 and STOP.** The result routes everything:

- **Keys absent / AI call errors in server log** → this is the 2A environment gap, **NOT** a product bug. Do not touch the modal. Reclassify as a test-environment task and go to Path A.
- **Keys present AND server returns clean 200 but no redirect** → genuine product bug. Go to Path B (still read-only investigation first).
- **Can't determine** → report what's blocking and don't guess.

---

## Path A — Environment gap (if Phase 1 shows no keys / AI error)

Not a code fix. Options, in order of preference — recommend one, don't implement without my nod:
1. **Stub the AI response in CI** — intercept the generation call in the test environment and return a fixture itinerary, so the redirect path is exercised without a live AI call. Tests the product logic (does it redirect on success) without depending on keys. Preferred: it makes journey-2 a real product gate again instead of a keys-availability gate.
2. **Provision test keys** to the gate's environment — real coverage, but costs tokens per run and couples CI to provider availability.
3. **Mark journey-2's generate path require-keys** and run it only against a keyed environment — honest, but leaves the default gate not covering generate.

State which the repo's Model B design favors (it's "deploy-only, no Actions DB" — so a stub at the deploy target may or may not fit) and why.

---

## Path B — Genuine product bug (only if Phase 1 proves keys present + clean 200)

Read-only investigation, traced to a line, before any fix:
1. On a successful generation (200 + itinerary created), where is the redirect *supposed* to fire? Grep `EnhancedPlanningModal` for the post-generate navigation (`setLocation`/`navigate` to `/itinerary-comparison/` or `/trips/`). Is it wired to the success callback, or to a state that never gets set?
2. Does the created itinerary's id/shape match what the redirect URL builder expects? A shape mismatch (the audit found several — `.id` on a raw `Response`, `service.name` vs `serviceName`) would mean generation succeeds but the redirect target is malformed → no navigation.
3. Is the failure silent — a swallowed promise rejection, an error caught and dropped, or the 200-HTML-not-404 class (a non-JSON 200 parsed as success then failing downstream)?
4. Report the mechanism with `file:line`. **Then stop** — describe the fix, don't apply it. If it's real, it's a serious one (core generate→itinerary path broken for real users, not just CI), and it earns its own scoped fix with a behavioral gate, not an inline patch.

---

## What NOT to do

- Don't touch `EnhancedPlanningModal` or any product code in Phase 1.
- Don't label this a product bug until the server log + keys check prove it.
- Don't "fix" the redirect speculatively — a change that makes a keyless CI pass by faking a redirect would hide the real generate path.
- Don't conflate this with journeys 4–7 (missing `helpers/auth`, separate) or earn-page-smoke (vite-hmr noise, separate).

---

## Context for sequencing (not part of this task)

This is one of three E2E reds the lockfile fix exposed. The other two are test-infra, not product: journeys 4–7 fail on a missing `e2e/specs/helpers/auth` module (never run yet — write the helper from the *existing* passing journeys' auth path, don't invent one); earn-page-smoke fails only on a "no JS errors" assertion catching Vite HMR WebSocket noise under `NODE_ENV=development` (fix by running the smoke gate against a prod build, or failing that, allowlist the `vite-hmr` pattern — the 4 card-count assertions already pass). And separately: confirm **journey-1** is actually passing now (not merely absent from the red list) — it's the Stage-1 exit gate that unblocks Stage 2.