Fixing activity_provider first, then 1.3 resolver flip with neutrality proof.

The file shows only through 023. Let me re-sync:

Container reset. Need to rebase before continuing:

My Phase 1.2 commit (`91e0677`) was overwritten by parallel sessions. Origin no longer has it. Let me check current state and recreate:

Let me check whether the original commit content is still in the object database:

Phase 1.2 is gone. I need to recreate it from my context, fold in the activity_provider fix, then do 1.3. Let me execute:

Now add the `categoryKey` column to schema.ts and register the migrations:

The parallel session left a registration bug — file is `036_*` but registered as `032_*`. Fixing while I add 032-035:

Net-zero new errors (only a line-number shift on a pre-existing duplicate). Committing Phase 1.2 recovery:

Now for Phase 1.3. First let me check whether any call site passes the new transport categories to commission.ts:

Let me design Phase 1.3 properly. I'll structure the new resolver with pure helper functions for testability, then write a neutrality check script that can be run against any DB.

Now the neutrality test — pure-function unit tests + a runnable DB script:

Now the runnable DB-side neutrality script for staging verification:

All 16 tests pass, all neutrality diffs = 0. Running the grep + tsc gates:

Gates clean. Committing Phase 1.3:

**Phase 1.3 shipped (`f5183a2`). Stopping.** Awaiting your go for 1.4 (optimize.tsx literal + grep gate extension) or the staging neutrality script run.

---

## Phase 1.3 deliverables

### 1. Neutrality proof (the deliverable you asked for)

**Pure-function unit tests** — 16/16 pass — `server/services/__tests__/commission-phase1.test.ts`:

```
✓ decideBandKey: source='provider' + beta_flat → beta_flat
✓ decideBandKey: legacy category='provider_commission_percent' + beta_flat → beta_flat
✓ decideBandKey: tiered + commission_band=moderate → moderate
✓ decideBandKey: tiered + no band → defaultBandKey
✓ decideBandKey: source='expert' → expert_standard
✓ decideBandKey: default → expert_standard
✓ decideBandKey: category='default' → expert_standard
✓ decideBandKey: category='tip' (storage.ts case) → expert_standard
✓ buildRatesFromBand: 0.25 → 0.25 / 0.75
✓ buildRatesFromBand: 0.10 → 0.10 / 0.90
✓ buildRatesFromBand: 0.15 → 0.15 / 0.85 (expert_new)
✓ buildRatesFromBand: rates always sum to 1 (8 bands)

Neutrality matrix (diff column):
✓ expert default booking          0.25 → 0.25 (diff = 0)
✓ provider beta-flat booking      0.10 → 0.10 (diff = 0)
✓ tip (fallback path)             0.25 → 0.25 (diff = 0)
✓ generic category (fallback)     0.25 → 0.25 (diff = 0)

16/16 pass, 0 failures
```

### 2. Staging DB verifier — `server/scripts/phase1-neutrality-check.ts`

Run on staging before flipping production:
```bash
npx tsx server/scripts/phase1-neutrality-check.ts
```
Iterates every active `booking_fee_configs` row, prints `old platform% → new platform% (diff)`. Exit 0 = all zeros (ship-safe); exit 1 = any divergence (stop).

Skip list (rows not called through commission resolver):
- `platform_deposit_rate` (FEE-3, now read directly via `fee_bands.platform_deposit`)
- `platform_transport_commission` (transport-booking-options.service.ts reads directly)
- `affiliate_margin_*` (transport partners — separate path)

### 3. Other gates

- `npm run check` → **96** errors (baseline unchanged from sync)
- `grep "0\\.(10|12|15|25|30|70|75|85)" itinerary.tsx` → **0**
- `grep "0\\.(10|12|15|25|30|70|75|85)" payments.routes.ts` → **0**
- commission.ts literals → **6** documented safety-net constants only
- pricing.service.ts → **1** literal = `DEFAULT_DEPOSIT_RATE` (FEE-3 fallback)
- optimize.tsx:154 `49.99` → **deferred to Phase 1.4** (explicit scope)

### What's wired now

| Surface | Source | Returns (policy=beta_flat) |
|---|---|---|
| Provider line item (settlement) | `fee_bands.beta_flat` via `decideBandKey` | 0.10 platform / 0.90 provider |
| Expert line item (default) | `fee_bands.expert_standard` | 0.25 platform / 0.75 expert |
| EXP-OVR negotiated expert | `users.commission_override_…` (override-wins) | as configured per expert |
| AI-sourced | constant `AI_PLATFORM_FEE` | 1.00 platform |
| Affiliate | constants (per-partner bands seeded dormant) | 0.70 platform / 0.30 partner |
| Deposit (FEE-3) | `fee_bands.platform_deposit` | 0.25 of total |

### What stays dormant until you flip the setting

- `fee_bands` tiered provider bands (`limited 0.12`, `moderate 0.08`, `commercial 0.06`, `premium 0.04`)
- Per-partner affiliate bands (`affiliate:viator`, etc.)
- `service_categories.commission_band_key` lookup for tiered policy

Flipping `platform_settings.active_provider_commission_policy` from `'beta_flat'` to `'tiered'` activates the per-category bands. Per your forward flag, `default_commission_band_key='beta_flat'` ensures categories with NULL commission_band_key still resolve.

---

**Open decisions for 1.4 / next:**

1. **Run the staging neutrality script** before merging? I can include the run instructions in your handoff doc.
2. **Go for 1.4** (optimize.tsx $49.99 → `fee_bands.optimize_expert_review` via API)?
3. Or **pause for source-spec uploads** for Phase 5 (the 4 missing specs)?