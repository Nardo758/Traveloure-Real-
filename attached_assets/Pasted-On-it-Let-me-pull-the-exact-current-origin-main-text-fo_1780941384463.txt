On it. Let me pull the exact current `origin/main` text for both files so the patches apply cleanly, then write them.

Both verified against `origin/main@3ddc5cc` — `useMutation`/`apiRequest`/`useToast` already imported client-side, `transportLegs`/`eq` already imported server-side. No new imports needed; both apply clean. Here they are — drafts only, I haven't applied either.

---

### Patch A — Client half → **push to `origin/main`** (`client/src/components/plancard/TransportSection.tsx`)

This is the half that's missing on `origin/main`. Named `bookMutation` to match your local copy, with the **correct `apiRequest("POST", url, body)` arg order** baked in (the bug you caught).

**A1 — add the mutation** (insert right after line 183, `const affiliateOptions = …`):

```diff
   const platformOptions = options.filter((o) => o.bookingType === "platform");
   const affiliateOptions = options.filter((o) => o.bookingType === "affiliate" || o.bookingType === "deep_link");
+
+  const { toast } = useToast();
+  const bookMutation = useMutation({
+    // apiRequest signature is (method, url, body) — NOT (url, opts).
+    mutationFn: async (optionId: string) => {
+      const res = await apiRequest("POST", `/api/transport-booking-options/${optionId}/book`, {});
+      return (await res.json()) as { checkoutUrl?: string; bookingId?: string };
+    },
+    onSuccess: (data) => {
+      if (data.checkoutUrl) {
+        window.location.href = data.checkoutUrl; // hand off to Stripe checkout
+        return;
+      }
+      toast({ title: "Booking started", description: "Your transport booking is being processed." });
+    },
+    onError: (err: any) => {
+      toast({
+        title: "Couldn't start booking",
+        description: err?.message ?? "Please try again.",
+        variant: "destructive",
+      });
+    },
+  });
```

**A2 — wire the Book button** (lines 243–249):

```diff
                   <Button
                     size="sm"
                     className="text-[11px] h-7 px-3 shrink-0 bg-green-600 hover:bg-green-700 text-white"
+                    onClick={() => bookMutation.mutate(opt.id)}
+                    disabled={bookMutation.isPending}
                     data-testid={`button-book-platform-${opt.id}`}
                   >
-                    Book
+                    {bookMutation.isPending && bookMutation.variables === opt.id ? "Booking…" : "Book"}
                   </Button>
```

> If your local tree already has this, the action is just **push it to `origin/main`** — that's the only thing that makes the "LB rebase inherits it" plan true. Reconcile against this version so the arg order is the fixed one.

---

### Patch B — Server half → **PR #46 commit** (`server/routes/transport-hub.routes.ts`, the `/book` endpoint)

```diff
       if (option.bookingType !== "platform") {
         return res.status(400).json({ error: "Not a platform booking option" });
       }

-      if (!option.variantId) {
-        return res.status(400).json({ error: "Booking option has no associated variant" });
-      }
-
-      // Fetch the variant to get tripId
-      const variant = await db.query.itineraryVariants.findFirst({
-        where: eq(itineraryVariants.id, option.variantId),
-      });
+      // Per-leg platform options carry transportLegId but not variantId.
+      // Resolve the variant via the leg when the option has no direct variantId.
+      let variantId = option.variantId;
+      if (!variantId && option.transportLegId) {
+        const leg = await db.query.transportLegs.findFirst({
+          where: eq(transportLegs.id, option.transportLegId),
+        });
+        variantId = leg?.variantId ?? null;
+      }
+
+      if (!variantId) {
+        return res.status(400).json({ error: "Booking option has no associated variant or leg" });
+      }
+
+      // Fetch the variant to get tripId
+      const variant = await db.query.itineraryVariants.findFirst({
+        where: eq(itineraryVariants.id, variantId),
+      });

       if (!variant) {
         return res.status(404).json({ error: "Variant not found" });
       }
```

Behavior-neutral by construction: when `option.variantId` is set, the new `if` is skipped and the lookup is identical to before — the multi-day-pass path is untouched.

---

### Apply order + the gate (unchanged)
1. **Push Patch A to `origin/main`** (or push your local) — so the client half is actually on the branch LB rebases onto.
2. **LB applies Patch B** to PR #46 → rebases on `origin/main` (inheriting A) → both halves now co-located.
3. **Run gate B with the populate-first step:** `GET /options` on a Tokyo/Paris/Kyoto + `taxi`/`private_driver` leg → your `SELECT … variant_id IS NULL` → `POST …/book` → expect **200 + `checkoutUrl`**, no "no associated variant" in logs; then a `variantId`-set option still 200s without entering the new branch; multi-day pass unchanged.
4. **Merge #46.**

The server diff is also on issue #43 — identical to Patch B and now confirmed clean against `3ddc5cc`. Want me to update that #43 comment with the "client half not yet on origin/main + verified-clean" note so the LB lane sees the current state, or leave the handoff as-is?