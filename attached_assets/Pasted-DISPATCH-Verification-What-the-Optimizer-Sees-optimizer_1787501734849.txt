DISPATCH — Verification: "What the Optimizer Sees" (optimizer honesty audit)

Reference: docs/design/optimizer-catalog-mock.html. This is not a UI screen — it's the ratified audit of what the itinerary optimizer reads, what it must never touch, and the "findings that need a decision." So this is a code + behavior verification, not a visual pass. Your job: confirm each ratified finding is resolved in the live optimizer path (they should already be — this is a confirmation, not a hunt). Report any that regressed; do not redesign.

A. "Findings that need a decision" — confirm each is closed

#	Mock finding	Confirm in code	Confirm in behavior
1	Unapproved listings can reach travelers through optimization	server/services/optimizer-baseline.service.ts → loadOptimizerCatalog filters approvalStatus = "approved"	Run an optimization on a destination that has a submitted/unapproved listing → it must never appear in a variant
2	No destination filter on the catalog pull	same file — location ILIKE %destination% scoping; a null-destination comparison gets the unscoped active+approved set (documented)	Optimize a Kyoto trip → variant items are Kyoto-scoped, not arbitrary cross-city rows
3	Nothing ever writes coordinates onto variant items	server/itinerary-optimizer.ts (~L969–L972, L1298) writes real lat/lng onto variant items (ledger 2026-08-22-optimizer-catalog-honesty)	After optimizing, variant/transport legs render on the map — located items only, never a guessed pin (§13)
4	AI-authored prices can be read back as trusted server prices	server/services/booking.service.ts (~L332) PROVENANCE RULE (ledger 2026-08-22-booking-price-provenance) — an LLM-written variant price is flagged, not silently trusted at booking	Attempt to book a variant item whose price came from the model → server re-derives/validates; the LLM number is not the charged amount unqualified
B. "What it does NOT see" — confirm the invariants hold

The optimizer only touches optimizable items; must-retain / expert-held items are never replaced, moved, or dropped. Verify with a trip that has an expert-added item → it survives every variant untouched.
Exactly two AI variants are produced, differing in ≥40% of line items (itinerary-optimizer.ts ~L268, L1226).
First run ignores preference signals — travelerProfile only weights later runs; a brand-new user's first optimization uses the default strategy pair.
C. Observations to confirm (document, don't necessarily change)

Grok-3 first with Claude Sonnet 4.5 fallback for the LLM call.
Whether Grok runs are cost-tracked — the mock notes they currently are not; confirm that's still the case and flag if it should change (decision item, not a bug to fix silently).
Deliverables: a pass/fail table for A (4 rows) and B (3 invariants), each with the file:line you confirmed and the behavior result; note anything in C. If a behavior can't be exercised (no unapproved listing to test with, etc.), say so — don't assume.