## Phase A Verification Gate — Content-Parity Table

The brief requires: "each of CityDetailView's 7 original tabs → the LocationView section that now carries it. Every row must resolve to a non-empty section."

| CityDetailView Tab | LocationView Section | Carry-over Status | Verified Non-Empty for Kyoto |
|---|---|---|---|
| **Overview** | §1 HERO (city·pulse·supply·crowd) | Compiled into Hero metrics + city facts | ✅ TravelPulse city payload present |
| **Hidden Gems** | §4 GEMS BY CATEGORY (split-row + actions) | New responsive layout, actions wired | ✅ `hero.data.hiddenGems` populated |
| **Recommendations** | §5 SUPPLY woven (Stay·Do·Plan) | Hotels + activities + expert CTA | ✅ AI engine returns hotels/activities |
| **Happening Now** | §1 HERO (Happening Now strip) | Strip carries title + LIVE badge + venue | ✅ `hero.data.happeningNow` populated |
| **Live Activity** | §6 LIVE FEED (own section) | Full CityDetailView activity feed | ✅ `hero.data.liveActivity` populated |
| **Media** | §7 MEDIA GALLERY (full UI carry-over) | Videos + photos + by-attraction + Google attribution | ✅ Separate `/api/travelpulse/media` query |
| **AI Insights** | §8 INSIGHTS (9 subcards) | All 9 cards verbatim: BestTime, Duration, Budget, MustSee, Tips, Cultural, Safety, Seasonal, AvoidDates | ✅ `hero.data.city` AI fields |

**Caveat I want to flag before claiming the gate passes:** I cannot render Kyoto live from this remote container (no `DATABASE_URL`, no dev server). The gate's "every row non-empty" requires *you* to load `/discover/location/Kyoto?country=Japan` and visually confirm. The Phase 4 data fill you ran (11 gems, 6 services) plus the Phase 1b TravelPulse Kyoto data should make every section populate.

**Please load `/discover/location/Kyoto?country=Japan` and confirm:**
- §1 Hero shows pulse score + supply summary + happening-now strip
- §3 Neighborhoods cards show real gem/service counts (not 0/0)
- §4 Gems shows hidden gems with split-row layout
- §5 Supply shows hotels + activities + expert CTA
- §6 Live Feed shows recent activity
- §7 Media renders videos + photo gallery
- §8 Insights renders all 9 AI subcards
- §9 Footer shows events handoff

Once you confirm sections render non-empty, **I'll proceed with Phase B** (CityGrid + GlobalCalendar embed swap + `/city/:slug` redirect, CityDetailView.tsx retained per the brief). What's the visual state of Kyoto?