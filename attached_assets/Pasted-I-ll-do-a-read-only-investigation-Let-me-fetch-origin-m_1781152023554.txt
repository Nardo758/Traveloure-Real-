I'll do a read-only investigation. Let me fetch origin/main and gather the data without any writes.

Collision confirmed at both 060 and 061 — different filenames. Let me dig into content equivalence, the migration-files.ts divergence, and the safety question.

Now the registration arrays and runtime wiring:

Let me confirm the merge-base state and the resolver/neutrality safety question for the branch's fee-band 060.

Investigation complete. **No writes made.** Full report:

## 1. Migrations on `origin/main`, 058 upward

| # | Filename on main | What it is |
|---|---|---|
| 059 | `059_pnc_engine_lookup_index.sql` | (shared — identical on branch) |
| **060** | `060_affiliate_booking_trip_link.sql` | Phase 2.1 — nullable `trip_id` FK on `affiliate_booking_requests` (schema-only) |
| **061** | `061_affiliate_offering_types.sql` | Phase 5.8 — seeds `service_offering_types` for the four `aff_*` categories |

Main's 060/061 are **completely different migrations** than the branch's. (Note: main's 060 is the trip-link that CLAUDE.md still calls `051_…` — it was renumbered to 060 on the way into main.)

## 2. Collision — yes, by number; different files; disjoint content

| Num | main file | branch file | Same content? |
|---|---|---|---|
| 060 | `060_affiliate_booking_trip_link.sql` | `060_concierge_booking_fee_band.sql` | **No** — unrelated (affiliate FK vs `fee_bands` seed) |
| 061 | `061_affiliate_offering_types.sql` | `061_seed_booking_concierge_offering_type.sql` | **No** — unrelated (`service_offering_types` seed vs `expert_offering_types` seed) |

Number collision, distinct filenames, **no patch-id overlap**. The branch's files are **ABSENT on main by name**, so two files of number 060 (and two of 061) would coexist after a naive rebase — a hard numbering collision in the registry.

## 3. Already on main, or unique to branch?
**Unique to the branch — not merged out-of-band.** `git cherry -v origin/main HEAD` marks both branch commits with `+` (not equivalent to anything on main):
- `+ 0428923` Phase 3.1 (fee band)
- `+ 3641bcf` Phase 3.2 (offering type)

Both files are absent from `origin/main`. No equivalence.

## 4. `migration-files.ts` divergence — yes
Both diverged from a common base whose tip entry was `059`:
- **main** ends `…059, 060_affiliate_booking_trip_link, 061_affiliate_offering_types`
- **branch** ends `…059, 060_concierge_booking_fee_band, 061_seed_booking_concierge_offering_type`

Branch is **missing main's two entries** AND **duplicates the 060/061 numbers** with different filenames. Runtime registration is the *same* file — `run-migrations.ts` on both sides does `import { MIGRATION_FILES } from "./migration-files"` (no inline copy), so there is **one** array to reconcile, not two.

## 5. Next free number + renumber requirement
- Highest on main = **061**. Next free = **062** (then 063).
- The branch **must renumber**:
  - `060_concierge_booking_fee_band.sql` → **`062_concierge_booking_fee_band.sql`**
  - `061_seed_booking_concierge_offering_type.sql` → **`063_seed_booking_concierge_offering_type.sql`**
  - and re-register both (in main's order) at the end of the array.
- Safe to renumber: the branch's two migrations (a `fee_bands` seed and an `expert_offering_types` seed) are **order-independent** w.r.t. main's 060/061 (which touch `affiliate_booking_requests` and `service_offering_types`) — disjoint tables, no ON CONFLICT key overlap, no data dependency.

## 6. SAFETY — prod-neutrality gate
**Not a concern on main, but flag the mislabel.** The branch's Phase 3.1 fee-band 060 (`0428923` — which *does* touch the resolver `commission.ts`, the neutrality surface) is **NOT on main**. Main's 060 is the *unrelated* `affiliate_booking_trip_link` (nullable FK, inherently side-effect-free). So:
- ✅ There is **no unguarded fee band on main** — the fee-AMOUNT seed + band-aware resolver change live only on this branch, behind the 3.1 neutrality check you already ran here.
- ⚠️ **Caution:** because both are called "060", don't conflate them. Anyone reading "060 is on main" must not assume the fee band shipped. It hasn't.

## 7. Expected rebase conflicts + correct resolution
- **`server/migrations/migration-files.ts`** — conflict at the array tail (both append after `059`). **Resolution:** keep main's `060_affiliate_booking_trip_link` + `061_affiliate_offering_types`, then append the renamed `062_concierge_booking_fee_band.sql` and `063_seed_booking_concierge_offering_type.sql`.
- **Runtime registration** — same file, same conflict; no separate inline list to fix.
- **The two `.sql` files** — no content conflict (distinct filenames), but you must `git mv` 060→062 and 061→063 so the chain-integrity test (sequential numbering, no dup/dangling) stays green.
- **CLAUDE.md** — likely conflict in the migration-log section; both sides added "Migration 060/061" notes. **Resolution:** keep both narratives, renumber the branch's notes to 062/063.

## Recommendation
**Renumber + rebase — the branch is NOT fine as-is.** Concretely:
1. `git rebase origin/main`
2. `git mv` 060→062 and 061→063; update the two registry entries (append after main's 061) and the two CLAUDE.md notes.
3. Re-run chain-integrity + tsc; confirm `fee_bands`/`expert_offering_types` seeds are still idempotent.

**Not cherry-pick** (the work isn't on main and shouldn't be pushed there outside this branch's flow). **Not leave-as-is** (the duplicate 060/061 numbers will break the chain-integrity test and the registry the moment main is merged in).

**Stopped — nothing executed.** Want me to perform the renumber + rebase on the branch?