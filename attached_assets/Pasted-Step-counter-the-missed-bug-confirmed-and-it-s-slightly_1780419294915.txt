Step counter (the missed bug): confirmed — and it's slightly richer than "phantom states." Line 641 indexes into ["draft","in_review","notes","pending","delivered"], and lines 120–121 show the UI actually defines notes ("Expert Notes") and pending ("Awaiting Approval") as visual stages. So the frontend was built for a 5-stage flow that the backend never implemented (3 states). The practical effect: draft renders "Step 1 of 5", in_review "Step 2 of 5", delivered "Step 5 of 5" — the progress jumps 1 → 2 → 5, skipping 3 and 4 for every expert. Good catch by Replit.
The NaN blast-radius claim: Replit's instinct is right but the mechanism is wrong. The line is parseFloat(svc.revenueShareRate ?? "0.30"). The ?? does catch null/undefined → substitutes "0.30", so a null revenueShareRate yields 0.30, not NaN. Replit's stated trigger ("column is null → parseFloat(null) is NaN") won't fire.
The real hazard is narrower in trigger but worse in outcome: an empty-string or non-numeric revenueShareRate slips past ?? (only null/undefined are caught), parseFloat("") → NaN, and because the DEFAULT_RATE fallback only applies when the expert has zero services, the NaN does not fall back — it propagates straight into expertShare/platformFee, so the whole earnings panel renders NaN. So: not triggered by null, and it doesn't degrade gracefully to 0.30 — it breaks the panel outright.
I've folded both into the spec — flag #6 added, flag #5 refined with the correct mechanism:Reconciled findings, prioritized decisions before tasks, recommended safe cleanupsReconciled findings, prioritized decisions before tasks, recommended safe cleanupsSpec is updated — flag #6 in, flag #5 refined, plus a #5a for the NaN guard.
On the line-number mismatch between Replit and me (e.g. it cites routes.ts:17848, I cite 17673): that's just different working copies — Replit's reading its live tree, I'm pinned to 68d588e. Every finding holds in both; ignore the offsets.
On Replit's suggested task order — mostly right, but I'd split it by what's safe to cut now vs what needs your decision first, because two of its items quietly bake in choices you haven't made:
Cut these as tasks now (no decision needed — just defects):

Flag #2 saveMutation (delete the no-op)
Flag #6 step counter (collapse "of 5" → "of 3")
Flag #5a NaN guard (Number.isFinite coalesce per service)

Decide first, then cut the task:

Flag #1 (the blocker). The fix shape is a decision: should routeLead() auto-write a trip_expert_advisors row when it assigns, or is there meant to be a human admin assignment step in between? Those are different builds. Replit's task says "wire them" — but wire them how is yours to call.
Flag #5 commission. Replit's order says "0.30 → 0.75." That's not a find-replace, it's a pricing decision — 75? 80? 85? flat or per-tier? — and it has to reconcile with the flat 30% at checkout (Lead Journey doc) and per-service revenueShareRate. Pick the number once, globally, or the Offerings doc (Piece 1) will just inherit the ambiguity.

Defer (product/architecture, not urgent):

Flag #3 PlanCard consolidation — real but needs a design call.
Flag #4 "AI Gaps" — rename now (honest + free) or expand later (real work). Cheap half is worth doing.

So: let Replit create the three safe-cleanup tasks immediately. Hold #1 and #5 until you've made the two calls — they're exactly the two decisions I flagged before writing this doc, and they also gate Piece 2 (Workflow).