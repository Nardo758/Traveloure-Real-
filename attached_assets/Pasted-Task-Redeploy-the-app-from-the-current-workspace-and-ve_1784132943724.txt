Task: Redeploy the app from the current workspace and verify the new build is live.

The workspace is on the correct commit (git log -1 should show f6e99866, "Merge pull request #196"), but the published deployment is running stale code from before these changes. Several API routers that exist in the codebase are being served as the Vite HTML catch-all instead of executing, which proves the deployment binary predates the current workspace. Do NOT edit any code — the code is correct and complete. Your only job is to rebuild and republish the deployment from the current workspace HEAD, confirm migrations ran, and verify.

Steps:

Confirm workspace state. Run git log -1 --oneline (expect f6e99866) and wc -l server/routes.ts (expect ~7311). If the commit differs, run git pull origin main first. Do not proceed if there are uncommitted local changes — report them instead.
Redeploy. Republish/redeploy the app so the deployment builds from the current workspace HEAD. If the Deployment is configured to build from a specific branch or commit rather than the workspace, update it to track main at f6e99866. Report which deployment type is configured (Autoscale / Reserved VM / Static) and whether it was pinned to an old commit.
Confirm migrations applied on boot. After the new deployment is live, check the deployment logs for the migration runner. Confirm these two lines appear (or that both migrations are already recorded):
115_purge_fever_mock_events.sql
116_content_impressions_tracking.sql
Report any migration errors verbatim.
Verify the new build is serving. Against the live deployment URL, confirm each of these — report the actual HTTP status and content-type for each:
GET /api/ea/clients → must be 401 with application/json (currently returns 200 text/html = stale)
GET /api/provider/settings → 401 application/json
POST /api/tracking/impression with body {"contentType":"gem","contentId":"t","sessionId":"test1"} → JSON containing impressionId (currently returns HTML = stale)
GET /api/travelpulse/global-calendar?month=7&limit=30 → 200 with at least one cityName in the body, and NO "db is not defined" error (currently 500)
GET /api/recommendations/user?city=Kyoto → real JSON, not a 500
Report back: the deployment type, whether it was pinned to an old commit, the migration log lines, and the status+content-type of all five verification endpoints. If any endpoint still shows the stale behavior after a confirmed successful republish, report exactly what commit/branch the deployment built from — that's the remaining question to resolve.
Do not modify application code, migrations, schema, or environment variables. This is a deploy-refresh and verification task only.