Important: everything — both gate endpoints **and** the slot (plancard-piece3 #55) — is now on `main`, merged in a tight 08:32–08:33 window. That's exactly the "5 PRs land at once, merged ≠ correct" scenario this session keeps catching. So before anything else I'll **verify the landed code on `main`** (my committed responsibility), then be straight about the runtime checks and the manifest.

**Landed code on `main` (`54aa7ef`) verified — the 5-PR pile-up didn't mangle anything.** All three pieces are correct and co-located:
- **#49** — `/api/upsell/click` present, `UPDATE … SET clicked = true … ORDER BY shown_at DESC LIMIT 1` (`upsell.routes.ts:643–652`): single most-recent impression, **no inflation**. ✅
- **#51** — `categoryKey` on the local type (`:92`), `?categoryKey` read (`:668`), resolved via loaded `categories` (`:671`), `setSelectedCategory(match.id)` (`:672`): one source, no map. ✅
- **Slot (#55)** — `PlanCardUpsellSlot` present, sends `?categoryKey=` (`:88`), POSTs `/api/upsell/click` (`:77`); 2 mounts wired in `PlanCard.tsx`. ✅

That's the "verify landed, not just diff-applied" step done — it's clean.

**One reframe you need to see clearly:** the gate moved. The plan was "don't let the slot reach `main` until gates verified" — but the slot (#55) *and* both gate endpoints are now **all on `main`**, while the two **runtime smoke tests never ran**. So the gate is no longer "before merge"; it's **"before `main` publishes."** Prod was latent (unpublished) last we checked, so there's still time — but **the two smoke tests must run before the next publish**, or you ship runtime-unverified CTAs. Don't let it rot now that nothing's blocking it (the `:341` failure mode again).

**The two runtime checks are yours to run (I can't — no DB/auth/browser here). Turnkey:**

*#49 — click attribution, no inflation:*
1. Logged in, open a trip whose PlanCard shows a slot (pre-trip or in-trip window) → tap an Explore candidate (fires `POST /api/upsell/click`).
2. ```sql
   SELECT id, offering_id, clicked, shown_at FROM upsell_impressions
   WHERE trip_id='<tripId>' AND surface='plancard_pretrip' AND offering_id='<offeringId>'
   ORDER BY shown_at DESC;
   ```
   Pass = **exactly one** (most-recent) row `clicked=true`; if shown multiple times, older rows stay `false`.

*#51 — Explore lands filtered:*
- Browser → `/discover?categoryKey=tour_guide` (or any real `category_key`) → pass = page opens with **that category selected**, not "all." (Same as tapping a slot's Explore.)

**Manifest: holding the flip until you confirm those pass.** The slots are *rendered* on `main`, but their CTAs are runtime-unverified — flipping the manifest to "working, must-not-regress revenue affordances" now would document an unconfirmed state, the exact ground-truth trap. The moment you give me the two greens, I'll re-verify and **take PR #50's manifest update against the confirmed reality**.

So: run the two smoke tests (before any publish) → paste the greens → I flip the manifest. Parked there.