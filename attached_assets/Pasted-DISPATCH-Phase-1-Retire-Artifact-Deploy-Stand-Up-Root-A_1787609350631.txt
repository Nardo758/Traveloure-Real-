DISPATCH — Phase 1: Retire Artifact Deploy, Stand Up Root Autoscale

audited@8e558f43 · lane: restore-root-deploy · executor: Leon (Replit UI) · agent: read-only observer, reports after each step on request

Locked
2026-08-24-client-tree-canonical. Artifacts are never published or deployed.
Fallback push ruling (replit.md @ 8e558f43): Replit checkout never pushes without "commit".
Agent makes no edits, no deployments, no workflow changes in this phase. If asked to act, it declines and cites this line.
Pre-flight (agent, on "preflight")
git status -sb          # expect: ## main...origin/main, clean
git rev-parse HEAD      # expect 8e558f43

If [ahead N]: git reset --hard origin/main, report, continue.
Report the current deployment's creation timestamp from the Deployments log or metadata. This is the outage start; record it.

Step 1 — Delete the artifact deployment (Leon)

Deployments pane → the deployment whose log shows artifact mode enabled static=1 runnable=0. Stop it. Delete it.

Pass: no deployment exists whose log contains registered static handler for artifact.

Say "step 1 done". Agent reports the Deployments list.

Step 2 — Create Autoscale deployment (Leon)

Deployments → New → Autoscale. Root directory: workspace root (not artifacts/*). Build: npm run build. Run: npm start. These should prefill from .replit [deployment]; if they don't, type them exactly. Deploy from main.

Pass, from the deployment log:

Contains the Express start line from server/index.ts (the serving on port or equivalent).
Does NOT contain static-only deployment or artifact mode enabled.
Build stage completes with dist/index.cjs and dist/public/ produced.

If build fails: paste the log verbatim, do not retry blindly. The root build passed locally at 8a113ef6 today; a failure here is environmental (secrets, Node version, memory).

Say "step 2 done". Agent reports the first 40 lines of the deploy log.

Step 3 — Unpublish artifacts (Leon)

Artifacts pane → traveloure → Unpublish. traveloure-marketplace → Delete (empty husk: dist/, node_modules/, one tsbuildinfo). mockup-sandbox → Unpublish if published; do not delete yet.

Pass: no artifact shows as Published.

Say "step 3 done". Agent reports ls artifacts/ and the Artifacts pane state.

Step 4 — Delete artifact workflows (Leon)

Workflows → delete artifacts/traveloure: web and artifacts/mockup-sandbox: Component Preview Server.

Pass: only Project, Start application, check-noop-migrations remain. Nothing listening on 20087 or 23636.

Say "step 4 done". Agent reports the workflow list and ss -ltnp | grep -E '20087|23636' (expect empty).

Step 5 — Hand-off

Say "verify". Agent runs Phase 2 as previously dispatched (deploy log, /api/health content-type, bundle greps, /experts tab count on production and dev host, dropdown labels, ready-made listing render, workflow list).

What Not To Do
Agent: no writes of any kind. No git operations beyond the pre-flight reset. No deployment triggers.
Leon: do not deploy from any artifacts/ root. Do not edit .replit. Do not delete artifacts/traveloure/ source — still the transcription reference.
Do not change environment secrets during this lane; if the Autoscale deploy needs one that's missing, stop and list it.
Open decision

If Autoscale prompts for a machine size or scaling config, take defaults. If it prompts to migrate the domain from the old deployment, yes — www.traveloure.com must point at the new one. Confirm the domain moved before "verify".

Ledger (Leon appends on Phase 2 pass)

2026-08-24-artifact-deploy-override, as previously drafted, plus outage window from pre-flight.