READ-ONLY reconciliation audit. Do NOT merge, open PRs, resolve conflicts,
rebase, force-push, or modify any branch. Inventory two divergent bodies of work
and report so a human can decide what's canonical. HARD STOP at the report.

CONTEXT: Two bodies of work exist and likely conflict.
  Lane A (gated CI lane): fix/e2e-migration-runner + the Stage 0/1 + migration/
    push commits. Careful, gated, mid-reconciliation.
  Lane B (7-stage sprint): fix/stage-2-event-coordination-merged, fix/phase-3-gaps,
    fix/stage-4-supply-side, fix/stage-5-transport, fix/stage-6-admin-trust,
    fix/stage-7-cleanup. Built on the SUPERSEDED plan.

The locked plan was: Stage 2 = MULTI-CURRENCY; Event = FAST-FOLLOW (not a launch
stage). The sprint appears to have built Event AS Stage 2 and skipped multi-
currency. Verify, don't assume.

── 0. STATE OF MAIN FIRST (most important) ──
- main's current HEAD commit + `git log --merges -20 main`.
- Which of the named branches (both lanes) are ALREADY merged into main? Is main
  already contaminated, or are these unmerged?
- On main RIGHT NOW, are these PRESENT or ABSENT:
  * the Stage 0 dead-router deletion (bookings-domain.routes.ts gone)
  * the 3 Stage 0 endpoint fixes: /api/cart/resolve-trip, /api/cart/migrate,
    /api/.../convert-to-itinerary
  * the cart ownership (IDOR) checks + custom-venues fix
  * any multi-currency build (see §2)

── 1. BRANCH INVENTORY ──
For every branch in both lanes: HEAD sha, commits ahead/behind main, merge-base
with main, and the set of files each touches. Flag files touched by BOTH lanes.

── 2. PLAN-CONFORMANCE (verify the divergence) ──
- Is Event Coordination built as a launch stage? Confirm the sprint's Stage 2 ==
  Event (Journey 7).
- MULTI-CURRENCY: is the actual BUILD present, or only the leaked
  `preferred_currency` column? Check: does stripe-payment.service.ts hardcode
  currency 'usd', or is it parameterized? Is there display-currency / FX logic in
  cart + payment pages? Report: multi-currency BUILT or MISSING. (Column existing
  ≠ feature built.)

── 3. INTEGRITY / LANDMINE CHECKS (did the sprint reintroduce deleted code) ──
- Grep all sprint branches for `bookings-domain.routes` and
  `temp_bookings-domain` — does the 1,520-line dead router (or a temp copy)
  reappear? Is its import back in server/routes.ts?
- Are the 3 Stage 0 endpoint fixes still intact on the sprint branches, or did a
  branch overwrite routes.ts and drop them?
- Are the cart ownership checks + custom-venues IDOR fix still present, or
  reverted by a parallel routes.ts edit?

── 4. CONFLICT SURFACE ──
List files touched by BOTH lanes with approx line regions — the merge hotspots.
Expect: server/routes.ts, trip-details.tsx, experience-template.tsx,
.github/workflows/*, schema files, migration-files.ts. For each, note what each
lane did so the conflict is legible.

── 5. VERIFICATION REALITY ──
- For each sprint stage's "E2E-X spec written": does the spec file exist, and has
  it ever RUN green in CI (a green Actions run), or is it compile/build only?
- Is E2E-1 (journey-1, flows A AND B) confirmed green ANYWHERE, or never run?
- Note: prod DATABASE_URL is timing out (app can't start) — so confirm whether
  ANY deploy-based E2E could have run at all. State plainly: "complete" =
  compiles, or = verified?
- e2e-journey-2 gate state per branch: if:false / hard / hollow-pass-via-skip?

── 6. REPORT (HARD STOP) ──
Produce:
- State-of-main summary (§0).
- Per-branch classification table: KEEP / CONFLICTS-WITH-LANE-A / WRONG-PLAN /
  REINTRODUCES-DELETED-CODE / UNVERIFIED.
- The multi-currency gap, stated explicitly.
- The conflict map (§4).
- A recommendation on what's canonical and what should NOT merge — but make NO
  changes. Nothing merges until the human decides.

DO NOT: merge, open PRs, resolve conflicts, rebase, force-push, delete branches,
or "fix" anything. Don't trust "stage complete" claims — verify against code.
Read-only. Report only.