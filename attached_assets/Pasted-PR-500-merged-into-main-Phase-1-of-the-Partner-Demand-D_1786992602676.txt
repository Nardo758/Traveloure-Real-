PR #500 merged into main — Phase 1 of the Partner Demand Data lane is landed:

The fabricated expert-surface analytics are deleted (synthetic funnel, responseTime, benchmark targets, CLV, seasonal + fallbacks), each replaced with the honest no_data/empty state; the travelPulse reads left for Item F.
Rulings R1–R9 recorded (2026-08-17-partner-demand-phase0-rulings).
Durable Phase 0 findings committed, with the eleventh-table correction (demand_signal_events) folded in.
The only red was the GitHub comment-API 503 flake on the report jobs — non-blocking, exactly as diagnosed. Watch cancelled, check-in deleted.

What's still open in the lane (not blocked by the merge):

Item A — the Replit session runs r7-db-pass.sql (Q1–Q8) read-only and commits R7_DB_PASS.md + posts the empty-state screenshots.
Item B — once r7-output.txt is back, I write the interpretation + verdict table (incl. the demand_signal_events substrate row).
Leon rules A2(a)/(b) — adopt-as-substrate vs sibling tables (the write-semantics tension).
Phase 2 dispatch — written against the verdict table + that ruling.
Paste r7-output.txt whenever the Replit run is done and I'll pick up Item B.

The merge means the lane's Phase 1 work is on main, so you pull main into Replit.

Pull command (Replit shell)
If checkout -B aborts on a dirty generated file (as it did last time), discard just that file and retry:

Dispatch — "confirm Phase 1 landed"
Unlike the create-flow pull, Phase 1 is removal-only — no migration, no schema change. So there's no column to verify at boot; the only thing a restart does is make the server serve the de-fabricated code. Run these in order.

1. Confirm you're on the merged main

Expect the Phase 1 commit (delete fabricated demand/analytics on expert surfaces), the ledger commit (R1–R9), the Phase 0 findings commit, and the merge of #500.

2. Grep gate — the fabrications are gone (should print nothing):

3. Typecheck — baseline unchanged

4. Restart the server (hard Stop → Run) so it serves the new code.

5. Verify the honest empty states (signed in as an expert — the seeded kyoto-interpreter@… or any expert account):

Performance → Analytics / funnel: the funnel shows only Inquiries → Booked → Completed (no "Profile Views"/"Quote Sent"); the benchmark cards show real values with no "Benchmark: 55%/$350" line and no invented verdict badge.
Performance → Demand / lifetime tab: "No lifetime-value data yet" where the fabricated CLV tiles used to be.
Market intelligence / seasonal: "No seasonal demand data yet" instead of the hardcoded Cherry-Blossom/Summer-Peak rows; an expert whose market doesn't match shows an honestly empty trending list, not a global fallback.
That's the whole landing check — no DB round-trip needed because nothing was added to the schema.

Optional, since you're already in Replit with the DB
This is the natural moment to knock out Item A (it needs exactly this environment):

Then paste r7-output.txt back here and I'll write Item B (the R7 interpretation + verdict table, including the demand_signal_events row). But that's additive — the Phase 1 landing check above stands on its own.