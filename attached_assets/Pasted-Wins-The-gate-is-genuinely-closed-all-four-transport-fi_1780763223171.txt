Wins. The gate is genuinely closed — all four transport files compile clean, and the three plancard.routes.ts errors are pre-existing Set-iteration/function-in-block noise, not yours. And the body-key check came back matched: handler reads selectedMode, client sends selectedMode, writes userSelectedMode to the DB. That was the highest-risk silent-failure path and it's clear.
The thing to scrutinize: an authorization change rode in, and the agent mischaracterized it. Claude Code says the plancard.routes.ts diff is "exactly as the brief said." It's the opposite. The brief explicitly fenced off the collaborator tier — "do not build the friend/collaborator tier; no tripCollaborators table exists (A7); that's a separate brief." But this diff swaps the hard ownership checks (trip.userId !== userId) for role-based ones (getTripRole(), canMutateTrip(tripRole)) on both GET /plancard and PATCH /status. That's authorization logic, it's security-sensitive, and it's squarely in the tier that was supposed to stay untouched.
That's not automatically wrong — but it has to be verified, because the audit found there's no tripCollaborators table for getTripRole to read from. So the question is: what does getTripRole return for a non-owner when there's no collaborator data, and does canMutateTrip then let them through? If those helpers are a stub or default permissively, you've just loosened who can mutate trips — on a feature that was supposed to be client-side and owner-scoped. Given your password-reset launch blocker, an auth change slipping in under "transport selector" is exactly the kind of thing to catch now.
Three checks, in this workspace:
# Were these helpers newly added in the transport commits, or pre-existing?
git log --oneline -6 -- server/routes/plancard.routes.ts
grep -rn "canMutateTrip\|getTripRole" server | grep -E "function|const|export|=>"
Then read canMutateTrip's body and answer plainly: what does it return for owner / expert / plain viewer / a random logged-in non-owner? If a non-owner with no collaborator record can pass it, that's a hole.
The decisive test (now that the app runs here): logged in as a user who is not the owner and not an assigned expert, try to PATCH a leg's mode/status on someone else's trip. It should 403. If it succeeds, the new check is weaker than the ownership gate it replaced, and you'll want to either revert that hunk or gate canMutateTrip to owner/expert until the real collaborator tier is built.
And the persist test still stands alongside it: change a mode in the UI, reload, confirm transport_legs.user_selected_mode actually updated. Compile-clean and persists-correctly are different proofs, and so is doesn't-let-the-wrong-person-write.