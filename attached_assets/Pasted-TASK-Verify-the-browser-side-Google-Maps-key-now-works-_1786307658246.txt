TASK: Verify the browser-side Google Maps key now works across the Workstation surfaces. Read-only — do not modify any code or key settings.

Step 0. Wait until at least 5 minutes after the key change was saved. Open the app in the browser with DevTools console open and keep it open for every check below — the three failure signatures to watch for are RefererNotAllowedMapError, BillingNotEnabledMapError, and any gm_authFailure log. Record the exact preview URL you're testing on (the referrer that must match the allowlist).

1 — Places autocomplete (the previously-degraded inputs). As an expert, open the Workstation landing page. In "Where is this build for?" type Osa (3+ chars). Expect: a Google suggestions dropdown with a "powered by Google" footer. Pick a suggestion. Then open a build and click the destination chip's pencil in the header — same test in the inline editor. Then expand an item in Edit items and type in its Location field — same expectation. If any of these stays a plain input with no dropdown, report which one plus the exact console error.

2 — Workspace plan map. Toggle the plan map open. Report whether it renders the Google map or the Leaflet fallback, and any console errors. (The Leaflet fallback with zero console errors would mean the key loaded but the map branch chose Leaflet — report it as-is, don't force anything.)

3 — Google Places source drawer. In the Add panel, select the Google Places source and search — live result cards should appear (this goes through the server key, which already worked, so it's a regression check, not a fix check).

4 — Error taxonomy if anything fails. RefererNotAllowedMapError → report the page's exact origin/URL from the address bar; the allowlist entry for the replit.dev preview may need to be exactly https://*.replit.dev/* (a bare https://.replit.dev/ doesn't match subdomains). BillingNotEnabledMapError → the key's project still has billing disabled — the referrer fix was necessary but not sufficient; report that distinctly. Neither error but no dropdown → capture the console and report verbatim.

5 — Report. PASS/FAIL per surface with the console state for each, the tested URL, and — if all pass — one screenshot of an autocomplete dropdown open on the Workstation landing field.

One note back on the flagged items: the fixed-port caveat is fine for now (dev is pinned to 5000 in .replit), and I'd take the separate dev-only key when you're ready — it also lets you keep the production key's referrer list tight (just traveloure.com + the .replit.app domain) instead of carrying localhost entries on it.