# W0.8 — Fabricated dashboard/analytics data sweep (§13)

**Tier:** Haiku. **Migration:** none. **Est.:** ~35k tokens. Filed: FOLLOWUPS **K5 / L7**.

## Defect (verified — §13 "never fabricate")

Several provider/expert dashboard + analytics surfaces render **invented numbers as if real**. Replace each
with honest real data, an honest empty state, or remove the panel. This is a mechanical sweep over an
enumerated list — do NOT improvise new metrics; if a real source doesn't exist, show empty/"New" or remove.

## Enumerated targets (fix or remove each; cite the line in your report)

**Expert dashboard** (`client/src/pages/expert/dashboard.tsx`):
- `:224-230` hardcoded `TravelPulseTicker` items ("Tech tours trending +80%…") — remove or wire to a real source.
- `:457` earnings progress bar hardcoded width `65%` — derive or remove the bar.
- The "This Month" label sits over an **all-time** sum (server sums `provider_services.totalRevenue` with no
  month scoping, `server/routes.ts:4410`) — relabel to "Total" OR scope the query to the month. Prefer relabel
  (no server change) unless a month-scoped number is trivially available.

**Provider dashboard** (`client/src/pages/provider/dashboard.tsx`): the mock panels flagged in the mapping pass
— hardcoded ticker (`:104-110`), "My Services" Premium/Standard/Basic list (`:225-245`), "Upcoming 5 Days"
(`:280-304`), Completion 94% (`:364`), Expert Partners numbers (`:382-393`). Replace with real data where an
endpoint exists, else honest empty state.

**Provider analytics** (`client/src/pages/provider/analytics.tsx`): hardcoded change-% deltas (`:77-102`,
"+12%" etc.) — remove or compute from real period-over-period data (remove if no real source).

**Expert analytics** (`client/src/pages/expert/analytics.tsx`): hardcoded `achievements` array (`:220`),
`suggestedPricing` literals (`:239-245`), "+15% vs last month" copy (`:862`), `instantPayoutBalance` dailyFee
`1.5` (`:283`); **dead-render sections** reading fields no endpoint returns —
`recentReviews`/`monthlyMetrics` (`:217-218`) and `overallStats` (`:208`) — either wire to a real source
(service_reviews are real) or remove the section so it doesn't render an empty shell as if data-backed.

**Server-side fabrication** (`server/routes.ts` — the analytics endpoints these consume): the fabricated
funnel (`:4445-4481`, `profileViews = bookings×3.5`, `quoteSent ×0.85`, CLV ×1.8, `repeatRate 35`), the
hardcoded `seasonalDemand` 6-region map (`:4589`), benchmark literals (`:4460`), and revenue-optimization
projections (`:3634`, ×1.15/×1.5). **Stop returning invented fields.** Where the consuming UI section is being
removed above, remove the field from the payload too; where the UI stays, return real data or omit the field.

## Traps

- Do NOT invent a *new* fabrication to replace an old one. Honest empty / "New" / removed — never a fresh guess.
- Where a real aggregate genuinely exists (summary totals, revenueByService, templateRevenue, market trending
  from `travel_pulse_*` tables, cross-sell click stats) — KEEP it; this sweep targets the *invented* values,
  not the real ones. Read the L7 finding for the real-vs-fabricated split before editing each file.
- Coordinate with W0.6: if Instagram/Share content is decided dormant, don't surface it here.
- If any target is ambiguous (unclear whether a real source exists) → log it in the report and leave it, don't
  guess.

## Gate

- Four standard gates.
- Behavioral / grep: a gate that asserts the specific fabricated literals are gone (e.g. grep the touched files
  for `3.5`/`0.85`/`repeatRate`/`65%`/`+12%`/`achievements`-literal and assert absence in the edited scope);
  the pages still render (empty states, not crashes) with a zero-data account.
