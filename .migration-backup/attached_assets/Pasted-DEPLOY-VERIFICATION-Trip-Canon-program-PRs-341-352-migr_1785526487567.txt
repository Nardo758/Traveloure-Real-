DEPLOY VERIFICATION — Trip-Canon program (PRs #341–#352 + migration 158 retirement)

0. Boot health. App loads, no startup errors. If the publish prompted about activity_bookings: confirm you picked "create table". (If the server log is visible: migrations 159/160/161 applied or already-stamped, no failures.)

1. Trip Card routing (the core change). As a traveler who owns a trip with items: open the dashboard Trip Card → each activity should show routing actions — "Send to expert" and "Add to checkout". Click "Add to checkout" → an "In checkout" badge appears and the item shows up in your cart. "Remove from checkout" → badge clears, cart row gone. "Send to expert" → "With your expert" badge. (Expected: instant flips, no errors.)

2. Cart → trip conversion is round-trippable (the defect you found). Add a platform service to the cart → "add to a trip" / convert → the item lands on the trip and can still be routed back to checkout and bought — it's no longer dead text. Items no longer vanish into an unpayable state.

3. Optimize (paid path). From the cart with a trip linked: run Optimize (pay the fee as normal) → variants generate from the trip's items (real days/durations, not everything crammed into day 1). If the trip has a booked/purchased item, no variant should propose replacing or double-booking it. Apply a variant → routed/booked items survive; "apply to cart" now actually adds bookable items (was always 0 before).

4. Optimize with an empty trip. Signed in, items in cart but nothing on the trip → Optimize should show a message prompting you to add your cart to the trip first (and open that dialog), not silently optimize the cart.

5. Share link. Open a trip's share link in an incognito window → the real Trip Card renders (not the old layout), with no prices/budget/booking/routing information visible to the anonymous viewer.

6. Profile. Payment Methods now has an Add card button → opens a real Stripe card form (test card 4242… saves and appears in the list). Travel Preferences chips toggle and persist — select some, Save, reload, still selected.

7. Admin trips dashboard (/admin → Plans/Trips): counts now read Upcoming / Active / Past and match reality by dates (a trip whose dates passed shows "past" even if it was never marked complete).

8. Two-trip context isolation (migration 161): start planning trip A (destination/dates set), then create/plan trip B in another tab → switching back, trip A's planning context is not overwritten by B's.

Report anything that deviates — include what you did, what you expected, and what you saw.