Dispatch 1 — checkout idempotency key (money path)
Context: service_bookings.idempotency_key carries a UNIQUE partial index created by migration 096 and re-asserted by 155. It is not declared in shared/schema.ts, so the publish-time drizzle-kit push drops it, and because both migrations are already stamped runMigrations() will never recreate it. It is load-bearing: the checkout dedup has a SELECT fast-path that two concurrent same-key requests both pass, so the unique index is the only thing that makes the second insert throw 23505 before Stripe is called. We need to declare it in schema.ts to make it durable, but a UNIQUE that meets pre-existing duplicates fails the publish and offers the destructive "copy dev database over production" option — so it cannot be declared blind.

Run against PRODUCTION, then against DEVELOPMENT, and report both separately:

Report: the row count from query 1 (and the rows themselves if any), the verbatim indexdef from query 2 for each environment, and the three numbers from query 3. Do not create, drop, or alter any index, and do not delete any rows — this is read-only reconnaissance. If query 1 returns rows, do not attempt to reconcile them; just report them.

Dispatch 2 — service_demand_requests keep-or-retire
Context: the table exists in the DB (created by migration 080, also present in the 000_baseline_schema.sql dump) but is not declared in shared/schema.ts, and it has zero code references — no reader, no writer, and no other table foreign-keys to it. We have now proven empirically that the publish-time drizzle push drops undeclared tables, not just indexes: with this exact table present-but-undeclared, the push plan emitted ALTER TABLE "service_demand_requests" DISABLE ROW LEVEL SECURITY; followed by DROP TABLE "service_demand_requests" CASCADE;. Migration 080 is already stamped, so once that happens it is never recreated. The recommendation is to let it go — but only if it holds no real data, which cannot be seen from the repo.

Run against PRODUCTION, then against DEVELOPMENT, and report both separately:

Report: the row count per environment, the grouped breakdown if non-empty, and whether the dependency query returns anything. Change nothing — no drop, no insert, no schema edit. If the table does not exist in an environment, say so (SELECT to_regclass('public.service_demand_requests');) rather than treating the error as a failure.

