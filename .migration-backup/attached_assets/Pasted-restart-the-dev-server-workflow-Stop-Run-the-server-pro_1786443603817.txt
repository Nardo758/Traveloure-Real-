restart the dev server workflow (Stop/Run) — the server process needs the new env, and workflows pick up [env] on start. Then verify from any new shell:

If the bucket variable prints, the deliverable rail is live against the real bucket — and the last unprovable piece of R4 becomes provable. Paste this dispatch to your Replit agent for the end-to-end proof:

The bucket env var is now set via .replit [env] (pull first, and restart the dev server workflow so the server process carries it — confirm via env | grep OBJECT_STORAGE and check the server no longer 503s uploads). Run the R4 protected-deliverable rail end to end against the REAL bucket for the first time: (1) as kyoto-interpreter@traveloure.test (TestPass123!), take a pdf-delivery listing (flip one's deliveryMethod to pdf in the dev DB if none exists) and upload a small real PDF via POST /api/provider/services/:id/deliverable-file (raw body, Content-Type: application/pdf) — expect 200, then confirm the row's serviceFile now starts with objstore:; (2) as a traveler with a CONFIRMED booking on that service (create/promote one in the dev DB as the test suites do), GET /api/service-bookings/:id/deliverable — expect streamed PDF bytes (Content-Type: application/pdf, Content-Disposition: attachment), byte-for-byte equal to the upload, and confirm the response contains NO URL and no storage key; (3) confirm exactly one deliverable_downloads row per fetch; (4) confirm an unauthenticated GET against the raw GCS URL for that key (https://storage.googleapis.com/<bucket>/<key>) is REJECTED (403/401) — this is the empirical answer to whether the bucket is private, which ruling 58 recorded as unverified; report the exact status code either way. Clean up fixture rows when done. Report all four results; push nothing unless a fix is required.

Step 4 is the one nothing else could answer: whether the bucket is actually private. Ruling 58 honestly recorded that as unverified-in-sandbox; a real 403 closes it, and anything else means the random-token keys are load-bearing and we should say so in the ledger.

If the grep prints nothing even after the workflow restart, tell me — that would mean workflows don't get [env] (unlike shells), and the fallback is passing the bucket ID through the workflow command instead, which is a one-line change.