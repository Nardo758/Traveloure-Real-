You are a Senior Security Engineer at Stripe.
Test admin-level access control — this goes DEEPER than the 
navbar RBAC test (which confirmed non-admins can't reach /admin/*). 
This tests what happens ONCE someone is inside the admin panel.

TEST 1 — Confirm baseline (should already hold, re-verify):
Non-admin roles (traveler, expert, provider, EA) cannot reach 
ANY /admin/* page directly by URL. Re-run quickly to confirm 
no regression since the navbar RBAC test.

TEST 2 — If admin sub-roles exist:
If there's more than one admin permission level, test that a 
lower-privilege admin cannot access higher-privilege-only 
actions (e.g. a support admin shouldn't be able to change 
platform fee percentages or approve payouts if that's meant 
to be restricted).

TEST 3 — IDOR check (Insecure Direct Object Reference):
For every admin page that shows a specific record by ID 
(e.g. /admin/users/:id, /admin/service-requests/:id):
1. Log in as admin, note a valid record ID
2. Try accessing records that shouldn't exist or belong to 
   edge cases (deleted users, other tenant's data if multi-
   tenant) — confirm proper 404/403, not a crash or data leak
3. Confirm admin API endpoints validate the requester IS an 
   admin on EVERY call, not just page load (test by calling 
   the API directly with a non-admin session)

TEST 4 — Action authorization (not just page access):
Confirm every WRITE action (from Prompt 1's inventory) checks 
admin role server-side, not just client-side button visibility. 
Test: with a non-admin session, try calling the underlying API 
endpoint directly (e.g. POST /api/admin/users/:id/ban) — must 
return 401/403, not succeed.

TEST 5 — Audit trail:
Does the admin panel log WHO did WHAT and WHEN for sensitive 
actions (banning a user, approving a payout, rejecting a 
provider)? If not, flag this as a gap — this matters for 
accountability and dispute resolution.

Report as table: Test | Action attempted | Expected | Actual | Pass/Fail

RESULT: one line — "X/Y PASS — safe/NOT safe to proceed"