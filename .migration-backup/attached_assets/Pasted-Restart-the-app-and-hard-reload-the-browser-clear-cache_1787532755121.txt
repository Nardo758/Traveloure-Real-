Restart the app and hard-reload the browser (clear cache) — this is a client/routing change.

2. Masthead treatment — each surface header is Fraunces serif + emoji + one-line sub, left-aligned:

Visit	Expect masthead
/destinations	📍 Destinations — "Explore destinations & trending cities."
/ready-made	🏅 Ready-Made Trips — "Buy a complete trip…"
/events	📅 Events — "Upcoming events & activities…"
/services	🛎️ Services — "Book tours, photography, transport…"

Each: title in a serif face with the emoji in front; no tab bar; search bar only on /services; no instructional-ad banner.

3. ⚠️ Body-follows-surface (the main fix — MUST test by clicking, not typing URLs).
A full page reload masks the original bug, so navigate in-app:

Start on /services (see the filter bar + service cards).
Click the navbar Marketplace dropdown → Destinations. Confirm the body changes to the trending-cities grid (NOT still showing services). The header AND body must both switch.
Repeat click-through for all four: Marketplace → Ready-Made Trips, → Events, → Services. Each time confirm the body content matches the header (Ready-Made shelf / GlobalCalendar / Services cards respectively).
Bug signature to watch for (should NOT happen): header changes to the new surface but body still shows the previous surface's content.

4. No duplicate headers:

/ready-made: exactly one "Ready-Made Trips" heading (the masthead). The body must not repeat "Ready-Made Trips" with the same "Buy a complete trip…" sentence. (An "Itinerary Templates" section heading below is correct — that's a different section.)
/destinations: one header only (📍 Destinations). The body must not show a stacked "Trending Cities" header under it — the city grid renders directly.
/events and /services: unchanged (no duplicate).

5. Dropdown-only nav + redirects:

Navbar Marketplace is a dropdown; its four items deep-link straight to the routes (no hub, no extra step).
/discover?tab=services&q=tea → /services?q=tea; /discover → /destinations; /service-providers → /services; /global-calendar → /events.

6. Health: no route renders 404/blank; browser console clean on all four surfaces.

Report back: screenshots of the four surfaces after click-through navigation (proving body + header both switched), plus a one-line pass/fail on the duplicate-header and redirect checks.

(Note: the Service-Provider storefront redesi