Phase 2A is verified end-to-end and on `main` (`c32180b2`) — clean completion. And `q9.sql` is finally in-tree, which closes the recurring "the script was never committed" snag for good.

**The verdict matters more than a checkbox: R14's expectation is overturned.**

R14 reset expectations to "likely **zero** qualifying markets today; Kyoto's real count unproven, may approach/fall below 10." Q9 settles it: **Kyoto clears the 10-floor with real trips — n=29** (strict: real account *and* non-authoring), and **n=53** on the looser R7-Q6b framing. Robust either direction. So the floors did exactly their job — and a real market passed honestly. That's the mechanism working in the *affirmative*: **Phase 4's one-pager can legitimately generate for Kyoto** when that phase runs. The pessimistic prior was wrong; the rule that produced it was right.

**Two reads to carry forward:**
- **Migration 241 is validated by data**, not just by hand: `backfill_kyoto == raw_kyoto == 72`. The resolver and its backfill agree with the raw destination text — no drift.
- **The `(unmapped)` bucket is the real headroom**: 145 trips / 90 real — *twice* Kyoto's volume — resolve to no market slug. That's R13's organic expansion-intent signal (the Lisbon/SF/Paris/Barcelona clusters), and it's the largest demand pool on the platform right now. Kyoto is the *only* mapped market with rows; the demand is mostly pointing outside the chosen 8. Worth foregrounding when Phase 4 sizing happens.

**Where the lane stands vs. the HARD STOP:** Leon's gate before Phase 3 needs three things together — 2A/2B evidence + the Q9 verdict + the 2.5 brief. **Q9 is now delivered and in the findings.** Still ahead: the **2B** rollup skeleton (over the one live slip-grain signal, now with `market_slug`, the R9 predicate, and `item_removed` all landed to build on) and the **2.5** presentation-research brief. Both are their own dispatches, and the HARD STOP explicitly wants your review of the assembled set before any Phase 3 work — so I'll hold here rather than start them unprompted.

Nothing pending on my end. Phase 2A: shipped, verified, honest.