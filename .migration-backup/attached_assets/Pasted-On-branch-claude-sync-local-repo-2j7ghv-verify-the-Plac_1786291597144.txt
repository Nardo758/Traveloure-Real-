On branch claude/sync-local-repo-2j7ghv, verify the Places/Advisor/Distribute build
landed in the Replit workspace at <REPLIT-URL>. Branch tip must be
cefc4b8 (commits 289e056, a999134, dae156f, cefc4b8). Read-only verification —
push nothing, fix nothing without reporting first. Ignore PR #232; unrelated.

0. Confirm git rev-parse HEAD, lockfile purity (grep -c replit.local → 0), and that
   boot logs show migration 184_ready_made_plan_type_custom applied.

1. GOOGLE PLACES (the one check only Replit can do — needs the live server key):
   as the expert, Workstation → Add → "Google Places" pill. Search "ramen".
   Expect: live result cards (Google badge, ★ rating, $ band, address), split
   "+ Day N ▾" button adds to a chosen day and the created item carries
   google_place_id; footer says "powered by Google · live search, nothing stored".
   Also confirm the "Platform services" pill no longer shows Google results.

2. ADVISOR: the right-rail tab now reads "Advisor". If the build has an item
   without a location, the routing-blocked card lists it and "Add locations"
   opens that item's editor row. Generate transport legs (Transport legs panel)
   if none exist, then: route-summary card shows per-day km + total, and the
   plan map's "Routes" toggle draws day-colored lines with distance chips.

3. DISTRIBUTE: the listing form says "Trip Theme" (not "Listing title"); the
   plan-type dropdown carries the new categories; picking "Custom…" reveals the
   theme input — saving without it must 400, with 3–80 chars must save. NOTE:
   changing plan type on an APPROVED listing re-enters review by design (§10) —
   use a draft listing for this check or expect the status flip.

4. STORE TEASER: open any approved+active listing's public page /ready-made/:id.
   Expect a route-preview map section between hero and What's-inside: real
   brand-styled geography (Kyoto: river/green edge/grid), unlabeled day-colored
   routes, "Stops unlock with purchase" badge, "© OpenStreetMap contributors".
   Fetch its /api/ready-made/:id/teaser-map.svg twice — byte-identical (seeded
   jitter). Confirm the SVG never appears for a non-approved listing (404).

5. SOCIAL KIT: in a build's Distribute tab, the story rail's frame 2 is
   "THE ROUTE" (numbered dots, no names, OSM line) with cover/highlights/CTA
   unchanged.

6. OPTIONAL (nice-to-have): run
   npx tsx scripts/generate-market-geography.ts kyoto 135.72 34.975 135.80 35.045
   and report whether the Overpass extract succeeds (do NOT commit its output).

Report PASS/FAIL per item with screenshots.