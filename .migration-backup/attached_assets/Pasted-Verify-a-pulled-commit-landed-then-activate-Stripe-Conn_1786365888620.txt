Verify a pulled commit landed, then activate Stripe Connect verification for Service Providers. Read CLAUDE.md first and follow it.

PART 1 — verify the pull landed:
1. Confirm `git log -1 --oneline` on branch claude/sync-local-repo-2j7ghv shows commit a0b6e5b ("provider console fix wave"). If not, stop and report.
2. Run `npm run build` and confirm it passes. Do NOT run bare `npm install`; if any install happens, `grep -c "replit.local" package-lock.json` must print 0 before committing anything (lockfile purity rule in CLAUDE.md).
3. Smoke-check as a service_provider user: /provider/settings shows the shared Stripe Connect card under "Verification & Payouts"; /provider/money shows "Money" as the title and, with zero bookings, "No bookings yet" instead of a 70/30 split; /provider/settings?tab=profile shows real initials (not "GE").

PART 2 — connect Service Providers to Stripe verification:
IMPORTANT: the rail is ALREADY BUILT — do not create new endpoints or a new Stripe flow. POST /api/stripe/connect/onboard, GET /api/stripe/connect/status, GET /api/stripe/connect/dashboard in server/routes/payments.routes.ts (lines ~1212-1327) already accept service_provider via isEarnerRole, create the connected account, and return Stripe-hosted onboarding links with /stripe/connect/return and /stripe/connect/refresh return routes. The client already calls them through <StripeConnectCard />. The work is configuration + end-to-end verification:
1. Check Replit Secrets for STRIPE_SECRET_KEY. It must be a TEST key (sk_test_...). If absent, tell me — I will add it via the Secrets pane. NEVER paste or commit a key into code, chat, or files. Without the key, the endpoints deliberately return a 503 "Payouts onboarding is not yet available" (that is the designed honest degrade, not a bug).
2. With the test key present: log in as a service_provider test account, open /provider/settings, and walk the real flow — status should read not_connected; "Set up payouts / Connect" should return a Stripe-hosted onboarding URL; complete Stripe's TEST-mode onboarding (test data); on return, status should flip away from not_connected and the "Open Stripe dashboard" link should work. If account creation fails with a Stripe error about Connect not being enabled, tell me — Connect must be enabled once in the Stripe dashboard (test mode); do not try to work around it in code.
3. One known rough edge, report-only unless I approve: /stripe/connect/return redirects everyone to /dashboard. For a service_provider the natural landing is /provider/settings. Propose the role-aware redirect but do NOT change routing without my approval (CLAUDE.md routing changes need decision-maker sign-off).
Do not touch payment/booking/money logic (§14/§15), schemas, or migrations. Report: pull verification result, each smoke-check, key present or not, and how far the onboarding walk got.