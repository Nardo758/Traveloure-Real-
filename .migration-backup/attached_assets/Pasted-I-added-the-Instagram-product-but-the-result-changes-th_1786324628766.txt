I added the Instagram product, but the result changes the picture — so here's what actually happened.

The product is added, but it's the wrong variant. Your app now shows "Instagram Graph API" in the Products sidebar alongside Facebook Login. However, its only sub-page is "Business API settings," which contains just use-case descriptions and permission info. There is no Instagram app ID, no Instagram app secret, and no Instagram-specific OAuth redirect URI field anywhere in it. I also tried the direct URL for the newer setup panel (/instagram-business/API-Setup/) and it bounced back to the dashboard, meaning that surface doesn't exist for this app.

What that means. The "Instagram API with Instagram Login" product — the Basic Display replacement with its own app ID/secret and its own OAuth flow — isn't being offered to your app at all. The most likely reason is that your app's type is "None," the unresolved item from earlier; that newer Instagram product is generally surfaced for apps created under the Business type. What you have instead is the Page-linked Instagram Graph API, which authenticates through Facebook Login and a connected Facebook Page rather than through a standalone Instagram login.

The practical upshot is actually simpler than the migration plan in your table. Since your Facebook Login flow already requests instagram_basic and already resolves the user's Page-linked Instagram Business account, you could drop the separate /api/instagram OAuth flow entirely and read Content Studio posts through the same Graph API token you already obtain — /{ig-user-id}/media instead of the old /me/media. That removes the need for INSTAGRAM_APP_ID and INSTAGRAM_APP_SECRET altogether, removes a second consent screen for your experts, and sidesteps the fact that this app can't do Instagram Login. The tradeoff is the one you already documented: experts must have a Facebook Page linked to an Instagram Business or Creator account.

If you'd rather keep the standalone Instagram login, that realistically means a new Meta app created as a Business-type app with the Instagram product — and app creation is something you should do yourself rather than have me do on your behalf.

One other thing I noticed while I was in there: the second violation did not auto-resolve as I expected. Action ID 4705599023001343 is still open and marked "Past due July 20, 2026," with a "Respond now" button, and the API access restricted banner is still up. I haven't opened or responded to it. Want me to pull up its detail so we can see what it's asking for?