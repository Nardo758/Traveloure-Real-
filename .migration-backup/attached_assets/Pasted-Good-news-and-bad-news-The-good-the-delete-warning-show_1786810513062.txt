Good news and bad news. The good: the delete warning shows 10 items in _deprecated_expert_city_queues — matching dev's 10 empty seed rows, so that part is safe. The bad: the publish hit exactly the trap our pre-publish script exists to catch — prod has duplicate event_id rows in fever_event_cache, so the lane-E UNIQUE index can't be created. Checking what that table is before prescribing the fix:

Identified fever_event_cache semantics
fever_event_cache is a genuine refresh-on-demand cache (the Fever service repopulates it per city — the UNIQUE index from migration 221 exists precisely to stop the concurrent-insert duplicates that prod accumulated before the index could land). So the fix is safe and mechanical. Here's the sequence:

1. On the dialog now: keep "Cancel deployment and retry once your schema conflicts are resolved" selected and click Cancel deployment.
⚠️ Do not pick "Copy your development database schema & data to production" — that overwrites all production data with dev data. It's the destructive option our CLAUDE.md explicitly forbids, and it's sitting right there looking helpful.

2. Get the production connection string — it's unavoidable now. Database pane → switch to Production → copy the connection string / DATABASE_URL.

3. Run the preflight script (this is the required pre-publish step from lane E — it reports every duplicate-blocked UNIQUE at once, whereas the publish dies on only the first of the 9 indexes riding this push):

4. Fix the fever duplicates it just flagged (run against prod — Database pane SQL runner in Production mode, or psql "<prod url>" -c "..."):

Any surviving row is fine — it's a cache and the service refreshes it.

5. If the preflight reported other tables with duplicates, paste its output to me and I'll give you the safe dedupe for each (they're not all caches, so don't improvise those).

6. Republish. When the rename question reappears, it's the same answer: "No, create new table" → Submit. The 10-row queue-table delete is confirmed safe.