Stripe Connect is now enabled on the platform account (test mode, Express accounts). Finish connecting Service Providers to Stripe verification. Read CLAUDE.md first.

CONTEXT: the rail is already built — POST /api/stripe/connect/onboard creates an Express account with the transfers capability (server/services/stripe-connect.service.ts) and returns a Stripe-hosted onboarding link; /stripe/connect/return and /refresh are the return routes; GET /api/stripe/connect/status polls Stripe directly and syncs users' stripe_account_status. Do not build new endpoints or add webhooks — status polling is the designed sync mechanism.

1. RE-RUN THE ONBOARDING WALK END-TO-END: log in as a service_provider test account, /provider/settings → Verification & Payouts → connect. Confirm account creation now succeeds (no "Connect not enabled" error). Complete Stripe's TEST-mode onboarding using Stripe's test values (e.g. 000-00-0000 for SSN/ID, test phone/address — Stripe's hosted form suggests them in test mode). On return, confirm GET /api/stripe/connect/status reports details_submitted/charges_enabled/payouts_enabled flipping status to "active", the DB stripe_account_status updates to match, and the "Open Stripe dashboard" login link works.

2. IMPLEMENT THE ROLE-AWARE RETURN REDIRECT (decision-maker approved): in server/routes/payments.routes.ts, /stripe/connect/return currently redirects everyone to /dashboard?stripe=connected. Change it to read the session user's role (shared/roles.ts helpers — isProviderRole / isExpertRole): provider-family → /provider/settings?stripe=connected, expert-family → /expert/settings?stripe=connected, anything else or no session → the existing /dashboard?stripe=connected unchanged. Apply the same role-aware treatment to /stripe/connect/refresh (?stripe=refresh). Touch NOTHING else in that file — no money endpoints (§14/§15). Verify both settings pages render sensibly when landed on with ?stripe=connected (the StripeConnectCard status should now show the new state; no code change needed there unless something is visibly broken — if it is, report, don't improvise).

3. VERIFY + COMMIT: npm run build passes; walk the flow once more after the redirect change to confirm a provider lands back on /provider/settings with the card showing the connected/active state. Do NOT run bare npm install; if the lockfile changed at all, grep -c "replit.local" package-lock.json must print 0. Commit with a clear message noting the return-route change was decision-maker approved (per CLAUDE.md routing rule) and push.

Report: onboarding walk result step by step (account created → onboarding completed → status active → dashboard link), the redirect behavior for provider vs expert, and the commit SHA.