DISPATCH — Post-#319/#320/#321 verification + bug hunt (report-only)

You are verifying deployed work on the production Traveloure app. Read CLAUDE.md first — it is the governing architecture record; §13 (honest data), §14 (server-derived money/identity), §17 (console IA program) define what "correct" means below.

Hard rules:

REPORT-ONLY. Do NOT commit, push, or edit any code from the workspace. Findings come back as a report; fixes are made on the GitHub side.
No destructive actions. Never accept Replit's "copy dev database over production" prompt under any circumstances. No schema changes, no npm install/npm audit fix (lockfile-pollution trap — npm ci only).
Evidence standard: every PASS/FAIL claim carries evidence — a curl output, SQL result, or screenshot. No inference; if you couldn't verify, say NOT VERIFIED, never PASS.
Test account policy: the seeded @traveloure.test accounts are deliberately purged from prod and must NOT be re-seeded. If a check needs an authenticated expert session, create ONE throwaway account via normal signup with a strong random password, grant it travel_expert role directly in the DB, and delete it (and its sessions) when done — record the email and the cleanup in the report.
Severity: P0 = security/money/data-loss · P1 = a shipped feature is broken in prod · P2 = degraded/cosmetic. Cite route + file for each finding.
§0 — Deploy freshness. Workspace at 61616cb5 (merge of #321) or later; npm ci clean; npm audit reports 0 vulnerabilities; republish completed. Confirm the deployed app is serving the new tree (the expert sidebar check in §3 is the live proof, since /api/version still reports sha:"dev" — known filed issue).

§1 — SECURITY P0 (test accounts). On the prod DB:

SELECT count(*) FROM users WHERE email LIKE '%@traveloure.test'; → expect 0.
SELECT email, role FROM users WHERE email LIKE '%test%' AND email NOT LIKE '%@traveloure.test'; → report every row. Specifically hunt for expert_test_001@… (from your previous report — it doesn't match the purge pattern; we need its full address, role, and whether it still authenticates).
Attempt login with test-admin@traveloure.test / the old seed password → expect 401.
Confirm the deployment env does NOT set ALLOW_TEST_ACCOUNTS.
§2 — SECURITY P1 (Direct channel / static ordering). Against the deployed URL, all with curl:

/ready-made/<a real approved listing id> → HTML contains the listing's real og:title (not the generic SPA title).
/services/<a real approved service id> and /p/<a claimed handle, if any> → same OG check.
/r/<a real short code, if any exist> → 302 redirect to the target.
/discover → 200 SPA; /api/service-offering-types → JSON (not HTML).
§3 — Nine-module console (§17, PR-Cb). Logged in as the throwaway expert, screenshot the sidebar. Expected EXACTLY: Today · Calendar · Inbox · Workstation | Catalog · Content Studio · Customers · Performance · Money | AI Assistant · Settings (11 entries; no Bookings, Assigned Trips, Messages, DMO Library, Analytics, Earnings, Profile). Warm console palette only — no #FF385C / rgb(255,56,92) on any console element.

§4 — C5 Inbox absorption. /expert/inbox has four tabs: Queue (default) · Assigned Trips · History · Messages, with ?tab= deep-links. Verify: /expert/bookings → redirects to /expert/inbox?tab=history; /expert/assigned-trips → ?tab=assignments; /expert/clients → /expert/customers. History tab renders real traveler names (or honest empty state — never "N/A" placeholders); Messages tab is a threads queue linking into /chat (empty state links there too). On the Workstation, an ASSIGNMENT trip's Distribute→Client card carries a collapsed "Suggest to client" panel (authored builds must NOT show it).

§5 — C6 Analytics fold. /expert/analytics → redirects to /expert/performance?tab=analytics and the full 9-tab analytics UI renders inside the Performance page (one sidebar, not nested shells). /expert/leaderboard → lands on the leaderboard sub-tab via &sub=leaderboard; same for /expert/revenue-optimization.

§6 — C7 DMO drawer. In a Workstation build's Add panel, the DMO source drawer rows carry Add, Refine (inline name/description/tags editor that saves), and — inside the open refine editor — Create social post (navigates to Content Studio prefilled). /expert/dmo-library → redirects to /expert/workspace. Confirm a saved refinement lands in expert_dmo_edits (SQL).

§7 — C8 renames/merges. /expert/money renders the Money page; /expert/earnings redirects to it. /expert/settings opens on the Verification tab with Profile as the first tab in order; /expert/profile → redirects to /expert/settings?tab=profile and the profile content renders inside Settings (no double sidebar/inset).

§8 — Calendar + Customers honesty (§13). /expert/calendar and /expert/customers render with real data or honest empty states — no fabricated events, counts, or CRM fields. Anonymous curl of /api/me/calendar and /api/me/customers → 401.

§9 — Traveler-surface regression sweep. /discover tabs, a service detail page, add-to-cart, and /my-bookings all render without JS console errors. The traveler surface keeps the #FF385C palette (the two-palette design — console warm, traveler brand-red).

§10 — Adversarial free pass. Beyond the checklist: hunt for anything broken the way a hostile QA would — dead buttons, 404/blank pages behind the new redirects, browser-console errors on each of the 11 console entries, links still pointing at retired routes, and any place the UI shows data the DB doesn't back (§13). Report everything with severity + route + evidence.

Report format: summary table (§ / check / PASS-FAIL-NOT VERIFIED) → P-findings ranked with evidence → the §1 %test% sweep results verbatim → cleanup confirmation (throwaway account deleted, sessions cleared).

