TASK: Restart the app server and verify the teaser-map endpoint end-to-end.
This is an OPERATIONAL fix + verification only — do NOT modify, create, or delete
any files, do NOT commit, do NOT install anything. The code is correct; the running
Node process predates it (started 14:00, code committed 16:21). If any check below
fails, STOP and report the raw output — do not attempt a code fix.

1. RESTART CLEANLY:
   - pkill -f "tsx server/index" (kill any lingering process)
   - Restart the "Project" workflow (the normal Run flow: npm run dev).
   - Wait until the app answers: until curl -s http://localhost:5000/api/version
     >/dev/null; do sleep 2; done

2. PROVE THE PROCESS IS FRESH:
   - ps -o lstart= -p $(pgrep -f "tsx server/index" | head -1)
     → must be a time AFTER the restart you just did.
   - In the boot log, find the migrations line — expect it to say 184 is already
     recorded (ledger 185/185, 0 newly applied; it was hand-applied earlier).

3. VERIFY THE ENDPOINT (the check that failed against the stale process):
   - curl -s -o /tmp/teaser.svg -w "%{http_code} %{content_type}\n" \
     "http://localhost:5000/api/ready-made/323f95ee-71de-4c36-a56c-82e8a373c899/teaser-map.svg"
     → expect: 200 image/svg+xml
   - head -c 60 /tmp/teaser.svg → expect it to start with <svg
   - Fetch it a second time and md5sum both → byte-identical (deterministic jitter).
   - curl -s -w "\n%{http_code}\n" "http://localhost:5000/api/ready-made/nonexistent-id/teaser-map.svg"
     → expect: 404 with a JSON {"message":...} body (NOT html — this proves the
     route is live and its visibility gate works).

4. VERIFY THE PUBLIC PAGE:
   - Open /ready-made/323f95ee-71de-4c36-a56c-82e8a373c899 in the webview/browser.
   - Expect: a route-preview map section between the hero and "What's inside" —
     brand-styled map with unlabeled day-colored route lines and dots, a
     "Stops unlock with purchase" badge, and a "Traveloure map · © OpenStreetMap
     contributors" line. Screenshot it.

5. REPORT: PASS/FAIL per step with the exact command outputs and the screenshot.
   Remember: any failure = report only, no code changes.