Dispatch — "confirm it landed"
Run these in order. The gate that matters is step 3: the seating column only exists after the migration runs at server boot.

1. Confirm you're on the branch with all 7 commits

Expect (newest first): the ledger row, the Logistics map-first reorder, the Seating field, the Capacity layout, the Basics-fidelity commit, plus the two earlier ones.

2. Restart the server (Stop → Run, or your restart control). This is what runs runMigrations().

3. Confirm migration 239 applied — in the server startup log, look for:

If you'd rather check the DB directly:

Expect a seating | character varying(16) row. If this is missing, the migration didn't run — do not proceed; re-restart and re-check the boot log.

4. Verify the create flow visually (/provider/services/new and an existing listing's …/edit?step=capacity / ?step=logistics):

Basics — offering + "Name it" side by side; Price + "One line about it" side by side; "Save draft" button present; counter reads "…asks for 140+…".
Capacity — Party size [min] to [max] inline, and a Seating dropdown beside it.
Logistics — leads with the framing notes + the map canvas (map-first), Meeting Location form below.
5. Prove the Seating round-trip (this is the piece I couldn't test from my session):

On a listing, set Seating → Shared, save.
Open the traveler page for that listing → the "Good to know" card should show "Seating: Shared — several parties may be seated together".
Set it back to "Not specified", save → that row should disappear (never a guessed default).
6. Sanity — nothing regressed

Notes
This does not open a PR. It puts the work on the branch in your Replit. Since #496 already merged, landing to main needs a new PR from claude/resume-artifact-work-vmvy96 — tell me and I'll open it.
Migration 239 is additive-nullable with no CHECK, so there's no publish-time CHECK trap and no risk of the destructive "copy dev over prod" prompt.
If the restart doesn't pick up the new column (step 3 fails), the most common cause is the server process not fully restarting — a hard Stop/Run, not just a hot reload, is what re-runs migrations.