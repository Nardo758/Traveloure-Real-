# DECISIONS.md — the decision ledger

**Rules (ruling 26):** append-only. One line per ruling: number, date, tag, decision, amends-pointer if any, context link. Amendments are NEW entries pointing back — never edits. When any spec/brief and this ledger disagree, the ledger wins; code is ground truth over every document; a ledger-vs-code disagreement is a finding, never a silent divergence. Briefs cite rulings by number — they may quote verbatim with the number, never paraphrase.
**Numbering (ruling 35 housekeeping, 2026-08-05):** rulings issued in conversation are PROVISIONAL until appended here; the appender claims the next free number at write time. The ledger is the number authority — a conversation-assigned number that collides yields to the ledger (e.g. the born-approved ruling was issued as 34 in conversation, collided with the Amadeus drop, and is filed as 35).
**Tags (ruling 26 §3, sharpened by 27):** `[guarded: <check-name>]` = enforced by a machine check that **runs in CI on every push/merge** (a script not wired into CI is not a guard — it is MISSING). `[advisory]` = judgment/process ruling, ledger only. `deferred:<lane>` marks a guard owed by a named lane; the ledger lint warns until the lane merges, then fails.

## Numeric series (open — all new rulings append here)

| # | Date | Tag | Ruling | Context |
|---|------|-----|--------|---------|
| 1 | 2026-08-02 | [advisory] | PR #348 merged (trip_contexts re-key), honest backfill. | SLIP_EXPERIENCE_DISPATCH.md |
| 2 | 2026-08-02 | [advisory] | Lane 3 = Option B: date-derived admin phase; trips.status deprecated-in-place, future owner = Phase 4 brief. | L3-trips-status-brief.md |
| 3 | 2026-08-02 | [advisory] | Optimizer read scope: optimizable = in_planning + ready_for_checkout; purchased = read-only constraints; with_expert excluded. | SLIP_EXPERIENCE_DISPATCH.md |
| 4 | 2026-08-02 | [advisory] | Drop policy (a): variants may not drop ready_for_checkout items; policy (b) documented future refinement. | SLIP_EXPERIENCE_DISPATCH.md |
| 5 | 2026-08-02 | [advisory] | Guest cart-fallback until G2; gates strictly on guest-ness; retirement is a G2 exit criterion. | SLIP_EXPERIENCE_DISPATCH.md |
| 6 | 2026-08-02 | [advisory] | Slip identity + transition log approved as Lane S (post reconcile Phase 1b). | SLIP_EXPERIENCE_DISPATCH.md |
| 7 | 2026-08-02 | [advisory] | Optimizer contract-row amendment ships with Lane 6 code. | SLIP_EXPERIENCE_DISPATCH.md |
| 8 | 2026-08-02 | [advisory] | Four-tab mockup ratified as the Spec A/B/C build reference. | SLIP_EXPERIENCE_DISPATCH.md |
| 9 | 2026-08-02 | [advisory] | Already-built inventory binding; agents build only remaining scope; Lane 6 = residue (rejection check, transactional apply, variant discard). | SLIP_EXPERIENCE_DISPATCH.md |
| 10 | 2026-08-02 | [guarded: check-trip-mint-owner-access] | Tracking number: reuse trips.trackingNumber; derivation retired; close NULL mint paths; tripwire. | SLIP_EXPERIENCE_DISPATCH.md |
| 11 | 2026-08-02 | [advisory] | Diary: new append-only item_transition_log; itinerary_changes keeps display semantics; variant_applied single-writer moves; feed derivation = named follow-up. | SLIP_EXPERIENCE_DISPATCH.md |
| 12 | 2026-08-02 | [advisory] | Same-transaction log writes for all status transitions incl. money-path; cosmetic events best-effort. (Amended by 18.) | SLIP_EXPERIENCE_DISPATCH.md |
| 13 | 2026-08-02 | [advisory] | Role-hygiene lane first; mint-path tripwire now, getTripRole architectural fix = named follow-up; jsonb class-B remediation precedes any workspace consumption. | SLIP_EXPERIENCE_DISPATCH.md |
| 14 | 2026-08-02 | [advisory] | Apply discards unshared losing variants only; selected variant + metrics retained (sanctioned copy, equals slip by construction); losing-variant-share "outdated proposal" treatment = named follow-up. | SLIP_EXPERIENCE_DISPATCH.md |
| 15 | 2026-08-02 | [advisory] | In-checkout rejection check uses the two-key predicate (providerServiceId ∥ title), FAIL-CLOSED (unmatched ⇒ reject); originalItemId column = follow-up only if false rejections occur. | SLIP_EXPERIENCE_DISPATCH.md |
| 16 | 2026-08-02 | [advisory] | Trip-scoped events log as trip-level rows (itemId NULL); grain follows the event. | SLIP_EXPERIENCE_DISPATCH.md |
| 17 | 2026-08-02 | [guarded: check-trip-mint-owner-access] | Tracking numbers minted on ALL 5 mint paths incl. NULL-owner authoring builds; no exemptions; tripwire covers. | SLIP_EXPERIENCE_DISPATCH.md |
| 18 | 2026-08-02 | [advisory] | Amends 12: flip+log atomic pair inside helper-internal transaction; outer money-path swallow retained; swallowed failure MUST alarm (ops-visible error w/ ids) + Lane S ships the bookings-vs-purchased reconciliation health-check. | SLIP_EXPERIENCE_DISPATCH.md |
| 19 | 2026-08-02 | [advisory] | varchar(20) tracking headroom = named follow-up (own verified ALTER, not opportunistic); migration numbering re-verified at merge (chain-integrity test is arbiter); traveler delete-optimize-row loss accepted per 11's follow-up. | SLIP_EXPERIENCE_DISPATCH.md |
| 20 | 2026-08-04 | [advisory] | Ground-truthed fixed flags become expected-PASS regression assertions citing fixing commit. | console-sigma lane review |
| 21 | 2026-08-04 | [guarded: matrix-lint] | Expected-fail ABSENCE rows carry an expiry tag (deferred:<lane>) and MUST flip to expected-PASS in the wave their lane merges. | console-sigma lane review |
| 22 | 2026-08-04 | [advisory] | Console reorder heuristic = second-query-engine class; assert divergence now; remediation (reorder consumes the optimizer's constraint service) = named follow-up, not harness scope. | console-sigma lane review |
| 23 | 2026-08-04 | [advisory] | Kyoto expert fixture seeded in Phase 2 via full approval lifecycle; tests never bypass the Kyoto submit gate; fixture-bench gaps feed the journey suite's role/fixture inventory. | console-sigma lane review |
| 24 | 2026-08-04 | [advisory] | Harness v1 = workspace machine only; per-item routing layer enters as deferred:phase-4 cells. | console-sigma lane review |
| 25 | 2026-08-04 | [advisory] | Client-computed next-status = client-trusted-input class; server owns transition derivation; expected-fail now, inversion = small named follow-up. Expert-favorable 0.75 fallback moves into fee_bands as a default row (safety net lives in the single source of truth); files to the standing fee-literal follow-up. | console-sigma lane review |
| 26 | 2026-08-04 | [guarded: check-decision-guards] | Execution Protocol adopted: durable/volatile rule, ledger authority, guard classification, merge write-back checklist, thin CLAUDE.md. | Execution Protocol dispatch |
| 27 | 2026-08-04 | [guarded: phase2-fee-gate, chain-integrity] | Guard gaps: wire, don't defer, for the two that exist — fee-literal grep and chain-integrity wired into CI in the protocol lane. Matrix lint = journey-suite lane deliverable #1 (see 21's deferred tag). Env allowlist remains a named candidate. Sharpened definition: guard = runs in CI on every push/merge; script-only = MISSING. | Execution Protocol Phase 0 review |
| 28 | 2026-08-04 | [advisory] | R-A..R-J folded into this ledger as a closed "Console Realign series" block under their existing letters; no renumbering; all future rulings use the numeric sequence only; the brief carries a pointer line. | Execution Protocol Phase 0 review |
| 29 | 2026-08-04 | [advisory] | CLAUDE.md findings-class content moves to one chronological archive doc (docs/findings/CLAUDE_MD_ARCHIVE.md), every entry with its as-of SHA; where a live audit doc owns a topic, the archive entry points to it; migration post-mortems stay intact in the archive. | Execution Protocol Phase 0 review |
| 30 | 2026-08-04 | [advisory] | PR template = the §4 merge write-back checklist and nothing else; any future section addition requires a ruling. | Execution Protocol Phase 0 review |
| 31 | 2026-08-04 | [advisory] | Old-but-determinable provenance gets the real SHA plus an explicit STALE flag (e.g. audited@0b47ea44 2026-06-03 — STALE, re-verify before use); audited@unknown is reserved for genuinely indeterminable provenance. | Execution Protocol Phase 0 review |
| 32 | 2026-08-04 | [advisory] | Fee-band scope reclassification: the EXPERIENCE_CART 0.30 literal was display/diagnostic-only (calculateCommission is the DB-free breakdown; real cart charges resolve per-item via resolveCommissionRates) — it matched no actual charged rate. Rule going forward: any move of a fee literal into fee_bands must state which surface the band controls (band-key constant + migration header) and ship a DB-backed test proving an admin band edit changes the resolved value and a missing/inactive band fails loudly. Supersedes ruling 25's implicit framing of 0.30 as the canonical checkout rate; migration 174 (experience_cart_checkout band + requireExperienceCartRate) implements it. | fee_bands migration completion review |
| 33 | 2026-08-05 | [advisory] | Launch gate RATIFIED (mdixon5030): "J1 green end-to-end on staging" is the Kyoto beta gate — the golden path (traveler builds → optimizes → routes to a real Kyoto expert → notes back → Stripe purchase → Trip Card) passing with database-fact assertions defines a launchable product. Critical path: Lane S → slip-dispatch phase 4 (surfaces + Get Expert Help remediation) → J1 green → beta. Everything off that path (afterlife lanes, feed derivation, bespoke-gate consolidation, most of Waves 2–4) is explicitly post-beta hardening. | Board recommendation, ratified in chat 2026-08-05 |
| 34 | 2026-08-05 | [advisory] | Amadeus DROPPED (mdixon5030): Self-Service upstream decommission killed test.api.amadeus.com (DNS-dead since ~2026-07-17) and stored credentials 401 on the replacement travel.api.amadeus.com. amadeus.service.ts short-circuits every method to empty results (no network, no health noise); provider-health entry marked dropped (isConfigured=false). Revival requires new credentials AND a new ruling. | Amadeus decommission finding (memory 2026-08-04), ruled in chat 2026-08-05 |
| 35 | 2026-08-05 | [advisory] | Two-layer born-approved enforcement for approval-lifecycle tables (provider services, ready-mades, applications): Layer 1 = CI schema gate failing born-approved insert paths/seeds; Layer 2 = DB initial-status DEFAULT + constraint rejecting born-approved inserts. Approval is always a transition with status history; journey-suite negative N11 cites both layers. Enforcement task: pending (to be filed by amendment — task-proposal throttled at append time). Renumbered from the collided conversation-number 34; Amadeus keeps 34 per the numbering rule above. | journey-suite Wave 1 dispatch |
| 36 | 2026-08-05 | [advisory] | Amends 35: the born-approved two-layer enforcement work is filed as project task #1042. | journey-suite Wave 1 dispatch |
| 37 | 2026-08-05 | [guarded: matrix-lint] | **Phase/wave names are NAMESPACED to their lane.** Write `Journey Wave 1`, `QA Punch List Wave 1`, `Backoffice Wave R` — never a bare `Wave 1`. WHY (the real cost, not a style preference): "Wave 1" already named at least two live things — the journey suite's first coverage wave (brief §7) and the QA Punch List's Wave 1 (`docs/planning/QA_PUNCH_LIST.md`, Aug 1, PRs #363/#364/#365) — and the journey-suite squash `d45fcd0f` (PR #421, titled "Journey Suite Wave 1") ALSO carried the Amadeus decommission of ruling 34, so the plain question "is Wave 1 done?" had no determinable answer from the repo. Applies to briefs, matrices, workflow names, commit subjects and spec comments. ENFORCED: `scripts/check-coverage-matrix.cjs` rule 5 (already CI-wired via `.github/workflows/build.yml`) fails on an unqualified `Wave <N>` in the journey lane's files; a line that deliberately QUOTES the ambiguous form carries `wave-name-ok`. Extend `NAMESPACE_SCAN_FILES` when a lane adopts the rule for its own docs. | Journey Wave 1 verification lane, 2026-08-05 |
| 38 | 2026-08-06 | [advisory] | **Checkout atomicity = CLAIM → AUTHORIZE → PROMOTE, reclaim-by-TTL.** `POST /api/checkout` committed every irreversible effect (booking rows, cart clear, `purchased` flip + diary row, provider notification + EMAIL) BEFORE `paymentIntents.create`, the one operation that can fail; on failure it answered a bare 500 and the same key short-circuited to `{duplicate:true}` which the cart page rendered as "Booking created!" — a checkout with no PaymentIntent reported as success, unrecoverable because with no PI there is never a webhook. **The claim ORDER IS LOAD-BEARING and stays** (§15; measured: 3 concurrent same-key checkouts = 3 real Stripe charges without the `service_bookings.idempotency_key` claim, 1 with it — commit `c3a0be03`), so option 1 ("write nothing until the PI exists") was REJECTED as a §15 deletion, and option 2 (compensating rollback) was REJECTED because rollback code runs in exactly the conditions that broke the operation. Ratified shape: the claim stays pre-Stripe but PROVISIONAL, and every commitment moves behind the authorization. **No new state:** `status='payment_pending' AND stripe_payment_intent_id IS NULL` is already an unauthorized claim by construction and consumers already key on that column — no migration, no enum value, no publish-push trap. TTL = **30 min**, reclaimed by an idempotent, race-safe, **logged** sweep (`checkout_claim_expired` diary row per void, rulings 12/16/18). Race safety = atomic conditional `WHERE status='payment_pending' AND stripe_payment_intent_id IS NULL` on BOTH the void and the authorization stamp, so a promote and a void can never both win. **A paid booking can never be voided**, via two layers: (1) a pre-flight `bookingDetails.stripeAttemptAt` marker written before the Stripe call — an unmarked row provably never reached Stripe and is voided with no network call; (2) marked rows are NEVER auto-voided, only reconciled against Stripe (`paymentIntents.list` on the `bookingIds` metadata) — found ⇒ PROMOTED, definitively-absent ⇒ voided, unreachable ⇒ QUARANTINED. Both legacy-table reconciliation paths (`handlePaymentSucceeded`, `POST /api/bookings/confirm-payment`) are inert for cart checkout and are filed as tasks #212/#213 — the sweep is designed assuming they stay broken. Option 3 (traveler-facing resume surface) stays a later enhancement. Journey J1/J2's payment leg flips from "declared unavailable ⇒ assert the committed rows anyway" (which encoded the defect) to the truthful pair `assertCheckoutAccepted` / `assertCheckoutCommittedNothing`; permanent Tier-3 negative **N16** + `checkout-claim-sweep.db.test.ts` (9 behavioural proofs). | checkout-atomicity lane, ratified in chat 2026-08-06 |
| 39 | 2026-08-06 | [advisory] | **Executes 38's filed tasks #212/#213 — money-path redundancy restored: ONE payment promotion, TWO callers.** Ruling 38 recorded that BOTH documented reconciliation paths were inert for cart checkout and designed the TTL sweep "assuming they stay broken"; that left the sweep as the **only** recovery mechanism for a crashed-mid-authorization checkout. Root cause confirmed exactly as filed: `handlePaymentSucceeded` (`stripe-payment.service.ts:564`) and `POST /api/bookings/confirm-payment` (`bookings.ts:125` → `booking.service.ts:592`) both query the LEGACY `bookings` table with `service_bookings` ids — disjoint id spaces, zero rows, no error. **New finding (refines, does not contradict, 38):** the webhook ROUTE was not wholly inert — `webhooks.routes.ts` carried an inline "Step 2" `UPDATE service_bookings … WHERE stripe_payment_intent_id = <pi> AND status='payment_pending'`, a SECOND divergent promotion with no diary row, no actor, no plan catch-up, and — keyed exclusively on a stamped PI id — no help in the one window only the webhook can rescue. 38's framing still holds: that path cannot see a claim whose PI was never stamped. **Shipped:** `promotePaidCheckout` (step 5 of `checkout-claim.service.ts`), driven by BOTH callers plus the route's Step 2 (deleted). Idempotency = the atomic conditional `WHERE status='payment_pending' AND stripe_payment_intent_id=<pi>` (the §15 discipline, never check-then-update), so a double signal is exactly ONE flip and ONE diary row. **Webhook-only capability:** only a signature-verified delivery may resolve bookings from `pi.metadata.bookingIds` and stamp a PI onto an unstamped claim (via the sweep's own `stampAuthorization`) — a client-supplied PI never can. **Late signal vs voided row:** never a resurrection (void wins after TTL) — it lands in a **reconciliation-exception** state, recorded as a DB fact (`bookingDetails.reconciliationException` + `checkout_reconcile_exception` diary row + `logger.error`) and surfaced at `GET /api/admin/bookings/reconciliation-exceptions`. Diary vocabulary gains `actorType='webhook'` and two event types (code-only — migration 171 deliberately carries no CHECK); **no migration, no schema change, no publish-push trap.** The legacy `bookings` rail stays live and untouched (still reached via `/booking-demo` and `/itinerary-comparison/:id` → `POST /api/bookings/process-cart`) — retiring it is a separate, unfiled question, NOT executed here. Permanent Tier-3 negatives **N17/N18/N19** (`checkout-payment-promotion.db.test.ts`, 10 proofs); N16 and the sweep's 9/9 suite unchanged and green; CLAUDE.md **§15c**. | legacy-reconciliation lane, 2026-08-06 |
| 40 | 2026-08-06 | [advisory] | **DETECTION made two-eyed: one drift job, both rails — and it DETECTS, it does not repair.** Rulings 38/39 made RECOVERY three-layered (TTL sweep · webhook · client confirm, one shared promotion), but DETECTION stayed one-eyed: `server/jobs/stripeReconciliation.ts` scanned ONLY the legacy `bookings` table, so **cart-checkout charges — the primary checkout — were invisible to the daily Stripe-vs-DB drift job**, matching zero rows without erroring (the same disjoint-id-space failure ruling 39 fixed on the promotion side). Nothing in the platform told anyone that money and the database disagreed about a cart purchase. **Shipped:** one job scanning BOTH rails with the id linkage that already exists — `service_bookings.stripe_payment_intent_id`, `pi.metadata.bookingIds`, the `idempotency_key` sibling convention, `refunds.stripe_refund_id`, and the legacy `charge.metadata.bookingId` — **no new writes anywhere on the checkout path**. Nine classifications (seven cart, the two legacy ones preserved verbatim in behaviour): `pi_succeeded_no_booking`, `pi_succeeded_claim_provisional`, `pi_succeeded_booking_voided`, `booking_confirmed_no_pi`, `booking_confirmed_pi_not_succeeded`, `amount_mismatch`, `refund_not_reversed`, `stripe_charge_no_booking`, `booking_no_stripe_charge`. **Ops surface = PERSISTED ROWS, not log lines** (migration 177, declared in `shared/schema.ts` same commit per the deploy-push rule): `reconciliation_exceptions` is APPEND-ONLY with a UNIQUE `dedupe_key` + `ON CONFLICT DO NOTHING`, so a month-long drift is ONE row stamped with the run that first saw it while the run row separately records detected-vs-new; `reconciliation_runs` carries **one row per pass INCLUDING a clean or skipped one**, because "no drift today" and "the scheduler died three weeks ago" were previously the same picture. Surfaced at `GET /api/admin/reconciliation/exceptions` + `/runs` on the existing `/admin/reconciliation` page — a **SIBLING** of ruling 39's `GET /api/admin/bookings/reconciliation-exceptions`, not a replacement: that one can only describe a row that exists, this one can describe money with no row behind it. **DETECT, DON'T REPAIR is the governing rule** — the job never voids, refunds, cancels or invents a booking; repair stays with the three recovery layers plus a human. **ONE narrow exception:** a PI-succeeded / still-provisional claim is handed to the EXISTING shared `promotePaidCheckout` with `actor="reconciliation"`, diary-logged — recovery layer 2's own logic arriving late, not new repair code. **AMENDS 39 on one clause:** its ordering-1 capability (resolve bookings from `pi.metadata.bookingIds` and stamp a PI onto an unstamped claim) was written "webhook only" because the signed delivery was then the only server-verified Stripe source; it is now gated on `SERVER_VERIFIED_ACTORS` = {webhook, reconciliation}, since the drift job reads the PaymentIntent from the Stripe API with the platform's OWN secret key — the same authority, arguably stronger (a pull, not a push). **The clause that does NOT move: a CLIENT-supplied PaymentIntent may never resolve or stamp anything** (still proven by N17c). The expected charge amount is SERVER-DERIVED as `SUM(total_amount + platform_fee)` over the PI's rows (§14) with a rounding tolerance that is the checkout's own arithmetic, not a rate — no fee literal. Legacy-scan disposition: the legacy rail is **still live** and its scan is **kept**, per §15c. Permanent Tier-3 negatives **N20/N21/N22** (`reconciliation-detection.db.test.ts`, 15 proofs); the sweep's 9/9 and the promotion suite's 11/11 are untouched and green; CLAUDE.md **§17**. | reconciliation-detection lane, 2026-08-06 |
| 41 | 2026-08-06 | [advisory] | **Ratifies 40, and states its invariant as PROVENANCE, not transport.** Ruling 39 wrote the ordering-1 capability (resolve bookings from `pi.metadata.bookingIds` and stamp a PI onto an unstamped claim) as "webhook only"; ruling 40 opened it to `SERVER_VERIFIED_ACTORS` = {webhook, reconciliation}. The generalization this ratification records, so the clause is never re-litigated actor by actor: **what gates the capability is the PROVENANCE of the PaymentIntent id — that the platform itself obtained it from Stripe as a verified actor — NOT the transport that carried it.** A signature-verified webhook delivery (a push) and the drift job's authenticated read with the platform's own secret key (a pull) are the same authority; the transport differs and is irrelevant. Two consequences follow: a future server-verified actor joins `SERVER_VERIFIED_ACTORS` without amending the principle, and no actor qualifies merely by arriving over a trusted-looking channel. **The clause that does NOT move, and is the whole reason for framing it this way: a CLIENT-supplied PaymentIntent id may never resolve or stamp anything** — a client id has no server provenance regardless of how it arrives, including over an authenticated session. Already proven by the existing Tier-3 negative **`N17c: a CLIENT may not stamp a PaymentIntent onto an unstamped claim`** (`server/__tests__/checkout-payment-promotion.db.test.ts:280`, asserting `promoted.length === 0`), verified present at `d4f59bb7`; **no new negative was built** — the client half of the invariant already carries its permanent proof. CLAUDE.md **§17b**. | provider-sigma lane Phase 0, rider (a) |
| 42 | 2026-08-06 | [guarded: check-money-endpoints, phase2-fee-gate] | **Provider money hardening — a RATE is client-trusted input, a PROVISIONAL claim is not the owner's to accept, and a consumerless irreversible endpoint is DELETED.** Executes the three findings the provider-sigma Phase-0 audit rated CRITICAL/HIGH (`docs/testing/PROVIDER_SIGMA_AUDIT.md`, audited@`d4f59bb7`; all three re-verified as STILL HOLDING at `9382d500`). **(1) MI-1 — rates resolve from `fee_bands` ONLY, and a rate-bearing field is never client-settable on ANY schema.** `provider_services.revenueShareRate` was exposed by `insertProviderServiceSchema`, parsed straight off `req.body` by POST **and** PATCH `/api/provider/services`, spread into the row, and read at `payments.routes.ts` as *"the final override (takes priority over config)"* over the `fee_bands`-resolved split at the real Stripe charge — a client-supplied rate reaching a payment decision (§14 in substance, §8 in spirit; the clamp was range-only, so `1.00` ⇒ a 0.00 platform fee). The audit found it stripped on NEITHER create nor update. **Required shape = STRIP-AND-CLAMP, two layers:** the schema omits it (layer 1) and `createProviderService`/`updateProviderService` strip it and DERIVE the value in STORAGE so every caller is covered (layer 2) — the same placement rationale as the approval-lifecycle strip. Derivation is ONE call into the EXISTING `resolveCommissionRates` with the same option shape /api/checkout uses (new thin helper `resolveServiceOwnerShareRate`); **no second rate implementation.** **Class sweep, not the instance:** every `createInsertSchema` in `shared/schema.ts` was crossed against every schema `.parse`d from a request body — exactly FOUR are client-parsed. Beyond `revenueShareRate` this stripped the whole rate/fee + payment-identity family on `insertLocalExpertFormSchema` (`bookingFeeType|Percentage|Fixed|Hourly`, `minBookingFee`, `feeSettings`, `stripeAccountId`, `stripeAccountStatus`, `stripeConnectStatus`, `canReceivePayments`, `totalEarnings`, `pendingPayout`, `payoutSchedule` — all mass-assignable at POST /api/expert-application, all dormant, which is why they had never surfaced); `serviceCategories.commissionBandKey` is admin-by-design and annotated, not stripped. **(2) SD-1 — a provisional claim is UNACCEPTABLE INPUT: rejected, never promoted.** `PATCH /api/provider/bookings/:id/status` checked the TARGET status and never the CURRENT one, so a provider clicking Accept on a `payment_pending`/unstamped claim (§15b — visible to them in Inbox and titled "Booked" on their calendar) promoted a purchase nobody had paid for, after which `voidClaim` AND `promotePaidCheckout` both matched ZERO rows and the claimed `vendor_availability_slots.booked_count` was destroyed with no code path back. The owner rail now carries a FROM-state allow-list, and the write is the §15 **atomic conditional** (`UPDATE … WHERE id = ? AND status IN (<expected>)`, new optional `expectedFromStatuses` on `updateServiceBookingStatus`; omitting it preserves every existing caller verbatim) — the pre-check is only the error message, the transition itself is the guard. **Sole author of claim-machine mutations remains `checkout-claim.service.ts`**; this is the owner rail REFUSING to touch a row that machine owns, not a second implementation. SD-2 (provider cancel of a paid booking issues no refund) is deliberately UNCHANGED — still unruled. **(3) AC-1 — DELETED, not hardened.** `POST /api/vendor-availability/:id/book` was `storage.bookSlot(req.params.id)` behind `isAuthenticated` and nothing else: any account could exhaust any provider's inventory, no booking row meant the TTL sweep had nothing to reclaim, and `releaseSlot` had no reachable caller. Zero consumers, proven at `9382d500`. An endpoint with **no consumer and an irreversible effect is deleted** — gating it would have preserved a second, unaudited way to consume inventory beside the checkout spine. `storage.bookSlot` survives as checkout's atomic claim. Proven by `server/__tests__/provider-money-hardening.db.test.ts` (**P1–P6**, 6 proofs: insert-strip and update-strip with the persisted rate equal to a `fee_bands` row READ FROM THE DB and an admin band edit moving it per ruling 32; provisional-accept rejected with row/slot/diary unchanged; the once-strandable row still reclaimable by the sweep AND promotable by `promotePaidCheckout`; 5 concurrent accepts ⇒ exactly ONE winner; route-inventory absence). Sweep 9/9, promotion 11/11 (incl. **N17c**), detection 15/15 untouched and green; tsc holds at 197. No migration, no schema DDL, no publish-push trap. | provider money-hardening lane, 2026-08-06 |
| 43 | 2026-08-06 | [guarded: check-money-endpoints, phase2-fee-gate, check-decision-guards] | **Every guard states its NEGATIVE SPACE; green means green-WITHIN-STATED-BOUNDS.** Closes provider-sigma Q9. Three independent instances surfaced in one audit of a single pattern — a guard passing TRUTHFULLY while the hole it exists to catch is real: `phase2-fee-gate` Pass B was case-sensitive with an `[A-Za-z]*` identifier tail and therefore blind to **every SCREAMING_SNAKE fee constant**, this codebase's DOMINANT convention (MI-3); `check-claims-only-user-lookups` is scoped to `claims.sub` and misses `claims.role` (AC-2); and the drift job's legacy loop needed its predicate revisited when its reach was extended (PR #435, the protocol advisory). **A green check with an unstated blind spot is worse than no check, because it is read as coverage.** Ruled: (a) every Guard-registry entry carries a one-line statement of what its predicate does **NOT** cover — the registry table below now has that column for all fourteen; (b) the two guards this lane touched PRINT their negative space on every successful run, so it reaches the person reading CI output and not only the person reading the ledger; (c) **a predicate change ships with COMMITTED fixtures, on the ledger-lint `--self-test` precedent** — a wrong predicate is invisible by construction (the check goes green either way), so the predicate itself is proven before its verdict is trusted, and both self-tests run in CI immediately BEFORE their guard. Applied here: `phase2-fee-gate.sh --self-test` (9 Pass-B fixtures) and `check-money-endpoints.cjs --self-test` (17 predicate fixtures). **Correction to the audit's MI-3, found by building it:** `-i` ALONE does not fix the fee gate — with an `[A-Za-z]*` tail, `PROCESSING_FEE_RATE` still cannot match, because after `FEE` the tail cannot cross the `_` to reach the `=`. BOTH `-i` and `[A-Za-z_]*` are load-bearing, and reverting either fails the fixtures. The gate additionally learned ruling 32's second disposition: `fee-literal-debt:#<task>` exempts a line from FAILING but is REPORTED on every run, so filed debt can never become a silent baseline (a bare `fee-literal-debt` with no id exempts nothing). New predicate hits, all adjudicated: `AI_PLATFORM_FEE`, `AFFILIATE_PLATFORM_FEE`, `AFFILIATE_EXPERT_SHARE`, `TRANSPORT_COMMISSION_DEFAULT`, `COMMISSION_CACHE_TTL_MS` and one prose line → `fee-literal-ok`; **`PROCESSING_FEE_RATE = 0.03` → `fee-literal-debt:#PS2`**, the one that is genuinely not a fallback behind any band. The money guard also gained the pass that would actually have caught MI-1: widening its `req.body` FIELD_RE to `rate\|share\|commission\|split` does **not** catch it and that is the finding, not a bug — the route writes `Schema.parse(req.body)` and SPREADS it, so no line ever contains both `req.body` and the field name and a line-level grep is structurally blind. The new pass intersects *insert schemas that EXPOSE a rate-bearing column* with *insert schemas `.parse`d from a request body*; that intersection IS the class "a client can set a rate". | provider money-hardening lane, 2026-08-06 (Q9) |
| 44 | 2026-08-06 | [advisory] | **Booking-ready provider fixture is OWNED BY provider-sigma (Phase 2); Wave 4 CONSUMES it — this REVERSES the prior Wave-4 disposition** and is recorded per the brief's own requirement that a reversal carry a ledger note. Closes provider-sigma Q7. Rationale on record: the §C money assertions and the §C.8 slot assertions cannot run at all without an approved service + availability slots + a Stripe-Connect test account, so the lane either builds the fixture or ships no money coverage — the exact gap it was chartered to close; console-sigma already proved the reconciling-seeder pattern end to end (`console-sigma-kyoto-bench.http.test.ts`, K4 — consume-don't-reseed, interrupted-run convergence, `{market}-{specialty}` convention), so this is application, not invention; and J11/J12 are post-beta, so a Wave-4-owned fixture would leave the provider money paths unarmoured for the entire interval. **Kyoto-market provider preferred** for gate coherence, and `kyoto-photography@traveloure.test` already exists as a bare `service_provider` row (`e2e-test-accounts.seed.ts:34`), so this is an EXTENSION of an existing bench account, not a new identity — consume, never re-seed. The **attributed short-link fixture** is scoped down by ruling 45: with the dual-rate model unbuilt, its only remaining consumer is attribution REPORTING (`short-links.routes.ts:213-228`), a much smaller ask than a rate-bearing fixture. | provider money-hardening lane, 2026-08-06 (Q7) |
| 45 | 2026-08-06 | [advisory] | **The dual-rate model is spec-ahead-of-code IN ITS ENTIRETY, the code's posture is the INTENDED one, and journey J12 is rewritten against it.** Closes provider-sigma Q4. Ground truth re-verified: there is **no `rails` band** anywhere in the seeded `fee_bands` set; `decideBandKey` takes no attribution input at all (its opts are `{source, category, categoryCommissionBand}`, where `source` is the provider-vs-expert discriminator, not an acquisition channel); and the checkout **explicitly forbids** the linkage — `payments.routes.ts:575-577` declares acquisition attribution an *"Analytics dimension only: never read into any fee/amount/payout decision"*, resolves it server-side, writes it to `service_bookings.acquisitionRef`, and stops. `TRIP_GRAVITY_AUDIT_FINDINGS.md:114` recorded only the repeat-pair rails RATE as spec-ahead; the **whole mechanism** is. **J12 (`JOURNEY_TEST_SUITE_BRIEF.md:62`) asserts "commission resolves to rails band via fee_bands" and CANNOT PASS as written** — it is rewritten to pin the current SINGLE-BAND truth (an attributed and an unattributed booking of the same service resolve to the IDENTICAL rate), with the dual-rate steps carried as `deferred:provider-backoffice-p1`. This is a divergence pin in the R22 shape: it is green because the divergence is present, and it FAILS LOUDLY the day a rails band is seeded — the same correction ruling 38 made to J1/J2's payment leg, rather than leaving a Wave-4 journey encoding a mechanism that does not exist. **The dual-rate headline requirement transfers to provider back-office Phase 1**, which owns building it. | provider money-hardening lane, 2026-08-06 (Q4) |

| 46 | 2026-08-06 | [guarded: check-money-endpoints] | **A booking may never be BORN carrying a PaymentIntent — and PRIVILEGED-FIELD MASS-ASSIGNMENT is now a STANDING CLASS whose required fix shape is STRUCTURAL.** Executes `#PS15`, filed by the provider money-hardening lane's own class sweep (ruling 42 / `PROVIDER_SIGMA_AUDIT.md` §8.2) and re-verified as STILL HOLDING at `281d355c`. **(1) The finding is a VIOLATION, not a tension.** Ruling 41's immovable clause — *"a CLIENT-supplied PaymentIntent id may never resolve or stamp anything"* — stands with **no carve-out**: `stripePaymentIntentId` is a `SERVER_VERIFIED_ACTORS`-only field, written **solely** via the shared promotion path (`stampAuthorization` / `resolveAndStamp`, `checkout-claim.service.ts`). `POST /api/bookings` (`routes.ts`) `.parse`d the `.omit()`-based `insertServiceBookingSchema` off `req.body` and **SPREAD** it into `createServiceBooking`, so a crafted request could BIRTH a booking already carrying its own PaymentIntent. Ruling 41 was proven on the PROMOTION side by **N17c**; a born-stamped row is **not a promotion**, runs no promotion code, and therefore could never trip it — yet it looks authorized to every consumer keyed on that column (the TTL sweep skips it as not-an-unstamped-claim, `promotePaidCheckout` matches it, the drift job trusts it as linkage). N17c watches the door; PS15 came through the wall. **Fix = the MI-1 shape, three layers:** `insertServiceBookingSchema.omit({stripePaymentIntentId})` (layer 1 — safe outright, since NO caller of `createServiceBooking` passes the field and the column's sole production writer is `stampAuthorization`; the audit's caution that the omit list is load-bearing for the `InsertServiceBooking` TYPE is true of `platformFee`/`insuranceFee`/`providerEarnings`/`status`, which checkout DOES pass, and **not** of this field), a storage-level strip in `createServiceBooking` (layer 2 — covers the two internal `as any` callers a type-level omit cannot reach, and logs when it fires), and the route-level **pick-based allowlist** below. **(2) THE STANDING CLASS, third instance.** `revenueShareRate`, the MI-1 sweep's dormant fee/payout family on `insertLocalExpertFormSchema`, and this. Recorded as a class: **a privileged column becomes client-settable by DEFAULT under a denylist schema, and nobody edits an omit list for a column that did not exist when it was written.** **The required fix shape is STRUCTURAL: allowlist (pick-based) schemas, so new privileged columns are unreachable by default.** Applied here — `createBookingRequestSchema` (`shared/schema.ts`) admits exactly `serviceId, tripId, contractId, bookingDetails, bookingMetadata`; `status`, `idempotencyKey`, `slotId`, the amount family and the PI are all unreachable, and `totalAmount` + the split are now SERVER-DERIVED (§14 from the catalog row; §8 from `fee_bands` via the EXISTING `resolveServiceOwnerShareRate` — ruling 42 rule 1, derivation delegates). The route previously trusted `req.body.totalAmount` for the row while using `service.price` for the revenue counter on the next line, so the two could disagree by any amount the caller chose. **(3) SCHEMA-POSTURE VERDICT + follow-up.** All **186** `createInsertSchema(...)` calls in `shared/schema.ts` are `.omit()`-based; **ZERO** are `.pick()`-based. Converting the layer is filed as **`#PS18`**, NOT executed here, and ships with its committed negative fixture (**B6**: the same fake privileged column, reachable under `.omit()`, unreachable under `.pick()`) built while the trace was fresh. **(4) The rows already on disk get DETECTION, never silent trust or silent repair** — a fix stops new rows and says nothing about old ones, and old ones are the detector's job (§17). New drift classification **`payment_provenance_unverified`** (`warning`, cart rail), surfaced at `GET /api/admin/reconciliation/exceptions` on `/admin/reconciliation`. **Its predicate follows ruling 41's invariant as STATED — PROVENANCE, not one implementation of it — so TWO independent forms of server provenance each clear a row:** the §15b pre-flight `bookingDetails.stripeAttemptAt` marker (the spine wrote it), **or** Stripe's own `metadata.bookingIds` naming this booking when the job reads the PaymentIntent with the platform's own secret key (a `SERVER_VERIFIED_ACTORS` read, §17b). Corroboration is not a forger's loophole: `metadata.bookingIds` is written server-side by `createPaymentIntent`, so a real PaymentIntent lifted from elsewhere names the bookings it actually paid for and never the row it was planted on, and a PaymentIntent absent from Stripe is never seen at all (**B4b** proves both halves). A marker-only predicate would have indicted every legitimate booking whose PI predates ruling 38 but which Stripe can still vouch for — **and, found by building it, would have flagged the detection suite's own fixtures**, which is how the second form was discovered. Blast radius is bounded by `loadCartBookings`' existing window scoping. **(5) Guard extended, with fixtures (ruling 43).** `check-money-endpoints.cjs`'s schema-mediated pass now carries a PAYMENT-IDENTITY column predicate (`stripe<Thing>Id` / `paymentIntentId`) beside the rate one — deliberately narrow (a `status` or a grant is NOT covered; ruling 42 stripped that family by name), with 10 new committed `--self-test` fixtures and its negative space restated: it knows the RATE and PAYMENT-IDENTITY classes and nothing else, which is exactly why the structural answer is `#PS18` and not a wider grep. Proven catching the pre-fix shape in isolation. **ARCHAEOLOGY: NOT ANSWERABLE FROM THIS LANE** — the shared dev bench is still owner-side and unreachable (`#PS17`, audit §8.6), so the counts are ZERO against a schema-only local Postgres and prove the QUERY, not the population. The prod question is stated for the decision-maker in the lane report and is what the new classification answers on every run from now on. Proven by `server/__tests__/booking-birth-provenance.db.test.ts` (**B1–B6**, 7 proofs; each fix-proving one verified FAILING against the reverted code). Sweep 9/9, promotion 11/11 (incl. **N17c**), detection 15/15 (**file untouched**), ruling 42's P1–P6 6/6 — all green; tsc holds at 197. No migration, no schema DDL, no publish-push trap. CLAUDE.md **§19**. | PS15 lane, 2026-08-06 |
| 47 | 2026-08-06 | [guarded: fee-resolution-authority] | **RATE AUTHORITY: `fee_bands` is authoritative, reached through ONE resolver (D0).** Audit C2/Q9 determined the mechanism behind a provider booking charging 0.25 while the configured provider band said otherwise: `provider_services.revenue_share_rate` was the FIRST operand at `payments.routes.ts:826/877/1090` (*"the final override (takes priority over config)"*), so band resolution never decided the rate — **an admin editing `fee_bands` could not change what an existing service charged**, defeating ruling 32's proof. Three override layers sat above the band (per-service snapshot → `users.commission_override_expert_share_percent` → band). **Ruled:** per-entity overrides survive ONLY as provenance *through* the resolver, never beside it; the per-service snapshot is dethroned; the hardcoded `"0.75"` schema default (`shared/schema.ts:715`) is DELETED as a fee literal (ruling 32), and existing rows are backfilled to **NULL** ("ask the resolver") rather than to a freshly computed rate — a new snapshot would go stale on the next band edit and re-create the defect. Backfill ships in the same migration (bc18f6f's missing-backfill gap, not repeated). Ledger rows stamp `rate_source` ∈ `band`\|`entity_override`\|`rails`\|`code_fallback`\|`flat` with `band_id` NULL for every value but `band`, so an overridden rate is never attributed to a band that did not produce it. Pinned by A1 (band edit reaches the next resolution, no service-row touch) and A7. | fee-ledger lane v1.1 D0; FEE_LEDGER_AUDIT.md §1a/§2 |
| 48 | 2026-08-06 | [guarded: fee-resolution-authority] | **STRUCTURE C — "split & disclosed": the provider fee model (D1).** Four provider commission bands resolved via `service_categories.commission_band_key` → `fee_bands`: `limited` 0.12 · `moderate` 0.08 · `commercial` 0.06 · `premium` 0.04 (**verified already present at these rates**, so migration 178 seeds none of them). Plus `traveler_service_fee` **0.07 capped at $25.00 per booking**, the cap stored as band data in the new `fee_bands.max_amount` column and enforced at resolution — never a constant in code (ruling 32), so re-capping is an admin edit, not a deploy. Plus `provider_rails` **0.08**, resolved as **min(category band, rails)** so rails can never RAISE a premium provider's rate (a 0.04 provider stays at 0.04), and a rails booking **waives the traveler service fee entirely** — that waiver is the provider's quotable pitch. Repeat-pair rails remains spec-ahead-of-code: the model and `source_attribution` accommodate it; detection is NOT built. Pinned by A2–A5. | fee-ledger lane v1.1 D1 |
| 49 | 2026-08-06 | [advisory] | **`beta_flat` (0.10) DEACTIVATED (D2).** Incoherent under structure C: a 10% take is worse for the provider than the `commercial` (0.06) and `premium` (0.04) bands it would sit beside, and it was the band the provider dispatch path selected for early adopters. Superseded by the four category-resolved bands. **Do not reactivate.** A founding-Kyoto promise, if wanted, is premium-band-for-life granted through ruling 47's entity-override mechanism with provenance — a separate later decision, not implemented by this lane. | fee-ledger lane v1.1 D2 |
| 50 | 2026-08-06 | [advisory] | **The traveler-side fee is KEPT and DISCLOSED as a first-class fee type (D3/D4).** Audit C1: the same computed number was ADDED to the traveler total (`payments.routes.ts:307`) and DEDUCTED from the provider base (`:878-879`) while only one side was recorded in `service_bookings.platform_fee` (`:961-964`) — on an $80 booking the platform retained $40 and recorded $20, and ~15 independent aggregations all under-reported take by half. **Ruled:** the traveler fee stays, resolved from its own band, shown as its own labeled checkout line, and recorded as its own `fee_ledger` type `traveler_service_fee`. `service_bookings.platform_fee` is **deprecated through beta** to a ledger-derived value and **dropped post-beta with a ledger note** (D4). Follow-ups filed, NOT absorbed: `/earn` copy verification, checkout line-item labels, and the business-plan revenue language (the "4–12%" framing understates structure C — investor-facing docs must not lag the ruled model). | fee-ledger lane v1.1 D3/D4; PROVIDER_CONSOLE_AUDIT.md C1 |
| 51 | 2026-08-06 | [guarded: fee-resolution-authority] | **Every category carries a commission band; a category may never exist without one (R1/R2).** 20 of 42 dev categories carried a band; the 22 without included live provider categories, and under ruling 48 a booking in one would have reached the fail-loud resolver and **thrown at checkout**. **R1 assignments:** all event/wedding/party categories → `limited`; Health & Wellness and Pets & Animals → `moderate`; Technology & Connectivity and Language & Translation → `commercial`; **Custom / Other → `moderate` as an explicit INTERIM** — the durable model is an admin assigning the band at approval time for custom services, filed in FOLLOWUPS.md, NOT built here; the interim exists only so checkout cannot throw. **R2 two-layer guard (ruling 35):** application-layer validation on category create (band required, must exist and be active) and on update (the band may no longer be CLEARED — the former "explicit inheritance" branch is rejected with a typed 400), PLUS `NOT NULL` on `commission_band_key` — backfill first, constraint second, **same migration**, so it cannot be violated by rows already on disk. Mapped BY NAME, not id, because environments carry different category sets. The resolver stays **fail-loud: no silent fallback rate, ever** — R1+R2 make the missing-band state unreachable rather than survivable (pinned by A8, which proves the throw survives a deliberately breached guard). **Delta recorded:** categories R1 did not name (Specialty/TaskRabbit/Travel/Trip/Visa Services, four `Affiliate:*`) took `moderate` under R1's own interim principle and are filed for explicit assignment; the `Affiliate:*` bands are inert for commission (affiliate resolves `affiliate_standard` via `source="affiliate"`) and exist only to satisfy NOT NULL. | fee-ledger lane R1/R2 |
| 52 | 2026-08-06 | [advisory] | **Fee capture becomes an APPEND-ONLY EVENT LOG (`fee_ledger`, migration 179).** A fee EVENT log, **not a general ledger** — no double-entry, no chart of accounts; the per-booking invariant `traveler_paid − provider_credited = Σ(fee_ledger.amount)` supplies the integrity. Corrections are **reversal rows** (`reverses_ledger_id`); no UPDATE and no DELETE path exists in application code, which is why the two existing in-place mutators (`booking-actions.service.ts:91-101` rewriting `platform_revenue`, `affiliate-reconciliation.service.ts:425-427` overwriting `expert_earnings.amount`) are SUPERSEDED rather than complemented. `band_id` is **NULLABLE with a `rate_source` discriminator** per ruling 47's three-layer reality. Seven DB CHECK guards (ruling 35), five indexes, and the full ruled `fee_type` taxonomy seeded so a later lane extending coverage adds a WRITER, not a migration. **Transaction strategy (b), ruled:** events are written at the **authorization-stamp transaction** — money is real at authorization (#433's principle), the Stripe PaymentIntent IS in scope there, and the §15b claim spine stays untouched (rulings 38–40 built it transaction-free deliberately; do not wrap it). Phase 0 established the constraint that forced this: `payments.routes.ts` has **zero** `db.transaction` calls, the completion flip is five unwrapped writes, and on the cart rail the fee is minted before a PaymentIntent exists (ruling 46). Coverage is phased with **honest accounting** — every uncovered census write point carries a `deferred:<path>` row and the reconciliation output states coverage every run. **No silent partial ledger.** | fee-ledger lane v1.1 §1B/§1C/§1D; FEE_LEDGER_AUDIT.md §1/§3 |

| 53 | 2026-08-10 | [advisory] | **Experts must be IDENTITY-verified to PUBLISH; the gate stays at publish, not payout (dispatch v1.2 §5 / v1.3 R1).** Decision-maker ruled *"Yes, experts need to verify their identity"*, then reaffirmed it against a proposed `verify-to-be-paid` alternative. **Rationale for the record:** the publish gate and the payout gate protect DIFFERENT parties — Stripe's identity check at payout protects the platform; verify-to-publish protects the TRAVELER from conducting an engagement with an unverified advisor, which is the trust wedge itself. So the platform gate is not redundant with Stripe's. `checkPublishVerificationGate` stays at the publish call site. **Pre-approved fallback, requiring a NEW ruling and never a judgment call:** publish allowed but *paid engagements* blocked, IF Kyoto recruiting data ever shows verification as the measured drop-off point. **This entry also closes the gap that caused the conflict — the original ruling lived only in conversation** (ruling 35's provisional-until-appended rule, learned the hard way: a later dispatch recommended the opposite placement because the ledger was silent). **Supersedes the publish-gate clause of the mis-numbered "Decision #39" prose section** (see ruling 55), which states identity AND business for every publisher: experts have NO business check because `local_expert_forms` carries no `business_verification_status` column at all — an individual expert is not a business. **Implementation** (fixes the P0 regression introduced by `be78a9c`, which blocked EVERY expert from publishing because the gate demanded a `service_provider_forms` row experts never have): ONE shared `checkPublishVerificationGate(userId)` helper called by both POST and PATCH `/api/provider/services` — duplicated branches were the root cause of the drift; role resolved by **DB lookup** in the `requireAdmin` shape (CLAUDE.md §2), never `req.user.role`; admin bypasses; `isProviderRole` → `service_provider_forms` both statuses; `isExpertRole` → `local_expert_forms` identity only, reporting `businessVerified: null` rather than `false` so an expert is never told they failed a check that does not exist for them; roles in neither family default-deny; error copy routed per role (`/provider-status` vs `/expert-status`); DRAFT saves ungated for everyone. Proven by `server/__tests__/f2-verification-gate.http.test.ts` (11 proofs) plus live verification in both directions (unverified expert blocked, verified expert publishes). **Tagged `[advisory]`, not `[guarded:]`, deliberately: that suite is NOT wired into any workflow, and ruling 27 says script-only = MISSING.** Wiring it is filed — this gate has now broken twice. | 92030ea; QA_PUNCH_LIST.md P0; dispatch v1.2 §5 + v1.3 R1 |
| 54 | 2026-08-10 | [advisory] | **The tsc baseline ratchet is DOWN-ONLY, and an improvement is banked in the PR that earns it (baseline 183).** `.github/workflows/build.yml`'s ratchet fails when the error count goes UP (net-new type errors) **and equally when it goes DOWN** — coming in under the ceiling is a failure whose only remedy is lowering `TSC_BASELINE` in the same PR. Ratified as correct: leftover headroom is precisely how a count creeps back up, so an improvement that is not locked in is an improvement lent to the next lane. Fired for real on PR #451 — this branch's cumulative work removed 7 errors (190 → **183**) and the gate refused to let them sit as slack. **Standing note for every lane:** the ratchet firing is not a defect to route around; the lock-in belongs in the improving PR, not a follow-up. | a6938de; PR #451 build gate; dispatch v1.3 R3 |
| 55 | 2026-08-10 | [advisory] | **Stripe-only KYC/KYB (Persona retired, no Plaid) takes its proper ledger number; the prose section headed "Decision #39" COLLIDES with table ruling 39 and yields to the ledger (ruling 35).** The Stripe-only verification ruling was written into this file as a free-standing `## Decision #39` section while **39 was already occupied** by the money-promotion ruling in the numeric table — exactly the collision ruling 35 anticipates (*"the ledger is the number authority — a conversation-assigned number that collides yields"*). The lint never caught it because it parses table ROWS only, so the duplicate is invisible to CI: recorded here rather than silently edited, per the append-only rule. **The substance stands and is re-affirmed:** expert identity → Stripe Identity; provider business verification → derived from Stripe Connect Express KYB via the `account.updated` webhook (`charges_enabled` / `details_submitted` / `requirements.disabled_reason`); Persona retired with `POST /api/identity/business/create-inquiry` and `POST /api/webhooks/persona` both returning **410 Gone** and the `PERSONA_*` env vars no longer required; **no Plaid** — payout identity relies solely on Stripe Connect. **One clause of that section is now STALE and superseded by ruling 53:** its publish gate reads `identityVerificationStatus === 'verified' AND businessVerificationStatus === 'verified'` for every publisher, which is provider-only truth — applying it to experts is what produced the P0 that blocked all of them. The prose section is left in place unedited (append-only); read it through rulings 53 and 55. | docs/DECISIONS.md prose section; identity.routes.ts 410s; ruling 53 |
| 56 | 2026-08-11 | [advisory] | **AMENDS 53 (append-only — 53 itself is unedited): the gate ruling 53 ratified was correct but sat where an expert listing never actually goes live; option (B) — approve always records, go-live follows verification — closes the two ungated doors (QA_PUNCH_LIST.md P0, found Aug 11 2026 by the R2 lane).** Ruling 53 pinned `checkPublishVerificationGate` at `POST`/`PATCH /api/provider/services` and proved it there (11 f2-verification-gate proofs) — correct as far as it went, but **overclaiming**, because it never stated the gate's reach: it fires only on `status:"active"`, and the expert ServiceForm's Submit-for-Approval sends `approvalStatus:"submitted"` + `status:"draft"` — the button experts actually use never reaches the gate at all. The two paths that actually flip a listing to publicly-live (`approval_status='approved' AND status='active'`, per `storage.ts:1649/1994` and `storefront.routes.ts:557`) were both completely ungated: (1) **admin approval** (`storage.approveProviderServiceListing`, the canonical way an expert listing goes live) set `approvalStatus:"approved"` AND `status:"active"` in one write with no verification check at all — an admin approving an unverified expert's listing published it, the exact outcome ruling 53 exists to prevent; (2) the owner's own **Activate toggle** (`PATCH /api/expert/services/:id/status`) accepted `status:"active"`, ownership-checked but not verification-checked. **Decision-maker's choice, ratified: option (B)** — admin approval is ALLOWED against an unverified owner (preserves the admin's review work; refusing it would waste a real review) but must NOT make the listing publicly live; go-live then follows the owner's own verification automatically. **Mechanism, no schema/enum/migration change (publish-trap avoidance, CLAUDE.md):** admin approval always sets `approvalStatus:"approved"`; it sets `status:"active"` ONLY when the LISTING OWNER (never the acting admin) already satisfies the same predicate the original gate checks; otherwise the row lands `approvalStatus:"approved" AND status:"draft"` — **held, not live, by construction**. This is distinguishable from owner intent for the same reason the §15b "unauthorized claim by construction" pattern works: the owner's own Pause toggle writes `status:"paused"`, never `"draft"` (verified: `client/src/components/backoffice/my-offerings-table.tsx:302`), so `approved+draft` unambiguously means "the gate held it" and `approved+paused` unambiguously means "the owner chose to pause it" — the sweep below reads exactly that distinction and must never conflate the two. **ONE predicate, extracted:** `resolvePublishVerification(userId)` (`server/services/publish-verification.service.ts`) carries the exact role/verification branch logic ruling 53 specified (admin bypass; provider = identity AND business via `service_provider_forms`; expert = identity only via `local_expert_forms`, `businessVerified: null`; anything else = default-deny) as the ONE definition of "may publish" — `checkPublishVerificationGate` (`server/routes.ts`) now calls it and only shapes the 403 body, so the two original call sites (`:2366` POST, `:2539` PATCH) are UNCHANGED in behavior (all 11 f2-verification-gate proofs stay green verbatim). **New enforcement points, all resolving through the same function, never re-implemented:** admin approval (`server/routes/admin.routes.ts`'s `POST /api/admin/provider-services/:id/approve`, evaluated against `service.expertId` — the owner, not `adminId`); the owner's Activate toggle (`PATCH /api/expert/services/:id/status`, gated whenever the target is `"active"`, same as the original two sites). **Auto-activation sweep:** `activateVerificationHeldListings(userId)` (same service file) re-evaluates the predicate and promotes that user's `approved+draft` rows to `active` — and is proven to leave an `approved+paused` row for the same user untouched, and to be idempotent (a second pass activates zero rows). Wired from the two verification-write paths that can newly satisfy the predicate: `storage.updateFormIdentityVerification` (called from the Stripe Identity webhook, `server/routes/webhooks.routes.ts:66`) and the Stripe Connect `account.updated` business-verification derivation (ruling 55) in the same file, both fire-and-forget/non-fatal so a sweep failure never fails the webhook 200. §13: the sweep fails CLOSED — a predicate that cannot be evaluated (lookup miss, wrong role) activates nothing. Proven by `server/__tests__/publish-verification-hold.http.test.ts` (9 proofs: unverified-expert admin-approve held+not-public, verified-expert admin-approve live+public, both repeated for a provider incl. a partial-verification provider still held, the owner-toggle block/allow pair, the sweep's held-activates/paused-never-touched/idempotent triple, and a fails-closed no-op) — all 11 original f2-verification-gate proofs verified still green alongside it. | 41fee08; QA_PUNCH_LIST.md P0; ruling 53; ruling 55 |
| 57 | 2026-08-11 | [guarded: publish-gate-and-fundamentals] | **The 38 proofs behind rulings 53 and 56 (plus D3/D4) are now CI-ENFORCED — closes the gap ruling 27 predicted ("a script not wired into CI is not a guard, it is MISSING") for all five suites at once.** Ruling 53's `f2-verification-gate.http.test.ts` (11 proofs) and ruling 56's `publish-verification-hold.http.test.ts` (9 proofs) were each committed and passing, and each ruling said so explicitly — 53: *"Tagged `[advisory]`, not `[guarded:]`, deliberately: that suite is NOT wired into any workflow… Wiring it is filed — this gate has now broken twice"*; 56 inherited the same gap on its own new suite. Two more committed-but-unwired suites sat beside them: `service-deliverable.http.test.ts` (9 proofs, D3 entitlement + the pre-purchase leak-prevention sweep) and `short-links-frame.http.test.ts` + `posting-opportunities-frame.http.test.ts` (7 + 2 proofs, D4 frame dedupe/analytics and opportunity→frame mapping). All five are `.http.test.ts` suites (`node:test` + raw `fetch()`), not Playwright — they need Postgres AND a real HTTP server on :5000, no browser. **New workflow, not an extension of an existing one:** `.github/workflows/publish-gate-and-fundamentals-gate.yml`, job/status-check name **`publish-gate-and-fundamentals (DECISIONS.md rulings 53, 56; D3/D4)`**, triggered `pull_request`/`push` to `main` (blocking, same shape as `fee-resolution-authority-gate.yml`). No existing workflow was both blocking AND server-booting: `journey-suite.yml` already boots Postgres + a production bundle + runs a bare `npx tsx --test <file>` step in the same job (proven there by `checkout-oneclick.stripe.db.test.ts`) but is push/schedule-only and by its own header can never gate a merge; `fee-resolution-authority-gate.yml` is the closest blocking shape but never boots a server (its suite calls the resolver in-process). This workflow is journey-suite's server-boot recipe grafted onto fee-resolution-authority-gate's blocking trigger — the cheapest correct combination. **DB setup delegates to `./.github/actions/ci-db-setup`** (migrations + sessions table; `seed-ci-users: false` since every suite mints its own users via `POST /api/auth/register`, same posture as journey-suite) — never inlined, per the `ci-db-setup-lint` gate. **Env:** `DATABASE_URL` from the throwaway Postgres service; `JOURNEY_DB_WRITES_OK=1` (every suite's own disposable-DB guard refuses fixture writes without it); `NODE_ENV=production`, `SESSION_SECRET`, `ALLOW_TEST_ACCOUNTS=1`, `SESSION_COOKIE_INSECURE=1`, an `sk_test_` `STRIPE_SECRET_KEY` stub (never `sk_live_` — CLAUDE.md's publish-trap note on `server/validate-env.ts`'s hard-stop), matching the boot env `app-routes-gate.yml`/`journey-suite.yml` already use. **`RATE_LIMIT_LOOPBACK_SKIP=1` is load-bearing, found by building this gate:** the five suites run as five SEQUENTIAL steps in one job rather than one combined `npx tsx --test a b c d e` invocation, because `node:test` runs multiple file arguments concurrently across worker threads by default and every suite registers fresh users from the same loopback IP in its `before()` hook — run together, reproduced locally at 28/38 failures, all `429 Too Many Requests` on registration; sequential steps plus the loopback rate-limit exemption (the same opt-out `journey-suite.yml` already carries, for the identical reason) eliminate the contention entirely, back to 38/38. Each suite step prints its own TAP pass/fail counts to the job log and a final always-run verdict step renders a per-suite pass/fail table into the run summary and fails the job if any suite failed — diagnosable from the log alone, no local re-run needed. **Negative space (ruling 43):** this gate proves exactly the 38 proofs in these five files and nothing wider. It does **not** exercise Stripe's real identity/business verification webhooks (`STRIPE_SECRET_KEY` is a non-functional `sk_test_` stub — Stripe is never actually called); does **not** cover the provider `ServiceForm` UI submit path (HTTP-only, no browser, no DOM); and does **not** prove the D3 deliverable file itself is access-controlled in storage — `service-deliverable.http.test.ts` proves the `serviceFile` FIELD is never leaked in any pre-purchase API response, but the file remains a pasted URL, unchanged by this gate (P1 punchlist entry, filed separately). **Rulings 53 and 56 are UNCHANGED — never edited (append-only, ruling 26).** They remain `[advisory]` rows exactly as written; THIS entry is the `[guarded: publish-gate-and-fundamentals]` one that carries their enforcement, read together with them going forward. Verified before landing: all 38 proofs green locally (11/9/9/7/2) against both a dev-mode boot and a production-bundle boot with the CI env above; `node scripts/check-decision-guards.cjs` passes with this tag (the guard name appears in the workflow's "Guard identity" `run:` step, not only its job `name:` — ruling 27's job-name-doesn't-count rule); `check-money-endpoints.cjs`, `phase2-fee-gate.sh` and `check-unmounted-routers.cjs` all still exit 0; tsc holds at the locked baseline (183) — this ruling is CI wiring + ledger only, no application code touched. | `.github/workflows/publish-gate-and-fundamentals-gate.yml`; ruling 26/27; ruling 53; ruling 56 |
| 59 | 2026-08-11 | [advisory] | **AMENDS 58 (append-only): the bucket-privacy question ruling 58 honestly recorded as "UNVERIFIED" is now CLOSED — empirically confirmed PRIVATE via live HTTP 403.** Ruling 58 wrote: *"This could NOT be confirmed with a live network call in this environment (no bucket is attached here — `REPLIT_OBJECT_STORAGE_BUCKET` is unset by design)"* and stated that residual exposure if the bucket turned out to be public-readable was bounded by key unguessability alone. **Empirical result (Aug 11, 2026):** `REPLIT_OBJECT_STORAGE_BUCKET` was injected via `.replit [env]` after the bucket was provisioned, the R4 deliverable rail was exercised end-to-end against the real bucket (upload → objstore: prefix stamped in DB → download stream → byte-identical round-trip → `deliverable_downloads` log), and an unauthenticated GET against the raw GCS URL `https://storage.googleapis.com/<bucket>/<key>` for the freshly-uploaded object returned **HTTP 403 Forbidden**. The bucket's own ACL is now the primary privacy guarantee; the crypto-random key component remains as defense-in-depth per ruling 58's stated rationale but is no longer the only control. No code change — ruling 58's wrapper (`server/infrastructure/object-storage.ts`) comment updated to record "EMPIRICALLY VERIFIED PRIVATE" in the same commit. | R4 live end-to-end proof, Aug 11 2026; ruling 58 |
| 60 | 2026-08-11 | [advisory] | **Language & localization RATIFIED as mocked (mockup §06).** TWO systems, never conflated: **(A) chrome** — menus/buttons/labels/notifications/emails, platform-translated via a standard i18n library with locale files, ONE selector serving providers, experts and travelers (account-persisted; guests via a topbar menu honored before Accept-Language). **(B) provider content** — listing names/descriptions/meeting-point text/storefront bios, translated only by the provider or a clearly-labeled machine draft: an AI draft is ALWAYS labeled and never publishes without provider review; a missing translation falls back to the original with an honest "shown in English"-style tag; content is never silently machine-translated at read time (§13 applied to language). Storage: `service_translations` (service_id, locale) additive migration DECLARED in shared/schema.ts (publish-trap rule), owner-gated writes. Build order: chrome first (EN + 日本語 — Kyoto market), content second. **Currency display is explicitly OUT of scope — a separate later ruling** (money formatting is not translation). Later named-not-built phases: localized share frames, PDF deliverables, emails. | mockup §06 (artifact eb83b261); decision-maker ballot Aug 11 |
| 61 | 2026-08-11 | [advisory] | **D6 — rails-attribution retrofit RATIFIED, on the fee-ledger lane (it owns the resolver).** The chain that does not exist today, built as a small named phase with D4's link machinery as input: frame/short-link `acquisitionRef` → resolver → `provider_rails` as **min(category band, rails)** (ruling 48's clause — rails never RAISES a premium provider's rate) + the traveler-fee **waiver**, ledger rows stamped `rate_source='rails'`. **The attribution pin lands WITH it:** a valid frame link → rails resolution + waiver logged; a fabricated/expired/wrong-provider ref → full rate. **The waiver marketing caption ("book through my link — skip the service fee") stays OUT of the caption engine until that pin is green** — the marketing promise must not precede the money truth. Amends the ruling-45 posture (acquisition attribution "analytics dimension only") by executing the transfer ruling 45 itself filed to provider back-office Phase 1; §14/§18 bind — the rate is resolved server-side from fee_bands, the ref only SELECTS the band lane, never carries a rate. | dispatch v1.2 §1; rulings 45, 48; decision-maker ballot Aug 11 |
| 62 | 2026-08-11 | [advisory] | **D7 + D9 RATIFIED — capture-side pair, with ONE decision-maker AMENDMENT at ratification.** **D7:** the `service_logistics` field set captured in the wizard NOW while provider count ≈ 0 — transport provision, temporal shape (duration, setup/buffer, bounds, IANA timezone), booking constraints (party min/max, lead time, change cutoff, can-anchor flag), group handling; consumers wire in later per lane priorities. **AMENDMENT (verbatim intent): "ensure the service provider can set EITHER a pickup RADIUS or a pickup ROUTE"** — the transport-provision capture must make radius-vs-route an explicit provider choice, not leave both implicit: radius = the `serviceRadius` ring around the confirmed pin (already rendering, display-only), route = `service_route_points` (already shipped, migration 192). The wizard presents the choice; declaring one does not silently delete the other's data (§13 — an existing route is surfaced, never clobbered by picking radius). **D9:** onboarding attestations keyed to method + category risk, inapplicable ones OMITTED with a reason — the D2 honesty shape at the top of the funnel. | dispatch v1.2 §3; mockup §07; decision-maker ballot Aug 11 |
| 63 | 2026-08-11 | [advisory] | **D8 — per-method COMPLETION definitions RATIFIED: one payout machinery, no per-method money forks.** Every completion event writes the diary row and triggers fee/payout through the SAME machinery as the in-person confirm-completion flip. The table: **in-person/hybrid** — confirm-completion flip as built; **property** — checkout date; **pdf** — delivered via entitlement, **auto-complete at 7 days** (7 days after first download, or 7 days undownloaded post-delivery) — buildable NOW because R4/R5's `deliverable_downloads` log exists (rulings 58/59), and this closes the held-earning-on-inattentive-traveler gap the P2 punchlist entry recorded; **call/voice** — session end per booked slot, provider-confirmed (no-show policy filed as its own follow-up, not invented here); **async** — SLA satisfied + scope delivered, provider-declared, disputable window; **bundle** — all components complete, partial-completion routes to the refund lane. Auto-complete timers are the §15 shape: atomic conditional flips, idempotent, never a compensating rollback. | dispatch v1.2 §2 amended by R5; punchlist P2; decision-maker ballot Aug 11 |
| 64 | 2026-08-11 | [advisory] | **RULING 62 EXECUTED — D7 logistics capture + the radius-or-route AMENDMENT, and the P1 pin report RESOLVED (its cause was route-stop canvas interactions, nothing to do with keys or env).** **(1) The pin gap (QA_PUNCH_LIST P1).** The decision-maker verified live before this build that the Google keys work, the meeting-pin picker renders with click-to-place + drag-adjust, and `POST /api/geocode` resolves — so the punchlist's candidates (a) env timing and (c) key restrictions were both eliminated, and candidate (b) was the whole fault: route stops in `client/src/components/provider/catalog-map-view.tsx` were located ONLY by name-to-geocode (`locateStop`) and their Leaflet markers were static, so a stop the geocoder missed could not be pinned by hand at all. Closed with the two affordances the meeting pin already had, implemented in **Leaflet** (this canvas is react-leaflet, NOT the picker's Google Maps): located stop markers are `draggable` and report `dragend` through a new opt-in `onStopDragEnd` prop on the SHARED `ServiceLocationMap`; a new opt-in `onCanvasClick` prop mounts a `useMapEvents` click bridge that is **only mounted while a placement mode is explicitly ARMED** ("Place a stop here" for a new stop, a per-row "Place on map" for an existing UNLOCATED one) — a bare map click never places anything, so pan/zoom is not fought. A traveler surface passes neither prop and is byte-for-byte unchanged in behaviour. **Both affordances are DRAFT-ONLY:** they mutate the local draft and set the pre-existing `dirty` flag; the pre-existing "Save route" button remains the one write, and that dirty-to-Save step IS the L27-P3 confirm posture here — deliberately NO second confirmation dialog. A click-placed pin is prompted inline for its name and Save is BLOCKED while any stop is nameless (an unnamed pin is not a stop). §13 held rather than papered over: click-to-place needs a canvas, and this map renders NOTHING when a service has no pin and no located stop, so the arm button is DISABLED with the reason stated instead of inventing a city-center viewport. `FitToContent` now refits on the SET of located stops, not their values, so a drag no longer jerks the viewport. Geocode Locate, off-map listing of unlocated stops, server-derived positions and the replace-list PUT with its 409 handling are untouched. **(2) D7 capture — migration 195**, 11 additive-nullable columns on `provider_services`, no DB CHECK (the migration-144 app-enforced posture), no backfill, all declared in `shared/schema.ts` (publish-trap rule): `transport_provision`, `pickup_coverage_mode`, `duration_minutes`, `buffer_minutes`, `earliest_start_time`, `latest_start_time`, `service_timezone`, `party_size_min`, `party_size_max`, `change_cutoff_hours`, `can_anchor`. **AUDITED AND DELIBERATELY REUSED, NOT DUPLICATED:** `leadTimeHours` (lead time), `serviceRadius` (the amendment's radius arm), `service_route_points` (its route arm, ruling 22/migration 192), `meetingPoint`/`pickupAvailable`/`pickupAddress`/`dropOffPoint`, `maxConcurrentBookings`, `deliveryTimeframe`, `latitude`/`longitude`/`locationPrecision`, `cancellationPolicyType`, and **`priceType`** — whose existing `per_person` vs `fixed`/`per_event` values already express ruling 62's per-group-vs-per-person group-handling flag, so no column was added for it. `transport_provided` (migration 119, `yes|no|not_applicable`, WITH a DB CHECK) is left ALONE and not merged into the finer-grained `transport_provision`: widening that CHECK is exactly the publish-time deploy-push failure CLAUDE.md warns about, and the two ask different questions. Vocabularies live in `transportProvisionEnum`/`pickupCoverageModeEnum` and are enforced by `insertProviderServiceSchema` field-level extends, so they survive `.partial()` and the UPDATE path is checked as hard as the insert. **(3) The AMENDMENT's NEVER-CLOBBER RULE** (decision-maker verbatim intent: *\"ensure the service provider can set EITHER a pickup RADIUS or a pickup ROUTE\"*): `pickup_coverage_mode` records WHICH of two already-shipped stores to RENDER; it never deletes, nulls or overwrites the other's data. The route handler writes the mode column and nothing else (it does not touch `serviceRadius`, and `service_route_points` keeps its single owner-gated replace-list writer); the ServiceForm payload keeps a saved radius under either coverage mode; and the wizard STATES the preserved side out loud (\"N route stops are saved — not shown while coverage is set to radius. Nothing was deleted\"). Traveler surfaces (`/services/:id`) render ONLY the chosen mode; a NULL mode — every pre-195 listing — renders both exactly as ruling 22 shipped it. **(4) Wizard placement** is the logistics/delivery step, shown only for place-anchored listings via the SHARED `isPlaceAnchored` predicate (`shared/service-fundamentals.ts`), never a second local method list. **NEGATIVE SPACE (ruling 43 — what this does NOT do): CAPTURE ONLY.** No consumer is wired: no transport resolver, no scheduler, no pricing or matching read, and — explicitly — **NO fundamentals checks were added for any of these fields**. Nothing beyond the coverage-mode rendering rule reaches a traveler surface. Every field's NULL means \"the provider never told us\" and is never read as a value. Proven by `server/__tests__/service-logistics-capture.http.test.ts` (**D7-L1..L5d, 9 proofs**: create/update round-trip, unrelated-PATCH non-erasure, unset-stays-NULL, never-clobber in BOTH directions, and out-of-set vocabulary rejection on create AND update) plus **14 chromium UI proofs** for the canvas interactions and the chooser. Keep-green: f2-verification-gate 11/11, publish-verification-hold 9/9, service-deliverable 9/9, deliverable-protected-rail 13/13, short-links-frame 7/7, posting-opportunities-frame 2/2, checkout-claim-sweep 9/9, checkout-payment-promotion 11/11, reconciliation-detection 15/15, booking-birth-provenance 7/7. `check-money-endpoints.cjs`, `phase2-fee-gate.sh`, `check-unmounted-routers.cjs`, `check-decision-guards.cjs` all exit 0; tsc holds at the CI-locked baseline 183. Tagged `[advisory]`, not `[guarded:]` — the new suite is committed but not wired into CI (the same gap ruling 58 recorded for its own suite). | ruling 62; ruling 22; QA_PUNCH_LIST.md P1; migration 195; `client/src/components/service-location-map.tsx`; `client/src/components/provider/catalog-map-view.tsx`; `client/src/components/ServiceForm.tsx`; `server/__tests__/service-logistics-capture.http.test.ts` |
| 65 | 2026-08-11 | [advisory] | **RULING 60 PHASE A EXECUTED — chrome i18n (EN + 日本語) is FRAMEWORK + the highest-traffic surfaces; content (system B) is NOT built.** Ruling 60 names TWO systems and forbids conflating them; this executes **(A) chrome only**. **Framework:** `i18next` + `react-i18next` (new deps, permitted by the build charter; installed off the real registry, postinstall scrub ran, `replit.local` count **0**). Locale files are namespaced per surface — `client/src/locales/{en,ja}/{common,nav,provider,catalog,settings,auth}.json`, **269 leaf keys per locale, EN and JA at exact parity** (common 19 · nav 82 · provider 31 · catalog 45 · settings 46 · auth 46). `fallbackLng: 'en'` is the deliberate chrome contract: an untranslated chrome key renders English **silently**, which is standard chrome behavior — ruling 60's honest-labeling rule binds CONTENT, not chrome. Belt-and-braces against the failure mode that WOULD be dishonest (raw key-soup like `nav.dashboard` reaching a user): every dynamic call site passes the English string as `defaultValue`, and a `parseMissingKeyHandler` degrades a truly-missing key to its last segment rather than the dotted path. **Resolution order, exactly as ruled:** authenticated user's saved preference (`users.preferences.settings.language`) → guest's localStorage choice → Accept-Language → `en`; `<html lang>` follows. Steps 2–4 run at i18n module load (BEFORE first paint, so a ja-JP browser never flashes English); step 1 lands in `useLocale` once the session resolves, since the account value does not exist at module-evaluation time. **`resolveLocale` is a pure function** taking the account value as a parameter — the session is the caller's concern — so the order is readable in one place and testable without a browser. **The ONE selector, both entry points, ONE persistence path (`useLocale`):** (a) Settings → Preferences card (mockup §06-A) and (b) a compact 🌐 menu mounted on the SHARED `backoffice-shell` (so provider/expert/EA consoles get one selector, not three) and on the traveler navbar for guests. Both write localStorage ALWAYS and, when signed in, ALSO `PATCH /api/me/preferences` — the EXISTING strict-allowlist shallow-merge machinery (`storefront.routes.ts`); the `language` field was ALREADY on that allow-list, so **no new endpoint, no new column, NO MIGRATION** — the preference rides the existing jsonb namespace. That field was tightened from free-form `string().max(20)` to an enum; **the enum is deliberately WIDER than the shipped locales** (`en, ja, es, fr, de`) because `expert/settings.tsx` has offered es/fr/de since before this ruling and narrowing to en|ja would have 400'd that page's entire settings save (notifications and timezone with it) for anyone who had picked one — an unshipped locale persists and resolves to nothing, exactly the pre-ruling behavior. **Surfaces translated (EN+JA):** provider console sidebar + all 14 console page titles, provider Settings chrome, Catalog chrome (buttons, filter pills, empty states, section headers, **health-check labels incl. the D2 "n/a" note and the delivery/pin chips**), traveler navbar + footer, and the auth surfaces (SignInModal + Signup). **A NON-OBVIOUS HAZARD FOUND AND DESIGNED AROUND — the English label was load-bearing as a TEST SELECTOR.** `provider-sidebar.tsx` derives `data-testid={`nav-${item.title.toLowerCase()…}`}` from the display title, and `layout.tsx` does the same for `link-nav-*` / `link-mobile-*` / `button-nav-dropdown-*` / `link-partner-*`; translating the label in place would have silently renamed every one of those selectors under a JA locale. Every config entry therefore KEEPS its English string (also the React `key` and the render fallback) and gains a separate `i18nKey` — proven by an explicit assertion that all 10 provider nav testids survive the switch. Test environment is unaffected for the same reason plus a second: the suites' English text assertions target surfaces this phase did not touch, and an en-US browser with no stored choice resolves to `en` (pinned as proof e2) — **no test was weakened**. **NEGATIVE SPACE (not built, deliberately):** provider CONTENT translation is ruling 60 **Phase B** — `service_translations` is NOT created and no content string is routed through `t()`; **currency display stays out of scope by the ruling's own words** (the footer's hardcoded "English (US)" pill now follows the live locale, the `USD ($)` pill beside it is untouched); emails, share images and PDF deliverables are untranslated; and every surface not listed above keeps hardcoded English and migrates later under the documented `// i18n: pending migration` marker convention — deliberately NOT half-wrapped. **The JA strings are machine-authored platform copy and native-speaker review is OWED before market launch** (filed on the punchlist with the glossary). Proven by 27/27 Playwright assertions (provider EN→JA incl. health labels; persistence across reload AND a fresh login with empty localStorage — the account copy verified as a DB fact, `users.preferences.settings.language='ja'`; guest switch + reload; Accept-Language; en-US default; and the EN-fallback surface rendering real English with **no raw key in the DOM**). All 11 keep-green suites at their expected counts (102 proofs), all guards exit 0, lockfile `replit.local` = 0, tsc holds at **183** (baseline unmoved). No migration, no schema DDL, no publish-push trap. | ruling 60 Phase A lane, 2026-08-11 |
| 58 | 2026-08-11 | [advisory] | **R4 lands the protected-deliverable rail: `objstore:<key>` STREAMS, a legacy pasted URL still REVEALS — closing the D3 gap ruling 41's PROMOTION-side provenance framing left open one product-layer up (QA_PUNCH_LIST.md P1).** D3 gated WHO could reach `GET /api/service-bookings/:id/deliverable` correctly (9 proofs, unchanged) but then `res.json({ fileUrl, ... })`'d the provider's pasted external URL verbatim — the entitlement gated a ONE-TIME URL REVEAL, not file access: once any buyer saw it, the link was permanently shareable, unrevokable, outside platform control. **Key-not-URL discipline (the trap this ruling avoids):** `server/infrastructure/object-storage.ts`'s pre-existing `uploadBuffer()` returns `objectPublicUrl(path)` — a raw `https://storage.googleapis.com/<bucket>/<key>` string. Persisting or returning THAT would recreate the identical hole on a different host; R4 persists ONLY the storage KEY, and the wrapper's return value from `uploadBuffer` is never stored or transmitted on this rail (see the privacy finding below). **Proxy, not signed URL:** the wrapper had no read function at all (`uploadBuffer`/`deleteObject`/`objectPublicUrl` only) — added `downloadBytes(path): Promise<Buffer>` (the `@replit/object-storage` client's `downloadAsBytes`), and the serve path downloads server-side and STREAMS the bytes (`Content-Type: application/pdf`, `Content-Disposition: attachment`) rather than minting a signed URL — a signed URL is itself a bearer credential with the same shareability problem at a shorter TTL; streaming discloses nothing shareable at all. **The `objstore:` prefix is the by-construction discriminator** (the ruling-56/§15b pattern): `provider_services.serviceFile` — the SAME column, no migration, no enum change (publish-trap avoidance) — now carries either `objstore:<key>` (managed, proxied, revocable) or a bare URL (legacy, unrevokable link-delivery); the endpoint branches on the prefix, and a legacy row keeps working exactly as before, now carrying an explicit `protected: false` honesty marker the client can render truthfully. **Bucket public/private finding:** checked, not assumed — `.replit`'s `[objectStorage]` stanza carries no `publicPaths` grant anywhere in the repo, and `@replit/object-storage`'s `Client` authenticates to GCS via Replit's sidecar-issued credentials (ADC dumped from the SDK's own `dist/index.js`: `REPLIT_SIDECAR_ENDPOINT`/`REPLIT_CREDENTIAL_URL`/`REPLIT_TOKEN_URL`), the standard PRIVATE-bucket posture for Replit App Storage — a bare unauthenticated GET against `objectPublicUrl()`'s URL is expected to 403. **This could NOT be confirmed with a live network call in this environment** (no bucket is attached here — `REPLIT_OBJECT_STORAGE_BUCKET` is unset by design, per the task brief). Stated honestly, not assumed either way: residual exposure if the bucket turns out to be public-readable is bounded by key unguessability alone, so storage keys embed a `crypto.randomBytes(16)` hex token (`deliverables/<serviceId>/<32-hex-chars>.pdf`) regardless of the finding — defense-in-depth that costs nothing and is never relied upon as the only control (the proxy path never calls `objectPublicUrl()` or discloses the key in any response). **Download log (R5):** new table `deliverable_downloads` (migration 194, declared in `shared/schema.ts` — publish-trap rule: booking_id/service_id/user_id FKs `ON DELETE CASCADE`, `protected` boolean, `downloaded_at`) gets exactly one row per SUCCESSFUL fetch (either shape), giving a future D8 "auto-complete after N days undownloaded" pass the download SIGNAL it does not yet have (P2) — this migration implements NO completion/auto-complete logic itself; D8 remains unruled. **Upload intake, no new dependency:** no multipart plumbing exists anywhere in this codebase (no multer/busboy/formidable — verified by grep; `vendor-management.service.ts`'s `uploadDocument` takes a buffer with no HTTP caller) — `POST /api/provider/services/:id/deliverable-file` uses `express.raw({ type: ["application/pdf","application/octet-stream"], limit:"20mb" })` scoped to that one route only, zero new npm dependencies. Owner-gated the same way `serviceImage` already is (session/ownership check, not a privileged §14/§18/§19 field — `serviceFile` needed no allowlist before and needs none now); validates the PDF by its `%PDF-` magic bytes (Content-Type is client-declared and not trusted alone) and a 20MB cap; degrades honestly with a `503 OBJECT_STORAGE_UNAVAILABLE` when the bucket env is absent — never a crash, never a fabricated success. **Test driver, no live Replit credentials required:** `object-storage.ts` gained an `OBJECT_STORAGE_DRIVER=memory` in-process `Map<string,Buffer>` driver, production-fenced by construction (`NODE_ENV !== "production"` required in addition to the env var) — and the real `@replit/object-storage` import was moved from a top-level `import` to a lazy `import()` inside `getClient()` specifically so the memory driver works even when that package cannot be installed at all (this sandbox's npm proxy blocks it; found while building this suite). `server/__tests__/deliverable-protected-rail.http.test.ts` (13 proofs) runs the REAL route code — upload then download — against the fake driver over real HTTP, asserting byte-for-byte equality between what was uploaded and what streams back, that the managed response carries no JSON/URL/key of any kind, that the legacy shape is unchanged plus `protected:false`, that entitlement negatives still 404 on a managed deliverable, and that `deliverable_downloads` gets exactly one row per success and zero on a 404. **Guard's negative space (ruling 43):** existing pasted-URL deliverables are NOT migrated — `provider_services` rows with a non-empty `serviceFile` lacking the `objstore:` prefix remain unprotected link-delivery by design (an owner must re-upload through the new endpoint to convert one); this is the same "detect, don't repair" posture as §17. A read-only inventory query at ledger-time on this dev DB found **0 such rows** (only this branch's own test fixtures carry `serviceFile` at all locally) — the prod count is unknown from this lane and is a P1 follow-up, not a blocker (the same `#PS17` shared-dev-bench limitation ruling 46 already recorded). ServiceForm's deliverable-field copy (`client/src/components/ServiceForm.tsx`) now states which case a given listing is actually in, factually: an `objstore:`-backed listing shows "platform-protected, revocable, pasting a link replaces it with an unprotected one"; anything else keeps the pre-existing unprotected-link honesty copy. All 9 `service-deliverable.http.test.ts` proofs, all 11 `f2-verification-gate.http.test.ts`, all 9 `publish-verification-hold.http.test.ts`, all 7 `short-links-frame.http.test.ts`, all 2 `posting-opportunities-frame.http.test.ts` and the 13 new R4/R5 proofs are green (51/51) against a dev server booted with `OBJECT_STORAGE_DRIVER=memory` + `RATE_LIMIT_LOOPBACK_SKIP=1`; `check-money-endpoints.cjs`, `phase2-fee-gate.sh`, `check-unmounted-routers.cjs` all exit 0; tsc holds at the CI-locked baseline exactly (183 — this sandbox's `node_modules` initially lacked `@replit/object-storage` entirely, which independently of this change already put the pre-R4 base commit at 184 locally; isolated via a throwaway `git worktree` diff to confirm this change was line-count-neutral against that pre-existing local artifact, then resolved for good by installing the package, after which the count read exactly 183). Tagged `[advisory]`, not `[guarded:]`, deliberately — the new suite is committed but not yet wired into any CI workflow (the same gap ruling 57 closed for the five prior suites); wiring it into `publish-gate-and-fundamentals-gate.yml` as a sixth step is filed, not executed here. | `server/infrastructure/object-storage.ts`; `server/routes.ts`; `shared/schema.ts`; migration 194; `server/__tests__/deliverable-protected-rail.http.test.ts`; QA_PUNCH_LIST.md P1; ruling 41; ruling 56 |
| 66 | 2026-08-11 | [advisory] | **RULING 63 EXECUTED — D8 per-method completion is ONE machinery, six conditions; and the completion event turned out not to exist for ANY method, not just pdf.** **(0) THE FINDING THAT REFRAMES P2.** The punchlist described the gap as pdf-shaped ("a pdf booking reaches `completed` only via the manual path: provider flips status → held earning → confirm-completion releases it"). That premise is wrong in its first clause: **the provider CANNOT flip a booking to `completed`** — `OWNER_SETTABLE_BOOKING_STATUSES` is `["confirmed","cancelled"]` and the handler answers *"Completion is confirmed by the traveler"* (deliberate, to stop self-crediting), while `POST /api/bookings/:id/confirm-completion` **requires the booking to already BE `completed`**. Grep of every writer of `service_bookings.status='completed'` on `main` returns exactly ONE: `admin.routes.ts`'s dispute-REJECT. So the escrow completion path was a **closed loop — unreachable for every delivery method** — and no service booking outside a rejected dispute has ever minted a held earning. P2's "held earning on an inattentive traveler" was therefore the *smaller* half of the defect. **(1) ONE MACHINERY, N CALLERS** (`server/services/booking-completion.service.ts`), the §15c "one promotion, two callers" shape one lane over: `completeBooking()` is the ONLY place a D8 rule moves a booking to `completed`. It adds **no money logic whatsoever** — it re-resolves eligibility server-side, then calls the EXISTING `storage.updateServiceBookingStatus(id,'completed',reason,['confirmed'])`, which already owns the platform-revenue row and both held earnings. Fee bands, `checkout-claim.service.ts`, `promotePaidCheckout` and every rate/amount are **untouched**: this lane wires WHEN completion happens, never HOW MUCH moves (§8/§14/§18). **RELEASE is deliberately NOT part of completion** — the held earning matures through the existing escrow window or the traveler's existing early confirm, which is exactly what gives ruling 63's async row its "disputable window" **for free, with no second constant**: `holdWindowDays('service_booking')` is already both the earning's `availableAt` and the `POST /api/bookings/:id/dispute` cutoff. **(2) THE TABLE AS IMPLEMENTED.** `in_person`/`hybrid` → `confirm_completion`, **untouched by every D8 caller** (ruling 63: "as built"; the timer and the owner rail both refuse it with a stated reason). `property`/`property_room` → `checkout_date`, from the `bookingDetails.checkOut` snapshot checkout already writes, eligible once that DAY has fully passed (never mid-stay). `pdf` → `artifact_timer`, both arms as worded: **7 days after the FIRST `deliverable_downloads` row** (migration 194 — needs no new state, so it covers every row however old), **or 7 days undownloaded post-delivery**. `call`/`video`/`voice_notes` → `session_end`, provider-confirmed and **evidence-gated against the booked slot's own end time**. `async_messaging` → `provider_declared`. `bundle` → `bundle_components`: per-component declarations accumulate on the EXISTING `bookingDetails` jsonb (keyed against the purchase-time `bundleComponents` snapshot — a component not in that snapshot is REJECTED, never merged), and only the last one completes the booking. **Method resolution has ONE home**: `completionRuleFor` in `shared/service-fundamentals.ts`, beside the D2 predicates — the job and the route carry no method list of their own. **(3) THE OWNER RAIL'S SELF-CREDIT OBJECTION IS ANSWERED, NOT WAIVED.** `POST /api/provider|expert/bookings/:id/complete` is not a status write: the rule is server-resolved from the service row, an owner may declare only the three rules ruling 63 assigns them, `session_end` cannot be declared before the slot's end time has passed, and the flip mints a **held** earning whose clearance window IS the traveler's dispute window — so a wrong declaration is disputable and reversible before any money moves. Body is an explicit **allowlist of one field** (`componentServiceId`, bundles only); acting user from the session, booking by path + ownership, undifferentiated 404 for a non-owner (§14/§19). The `PATCH …/status` rail still refuses `completed` and was **not** loosened. **(4) TIMERS = §15 SHAPE.** `server/jobs/bookingAutoCompletion.ts` DETECTS and drives the shared function with an actor tag; hourly, beside the earnings-release scheduler. The flip's atomic conditional is the guard, so a double run, two overlapping passes, or a pass racing the owner produce exactly ONE flip, ONE earning set and ONE diary row — the loser is a no-op and **there is no compensating rollback anywhere in this lane**. `payment_pending` is never an accepted from-state: a provisional claim stays the claim machine's (§15b/§18b). **(5) OBSERVABILITY — the choice, stated (build charter §2): NO runs table.** Each flip records itself twice — `bookingDetails.completion` on the booking row (rule, actor, timestamp and the EVIDENCE the decision rested on; present even for a tripless booking) and an `item_transition_log` `booking_completed` row (new event type, new `provider`/`auto_complete` actor types — code-only vocabulary, no CHECK) when the booking carries a trip, which the diary requires by construction. Each RUN logs one structured line **including a run that did nothing**, so a dead scheduler is an absence of lines rather than a quiet day (§17 rule 2's spirit without its table). **(6) MIGRATION 196 — the only schema change**, one additive nullable `provider_services.deliverable_uploaded_at`, no CHECK, no backfill, declared in `shared/schema.ts` (publish-trap rule). It exists because ruling 63's undownloaded arm measures from POST-DELIVERY and nothing recorded when a deliverable landed (`updated_at` moves on any edit), so without it a provider uploading 30 days late would have had the timer fire at day 7 on a booking where **nothing was delivered**. It is a MONEY-TIMER field, so it is stripped in **both** §18 layers — `insertProviderServiceSchema.omit()` and a strip-and-DERIVE in `updateProviderService` that stamps it only when the deliverable value genuinely CHANGES (an unrelated re-save must not push every open booking's timer out). §13: NULL is never read as a value — that arm is skipped with the reason `no_delivery_timestamp`, while the downloaded arm still covers the row. **(7) NEGATIVE SPACE (ruling 43) — what this does NOT build.** **NO-SHOW POLICY IS NOT INVENTED HERE** — ruling 63 files it as its own follow-up and this lane touched none of it: a call whose slot ended is completable whether or not anyone attended, and there is no attendance signal, no no-show state and no no-show refund path. **No release logic** — completion never releases early, and `confirm-completion`/`releaseMaturedEarnings`/`reverseEarningsForBooking` are byte-for-byte unchanged. **No refund lane** — ruling 63 routes partial bundles to the EXISTING refund lane and this lane deliberately does not duplicate or extend it; a partial bundle simply never completes and pays out nothing. **No in-person fix** — that rule is "unchanged" by the ruling's own words, so the deadlock in finding (0) **remains open for `in_person`/`hybrid`** and is filed, not silently repaired under cover of this ruling. **No UI** — the owner-complete endpoint has no client surface yet. **No `voice_notes` path in practice**: D2 does not classify it as scheduled, so such a booking normally carries no `slot_id` and the session-end gate refuses it with `no_booked_slot` — reported as a gap, not papered over with a guessed session end. **No CI wiring** — hence `[advisory]`, the same gap rulings 58/64 recorded. Proven by `server/__tests__/booking-completion-machinery.db.test.ts` (**17/17**, ten of them negatives: both arms firing and both arms NOT firing early; the §13 no-delivery-timestamp and no-checkout-date skips; the double-run one-flip/one-earning/one-diary proof; a call booking untouched by the timer; a `payment_pending` claim never completed; in-person refused by every D8 caller; session-not-ended and no-slot both refused with zero earnings; async completed with earnings HELD and the EXISTING release job refusing to release inside the window; a partial bundle recorded-but-not-completed with zero earnings and a bogus component rejected; non-owner 404 / unauthenticated rejected; and the delivery clock proven un-settable by a client and derived server-side). Keep-green: f2-verification-gate 11, publish-verification-hold 9, service-deliverable 9, deliverable-protected-rail 13, service-logistics-capture 9, short-links-frame 7, posting-opportunities-frame 2, checkout-claim-sweep 9, checkout-payment-promotion 11, reconciliation-detection 15, booking-birth-provenance 7 — **102/102**. `check-money-endpoints.cjs`, `phase2-fee-gate.sh`, `check-unmounted-routers.cjs`, `check-decision-guards.cjs`, `check-omit-schema-ratchet.cjs` all exit 0; lockfile `replit.local` = 0; tsc holds at the CI-locked baseline **183**. Closes QA_PUNCH_LIST P2 and finding #12 (build commit `f3984da`). | ruling 63; rulings 58/59 (migration 194); QA_PUNCH_LIST.md P2 + finding #12; migration 196; `shared/service-fundamentals.ts`; `server/services/booking-completion.service.ts`; `server/jobs/bookingAutoCompletion.ts`; `server/config/completion-windows.config.ts`; `server/__tests__/booking-completion-machinery.db.test.ts` |
| 67 | 2026-08-11 | [advisory] | **RULING 62's D9 CLAUSE EXECUTED — attestations keyed to method + category risk, with the D2 honesty shape; a TITLE-CLAIM affirmation, NOT a licensing gate.** Ruling 62, verbatim: *"D9: onboarding attestations keyed to method + category risk, inapplicable ones OMITTED with a reason — the D2 honesty shape at the top of the funnel."* Concrete first application is QA_PUNCH_LIST **SS-5** / SIX_SIGMA_PROVIDER_PASS §3 **R-1**: Japan **deregulated paid guiding on 2018-01-04** — no licence is required to trade and this platform must never behave as though one were — but the title 通訳案内士 (*Tsūyaku Annai-shi*) is legally protected and an unlicensed guide may not use a title implying official certification. Listing name and description are free text with nothing asking, so the honest intervention is an affirmation about the **CLAIM**, never a gate on the **TRADE**. **(1) THE APPLICABLE SET IS SERVER-DERIVED — a client never decides what applies to itself.** The keying predicate lives in ONE shared module, `shared/service-attestations.ts`, beside (and importing) the D2 predicates: `isPlaceAnchored`/`isClassifiable` are REUSED, never re-implemented, and this module adds only the CATEGORY axis. The server re-resolves the set from the LIVE row — delivery method, product shape, and the row's category **key AND slug** — on every read and again on every write; applicability is never stored, so a listing that changes delivery method never carries a stale "applies to you" claim. Both category identifiers are matched because the live taxonomy has rows with a `category_key` and rows with only a `slug` (e.g. Language & Translation), and keying on one alone would have silently missed half of it. This is §14's derive-it-server-side posture generalized from the AMOUNT to the APPLICABILITY. **(2) THE CATALOG — three types, each with a stated rationale, deliberately not a compliance matrix.** `title_claim_honesty` (SS-5): **guide/interpreting categories, EVERY delivery method** — a pdf itinerary sold by a "government certified guide" makes exactly the same claim, so the method axis must NOT gate it; `language-translation` is included on purpose because 通訳案内士 is literally an *interpreter*-guide title. `in_person_safety_basics`: **place-anchored methods, any category** — the only shape that puts a traveler physically in a provider's care, where the platform verifies nothing and carries no cover, and had never said so. `food_safety_disclosure`: **food categories AND place-anchored** — both axes, because a recipe pdf serves nobody. Each type carries EN+JA label and body; the JA is **machine-authored platform copy** and native review is filed on the EXISTING **I18N-1** debt, as a set with the ruling-65 glossary — this is legally-loaded copy in the launch market and must not be read as reviewed. **(3) §13 HONESTY — THREE states, not two.** APPLICABLE (rendered, affirmable); **OMITTED WITH A STATED REASON** when the row lacks the signal to decide (no category / no delivery method) — never guessed, never rendered as compliant, and **not affirmable**: affirming an undecidable key is a 400 carrying the reason, because recording a statement about a condition the platform cannot say applies is the dishonest artifact D9 exists to prevent; and SILENCE when we know it does NOT apply — no entry at all, the `delivery_asset` precedent in `provider-listing-health.routes.ts` (an omission entry still reads as "this could apply to you"). A decision we CAN make on one axis outranks missing data on the other, so only genuine ignorance produces an omission. Each reason names **what ITS OWN missing signal would have decided** and is LOCALIZED like the catalog copy — both of those were defects found by driving the real wizard (one generic sentence rendered twice, and rendered in English inside an otherwise-Japanese card), not by reading the code. **(4) STORAGE — migration 197, `service_attestations`**, child rows of `provider_services` on the `dmo_extracted_places`/`service_route_points` pattern: ON DELETE CASCADE, **UNIQUE (service_id, attestation_key)** — and that UNIQUE is the idempotency MECHANISM, not a nicety (the write is `INSERT … ON CONFLICT DO NOTHING`, so a replay keeps the FIRST affirmation's timestamp and never re-dates the record). `affirmed_by` is the SESSION user, ON DELETE SET NULL so deleting an account never erases the historical fact. No DB CHECK on the key (app-enforced vocabulary — the migration-144/195 posture, publish-trap avoidance); table + UNIQUE + index declared in `shared/schema.ts` (publish-trap rule). Chosen over a jsonb column because an attestation is an evidentiary record that must be joined to a user and read back years later (ruling 13's class-B posture), and because rows make idempotency structural rather than application logic. **(5) WRITE PATH — owner-gated, §19 ALLOWLIST, append-only.** `GET/POST /api/provider/services/:id/attestations` (`server/routes/service-attestations.routes.ts`, MOUNTED). The body is a hand-written zod object of exactly ONE field (`affirm: string[]`) — deliberately NOT a `createInsertSchema(...).omit()` over a table whose every other column is server-stamped, which is precisely the denylist shape ruling 46 named; **no `createInsertSchema` call was added, so the #PS18 ratchet count is untouched at 190.** `affirmedAt`/`affirmedBy` are stamped in storage from the session; an unknown key, an inapplicable key or an undecidable key each 400 with a distinct code and **one bad key refuses the WHOLE batch** (no partial write). Non-owner gets an undifferentiated 404. **(6) CAPTURE SURFACE — the ServiceForm wizard's "Terms & requirements" step** (the C9 precedent: per-listing curation belongs to the "what I sell" module), in a NEW fully-i18n-wrapped component `client/src/components/provider/service-attestations-card.tsx`. It renders only when the shared resolver returns a non-empty applicable set; an already-affirmed row is checked, DISABLED and date-stamped (a statement made, not a setting). The affirmation is a child row, so it is written by a second call AFTER the listing save; a failure there does NOT report the save as failed (§13) but raises its own honest toast. **Omission surfacing — the judgment, stated:** omissions render as ONE muted de-duplicated line INSIDE the card and only when something is already affirmable, because every omission here is fixable in step 1 of this same wizard; a listing with nothing applicable shows no card at all rather than a page of caveats. **The wizard's own toast stays hardcoded English on purpose** — ServiceForm is on I18N-2's "migrate the WHOLE surface in one commit" list and a single `t()` there would half-wrap it; the new card is a new file and is wholly wrapped. **(7) NEGATIVE SPACE (ruling 43) — what this deliberately does NOT do, each filed rather than silently skipped.** **NO PUBLISH GATE** — nothing here can block `status:'active'`; whether an unattested listing should be blocked is NOT ratified and is filed as a decision-maker question (**SS-5a**). **NO TRAVELER-FACING SURFACE** — no trust badge, no attestation on `/services/:id`; displaying these to travelers is its own ruling (**SS-5b**). **No licence-document upload or verification**, **no admin review queue change**, **no scanning of listing text for violating phrases** (**SS-5c**) — that last one is the arm that would actually catch a lie, and it is a different, unruled product. **No un-affirm/DELETE path** — the record is append-only. **No CI wiring** — hence `[advisory]`, the same gap rulings 58/64/66 record for their committed suites. Proven by `server/__tests__/service-attestations.http.test.ts` (**13/13**, eleven of them refusals: the pdf-vs-in-person and food-vs-remote keying negatives; the omitted-with-reason §13 proofs incl. per-key distinctness and JA presence; inapplicable, undecidable and unknown keys each refused with nothing written; one-bad-key-refuses-the-batch; client-supplied `affirmedAt`/`affirmedBy`/`userId`/ `serviceId` all ignored; non-owner 404 on both verbs and unauthenticated rejected; three CONCURRENT replays yielding one row with the first timestamp plus a direct proof that the DB UNIQUE really rejects a second row; and an affirmation whose key stopped applying still reported). **Reachability was proven in the real browser, not just the API** (the SS-1 standing lesson — *proved correct, never proved reachable*): the card renders in the live wizard for four keying cases incl. Japanese, and a real checkbox + the real Save button persisted a row end-to-end. Keep-green: f2-verification- gate 11, publish-verification-hold 9, service-deliverable 9, deliverable-protected-rail 13, service-logistics-capture 9, short-links-frame 7, posting-opportunities-frame 2, checkout-claim- sweep 9, checkout-payment-promotion 11, reconciliation-detection 15, booking-birth-provenance 7, booking-completion-machinery 17 — **119/119**, plus **27/27** `scripts/i18n-chrome-proofs.mjs` (EN/JA key parity exact, 35 provider keys both sides). `check-money-endpoints.cjs`, `phase2-fee-gate.sh`, `check-unmounted-routers.cjs`, `check-decision-guards.cjs`, `check-omit-schema-ratchet.cjs` all exit 0; lockfile `replit.local` = 0; tsc holds at the CI-locked baseline **183**. Addresses QA_PUNCH_LIST **SS-5** (build commit `f0ac5a0`); the not-built arms are filed as **SS-5a** (publish gate), **SS-5b** (traveler display) and **SS-5c** (listing-text scanning). | ruling 62 (D9); QA_PUNCH_LIST.md SS-5 + SS-5a/b/c; docs/findings/SIX_SIGMA_PROVIDER_PASS.md §3 R-1; migration 197; `shared/service-attestations.ts`; `server/routes/service-attestations.routes.ts`; `client/src/components/provider/service-attestations-card.tsx`; `server/__tests__/service-attestations.http.test.ts` |
| 68 | 2026-08-11 | [guarded: fee-resolution-authority] for the resolution + attribution clauses asserted in the CI-wired suites; [advisory] for the rest | **RULING 61 EXECUTED — D6 rails attribution: the chain now EXISTS, and it starts default-deny.** **(0) THE AUDIT THAT SHAPED IT.** The resolver half was already built and CI-pinned (rulings 47/48/51: rails is min(category band, rails), rails waives the traveler fee, a bandless category throws) — but `resolveProviderRate`'s `isRails` flag **had no caller anywhere in the repo**, and `fee_ledger` (migration 179) **had no writer at all**. So "rails" existed as a resolver capability with nothing able to switch it on and nowhere to record it. This lane builds ONLY the missing links: capture → validation → resolver input → ledger stamp. **(1) THE REF IS A SELECTOR, NEVER A VALUE (§14/§18).** The client already relayed a captured `?ref=` into the checkout body (S4/migration 141, analytics-only per ruling 45) and **no client code changed** — `server/services/rails-attribution.service.ts` re-reads that opaque token against `short_links` and re-authorizes it against THIS traveler and THIS listing's owner. It can express neither a rate nor an amount; a forged token buys nothing. The RATE is **ONE call into the existing single resolver** (§18 rule 1 — min(band, rails) and the waiver stay inside it; this lane re-implements neither). **(2) WHAT THE PIN REFUSES, each degrading to the FULL rate and never to an error:** fabricated (`unknown_ref`), malformed, **expired** (`expires_at`, migration 198), **wrong-provider** (`owner_mismatch`), **self-referral** (a provider may not discount their own take by clicking their own link), non-provider owner, categoryless service, and a fail-loud band throw (`band_unresolvable` — an optional discount lane must never turn a breached R2 guard into a failed purchase). A per-entity override (ruling 47) outranks rails and deliberately does **not** carry the waiver along with it. Every refusal is recorded on the booking row (`bookingDetails.railsAttribution`, the ruling-66 tripless-booking precedent) **and** logged — never a silent grant, never a blocked purchase. **(3) OWNER MATCH, NOT TARGET MATCH — stated because it is a judgment.** Eligibility asks whether the link's OWNER owns the service being booked, so a provider's storefront/story link carries rails onto any of THEIR OWN listings in the same cart. It never crosses to another owner's listing. **(4) ONE DECISION, TWO LOOPS.** `/api/checkout` resolves per-item rates **twice** (quote loop and booking loop). The attribution decision is made ONCE in a pre-pass both loops read, so the amount quoted and the amount claimed cannot disagree. A rails line takes its share from the resolver and deliberately **outranks the per-service `revenueShareRate` snapshot** that ruling 47 dethroned; a non-rails line is byte-identical to before. **(5) THE CONSEQUENCE, STATED HONESTLY.** A rails booking is the FIRST charge path in the platform to price through the D1 single resolver; direct provider bookings still resolve the legacy `expert_standard` band pending the charge-path repoint (lane item 1C, ruling 45's "spec-ahead-of-code in its entirety"). So a rails booking today is cheaper for TWO reasons at once — the rails min() and the un-migrated direct path. Both numbers are decision-maker-ratified band values, the direction is always a SMALLER platform take (never an overcharge to a traveler or an underpayment to a provider), and it is reachable only through a provider's own validated link. **1C remains owed.** **(6) THE LEDGER — a rails-only slice, and it says so.** `server/services/fee-ledger.service.ts` is migration 179's first writer: ONE `provider_commission_rails` row per rails booking, `rate_source` stamped from the resolver (`rails`, or `band` in the premium case where the category band undercut rails — the fee TYPE names the LANE, `rate_source` names the BAND that decided), written at the **authorization stamp** (179's own strategy (b), so the PaymentIntent is in scope and a sweepable provisional claim never gets a row in an append-only table). Idempotent by a per-booking key on the existing UNIQUE index, driven by TWO callers (the inline post-authorization write and `promotePaidCheckout`'s catch-up for the server-died-mid-authorization window) — a double signal lands ONE row and the replay never rewrites the first recorded fact. **DEVIATION, DELIBERATE:** the waiver is **not** a zero row, because migration 179 CHECKs `amount <> 0` on purpose ("a zero-amount fee event … hides a resolution bug behind a real-looking row") — it rides the rails row as a first-class metadata fact-set (waived, basis, the band that priced it, the amount given up) carrying the §13 flag that the D3 traveler fee **is not billed on the direct path today**, so nobody may read the record as "we stopped charging them $X". **(7) NEGATIVE SPACE (ruling 43) — what this deliberately does NOT do.** **THE WAIVER MARKETING CAPTION STAYS OUT OF THE CAPTION ENGINE** — zero caption/share-image diffs in this lane; ruling 61 unlocks it only on a green pin, and scheduling it still needs a decision-maker go. **No analytics surface** (measurement stays on Performance, ruling 22d), **no rate-editing UI**, **no band value changed**, **no expert rails model invented**, **no `/r/:code` behaviour change** (an expired link still redirects and still counts its click; S4 analytics attribution is untouched — a click that happened is a true analytics fact whatever the fee lane says). `short_links.expires_at` has **no writer yet** (owner/admin expiry control filed) — it is declared read-side first because the alternative was to let ruling 61's "expired" refusal silently not exist. `fee_ledger` remains a RAILS-ONLY slice: direct checkout, the legacy rail, ready-mades, templates, coordination, tips and affiliate margin still write nothing, so the per-booking invariant is not yet assertable platform-wide — **do not aggregate the table as if it were complete.** **(8) PROOFS.** `server/__tests__/rails-attribution.db.test.ts` **R1–R11** (11) added to the CI-wired gate as a second step in the same job, plus **A9** in the gated resolver suite (9/9) — an admin edit to `provider_rails` reaches the next rails resolution, so the boolean selects a lane and never carries a rate. Migration 198 is one additive nullable timestamp, no CHECK, no backfill, declared in `shared/schema.ts` (publish-trap rule) and omitted from `insertShortLinkSchema` (§18 rule 3 — a money-timer field with no consumer is still stripped). Sweep 9/9, promotion 11/11, detection 15/15, provenance 7/7 and the rest of the 132-proof battery untouched and green. | dispatch v1.2 §1; ruling 61; CLAUDE.md §8/§13/§14/§15/§17/§18/§19 |


## Console Realign series (CLOSED — ruling 28; original letters preserved; full text lives in docs/briefs/CONSOLE_REALIGN_BRIEF.md §"Ratified decisions")

| ID | Date | Tag | Ruling (verbatim first clause; full text in the brief) |
|----|------|-----|--------|
| R-A | 2026-08-03 | [advisory] | "The existing dashboard design stays byte-for-byte EXCEPT: (1) a compact slip strip caps the embedded Plan Card … (2) the 'New experience' CTA keeps its look but opens the intake panel (R-C)." |
| R-B | 2026-08-03 | [advisory] | "All trip creation converges on `POST /api/trips` → `storage.createTrip` (mints owner row + trackingNumber)." |
| R-C | 2026-08-03 | [advisory] | "'Plan new' becomes a PANEL (opened from any + New plan / New experience CTA), not a sidebar destination." |
| R-D | 2026-08-03 | [advisory] | "The chat stays; a live DRAFT PANEL (TripContext fields) fills only from what the conversation actually establishes … No trip is persisted unless created." |
| R-E | 2026-08-03 | [advisory] | "Planning-phase arrivals land on the slip: create (any door), both apply paths (the `?autoApply=1` divergence dies), My plans 'View' …" |
| R-F | 2026-08-03 | [advisory] | "Additive nullable `trips.finalized_at` timestamp (NOT a revival of the dead `trips.status` — Lane 3 Option B stands)." |
| R-G | 2026-08-03 | [advisory] | "Retire entries: Plan new (→ panel), Messages (→ Inbox's Messages tab; /chat stays the thread page), Notifications page …" |
| R-H | 2026-08-03 | [advisory] | "Home gains 'While you were away' … and 'Today's move' … Every line traces to a real row — no invented nudges." |
| R-I | 2026-08-03 | [advisory] | "§13 widget fixes — MANDATED, ship first: TravelPulsePanel wired to real endpoints; greeting derived from real notifications or removed; PlanCard.tsx tripId filter fixed." |
| R-J | 2026-08-03 | [advisory] | "Connected AI — MCP connector, LATER lane, gated on R-B … agents build & stage, humans pay." |

## Guard registry (reality as of 2026-08-04 — ruling 27 note: the protocol name-checked six from memory; reality has eleven)

In-CI guards (name → script → CI location → **negative space**).

**Read the last column first (ruling 43).** Every guard here passes TRUTHFULLY within its predicate and
says nothing whatever outside it. Three holes were found in a single audit behind three green checks,
so a green result means *green-within-stated-bounds* and nothing more. A guard whose negative space is
not stated is being read as coverage it does not provide. When you EXTEND a guard's reach, revisit its
predicates in the same change (protocol advisory) **and** update this column.

| Guard | Script | CI | Negative space — what the predicate does NOT cover |
|-------|--------|-----|-----|
| mint-path owner tripwire + trackingNumber tripwire (rulings 10, 17) | `scripts/check-trip-mint-owner-access.cjs` | build.yml | Covers the KNOWN mint call sites by name; a trip minted through a new helper, a raw `INSERT INTO trips`, or a seed/migration is invisible. Proves an owner row and a tracking number are WRITTEN, never that either is CORRECT. |
| claims-only lookups | `scripts/check-claims-only-user-lookups.cjs` | build.yml | Scoped to `claims.sub` IDENTITY reads only. **A `claims.role` read is entirely outside it** — which is why `/api/provider/bookings`'s stale session-snapshot role (audit AC-2) passed unseen. It also says nothing about whether the resolved id is AUTHORIZED for what follows. |
| money-endpoints (+ commission-literal + **privileged-field** mass-assignment passes: rate, ruling 42; **payment-identity, ruling 46**) | `scripts/check-money-endpoints.cjs` | build.yml | `req.body` check is LINE-LOCAL — a body value read on one line and used on another is invisible. Scans only `server/routes` + `server/services` + `server/routes.ts`; `server/jobs`, `server/utils`, `server/storage.ts` and all client code are out of scope. `MONEY_OP_RE` is a NAME heuristic, so money moved through an indirectly-named helper is not recognised as a money handler. Handler ranges end at the NEXT route registration, so a body read in a helper declared between two routes is misattributed. The commission-literal pass knows only the values 0.9/0.1/0.75/0.25 in commission/payment/fee/payout/checkout-NAMED files. The mass-assignment pass reads `shared/schema.ts` only — a privileged field on a HAND-WRITTEN zod object, or in another schema file, is out of scope — and it knows exactly TWO classes: rate columns named rate/share/commission/split, and payment-identity columns spelled `stripe<Thing>Id`/`paymentIntentId`. **Every other privileged column a denylist schema forgets — an amount, a `status`, an authorization grant (`#PS16`) — is still reachable and still invisible here; that is the standing class of ruling 46, whose answer is pick-based schemas (`#PS18`), not a wider grep.** Its parse detection also keys on the `insert*Schema` NAME, so a DERIVED schema (`.pick()`/`.extend()` under another name, e.g. `createBookingRequestSchema`) is outside the pass — covered instead by the B6 assertion on its exact key set. **Nothing here proves the resolved rate is CORRECT — only that the client did not choose it.** |
| unmounted-routers | `scripts/check-unmounted-routers.cjs` | build.yml | Proves a router is MOUNTED, not that its routes are REACHABLE: Express is first-registered-wins, so an inline duplicate of the same path silently shadows a mounted router's copy (the `routes.ts:953` mount order is load-bearing and unguarded). Says nothing about auth, ownership or whether any endpoint has a consumer. |
| linkage-preservation | `scripts/check-linkage-preservation.cjs` | build.yml | Static check on the linkage fields it enumerates; a new linkage column, or one dropped at runtime rather than in source, is outside it. Proves the field is CARRIED, never that the value is right. |
| upsell-trust-contract | `npm run test:upsell-contract` | upsell-trust-contract.yml | Unit-level over the upsell engine's own contract; no DB, no route, no cross-surface behaviour. Does not cover the query service or any placement/pricing path that consumes its output. |
| noop-migrations | `scripts/check-noop-migrations.ts` | (Replit workflow gate) | **Not in GitHub CI** — a Replit workflow gate, so it does not run on a PR from another surface. Detects an empty/no-op migration; says nothing about whether a migration is CORRECT, idempotent, or safe against the deploy-push CHECK trap. |
| fee-config-parity | `scripts/verify-fee-config-parity.ts` | selection-controls-gate.yml | Compares the two fee-config representations for AGREEMENT; if both are wrong in the same way it passes. Does not reach `fee_bands` resolution at charge time, nor any per-service override. |
| migration-ledger idempotency | `scripts/verify-migration-ledger.ts` | selection-controls-gate.yml | Ledger/registry consistency only. Does not execute migrations, and cannot see an object the deploy-push drops because `shared/schema.ts` never declared it (the index/table trap). |
| fee-literal grep (phase2-fee-gate) | `scripts/phase2-fee-gate.sh` | build.yml (wired by ruling 27; predicate fixed + self-tested by ruling 43) | A GREP over source text: it cannot see a rate that arrives at RUNTIME — a client-supplied rate column (this is exactly how MI-1 hid), a DB row, an env var. Passes A/C match a FIXED value list, so a fee literal of an unlisted value is invisible. Pass B needs the number on the SAME LINE as a fee-ish identifier, so a rate assembled across lines, held in a map literal (e.g. `AFFILIATE_MARGIN_DEFAULTS`'s per-partner values), or named only `share`/`split`/`margin` is out of scope. `.test`/`.seed`/migration/config paths are excluded by design. Lines carrying `fee-literal-debt:#<task>` are REPORTED but do not fail. |
| chain-integrity | `server/migrations/__tests__/chain-integrity.test.ts` | build.yml (wired by ruling 27) | Verifies the registry chain's shape and ordering; does not APPLY the migrations, so a statement that fails against real data (the publish-time CHECK trap) passes here. |
| decision-guards ledger lint | `scripts/check-decision-guards.cjs` | build.yml (ruling 26) | Checks that a `[guarded: X]` tag corresponds to an actual `run:` command somewhere in CI. It cannot tell whether X's PREDICATE is any good — that gap is precisely what ruling 43 exists for. `deferred:<lane>` only warns. |
| matrix-lint | `scripts/check-coverage-matrix.cjs` | build.yml | Lints the matrix DOCUMENT (shape, expiry tags, ruling-37 wave namespacing) over `NAMESPACE_SCAN_FILES`. It never runs a single assertion, so a matrix row can be perfectly formed and completely untrue. |
| env-allowlist | `scripts/check-env-allowlist.cjs` | build.yml | Checks env-var NAMES against the allowlist. Says nothing about whether a variable is SET, valid, or pointed at the right environment — `DATABASE_URL` being absent is invisible to it. |
| **fee-resolution-authority** (rulings 47, 48, 51; **D6 attribution ruling 61**) | `server/__tests__/fee-resolution-authority.db.test.ts` + `server/__tests__/rails-attribution.db.test.ts` | `fee-resolution-authority-gate.yml` (two steps, one job) | Proves RESOLUTION and — since ruling 61 — the ATTRIBUTION half of the D6 chain, never the CHARGE. It asserts that `fee_bands` decides the rate (A1 — a band edit reaches the next resolution with no service-row touch), that rails is min(band, rails) and waives the traveler fee, that the traveler-fee cap is band data, that an entity override is stamped with a NULL `band_id`, and that a bandless category THROWS. It says **nothing** about whether a resolved rate is ever WRITTEN to `fee_ledger`, whether the ledger balances per booking (that is the invariant gate, not this one), or about any money path beyond provider commission and the traveler service fee — the other **11 of 12** census paths are entirely outside it. It also cannot see the CHARGE: both files exercise services directly, so a caller that resolves correctly and then charges something else is invisible here — including `/api/checkout` itself, whose wiring of the rails decision into the per-item split is covered by NO assertion in this gate. On the D6 half (rails-attribution.db.test.ts, ruling 61) it proves that a valid owner-matched ref selects the rails lane with the waiver, that a fabricated/expired/wrong-provider/self-referral/expert-lane ref degrades to the full rate with an enumerated reason, that the rate still moves with a `fee_bands` edit under an unchanged ref, and that the rails ledger event lands exactly once with its waiver metadata. It does NOT prove that the ledger row's amount equals the amount Stripe actually captured, that a NON-rails booking writes anything at all (no writer exists — the table is a rails-only slice today), or that any surface renders the waiver.
| **omit-schema-ratchet** (ruling 46, §19, `#PS18`) | `scripts/check-omit-schema-ratchet.cjs` | build.yml | A COUNT ratchet on `.omit()`-based `createInsertSchema(...)` call sites across the two drizzle-kit schema entry files, never an identity check — it proves the aggregate denylist total never grows, not that any specific named schema was converted to `.pick()`, and a bare (no-modifier) call folds into the same denylist bucket. It never checks whether an omitted field LIST or a `.pick()`'s field list is CORRECT — only that the shape moved. Scoped to `shared/schema.ts` + `shared/guest-invites-schema.ts` only; a hand-written zod object or an insert schema in another file is outside it. A schema DERIVED via `.pick()`/`.omit()` on an existing insert-schema VARIABLE (ruling 46's `createBookingRequestSchema` shape) does not match the `createInsertSchema(...)` call-site pattern and is not counted in either direction. It does not know whether a schema is ever `.parse()`d from a request body — that reachability judgment stays with `check-money-endpoints.cjs`'s schema-mediated pass.

Named guard candidates (not yet guards — MISSING until in CI): single-writer-per-event-type (11) · log append-only route inventory (per 12/18) · no-second-scheduling-heuristic (22 remediation) · apply-never-writes-routing_status grep · **`scripts/invariants.mjs` (exists in the repo, in NO workflow and NO package script — per ruling 27 it is MISSING, not a guard; provider-sigma AB-3 / `#PS9`)**.

---

## Decision #39 — Stripe-only KYC/KYB; Persona KYB retired; no Plaid for Experts

**Date:** Aug 2026

**Decision:** All provider identity and business verification flows now run exclusively through Stripe.

- **Expert identity** → Stripe Identity (unchanged).
- **Provider business verification** → Stripe Connect Express onboarding; Stripe performs KYB during the Express flow and signals the result via `account.updated` webhook. The `businessVerificationStatus` on `service_provider_forms` is derived from `account.charges_enabled`, `account.details_submitted`, and `account.requirements.disabled_reason`.
- **Persona KYB retired** — `POST /api/identity/business/create-inquiry` and `POST /api/webhooks/persona` both return 410 Gone. The `PERSONA_*` env vars are no longer required.
- **No Plaid** — the payout identity path relies solely on Stripe Connect; Plaid is not added to the platform.

**Why:** Stripe Express already performs KYB as part of its onboarding. Running a separate Persona check was a redundant compliance burden, added friction, and required maintaining a second HMAC-verified webhook surface. Centralising on Stripe reduces the external vendor surface to one, simplifies the deploy-time secret checklist, and keeps the compliance signal (KYB result) co-located with the payment rail (Stripe Connect).

**Publish gate (F2):** Service creation/update now enforces `identityVerificationStatus === 'verified' AND businessVerificationStatus === 'verified'` server-side (403 + `VERIFICATION_GATE` code) and in the ServiceForm UI (banner + disabled Publish button).

## Protocol advisories

Process lines that are not themselves rulings but bind the same way. Each cites the concrete incident that produced it.

- **Extending a scan's reach requires revisiting its predicates in the same change.** A scan's filters silently encode
  the assumptions of the *original* scope; widening what it looks at without re-reading what it filters on turns those
  assumptions into false findings. **Incident — the PR #435 legacy-drift near-miss (ruling 40, commit `056f5f44`):**
  the reconciliation job's legacy loop keys on `charge.metadata.bookingId` (**singular**, the legacy convention) and
  compares against an id set built only from the legacy `bookings` table. Cart PaymentIntents carry
  `metadata.bookingIds` (**plural**). Adding cart-rail coverage without revisiting that loop's predicate would have made
  **every cart purchase** fall through into the legacy `stripe_charge_no_booking` branch and be indicted as legacy
  drift — a detector manufacturing a platform-wide false alarm about the primary checkout. The guard is the deliberate
  `if (!bookingId) continue;` line at `server/jobs/stripeReconciliation.ts:612`, with the reasoning recorded in the
  source comment at `:605-608`. Applies to CI grep gates as much as to runtime scans: a gate whose predicate was written
  for one naming convention keeps passing truthfully while missing everything written in another
  (provider-sigma Phase 0, MI-3 and AC-2). | provider-sigma lane Phase 0, rider (c) |
