# Lane report — D-18 + D-41 (migrations 297 / 298)

**Branch:** `task-d18-d41-columns` · **Worktree:** `/home/user/wt-d18-d41` · started 2026-09-15.
This lane RESUMED after a container restart killed its predecessor. The predecessor's uncommitted
work was found intact and is being built on, not redone.

## Rulings being executed

- **D-18 = A** (ledger `2026-09-15-d18-announced-marker`): `ready_made_purchases.notified_at`
  (migration 297). Stamped ONLY by the one shared sender `notifyBuyerOfReadyMadeDelivery` via
  `storage.markReadyMadePurchaseNotified` once the buyer's notification row exists. §17's drift
  job DETECTS a delivered purchase with `notified_at IS NULL` older than a config window and hands
  it to that SAME notifier; the job never writes the column. Window is a config value (no literal).
  Append-only exception row with a dedupe key if the notifier still cannot send. NULL = never
  announced (§13), no backfill.
- **D-41 = yes** (ledger `2026-09-15-d41-item-quantity`): `itinerary_items.quantity`
  (migration 298), NULL = one unit. Written ONLY by `server/services/cart-projection.service.ts`.
  `syncItemProjection` carries the count both ways through the one copy-down; the materialize /
  convert-to-itinerary rails carry the cart line's count onto the item through the one shared
  `buildPlanItemValues`, LIFTING the `quantity_gt_one` refusal (D-16 (a)). Units are set on the
  CART line via D-14's rails (`shared/cart-quantity.ts` `archetypeAsks`), never on the item.
  Checkout math untouched.

## State inherited (uncommitted, verified by `git diff` at resume)

- `server/migrations/297_ready_made_purchases_notified_at.sql` + registry entry
- `server/migrations/298_itinerary_items_quantity.sql` + registry entry
- `shared/schema.ts`: `readyMadePurchases.notifiedAt` declared and OMITTED from
  `insertReadyMadePurchaseSchema`; `itineraryItems.quantity` declared and OMITTED from
  `insertItineraryItemSchema`
- `server/storage.ts`: `markReadyMadePurchaseNotified` (interface + impl, atomic conditional
  `WHERE id = ? AND notified_at IS NULL`) and `quantity` added to
  `stripItineraryItemRoutingFields`

## Progress

- [x] resume, merge `origin/main` (already up to date), read inherited diff
- [x] D-18: notifier stamp (`ready-made-notifications.service.ts` — stamps when the bell row
      EXISTS, not only when this call inserted it; result widened to
      `{notified, emailed, announced, stamped}`)
- [x] D-18: config window (`server/config/ready-made-announce.config.ts`,
      `READY_MADE_ANNOUNCE_GRACE_MINUTES`, env `READY_MADE_ANNOUNCE_GRACE_MINUTES`, default 60)
- [x] D-18: drift-job R6 (`stripeReconciliation.ts`) — detects `cloned` + NULL marker past the
      grace, hands to the shared notifier, records `rm_delivery_not_announced` (warning) ONLY when
      the announcement still does not exist; loader gained `notified_at` + a LEFT JOIN on
      `ready_made_trips` for the listing's own title/market; new kind added to
      `RECONCILIATION_EXCEPTION_KINDS` (no DB CHECK on that column → no migration)
- [x] D-41: `syncItemProjection` writes `quantity: item.quantity ?? 1` (was the literal `1`)
- [x] D-41: `buildPlanItemValues` carries the line's count (above one only — NULL already means
      one unit, and `cart_items.quantity` DEFAULT 1 is usually the column's answer not the
      traveler's); `quantity_gt_one` refusal LIFTED and the skip reason retired (§18c)
- [x] tests — two new PURE pin files (`server/__tests__/ready-made-announce-marker.test.ts` A1–A7;
      `server/__tests__/plan-item-units.test.ts` U1–U7), five new DB cases (N27a–N27e appended to
      `reconciliation-detection.db.test.ts`), and three REPAIRED cases in
      `guest-cart-becomes-plan.http.test.ts` (G6 now asserts "a unit column exists, a party column
      does not" + NULL = one unit; G7 and G13 assert the LIFT and the 3-unit round trip). Both new
      files wired into `build.yml`'s main job; `check-test-files-wired` reports no new orphan.
- [x] ledger rows `2026-09-15-d18-announced-marker` and `2026-09-15-d41-item-quantity`
- [x] punchlist: D-18 and D-41 struck through and answered; D-16 (a)'s standing refusal note and
      the §1 trailing note updated to say it is LIFTED
- [ ] validation, commit, push, PR

## Validation (run in this worktree)

| check | result |
|---|---|
| `npx tsc --noEmit -p . \| grep -c 'error TS'` | **129** (TSC_BASELINE 129 — equal, neither up nor down) |
| `npm run build` | **PASS** |
| `node scripts/check-decision-guards.cjs` | **decision-guards lint OK (0 deferred warning(s))** |
| `node scripts/check-money-endpoints.cjs --self-test` | **self-test OK (37 predicate fixtures)** |
| `node scripts/check-money-endpoints.cjs` | **exit 0** |
| `node scripts/check-test-files-wired.cjs` | **473/503 reachable; 30 orphan(s)** — all 30 are pre-existing playwright specs; both new files reachable |
| `node scripts/check-test-files-wired.cjs --self-test` | **self-test OK** |
| migration chain integrity | **2/2 pass** |
| `grep -c replit.local package-lock.json` | **0** |
| `ready-made-announce-marker.test.ts` | **7/7 pass** |
| `plan-item-units.test.ts` | **7/7 pass** |
| `reconciliation-detection.db.test.ts` | **47/47 pass** (incl. the five new N27 cases) |
| `guest-cart-becomes-plan.http.test.ts` | **16/16 pass** (incl. repaired G6/G7/G13) |
| `privileged-field-completeness-strip.test.ts` | **10/10 pass** |
| `shared/cart-quantity` + `expert-work-protected` | **40/40 pass** |
| `shared/item-kind` | **38/38 pass** |

DB tests were run against a **local Postgres** stood up in this sandbox (`traveloure`, migrations
applied through 298, sessions table created, production bundle served on :5000) — not claimed from
CI. Note for anyone reproducing: the guest-cart HTTP suite needs `JOURNEY_DB_WRITES_OK=1`, a
`localhost` (not `127.0.0.1`) `DATABASE_URL` host, and `scripts/create-sessions-table.ts` to have
been run, or every authenticated call 401s and 13/16 fail for reasons that have nothing to do with
the change (verified by running the pre-change file from `HEAD`, which failed identically).

## Deliberately NOT done

- **`client/src/pages/admin/reconciliation.tsx`'s `KIND_LABELS` was not touched.** It already omits
  every `rm_*` kind and falls back to the raw kind string (`KIND_LABELS[e.kind] ?? e.kind`), so
  adding a label for the new kind alone would make the map look complete when it is not. Recorded
  here rather than half-fixed.
- **`checkedReadyMadePurchases` and the new `readyMadeAnnounceHandOffs` are still not persisted on
  the `reconciliation_runs` row.** That is the limit the V-3 lane already stated in the result
  type's own comment: a new column there is a migration PLUS an edit to the two admin SELECTs that
  name their columns explicitly. A hand-off that FAILED is durable (it is an exception row); a
  hand-off that SUCCEEDED is in the pass response and the log line.
- **No index on either new column**, per the ruling and because nothing filters or joins on them.


## CI round 1 — two red gates, both fixed on head `48cf755` (2026-09-15)

1. **`fee-literal-guard` (`scripts/phase2-fee-gate.sh`)** — `shared/schema.ts` cited the prior lane
   by PR NUMBER, and that number's digits are in the gate's fee-cents literal list, so a prose
   citation read as a hardcoded fee. Reworded to cite the LEDGER ROW
   (`2026-09-14-readymade-notifications`) instead, in all three code files that carried it
   (`shared/schema.ts`, `server/migrations/migration-files.ts`, migration 297's own header). The
   `docs/PUNCHLIST.md` copy is the original question preserved verbatim and is out of the gate's
   file scope. `bash scripts/phase2-fee-gate.sh` now exits 0.
2. **`ready-made-clone-fields` (C7 exhaustiveness)** — a new `itinerary_items` column must be
   DECIDED by the clone allowlist, and `quantity` was undecided. Added to `CLONE_EXCLUDED_FIELDS`
   in `server/services/itinerary-item-clone.ts` (NOT carried): a plan item's unit count is the
   BUYER's own cart-line answer projected onto it (D-14's admission rule, D-41's one writer), the
   author's count is not an answer the buyer gave, and this fulfilment writes no cart line through
   which it could be admitted. §13 — the clone carries NULL, which MEANS ONE UNIT, so the row
   states exactly what every other un-carted item on that plan states. 13/13 green.

`git merge origin/main` re-run before the re-push: **already up to date** (origin/main has not
moved since the first merge at `46d383a25`).

### Validation, re-run after the fixes

| check | result |
|---|---|
| `npx tsc --noEmit -p . \| grep -c 'error TS'` | **129** (TSC_BASELINE 129) |
| `npm run build` | **PASS** |
| `bash scripts/phase2-fee-gate.sh` | **exit 0** (was RED) |
| `node scripts/check-decision-guards.cjs` | **OK (0 deferred warning(s))** |
| `node scripts/check-money-endpoints.cjs --self-test` / plain | **OK** / **exit 0** |
| `node scripts/check-test-files-wired.cjs` (+ `--self-test`) | **473/503, 30 pre-existing orphans** / **OK** |
| migration chain integrity | **2/2** |
| `grep -c replit.local package-lock.json` | **0** |
| `ready-made-announce-marker` + `plan-item-units` | **14/14** |
| `reconciliation-detection.db` | **47/47** |
| `ready-made-clone-fields.db` | **13/13** (was RED) |
| `guest-cart-becomes-plan.http` | **16/16** |


## CI round 2 — one red gate, fixed

**`unit-suite-shared`** — `shared/__tests__/cart-quantity.test.ts` **Q8** ("no second copy of the
archetype rule exists anywhere in the repo") flagged `server/services/itinerary-item-clone.ts`. The
round-1 fix explained the clone exclusion in a way that NAMED `archetypeAsks`, and that name was in
a STRING (the exclusion's reason text), not a comment — Q8 strips comments precisely so a rule
quoted in prose is not mistaken for an implementation, but a string survives the strip. The guard
was right on its own terms: a file that speaks the decision's vocabulary without importing the one
module that owns it is exactly the shape it exists to catch. The reason text now describes the rule
("the ONE shared cart-quantity derivation … (D-14)") without naming the function, and says so in
the reason itself. `shared/__tests__` is **237/237**; the clone suite is still 13/13 and both pure
pin files 14/14; tsc 129, fee gate exit 0, decision guards OK.
