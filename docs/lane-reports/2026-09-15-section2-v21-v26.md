# Lane report — section-2 re-check, V-21..V-26 (2026-09-15)

**Lane:** verify-and-file. **Docs only — no code was changed.**
**Branch:** `task-punchlist-v21-v26` off `origin/main`.
**As-of SHA:** `46d383a25bdf84c9c4ceb659cea30fbca127ce9f`.
**Product:** six new rows `V-21`..`V-26` in `docs/PUNCHLIST.md` §2, plus a one-line provenance
note under that heading naming this re-check and the sha.

Every candidate was opened on `origin/main`, quoted at `file:line`, and mapped to its governing
rule. **All six STAND** — none was closed elsewhere, so no struck row was needed (the V-1/V-2
precedent was held ready and not used).

## Verdicts

| Row | Verdict | Governing rule | Fix shape | Size |
|---|---|---|---|---|
| V-21 | **stands** | §8 (no rate literal outside `fee_bands`/config), §13, LD 43(b) | delete the credits section + the forfeiture clause; replace each published rate with a sentence naming no number | small to edit; the WORDING is a decision-maker call |
| V-22 | **stands** | §13/§14 (`total_amount` NOT NULL cannot hold "not quoted yet") | **none proposed — owned by D-30** | owned |
| V-23 | **stands** | §18b / §15 (the transition is the guard) | pass `expectedFromStatuses` to the existing 4th parameter; check the discarded return | small |
| V-24 | **stands** | §18b / §15 (same, and the flip MINTS) | `expectedFromStatuses: ["disputed"]`; check the return; no `completed_at` re-stamp | small |
| V-25 | **stands** | §15 (check-then-update is the bug), §18 rule 1 | give `updateCoordinationStatus` the same from-state parameter; SQL jsonb append for the history. *Traveler-arm ORDERING rule is D-36..D-39's* | small |
| V-26 | **stands** | D-14 (units), §15/C3 (the statement is the claim) | a unit-count parameter on `bookSlot` AND `releaseSlot`, guard inside the same statement | small-to-medium |

## Evidence, in one line each

- **V-21** `client/src/pages/terms.tsx:293-300,516` sells the credits system that answers **410**
  at `server/routes/payments.routes.ts:251,255,261,265`; `:208,252,304,307` publish four rate
  ranges as literals. `client` IS in `scripts/phase2-fee-gate.sh`'s `ROOTS`, so this is **not** a
  root gap — all three passes return zero hits on the file because Pass A/C match a fixed value
  list and Pass B needs the number on the same line as a fee-ish identifier followed by `:`/`=`.
  Prose is outside the predicate. §18d: green-within-stated-bounds.
- **V-22** `server/routes.ts:1817` `Number(service.price ?? 0)`, committed at `:1893-1908`; the
  four siblings refuse at `routes.ts:6608,6777,8622` and `payments.routes.ts:1011`.
- **V-23** `server/routes/bookings.ts:801` is a 3-arg call; the guard falls back to `eq(id)` at
  `server/storage.ts:3255-3257`. The window cutoff at `:772-786` is a read-then-write.
- **V-24** `server/routes/admin.routes.ts:1364` is a 2-arg call; `storage.ts:3249` stamps
  `completed_at` and `:3279` mints. The return value is discarded and `:1378-1383` reports success.
- **V-25** `server/routes.ts:9914-9924` applies `FORWARD_ORDER` only on the coordinator arm; the
  write at `:9926` reaches `server/storage.ts:4570-4573`, an unconditional `UPDATE … WHERE id`,
  whose `stateHistory` append is a read-modify-write at `:4557-4567`.
- **V-26** `server/storage.ts:4469-4485` increments by 1 and guards only a FULL slot; the money on
  the same line is `resolveItemBaseAmount` at `server/routes/payments.routes.ts:402`, and the
  seat-shaped archetype derives `quantity = party_size` at `shared/cart-quantity.ts:233-245`.
  `releaseSlot` (`storage.ts:4489-4501`) is symmetrically 1-unit — the two agree today, which is
  why both halves must ship together.

## Fix classes, for grouping lanes

- **Class A — §18b from-state on the shared booking writer: V-23 + V-24.** One parameter that
  already exists (`updateServiceBookingStatus`'s `expectedFromStatuses`), two call sites, the
  `OWNER_BOOKING_TRANSITIONS` table (`server/routes.ts:6848`) as the landed precedent. One lane.
- **Class B — §18b on the coordination writer: V-25(b).** Same class, a different writer; can ride
  with Class A. Its traveler-arm **rule** is D-39's lane and must not be invented here.
- **Class C — copy/§8: V-21.** Alone. Needs the decision-maker on the replacement wording.
- **Class D — owned elsewhere: V-22 → D-30.** File only.
- **Class E — inventory units: V-26.** Alone, unblocked (it is the CART side; D-41 is the plan
  side and does not block it).

## Validation

- `node scripts/check-decision-guards.cjs` → exit 0.
- `grep -c replit.local package-lock.json` → 0.
- No source file was touched.
