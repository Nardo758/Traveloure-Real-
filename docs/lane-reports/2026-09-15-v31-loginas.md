# Lane report — V-31: `loginAs` verifies the session

**Ledger row:** `2026-09-15-v31-loginas-verifies`
**Punchlist row:** section 2, **V-31**
**Branch:** `task-v31-loginas-verifies` (worktree of `origin/main` @ `42e5213ef`)

---

## 1. The defect, restated from the row

`playwright/utils/auth.ts`'s `loginAs(page, email, password)` filled `/login`, clicked submit and
waited only for the URL to stop containing `/login`. It never checked that a session cookie was set
and never read `GET /api/auth/user`. **A login that did not happen returned normally**, and every
assertion downstream described the ANONYMOUS site while reading as authorization coverage.

A second helper of the same name — `playwright/tests/personas/_persona-helpers.ts:78`
`loginAs(request, email, password?)` — posted `/api/auth/login`, asserted a 200 and returned the
actor. Two helpers, one name, two contracts: the §18 rule 1 shape underneath the row, and the
reason the silent one survived as long as it did.

**Callers, counted on `origin/main`@`42e5213ef`** (`grep -rn loginAs playwright/ e2e/`): the row
names 29 calls in four suites — `seam-cross-console` (16), `phase-4-7-advanced-flows` (7),
`phase-3-traveler-flows` (5), `stripe-init-deferral` (1). **Two more suites import the same helper
and the row does not name them**: `discover-bento-real-data` (1) and `phase-1-expert-setup` (1),
for **31 calls in six suites**. `e2e/` contains no `loginAs` at all.

## 2. What this lane changed

### 2.1 ONE `loginAs`, and it throws

`playwright/utils/auth.ts` is rewritten around one proof and one login rail:

* **`assertAuthenticatedSession(target, expectedEmail)` — THE ONE PROOF.** The jar carries the
  `connect.sid` cookie, `GET /api/auth/user` answers **200** through that same jar, and the user it
  names **is** the account asked for (email, case-insensitive). Anything else **throws**, with the
  evidence in the message (the cookie names actually present, the status, the principal actually
  found).
* **`loginAs(target, email, password)`** posts `/api/auth/login` through the caller's own cookie
  jar, then calls that proof. `target` is a `Page` **or** an `APIRequestContext` — `page.request`
  shares the browser context's jar, so proving the jar proves the page. It **returns the
  authenticated user**, so a caller can assert against the real principal instead of assuming one.
  Given a `Page` it leaves the browser signed in and on `/` (`domcontentloaded`), terms modal
  accepted if one appeared.
* **`logout(target)`** signs out and **proves the session is gone** (`GET /api/auth/user` → 401).

The header states what it proves and **what it does not** (§18d): it does not exercise the `/login`
FORM; it does not check role, entitlement or console access; it asserts the cookie by NAME; and it
reads `BASE_URL`, which it does not cross-check against Playwright's `baseURL`.

### 2.2 The persona helper is now a CALLER (§18 rule 1)

`_persona-helpers.ts`'s `loginAs` keeps its name, its persona-password default and its narrower
request-scoped type, and delegates the whole login-and-prove decision to the shared helper. There is
one implementation of "what proves a session exists" in the repository.

### 2.3 Two silent-pass helpers deleted (§18c)

`isLoggedIn()` (returned `false` on any error) and `waitForAuth()` (swallowed its own timeout with
the comment `// May already be logged in`) had **zero consumers** anywhere in `playwright/` or
`e2e/` and no state-bearing effect. Deleted rather than gated. `acceptTerms` is kept — it has an
internal consumer — and is now named for what it is: best-effort, asserting nothing.

### 2.4 A defect this lane found in its own first draft, and fixed before it shipped

The first draft of `logout` signed out through **`GET /api/logout`**. Measured against the local
production build: **404**, and `GET /api/auth/user` still **200** — the session survived. That route
is registered only inside `setupAuth`'s Replit-OIDC block. The canonical rail is
**`POST /api/auth/logout`** (`emailAuth.ts:530`), which `client/src/lib/__tests__/logout-endpoint-pin.test.ts`
already pins for the client. The helper now uses it and is held to the same pin; the endpoint choice
is written down in the header so it is not re-introduced.

## 3. The harness

A database built **from empty** by nothing but `ci-db-setup`'s three steps, and the production build
served exactly as `admin-test-email-gate.yml` serves it:

```
createdb traveloure                                   # empty
npx tsx server/migrations/migrate-entry.ts            # 301 applied, 0 skipped
npx tsx scripts/create-sessions-table.ts
npx tsx scripts/seed-ci-test-users.ts
npm run build && node dist/index.cjs                  # :5000, admin-test-email-gate.yml env block
                                                      # (NODE_ENV=production, ALLOW_TEST_ACCOUNTS=1,
                                                      #  SESSION_COOKIE_INSECURE=1, stub keys)
npx playwright test --project=chromium --workers=1 --timeout=120000 --reporter=line <spec>
  with BASE_URL, PW_AUTH_SETUP=1, DATABASE_URL, PG*
```

`/api/ready` returned HTTP 200 `{"ready":true}` before every run.

**One harness deviation, stated:** this session's egress policy blocks `cdn.playwright.dev` (and
`playwright.azureedge.net`, `storage.googleapis.com`, `dl.google.com`), so `npx playwright install
chromium` cannot run here. The runs below used the image's pre-installed
`/opt/pw-browsers/chromium-1194` (Chromium 141) via `PLAYWRIGHT_BROWSERS_PATH`, against a
`playwright-core` that expects build 1208. **No repo file was changed to accommodate this.** It is
the reason this lane has no GitHub Actions run URL, and therefore why nothing was armed (§6).

## 4. Per-suite BEFORE / AFTER

Every number below is from this harness, `--workers=1 --timeout=120000`, the same invocation both
times. The only difference between a BEFORE and an AFTER run is the two helper files.

| suite | BEFORE | AFTER (helper only) | AFTER (+ this lane's test-side repairs) |
|---|---|---|---|
| `stripe-init-deferral.spec.ts` | **1 failed, 1 passed** (16.0s) | **2 failed** (see flake note) | **2 passed** (4.4s) |
| `phase-3-traveler-flows.spec.ts` | **5 failed, 1 passed** (10.2m) | **5 failed, 1 passed** (2.2m) | not re-run (no repair applied) |
| `phase-4-7-advanced-flows.spec.ts` | **4 failed, 2 passed** (6.4m) | **3 failed, 3 passed** (2.8m) | not re-run (no repair applied) |
| `seam-cross-console.spec.ts` | **5 failed, 0 passed** | **5 failed, 0 passed** | **5 failed, 0 passed** |

The counts move very little. **That is not the result — the result is what the failures now SAY**,
and what the passes are now worth.

### 4.1 `seam-cross-console` — the whole suite was measuring the signed-out site

**BEFORE, all five seams died at their first authenticated call**, and the spec's own message
blamed the server:

```
Error: [API BROKEN] POST /api/trips → 401: {"message":"Sign in to create a plan"}
Error: [API BROKEN] GET /api/admin/revenue → 401: {"message":"Authentication required"}
Error: [API BROKEN] GET /api/admin/services → 401: {"message":"Authentication required"}
```

Every assertion in the file — 59 `expect`s across the lead pipeline, the experience build, the
money attribution, the supply feed and the intelligence seam — was **unreachable**. The suite could
not have found a seam defect if one existed; it reported "[API BROKEN]" about four healthy
endpoints, which is the §13 falsehood V-31 exists to stop.

**AFTER, every seam authenticates and reaches real ground**, and each one fails somewhere specific
(the two marked ✎ were repaired by this lane; the rest are left RED and reported):

| seam | AFTER (helper only) | after this lane's repairs | class |
|---|---|---|---|
| 1 Lead pipeline | `POST /api/leads/route` → **404 Route not found** | unchanged | **PRODUCT — filed as V-32** |
| 2 Experience build | `POST …/itinerary-items` → 400 `estimatedCost` Expected string, received number | ✎ repaired; now reaches `GET …/itinerary-items` → **403 Access denied** as the assigned expert | **PRODUCT — filed as V-33** |
| 3 Money | `[Seam 3 BROKEN] Provider has no active services` | unchanged | fixture/seed — the spec says so itself |
| 4 Supply → feed | `PATCH /api/admin/services/:id/status` → 400 `Invalid status` | ✎ repaired (`inactive` → `paused`); now `TypeError: listBefore.find is not a function` on the discover payload | test-side shape assumption, left for a suite-owning lane |
| 5 Intelligence | discover list empty after refresh | unchanged | fixture/seed |

### 4.2 `phase-3-traveler-flows` — same score, opposite diagnosis, and a fifth of the time

BEFORE: all five traveler tests spent **120 s each** timing out on
`page.fill('[data-testid="trip-destination"]')` — a selector that exists in **no client file** —
while four of them were not signed in at all. 10.2m.

AFTER: **3.2–3.5 fail in about a second each**, with the reason:

```
Error: [auth] POST /api/auth/login → 401 for test-traveler-edinburgh@traveloure.test.
       The account may not be seeded in this database.
```

Those four accounts (`edinburgh`, `cartagena`, `jaipur`, `porto`) **do not exist** — measured
directly against the running server: five of the nine accounts these suites log in as answer 200,
four answer 401. 3.1 (the one traveler that IS seeded) still times out on the missing form
selector. Total 2.2m.

**Which assertions were vacuous, exactly:** this suite has **two** `expect`s in six tests.
`[Phase 3.2]`…`[Phase 3.5]` contain **none at all** — under the old helper they were four
signed-out browser sessions asserting nothing. And `[Phase 3] Summary`, the only test that passes
in both columns, asserts `expect(trips.length).toBe(5)` on a **local array literal declared three
lines above it** (`phase-3-traveler-flows.spec.ts:346-355`). It passes anonymously, it passes
authenticated, and it will pass if the platform is switched off.

### 4.3 `phase-4-7-advanced-flows` — one test became true

BEFORE 4 failed / 2 passed → AFTER 3 failed / 3 passed. The test that changed is
**`[Phase 4] Expert Review - Aiko Reviews Kyoto Trip`**: signed out, `/expert/clients` showed no
"Kyoto" and the step timed out at 120 s; signed in as `kyoto-food@traveloure.test`, the expert's
client list renders and the step completes. The remaining three:

* `[Phase 5] Executive Assistant Dashboard` — `[data-testid="ea-dashboard"]` exists in no client
  file (stale expectation; the EA console moved).
* `[Phase 6] Cartagena` — now fails at LOGIN with the unseeded-account message (was a 120 s
  locator timeout).
* `[Phase 7] Booking & Payment` — `button:has-text("Pay")` never appears.

`[Phase 4-7] Summary`, which passes in both columns, contains **no assertion at all** — it is a
`console.log` that prints "✓ Phase 7 Complete … Payment processed via Stripe" on a run in which no
payment was processed (`phase-4-7-advanced-flows.spec.ts:400`). A test that prints a claim it did
not make is the same §13 shape as a login helper that cannot fail.

### 4.4 `stripe-init-deferral` — now 2/2, and the one suite that is ready to arm

BEFORE: the `/cart` test (named "for authenticated traveler") ran anonymous and timed out on five
selectors — `cart-empty`, `cart-items`, `checkout-surface`, `.stripe-checkout`, `.order-summary` —
**none of which exists in `client/src`**. Repaired to the cart page's own `text-page-title` /
`step-pill-cart`, and with a proven session it passes: **2 passed (4.4s)**.

**Flake, named rather than buried:** in the helper-only AFTER run the *other* test (`on "/"`, which
calls no helper at all) failed on `expect(bodyText?.length).toBeGreaterThan(100)` with
`Received: 9` — a landing page caught mid-render straight after the seam suite had hammered the
server. Re-run three times in isolation: **1 passed (2.7s)** each time. It is not attributable to
this change and is not claimed as one.

## 5. Defects found

### FILED as section-2 rows

* **V-32 — `POST /api/leads/route` has a comment and no handler.** `server/routes/payments.routes.ts:2867`
  is the comment `// POST /api/leads/route — score experts and auto-assign`, followed immediately
  by `export default router;` at `:2869`. No `/leads/route` registration exists anywhere under
  `server/`; the endpoint answers `404 {"code":"NOT_FOUND"}`. The handler was in
  `server/routes.ts:18777` before the June 2026 route-defragmentation commit `a8d7a8bf2`, which
  carried the comment across and left the body behind. `server/services/lead-routing.service.ts`
  is live and imported by `content-matching.service.ts`, so the service survives and only its HTTP
  door is gone.

* **V-33 — the item-read gate's expert arm keys on `trips.expertId`, which nothing writes.**
  `server/routes/trips.routes.ts:359-371` grants `GET /api/trips/:tripId/itinerary-items` to
  owner ∥ `trip.expertId === userId` ∥ managing EA ∥ share-token, and **never consults
  `trip_expert_advisors`**. Nothing in `server/` assigns `trips.expert_id` (grep over drizzle
  `.set({ expertId … })` and raw `expert_id =`; the one hit is `affiliate_booking_requests`), and
  in this database 0 of 6 trips carry one. Reproduced end to end: the traveler assigned the Kyoto
  expert through `POST /api/trips/:id/expert-advisor` (200; `trip_expert_advisors` row written,
  status `pending`), and the expert's read answered **403 Access denied** while the server logged
  `[IDOR ATTEMPT] User … tried to access resource owned by …` about a legitimately assigned
  advisor.

### RECORDED for the coordinator, not filed

* **`playwright/utils/forms.ts` drives 16 `data-testid`s out of 19 that exist nowhere in
  `client/src`** — `trip-destination`, `trip-start-date`, `trip-end-date`, `trip-guests`,
  `trip-budget`, `trip-type`, `activity-name`, `activity-date`, `activity-description`,
  `profile-bio`, `hourly-rate`, `service-price`, `service-currency`, `business-name`,
  `business-description`, and a template-literal language id. This is the shared helper behind
  phase-1, phase-2, phase-3 and phase-4-7, and it is why those suites cannot pass however good
  their sessions are. Repairing it is re-authoring them against the ONE planning modal
  (Locked Decision 33) — a rewrite, not a repair, and a product-surface decision this lane did not
  make.
* **Two tests that pass while asserting nothing** — `[Phase 3] Summary` (asserts a literal array's
  length) and `[Phase 4-7] Summary` (a `console.log` claiming a Stripe payment). Both pass in
  every column of the table above.
* **Four of the nine accounts these suites use are seeded nowhere** —
  `test-traveler-edinburgh|cartagena|jaipur|porto@traveloure.test`. `seedE2EAccounts`
  (`server/seeds/e2e-test-accounts.seed.ts:32-37`) creates five accounts and the fixture registry
  (`playwright/fixtures/test-accounts.ts`) names dozens. Whether to seed them or to retire those
  tests is R-12/R-13's question, not this lane's.
* **`seam-cross-console` Seam 4 now dies on the discover payload's SHAPE**
  (`listBefore.find is not a function`) — the spec assumes an array where the endpoint answers
  something else. Left as found; changing it is an assertion about the discover contract.

## 6. Wiring — nothing was armed, and why

The four suites stay on the 233-row orphan list. `unwired-spec-gate.yml` arms a spec only with a
`spec-green: <spec> <actions run URL>` line in the PR body (`scripts/check-spec-arming.cjs`), and
this session cannot produce a GitHub Actions run URL for any of them: the egress policy blocks
every Playwright browser download host, so the only browser available is the image's pre-installed
one and the only runs are local. **`stripe-init-deferral.spec.ts` is the one that is ready** — 2/2
green on a database built from empty — and arming it is a two-line change to that workflow plus a
green run URL, in a follow-up. The other three are blocked on the fixture and rewrite questions
above, not on wiring.

The inventory count is unchanged by design: **233 orphans before, 233 after**
(`test-files-wired: 278/511 reachable; 233 orphan(s)`). A suite leaves that list by being run or by
being gone (ledger `2026-09-14-test-files-wired-orphans`), and this lane did neither.

## 6b. Relation to the orphan triage (PR #941)

`docs/lane-reports/2026-09-15-orphan-triage.md` landed on `main` while this lane ran. It classified
the 30 orphaned Playwright specs **statically** — its own table says "not run here — the
2026-09-14 per-spec classification stands" for both `seam-cross-console.spec.ts` and
`stripe-init-deferral.spec.ts`. This lane RAN four of them, twice each, so §4 above supersedes the
per-spec reasons for those four and leaves the triage's own figures untouched: it wired nothing and
so did this, and the inventory reads 233 in both.

## 7. Validation

| check | result |
|---|---|
| `npx tsc --noEmit` | **129** errors = `TSC_BASELINE` (`build.yml:175`), unchanged |
| `npm run build` | exit 0 |
| `node scripts/check-decision-guards.cjs` | `decision-guards lint OK (0 deferred warning(s))` |
| `bash scripts/phase2-fee-gate.sh` | exit 0 |
| `node scripts/check-test-files-wired.cjs --self-test` | `self-test OK (7/7 fixtures)` |
| `node scripts/check-test-files-wired.cjs` | `test-files-wired: 278/511 reachable; 233 orphan(s)` — **identical before and after** |
| `grep -c replit.local package-lock.json` | `0` |

`playwright/` is outside `tsconfig.json`'s `include`, so the two helper files were additionally
typechecked under a temporary config: **no error in either of them** (the directory carries ~35
pre-existing errors in other specs, none touched).

## 8. What this lane did NOT do

* It did not wire anything (§6) and did not move the orphan count.
* It did not touch `server/` or `client/`. Both product defects are FILED (V-32, V-33), not fixed.
* It did not repair `phase-3` or `phase-4-7`. Their shared driver
  (`playwright/utils/forms.ts`) targets a trip form that Locked Decision 33 replaced with the one
  planning modal; making them pass is re-authoring them, which is a product-surface decision.
* It did not seed the four missing traveler accounts, and did not retire the tests that need
  them — that is R-12/R-13's question.
* It committed **no new spec**. The helper's throw was proven locally against the running server
  (a real login returns the principal; `logout` proves the 401; an anonymous jar, an unseeded
  account and a wrong password each throw), but that proof is not committed, because a new spec
  that no workflow runs is one more file that reads as coverage and is not — the exact class this
  punchlist section exists to shrink. It is named here as the gap it is.
