# Create-flow refinements — one-card Basics, ideas rail, post-publish nudge

Ratified by the decision-maker Aug 14, 2026 (DECISIONS.md ledger row 114; proposed and approved in
the provider-batch Run-2 session; artifact d1c16852's Basics layout is superseded on these points).
`mockup.html` is the clickable proposal exactly as approved; `mockup-*.png` are its four states.
Implemented same session — live verification screenshots are `final-*.png` in
`docs/testing/assets/provider-batch-run2/`.

Scope: (1) the wizard Basics collapses to ONE card whose header is the picked offering (name +
category chip + tagline + one Change); the pre-pick full-catalog state is deliberately untouched —
it is the inspiration surface. (2) Workstation gains the personalized "Ideas for your business"
rail (registered categories minus listed offerings, taglines from the /earn catalog rows, §13).
(3) Listing Home shows a one-line nudge back to the rail while a listing is in review.
