# Provider console mockup — click-through export

Exported Aug 14, 2026 from claude.ai artifact `d1c16852-4f7e-483b-916c-fe00ce7e5a76` ("Provider console —
service creation redesign") at the decision-maker's direction, as the Part-3 reference for
`docs/testing/PROVIDER_BATCH_EXERCISE_RUN2.md`. `mockup.html` is the interactive mock itself (self-contained,
open in any browser and click through); the `mock-*.png` files are one full-page screenshot per distinct
view/state, captured by driving it at 1440×1000.

**Authority scope:** ratified rulings (DECISIONS.md, the six-station shell decision, Wave-3 ballots) outrank
this mock; the mock is the visual target only where no ruling speaks. Note that several of its own sections
self-label **"Proposed — ratify or amend"** (property builder gap #1, availability engine gap #2, edit-split
gap #17, delete gap #18, the Distribute nav entry) — those are proposals awaiting a ruling, not ratified
targets, and the conformance matrix grades them accordingly.

**Ruling 112 amendments (Aug 14, 2026 — decision-maker ruled all ten §Q of the Run-2 doc):** the Logistics
consolidation, autosave, "Next available" chip, property min-stay + cancellation bands, three-card Listing
Home rail, edit-split policy (gap #17), Distribute-owned handle editing, and the travel-surcharge zones layer
are **ratified as drawn here**. Two places the mock is now the stale side: **transport** stays the build's
4-state segmented control (not this mock's single pickup toggle), and the **Property tile's** "builder not
yet specified" note predates the shipped builder. One amendment this mock should gain when next revised:
**Calendar needs an explicit access point into the availability editor** (today its chips deep-link but the
page offers no standing affordance). Palette: the earthy User-Console set used here (#1A1A18/#FAFAF8/#35605A)
is the provider console's palette; Navy/Teal/Gold remain traveler-brand tokens.
