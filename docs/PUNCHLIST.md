# Punchlist — pick up here

> Compiled 2026-09-08 at the end of the console/AI-concierge program's build nights.
> Companion documents: `docs/ROADMAP.md` §A–§G (the open register and the agreed cheapest path),
> `docs/design/CONSOLE_AND_AI_CONCIERGE_BRIEF.md` (design of record), `docs/OPERATING_PROCEDURE.md`
> (how a lane is briefed and landed). **Evidence discipline:** every item below is marked
> **[verified]** (checked in this repo at the sha noted) or **[reported]** (asserted by an audit and
> NOT re-checked here). Do not treat a `[reported]` line as fact until someone opens the file.

## 0 · How to resume

1. `git fetch origin && git checkout -B <task-slug> origin/main`. Never commit on `main`.
2. Read `docs/OPERATING_PROCEDURE.md` §3 (lane brief) and §4 (serial landing).
3. Cost levers that worked, measured: **batch adjacent lanes into one agent run** (three lanes cost
   ~350–450k tokens together versus ~400k each alone); **forbid repo sweeps**; **cap ledger rows at
   150 words and PR bodies at 250**; **do not stand up a local server or DB unless extending an
   armed spec** — CI proves it for free. Run lanes **serially**: the one parallel pair collided on a
   shared module and the reconciliation cost more than the lane.
4. Two recurring traps, both of which cost CI cycles this session: **a pin that asserts a literal
   string** (an exact URL, a handler slice, a fee total) goes red on a correct refactor — repair it
   to assert the invariant, never delete it; and **`docs/DECISIONS.md` unions locally but not on
   GitHub**, so any two open PRs that both append a row will show "dirty" until you merge `main` in.
5. **STATE AS OF 2026-09-15, re-checked at `5d5f7ccc3292a967de26f02292ef30197051151d` by the
   section-3 re-sweep.** **Section 1 is FULLY RULED** — every question D-1..D-41 has an answer or a
   named owner; **UPDATED 2026-09-15 (later): section 1 has SIX NEW OPEN ROWS, D-45..D-50**, filed by the L16 Ask-AI drawer brief (`docs/design/ASK_AI_DRAWER_BRIEF.md`, ledger `2026-09-15-l16-ask-ai-drawer-brief`) — the create rail's thread/throttle/tier/input questions (D-45, D-46, D-47, D-50), the advisor's ask/pay/apply standing and the coverage read (D-48), and re-finalize on a final plan (D-49). Nothing above them re-opened; **section 2 is CLOSED** except the two rows struck open with their owners named
   (V-22, V-25(a)) and the five this re-sweep filed (V-27..V-31 — of which **V-27 and V-28 are now
    CLOSED**, ledger `2026-09-15-v27-v28-privacy-kind-labels`, leaving V-29, V-30 and V-31 open). **Section 3 is now ONE open
    row — R-7** (needs the production database). **R-4 CLOSED 2026-09-15** (ledger `2026-09-15-d22-dates-confirmed`);
    **R-12 and R-13 CLOSED 2026-09-15** (ledger `2026-09-15-orphans-t4-t7-red-suites` — both wired into
    `suite-ai-draft-and-availability` and repaired by ONE shared read-back fixture, no assertion touched); R-10 and
    R-11 were promoted to verified defects (V-31, V-30) and struck with pointers. The T-4..T-7 lane also settled the
    triage block's 401/403 candidate with no code change — both admin guards already implement the ruling — and
    corrected its "two DEAD suites" sentence to one.
    **Build lanes remaining:** D-20/D-21 (the AI-task charge), ~~D-22 + R-4~~ (**LANDED
    2026-09-15**, ledger `2026-09-15-d22-dates-confirmed` — the marker and the re-date rail; the
    availability-revalidation third lane is still open), D-24..D-26 + D-40 as one hybrid lane, D-27,
   D-28..D-31 (quotes), D-32..D-35 (bundles), D-36..D-39 (completion and dispute), and the HELD
   offering-key drop (PR #907 — needs the production preview run, section 4). **UPDATED 2026-09-15: V-29 is CLOSED** (`2026-09-15-v29-one-trip-write-resolver` — ruled option B; the eight write-gating rails now read ONE predicate and the collaborator-only resolver is deleted, with the owner/author/admin delta stated in that row). **UPDATED 2026-09-15: V-30 is CLOSED** (`2026-09-15-test-guard-prose-echo`) and the reachability inventory's true figure is **274/507 reachable, 233 orphans**, not 477/30 — `2026-09-13`/`2026-09-14`'s ledger rows carry dated corrections. **V-31 STAYS OPEN**: only its reachability clause was settled, the `loginAs` silent-pass is unfixed. **UPDATED 2026-09-15 (later): V-31 is now CLOSED** (`2026-09-15-v31-loginas-verifies`) — `loginAs` proves the session and throws, the second same-named helper is a caller, and `logout` proves the 401. The four suites are STILL ORPHANS: nothing was armed, the inventory's ORPHAN count is unchanged at **233** (278/511 reachable when this lane measured it; 279/512 after merging main's own newly-wired suite — the denominator moved, the orphan count did not), and the reason is in the ledger row. That lane filed **V-32** (`POST /api/leads/route` — comment, no handler, 404 since the June route defrag) and **V-33** (the item-read gate's expert arm keys on `trips.expertId`, which nothing writes), so section 2's open rows are V-22, V-25(a), V-32 and V-33. **UPDATED 2026-09-15 (later still): V-32 and V-33 are both CLOSED** (`2026-09-15-v32-v33-leads-door-item-read-gate`) — the `/api/leads/route` door is ruled RETIRED rather than restored (two ratified rails already serve it) and the read gate's expert arm now asks the canonical §12 READ predicate instead of a column nothing writes; that row also records the CORRECTION that V-33's defect was in `GET /api/trips/:id`, not the itinerary-item readers, which were already correct. Section 2's open rows are now **V-22** and **V-25(a)**. The orphan count is UNCHANGED at 233 — that lane armed no spec. R-12 and R-13 stay open and are confirmed members of the true orphan list; re-classifying the other 203 suites it revealed is an unscheduled follow-up lane.

## 1 · Decisions still needed (nobody can build these without an answer)

> **THIS IS THE ONE DECISION REGISTER (merged 2026-09-11).** It supersedes the two lists that asked the
> same questions elsewhere: `docs/ROADMAP.md` §A (the Console & AI Concierge open register) and §19 of
> `docs/superpowers/specs/2026-09-08-offering-commerce-trip-slip-contract-design.md` (the offering
> commerce contract). Both now point here. Two registers guarantee one gets answered and the other
> quietly does not — D-1 and D-2 below were each asked twice already, in different words.
> Each row names where it came from and what it blocks.

| # | Question | Recommendation |
|---|---|---|
| D-1 | **Is a purchased ready-made trip a finished plan or an editable template?** Today the clone is an ordinary `draft` with placeholder dates and no final snapshot **[verified, citation repaired 2026-09-13:** `ready-made-purchase.service.ts:217-219` derives the window and `:255-258` writes `status:"draft"`; the old `:108` now sits inside `ensureReadyMadeAuthorCredit`**]**, while the buyer was sold "a ready-made trip". | Deliver **both**: write a final snapshot at purchase so the buyer receives a finished Trip Card, and let them reopen it to edit. Otherwise the copy must stop implying a finished product. |
| ~~D-2~~ | ✅ **ANSWERED 2026-09-15 (option A)** — decision-maker ruling, ledger `2026-09-15-d2-one-revision-not-a-consultation`, **AND THE COPY LANDED THE SAME DAY**: a ready-made purchase includes **ONE ASYNCHRONOUS REVISION AND NOTHING ELSE**, and every buyer-facing surface now says exactly that. The consultation half had no storage, no scheduling and no endpoint — only copy — so it is not made real, it is **withdrawn**: the word leaves the listing detail's inclusion promise and buy-card list (`ready-made-detail.tsx`), the buyer's slip entitlement card (`concierge-card.tsx`) and the purchase email in both bodies (`email.service.ts`). **No schema change, no migration, no new rail, no consult column** (there never was one, and none is added). Pinned by `ready-made-clone-fields` R1 ("consult" added to the email's forbidden words) and the new `ready-made-entitlement-copy` D1–D4 in `client/src/lib/__tests__` (both already wired in `build.yml`). Expert consult **listings** — real bookable advisory services — are untouched. Original question: **What is the included consultation?** It is stored as an entitlement on `ready_made_purchases`, not a `service_bookings` row, so it inherits no scheduling, acceptance, cancellation or refund machinery. **[verified 2026-09-13: the REVISION half has a rail (`POST /api/ready-made/purchases/:id/request-revision`); the CONSULTATION half had none — only live copy.]** | Decide it is **one asynchronous revision** and say exactly that everywhere, or make it a real schedulable booking. The wording and the storage must agree. |
| ~~D-3~~ | ✅ **ANSWERED 2026-09-15 (option A)** — decision-maker ruling, ledger `2026-09-15-d3-readymade-separate-checkout`, **AND THE COPY LANDED THE SAME DAY**: a ready-made trip and service bookings are **NEVER MIXED IN ONE CHECKOUT**. The ready-made purchase stays its own PaymentIntent against its own table (a digital product delivered at fulfilment); the bookable services inside the plan are reservations bought separately at their own listing price through the cart. **THE SEPARATION IS NOW VISIBLE ON EVERY SURFACE THAT PRICES A PLAN:** the store card (`discover.tsx` `ReadyMadeThemeCard`, whose price badge sits beside an "N items" chip) carries a separation line; the feed tile (`components/feed/ready-made-card.tsx`) labels the number **plan price**; the detail page (`ready-made-detail.tsx`) heads its contents **"What's inside the plan"** instead of "What's included", captions the price with what it buys, and states the separation beside both the counts and the buy-card list; the purchase email (`email.service.ts`) says in both bodies that what was paid for is the plan and the bookings are separate. **NO CHECKOUT CHANGE, NO MIXED INTENT, NO SCHEMA, NO NEW RAIL.** **The SLIP needed nothing:** a cloned item keeps its `providerServiceId`, arrives with no booking/slot/dates and `routing_status = 'in_planning'` (`itinerary-item-clone.ts`), so the EXISTING neutral **"Planning"** routing pill already renders it as in-the-plan-not-booked (LD 39 — it is not `ready_for_checkout` until the buyer carts it) and the origin chip already says "from your expert"; no new chip was added. Pinned by `ready-made-entitlement-copy` **D5–D8** in `client/src/lib/__tests__` (D8 sweeps every `client/src` file that links to `/ready-made/:id`, derived not listed) and by `ready-made-clone-fields` **R1** (the email must say "booked separately" and must not claim inclusion) — both already wired in `build.yml`. Original question: **Should a single checkout mix a ready-made trip with services?** It cannot today: ready-made mints its own PaymentIntent (`ready-made.routes.ts:1297-1302`) against its own table, and no ready-made rail reads the cart's `metadata.bookingIds` — a mixed checkout is structurally impossible. **[verified 2026-09-13]** | Keep them separate (a digital product and a reservation are different fulfilment), and make the separation **visible** so nothing implies the ready-made price includes the services inside it. |
| ~~D-4~~ | ✅ **ANSWERED 2026-09-15 (option A)** — decision-maker ruling, ledger `2026-09-15-d4-item-kind-contract`, **AND THE CONTRACT LANDED THE SAME DAY**: YES, a bookable itinerary item must name a real `provider_services` row, and every plan item now carries one of **four kinds**, DERIVED from the row's own link columns and **stored nowhere** — `bookingId` ⇒ **included**; else `providerServiceId` ⇒ **bookable separately**; else `affiliateProductId` ⇒ **external**; else **recommended**. ONE derivation and ONE label map (`shared/item-kind.ts`, §18 rule 1); the kind is never merged with `routingStatus` or with an `affiliate_booking_requests.status` (LD 44). It renders on the slip item row, the PlanCard full stage (hence the Trip Card, LD 45 (6)), the cart — where a trip-routed line that names no service shows its kind and **no price**, because checkout skips it — and the ready-made detail, off a `byKind` count added to the approval-time `insideCounts` snapshot (absent on older listings ⇒ the block draws nothing, never zeros, §13). **AUTHORING CONTRACT:** on the ready-made AUTHOR branch only (`isTripAuthor`), a priced item naming nothing bookable is **refused with a 400** naming the rule — on create (`POST /api/trips/:tripId/itinerary-items`) and on edit (the trips-router PATCH, judging the MERGED row); refusal rather than a silent downgrade, because a dropped price the author never learns about is first noticed by a buyer. A traveler's own cost estimate on their own plan is untouched. **`recommended` vs `external` WAS derivable** from the existing `affiliate_product_id` column, so **no column was proposed and no D-23 row was opened**; the residual honesty limit is that `external` means "partner-fulfilled", never "our agent can book it" (§16). **NO SCHEMA, NO MIGRATION, NO BACKFILL, AND CHECKOUT CHARGES EXACTLY WHAT IT DID** — a legacy priced free-text row keeps its column and reads as `recommended` with its price hidden by the reader (§19b posture). Pinned by `authored-item-price-contract` A1–A5 (wired into the `ready-made-clone-fields` job; A1/A3/A5 fail on the pre-fix router), `shared/__tests__/item-kind.test.ts` K1–K6 and `client/src/lib/__tests__/item-kind-chip.test.ts` D1–D5. Original question: **Must a bookable itinerary item reference a real provider service?** An item without one renders as reference-only and adds nothing to the total — `cart.tsx:1950`, and checkout's subtotal AND booking loops both `if (!item.service) continue;`. **[verified 2026-09-13]** | Yes — make it an explicit authoring contract for ready-made authors, and label every item as included / recommended / bookable separately / external. |
| ~~D-1~~ | ✅ **ANSWERED 2026-09-15 — option B (ledger `2026-09-15-d1-ready-made-editable-template`): an EDITABLE TEMPLATE with the expert's rows protected by LD 42 D3, which is what `main` does since `2026-09-14-clone-items-are-expert-work`. No marker, no refusal, no snapshot at purchase; copy says editable plan on placeholder dates.** ~~Is a purchased ready-made trip a finished plan or an editable template?~~ | Ruled B. The re-date rail is R-4's lane; the placeholder-dates marker is D-22. |
| ~~D-5~~ | ✅ **ANSWERED 2026-09-15 (option A)** — decision-maker ruling, ledger `2026-09-15-d5-no-milestone-billing`, **AND THE COPY LANDED THE SAME DAY**: there is **NO MILESTONE, STAGED, INSTALMENT OR DEPOSIT-THEN-BALANCE BILLING FOR EXPERT PLANNING WORK**. A large engagement is sold as a listing like any other — charged **once at checkout**, advisor access granted on authorization (ruling 11, ledger `2026-09-15-plan-work-one-rail`) — or not at all. **NOTHING WAS BUILT:** the lane's whole job was that no surface promises what does not exist (§13), and the sweep of `client/src`, the locales and the email bodies found four that did. **REWORDED:** `pricing.tsx` and the landing price ladder (`components/landing/how-it-works.tsx`) both rendered an `N% deposit` for a done-for-you EVENT, off the **display-only** `concierge:done_for_you_deposit_pct` band that only `GET /api/pricing` reads — while that engagement's fee is server-quoted and captured **ONCE** (`coordination_states.fee_payment_status` unpaid|pending|paid, one `fee_payment_intent_id`) — so both now say **one coordination fee**; the slip's expert picker (`AssignExpertDialog.tsx`) dropped its `$X/hr` chip, the dialog's very next step being the offering whose listed price is what is charged; and the expert application (`travel-experts.tsx`, `/become-expert`) relabels **Indicative** Hourly Rate and replaces `Average expert rates: $50-150/hour` with what a local expert is actually paid for. **CONFIRMED REAL AND OUT OF SCOPE, kept verbatim:** §15d's deposit/balance on a `service_bookings` RESERVATION (the `provider_services.deposit_enabled` opt-in) and the FAQ sentence describing it, the coordination fee itself (§8), the traveler's own vendor-contract payment milestones, the bachelor-template group cost-split filter, and `milestone_birthday` the occasion. **NO FEE, BAND, MONEY PATH, SCHEMA, MIGRATION, ROUTE OR RAIL WAS TOUCHED.** Pinned by the new `client/src/lib/__tests__/no-milestone-billing.test.ts` **M1–M4** (whole-directory `unit-suite-client-lib`), whose file set is DERIVED — every `client/src` file whose code names a paid-planning rail — and whose M3 fixtures include the four pre-fix lines verbatim, so the pin fails on the code it replaced. Original question: **When is Expert planning work charged for a LARGE engagement?** **[verified 2026-09-15: milestone billing had no rail, no column and no ruling — only two pricing surfaces claiming a deposit.]** | Do not build milestone billing until an engagement rail exists; until then a large engagement is sold as a listing like any other, or not at all. |
| D-2 | **What is the included consultation?** It is stored as an entitlement on `ready_made_purchases`, not a `service_bookings` row (`shared/schema.ts:10147-10155`), so it inherits no scheduling, acceptance, cancellation or refund machinery. **[verified 2026-09-13, and WORSE than stated: the REVISION half has a rail (`POST /api/ready-made/purchases/:id/request-revision`); the CONSULTATION half has no storage, no scheduling and no endpoint — only live copy (`ready-made-detail.tsx:520` "Includes 1 consultation + 1 revision").]** | Decide it is **one asynchronous revision** and say exactly that everywhere, or make it a real schedulable booking. The wording and the storage must agree. |
| D-3 | **Should a single checkout mix a ready-made trip with services?** It cannot today: ready-made mints its own PaymentIntent (`ready-made.routes.ts:1297-1302`) against its own table, and no ready-made rail reads the cart's `metadata.bookingIds` — a mixed checkout is structurally impossible. **[verified 2026-09-13]** | Keep them separate (a digital product and a reservation are different fulfilment), and make the separation **visible** so nothing implies the ready-made price includes the services inside it. |
| D-4 | **Must a bookable itinerary item reference a real provider service?** An item without one renders as reference-only and adds nothing to the total — `cart.tsx:1950`, and checkout's subtotal AND booking loops both `if (!item.service) continue;`. **[verified 2026-09-13]** | Yes — make it an explicit authoring contract for ready-made authors, and label every item as included / recommended / bookable separately / external. |
| D-5 | **When is Expert planning work charged for a LARGE engagement?** Ruling 11 (2026-09-08) answered the ordinary case — a planning-tier listing is charged at checkout and the purchase grants advisor access on authorization. Milestone billing for a multi-week engagement has no rail and no ruling. *(from the commerce contract §19.1)* | Do not build milestone billing until an engagement rail exists; until then a large engagement is sold as a listing like any other, or not at all. |
| ~~D-6~~ | ✅ **ANSWERED 2026-09-15 (option A)** — decision-maker ruling, ledger `2026-09-15-d6-acceptance-for-artifacts`, **AND THE BRIEF LANDED THE SAME DAY** (`docs/design/EXPERT_ACCEPTANCE_BRIEF.md`). **ARTIFACTS ARE ACCEPTED, NOT TIMED OUT:** a plan, a brief, edited media — anything delivered as a file or a written deliverable — requires **traveler acceptance** before the booking completes and the held earnings are minted. **A SILENT TIMEOUT NEVER COMPLETES IN THE SELLER'S FAVOUR:** an unanswered window goes to a stated `awaiting_acceptance`, and then, after a period the decision-maker will set (**config, never a literal** — §8 posture, the `completion-windows.config.ts` precedent), to **admin review** in the EXISTING queue (`GET /api/admin/disputes`) — never to auto-completion. **Sessions and live support stay seller-declared with a dispute window — that is D-7, still open**, and the brief leaves its seam untouched (`OWNER_DECLARED_COMPLETION_RULES` / `session_end` are unchanged by every lane it sequences). **The revision allowance is the listing's own `provider_services.revisions_included`, READ AND NEVER RESTATED** (§18 rule 1); a request inside the window consumes one and re-opens delivery. **THE RULING AMENDS AN EXISTING RAIL, which is the finding:** `artifact_timer` (`shared/service-fundamentals.ts`, fired nightly by `server/jobs/bookingAutoCompletion.ts`) today auto-completes a `pdf` booking 7 days after first download — or 7 days undownloaded post-delivery — and that flip mints the held earning inside `storage.updateServiceBookingStatus`'s own transaction. **COLUMNS ARE NOT DECIDED BY THIS LANE** and no migration was written: they are isolated as **D-24 – D-27** below, each with a question and a recommendation. Original question: **Which Expert outputs require traveler acceptance before completion and earnings release, and what revision allowance comes with a planning artifact?** *(commerce contract §19.2–3)* | Acceptance for artifacts (a plan, a brief, edited media); seller-declared for sessions and live support. One revision included, stated on the listing, so "accepted" is never a silent timeout in the seller's favour. |
| D-7 | **Who may declare completion for physical-action and coordination work, and how are reimbursable expenses quoted, approved, evidenced and refunded?** *(commerce contract §19.4–5)* | Seller declares, traveler has a stated window to dispute. Expenses are quoted and approved BEFORE they are incurred or they are not reimbursable — never reconciled after the fact against a receipt nobody agreed to. |
| ~~D-8~~ | ✅ **ANSWERED 2026-09-15 (option A)** — decision-maker ruling, ledger `2026-09-15-d8-quotes-with-expiry`, **AND THE BRIEF LANDED THE SAME DAY** (`docs/design/CUSTOM_QUOTE_BRIEF.md`). **A QUOTE IS AN OBJECT WITH AN EXPIRY:** the provider issues a quoted amount with an explicit `expires_at`; the traveler accepts inside the window through an **atomic claim carrying the expiry in its WHERE clause** (§15); acceptance mints the `service_bookings` row with the **quote row's amount as the server-derived total** (§14 — the body carries a quote id, never an amount) through the **EXISTING** checkout spine, no second booking-birth rail. The listing's `deposit_*` config then applies **unchanged** (`resolveDepositPlan` → `deposit_amount` → the §15d balance rail). An **expired quote is re-quoted** — a NEW row with `superseded_by` stamped on the old — and is **never silently honoured or silently refused**. Validity is a config value, or a provider choice under a configured ceiling, **never a literal** (§8; the `completion-windows.config.ts` posture). Two facts the brief records and does not fix: the commerce contract already resolves `P5` / `server_quote` / `after_quote` and snapshots them onto bookings for a listing it cannot sell, and `POST /api/expert-booking-requests` births a **`"0"`-priced `service_bookings` row** for a `custom_quote` listing (`server/routes.ts:1779,1855`) while four sibling rails refuse one. **COLUMNS ARE NOT DECIDED** — isolated as **D-28 – D-31** below. ~~Can custom quotes require deposits, and how long does a quote stay valid? *(commerce contract §19.6)*~~ | ~~Yes to deposits, with an explicit expiry on every quote.~~ |
| ~~D-9~~ | ✅ **ANSWERED 2026-09-15 (option A)** — decision-maker ruling, ledger `2026-09-15-d9-partial-bundle-completion`, **AND THE BRIEF LANDED THE SAME DAY** (`docs/design/BUNDLE_PARTIAL_COMPLETION_BRIEF.md`). **PARTIAL COMPLETION IS ITS OWN STATE AND IT NAMES THE COMPONENT:** the parent moves to **`partially_completed`** naming the failed component; that component alone is refunded **at its snapshotted price** (§14 — never the listing's price today), as an atomic claim with a **component-scoped** Stripe idempotency key (§15); the ledger reverses **only that component's share** through the EXISTING proportional `reversePlatformRevenueForBooking` fraction; acceptance and dispute per component inherit **D-6** and **D-7**. **`completed` still means EVERY component** — the all-or-nothing test is not weakened, `partially_completed` sits beside it. Four facts the brief records: per-component COMPLETION already lives on `booking_details.componentCompletions` (`booking-completion.service.ts:571-624`) but the purchase-time snapshot carries **name and id and NO PRICE** (`payments.routes.ts:1074`); `refundServiceBooking` flips the **whole** row to `refunded`; earnings are ONE set per booking by migration 203's unique indexes; and a component refund must write a `refunds` row and must never rewrite `total_amount`, or §17 raises a false exception on every pass. **COLUMNS ARE NOT DECIDED** — isolated as **D-32 – D-35** below. ~~What happens when one bundle component fails after others are delivered? *(commerce contract §19.7)*~~ | ~~Partial completion is its own state; refund the failed component and say so.~~ |
| ~~D-7~~ | ✅ **ANSWERED 2026-09-15 (THE SPLIT)** — decision-maker ruling, ledger `2026-09-15-d7-completion-split`, **AND THE BRIEF'S SECOND HALF LANDED THE SAME DAY** (`docs/design/EXPERT_ACCEPTANCE_BRIEF.md` **Part II**). **COMPLETION = option A:** for physical-action and coordination work — the session-shaped methods `call`/`video` (`session_end`), `async_messaging`/`voice_notes` (`provider_declared`), `in_person` and **`hybrid`, RULED HERE to D-7** (place-anchored: a session actually run is not withheld behind acceptance of a document) — the **SELLER DECLARES** completion, the traveler has a **stated dispute window** before earnings release, an **undisputed window completes and releases**, and a **dispute goes to the EXISTING admin queue** (`GET /api/admin/disputes`, `WHERE status = 'disputed'` — no second queue, no disputes table). **THE GAP IS A WORD, NOT A WIRE:** today the owner completion rail flips straight to `completed`, so the booking *reads* completed the instant the seller declares while the money is correctly held — the §13 lie the new `completion_declared` state fixes. **The window is NOT a new constant** — it is `holdWindowDays('service_booking')`, reached through a delegating accessor in `completion-windows.config.ts` (whose header forbids a parallel constant); §8 posture, env-overridable. Every transition an atomic conditional (§15/§18b), actor from the session (§14). **COORDINATION HAS NO EARNING TO RELEASE** — the fee is captured up front as `platform_revenue` (`fee_payment_status`, which HAS a DB CHECK) and no coordinator earning is ever minted, so the window gates the admin REFUND there, not a payout. **EXPENSES = option B for now:** NO reimbursable-expense machinery exists, none is built, and **no surface may promise one** — swept clean and pinned by `client/src/lib/__tests__/no-expense-reimbursement.test.ts`; the target spec is recorded verbatim in Part II §16. **COLUMNS ARE NOT DECIDED BY THIS LANE** and no migration was written: isolated as **D-36 – D-40** below. Original question: **Who may declare completion for physical-action and coordination work, and how are reimbursable expenses quoted, approved, evidenced and refunded?** *(commerce contract §19.4–5)* | Seller declares, traveler has a stated window to dispute. Expenses are quoted and approved BEFORE they are incurred or they are not reimbursable — never reconciled after the fact against a receipt nobody agreed to. |
| D-8 | **Can custom quotes require deposits, and how long does a quote stay valid?** *(commerce contract §19.6)* | Yes to deposits, with an explicit expiry on every quote. An expired quote is re-quoted, never silently honoured or silently refused. |
| D-9 | **What happens when one bundle component fails after others are delivered?** *(commerce contract §19.7)* | Partial completion is its own state; refund the failed component and say so, rather than completing the parent or reversing the whole bundle. |
| D-10 | **Which external partners can provide confirmation, change and cancellation callbacks, and what minimum evidence lets an external booking appear as confirmed?** *(commerce contract §19.8–9)* | Until a partner provides a callback, an external booking never reaches "confirmed" on our surfaces — it stays requested with the partner named. No partner today provides one. |
| ~~D-10~~ | ✅ **ANSWERED 2026-09-15 (option A)** — decision-maker ruling, ledger `2026-09-15-d10-confirmed-needs-partner-evidence`, **AND THE CODE LANDED THE SAME DAY**: an external/affiliate booking reaches `confirmed` ONLY on **PARTNER-ORIGINATED** evidence — a partner callback (**no partner provides one**) or the affiliate network's reported conversion matched back on `sub_id` by the existing reconciliation matcher (`TP_SUBID_ATTRIBUTION`). A human agent's typed confirmation reference is real and stays visible, but it yields `purchased_by_human` — "reference recorded, awaiting the partner's confirmation" — never `confirmed`. Partner-side **changes and cancellations have no signal**: the agent handles them by hand and the surface says so. LD 44 (e)'s vocabulary is unchanged; this narrows who may WRITE its last value. `confirmed` left the human-settable allowlist (the PATCH rail 400s naming the rule), the agent press became a §15 atomic purchase claim, and the matcher is the ONE writer of `confirmed`. **No schema change, no migration, no backfill.** Original question: *Which external partners can provide confirmation, change and cancellation callbacks, and what minimum evidence lets an external booking appear as confirmed?* *(commerce contract §19.8–9)* |
| ~~D-11~~ | ✅ **ANSWERED 2026-09-15 (A)** — decision-maker ruling, ledger `2026-09-15-d11-no-item-booking-exception`, **AND THE CODE LANDED THE SAME DAY**. A `service_bookings` row naming a trip with no `itinerary_items.booking_id` pointing at it is a **MIGRATION EXCEPTION, MARKED AND AUDITED**. **The rail inventory the ruling is judged on:** the **checkout claim** (`payments.routes.ts`) links the item at PROMOTE via `markItemPurchased`, so it is NOT a class — a cart line it could not link is precisely the finding; **transport-commerce** (`stripe.service.ts`, NULL `service_id` by the Service Model exception) and **`POST /api/expert-booking-requests`** (`server/routes.ts` — which IS the "direct path" at ~:1855, not a second rail) carry no item reference and are the **two ratified classes**, each stamping a server-composed `booking_details.noItemReason` through the ONE composer in `shared/no-item-booking.ts`; **ready-made** author/clone/purchase births no `service_bookings` row at all (`ready_made_purchases` is its store); the **legacy `bookings`** rail is a different table and out of scope; the beta seeder names no trip. **`POST /api/bookings` has no item reference at all** — its five-key allowlist names none and nothing downstream writes `booking_id` — so every trip-bearing row it births was a stray by construction, and it now **refuses a `tripId` outright** (400 `plan_booking_needs_cart`, before any write; the plan-linked path is the cart, LD 39). The mark is a §19d server-authored key: in `SERVER_AUTHORED_BOOKING_DETAIL_KEYS`, stripped at both layers, B7's pin extended. **Detection, never repair (§17/§19b):** drift kind `trip_booking_without_item` (warning, cart rail), keyed on the booking, append-only, **no backfill** — `payment_pending` and voided/cancelled/refunded rows are out of the predicate, an UNKNOWN status is not, and an unrecognised reason exempts nothing. Original question: **Is a trip-level obligation with no item link a supported product pattern or a migration exception?** *(commerce contract §19.10)* | Migration exception, marked and audited, so it cannot quietly become the normal shape. |
| ~~D-12~~ | ✅ **ANSWERED 2026-09-15 (option A)** — decision-maker ruling, ledger `2026-09-15-d12-service-bookings-canonical`, **AND THE CODE LANDED THE SAME DAY**. `service_bookings` is the **CANONICAL** booking rail; the legacy `bookings` rail takes a **DATED no-new-writes switch**, as CONFIG and never a literal — `server/config/legacy-bookings.config.ts`, ONE accessor `legacyBookingsNoNewWritesFrom()`, env **`LEGACY_BOOKINGS_NO_NEW_WRITES_FROM`**. **Unset = nobody has decided ⇒ writes allowed** (an absent variable must never close a live money rail); a future date is decided-but-not-in-force; from a past date `POST /api/bookings/process-cart` answers **410 `{message, reason:"legacy_rail_closed", canonical:"/api/checkout"}` BEFORE any read or write** — pinned by zero DB statements, not by "no booking row", because `processCart` can mint a TRIP first. A malformed value **fails the boot**. **RETIRED:** `/booking-demo` (route + page) and the itinerary-comparison board's "Book Now", whose cart lines the browser built from display fields with no `provider_service_id` — nothing on that board was service-linked, so adopting (`apply-to-trip`, which preserves the catalog link) and checking out from the slip is the honest path (LD 39, LD 45 (4)); zero-mount `BookingFlowModal`, `PlanningWithBooking` and `ItineraryComparisonWithBooking` deleted (§18c). **SURVIVING, cited and pinned (C8/C9):** the rail's GET, `confirm-payment`, `bulk-status`, `POST /api/bookings/refund`, the legacy confirm/cancel paths and §17's `scanLegacyRail`. ✅ **CUTOFF DATE SET 2026-09-15 (delegated to the coordinator; ledger `2026-09-15-d12-legacy-cutoff-date`): `LEGACY_BOOKINGS_NO_NEW_WRITES_FROM=2026-10-01`.** No production write count was needed — the lane retired both callers of the rail's only write door, so the count is zero by construction from that deploy; two weeks is notice, not measurement. **OPERATOR STEP STILL OWED:** set that env var in the Replit production deployment; until it is set the shipped state is unset and the rail is open (an absent variable never closes a money rail). Original question: **Which booking rail becomes canonical, and when do new writes to the legacy rail stop?** *(commerce contract §19.11–12, and the legacy rail's own fee fix on 2026-09-08 touched both)* | `service_bookings` is canonical. The legacy rail needs a dated "no new writes" decision before anything is made read-only; historical reads and refunds survive retirement. |
| ~~D-13~~ | ✅ **ANSWERED 2026-09-12** (ledger `2026-09-12-listing-names-its-expert-offering`) **AND BUILT THE SAME DAY** (ledger `2026-09-12-listing-expert-offering-key`, migration 292): the FK, the §19 pick-based admission on both `/api/provider/services` rails, the wired `loadOfferingListingInput`, the "What you sell" authoring control on the shared `ServiceForm`, and E2/E3/E4/E6 flipped to working end to end. The answer as ratified: add the third FK `provider_services.expert_offering_type_key` → `expert_offering_types`, ON DELETE SET NULL — not a catalog merge (LD 4), not a new table (the FAQ). Additive nullable, no CHECK, declared, registered, no backfill; writes allowlist-only and **no role gate** — the FK is the whole constraint, and naming an offering type says what is sold, never who the seller is (it is not a credential; LD 27's verification is untouched). Acceptance: the `archetype-fixtures` suite's E2/E3/E4/E6 flip to working. Original question: **How does a LISTING say which expert offering it is?** Migration 107 put `offering_type_key` on the two application FORMS, so an account's role is known and a listing's offering is not. OC-A4 rightly refuses to file a listing under its owner's role (that would infer the product from the person, §13), which leaves E2/E3/E4/E6 unclassifiable — V-12. *(found by `2026-09-12-archetype-fixtures`)* | Add a THIRD FK — `provider_services.expert_offering_type_key` → `expert_offering_types` — mirroring migration 107's shape. **This is not a merge of the two catalogs** (LD 4) and not a new service table (the FAQ): it is the same pointer the forms already carry, on the row that actually needs it. Additive nullable, no CHECK, declared in `shared/schema.ts`. Needs decision-maker approval because it touches the catalog architecture. |
| ~~D-14~~ | ✅ **ANSWERED 2026-09-15 (option A — the column stop-and-ask)** — decision-maker ruling, ledger `2026-09-15-d14-quantity-is-units`, **AND THE CODE LANDED THE SAME DAY.** `cart_items.quantity` means **UNITS of the listing** (the price multiplier `resolveItemBaseAmount` reads) and `cart_items.party_size` (migration 206) means **the PARTY**, which is **never a multiplier**. Which question a cart surface asks is ONE derivation — `shared/cart-quantity.ts` `archetypeAsks` — read by all three write rails and by `/cart`: a **stay** (`product_shape` property/property_room, or `pricing_unit = 'per_night'`) and a **bundle** ask GUESTS and pin the unit count to 1; a **seat-shaped place service** (`in_person`/`hybrid`) asks SEATS and the server DERIVES `quantity = party_size` (§14's posture on the multiplier); an **artifact/async** listing (`pdf`/`voice_notes`/`async_messaging`) asks neither. A multi-unit body on a pinned archetype is **REFUSED 400 naming the rule, never clamped** (§13), and a RE-ADD no longer increments one — the dedupe `+ 1` is how a villa added twice came to be priced twice. **§13 on the fifth shape:** a live remote session (`call`/`video`) and an unclassifiable row are `unruled` and keep today's behaviour exactly, named rather than guessed. **CHECKOUT MATH UNTOUCHED** (a stay is still nights × rate; slot capacity is still `storage.bookSlot`, which claims exactly ONE unit per slot-bound line and reads neither column — recorded, not changed) and **no schema change, no migration**. Proven by `shared/__tests__/cart-quantity.test.ts` (Q1–Q8), `server/__tests__/cart-quantity-admission.http.test.ts` (V1–V10) and `client/src/lib/__tests__/cart-quantity-controls.test.ts` (W1–W6). **The plan side is NOT closed: `itinerary_items` has no unit column, so D-16(a)'s refusal STANDS — see D-41.** Original question: **What does `cart_items.quantity` MEAN, per archetype?** The cart carries exactly ONE number, and the archetypes need it to mean two different things: **P1** (scheduled place service) with 7 people is **7 seats** against slot capacity; **P6** (a stay) with 7 people is **ONE** booking whose guest count is 7; **P7** (a bundle) has components that each carry their own count. `quantity: 7` is right for the first and bills the second seven times. The commerce contract already names them as DIFFERENT FACTS — P1's `requiredContext` carries **`party_size`**, P6's carries **`guests`** — and neither of them is `quantity`. `provider_services` carries `partySizeMin`/`partySizeMax`, but the cart row has nowhere to put the traveler's answer. **[verified by click-through 2026-09-13** — the local stand-up in ledger `2026-09-12-…`'s successor lane; `cart_items` has `quantity`, `slotId`, `scheduledDate` and no party column]**. Blocks **OC-B3** (required-context enforcement) and every multi-person purchase. *(Capacity is claimed at checkout, never at add-to-cart, so this is a correctness question about what is billed — not a race.)* | **Keep `quantity` meaning units of the listing, and capture the party as its own required-context fact per archetype.** They are different answers to different questions, and collapsing them into one number is exactly how a villa for seven gets billed seven times. |
| ~~D-15~~ | ✅ **ANSWERED 2026-09-13 — GUEST OPTIMIZATION IS NOT BUILT, AND IS NOT WHAT WAS MISSING.** A guest is not missing *optimization*; a guest is missing a **plan**. Optimization is one operation ON a plan — it rewrites `itinerary_items` on a `trips` row under the item-mutation predicate (LD 42 D17) — so give guests plans and it follows under predicates that already exist; don't, and there is nothing to optimize. Separately, **a paid operation for an anonymous actor is refused on its own terms**: §14 derives the actor from the session, `trip_entitlements` and `plan_memberships` key on a user, and a Stripe customer keys on a user — an anonymous optimize needs a second money rail with its own refund and identity story. Guest PLANNING may be worth building; guest PAID optimization is not. *(`trips.userId` is nullable, but that is the expert-authoring case — NULL means no traveler principal, so every owner-gated predicate fails. It is an unowned draft, not a guest trip; G2 is held as architecture because the hard part is identity, not a column.)* **The ruled flow is sign-in AT THE MOMENT, with the guest's work preserved** — the LD 42 D5 pattern (gate checked before anything is minted). Built by the lane below. | Guest → cart → sign-in → `migrate` → `resolve-trip` → **the cart's lines become the plan's items** → Optimize is then meaningful. G2 stays held: it is a *consolidation* (ledger row 5 makes retiring the guest-cart fallback a G2 exit criterion), not an addition. |
| ~~D-16~~ | ✅ **ANSWERED 2026-09-15** — decision-maker ruling, ledger `2026-09-15-d16-plan-holds-venues-and-content`, **AND THE CODE LANDED THE SAME DAY**: **(a)** → **D-14** (settled: `quantity` is UNITS) and **D-41**, which was **ANSWERED the same day** (ledger `2026-09-15-d41-item-quantity`, migration 298: `itinerary_items.quantity`, NULL = one unit) — so the multi-unit refusal that stood here is now **LIFTED**, and `quantity_gt_one` is deleted rather than left unreachable (§18c); **(b) option A** — `itinerary_items.custom_venue_id`, varchar FK → `custom_venues` **ON DELETE SET NULL**; **(c) option A** — `itinerary_items.content_type` varchar(20) + `content_id` text, a soft reference like `gem_id`/`dmo_extracted_place_id`. Migration **295**: additive-nullable, **NO CHECK, NO DEFAULT, no backfill**, columns + `idx_itinerary_items_custom_venue_id` declared in `shared/schema.ts`. `syncItemProjection` carries all three in BOTH directions, so a custom-venue line and a content line now MATERIALIZE at `POST /api/cart/resolve-trip`; `POST /api/cart/convert-to-itinerary` is a CALLER of the same builder behind a `.strict()` pick-based allowlist. **The priceless-listing refusal REMAINS and is still NAMED on the response; the multi-unit one is gone (D-41, migration 298).** §19: all three omitted from `insertItineraryItemSchema`, re-admitted by no pick. Original question: *Four kinds of cart line have no plan representation at all.* Ledger `2026-09-13-guest-cart-becomes-plan` made a cart line become an `itinerary_items` row at `POST /api/cart/resolve-trip`, but only where the round trip back through `syncItemProjection` is FAITHFUL — because the moment a cart row is linked, the projection is entitled to rewrite it from the item. That refuses four classes, each for its own reason and each NAMED on the response rather than dropped: a **multi-unit line** (the projection writes `quantity: 1`, and `resolveItemBaseAmount` prices a line `rate × quantity`, so materializing it would silently change what the traveler is charged — this is **D-14**'s question, **ANSWERED 2026-09-15** — ledger `2026-09-15-d14-quantity-is-units`: `quantity` is UNITS of the listing. The refusal nevertheless STANDS, and now for a named reason: `itinerary_items` has **no unit column**, so the plan cannot carry a multi-unit line faithfully and the next sync would write `quantity: 1` back over it. Lifting it needs **D-41**); a **custom-venue line** (no `itinerary_items` column points at `custom_venues`); a **content line** (a gem / hotel / activity row names CONTENT, not a listing, and the projection would rewrite its `contentType`/`contentId` and lose the link back to the source — `/api/cart/convert-to-itinerary` is that shape's existing home and it MOVES the row rather than projecting it); and a **priceless listing** (the projection would delete its cart row on the next sync). So a traveler whose cart holds a gem, a venue they added themselves, or three of one listing resolves a plan that honestly does not hold those lines. **That is the right answer today and the wrong answer forever.** *(No data is lost: every refused line stays in the cart exactly as the traveler left it.)* Proposed at the time: Three separate decisions, not one: **(a)** D-14 is SETTLED (`quantity` = units; ledger `2026-09-15-d14-quantity-is-units`), and the materializer will follow it as soon as the plan has somewhere to put a unit count — **D-41**; **(b)** an `itinerary_items` → `custom_venues` link (additive nullable FK, no CHECK) would let a traveler's own venue sit on the plan — needs decision-maker approval, it touches the item model; **(c)** a content line needs either a durable content link on the item or a ruling that Discover content reaches a plan ONLY through convert-to-itinerary, which deletes the cart row. Until then the refusals stay NAMED — never silently reduced, never invented. |
| ~~D-17~~ | ✅ **ANSWERED 2026-09-15 (option C)** — decision-maker ruling, ledger `2026-09-15-d17-d8-specs-armed`. **The lane ran both specs to completion on a CI-shaped Postgres built from empty, and neither could be wired — so D8's wording needs the decision-maker's amendment, which is in that lane's report and is NOT a lane's to write into CLAUDE.md.** `finalize-booking-modal` passes **2/3**; its third case is blocked on an **affiliate-bookable supplier** (an `affiliate_products` row under an approved `affiliate_partners`), which **no CI seed creates and no application route can create** — there is no `POST` for either table anywhere in `server/` — so it stays orphaned rather than half-wired (the `tripstrip-count-accuracy` precedent). `journey-1` is **`e2e/specs/journey-1.spec.ts`**, a different harness from the one this row assumed, reached only by `e2e-tests.yml` (schedule/dispatch, and SKIPPED without `E2E_STAGING_BASE_URL`); its D8 case is green after repair (1/1) but the FILE is 4 red (1A/1B checkout, `/local-experts`), so it is not wired either. **Both specs were REPAIRED, neither weakened:** the finalize spec asserted only the notice's ABSENCE on a finalized card and now asserts the POSITIVE half too (pre-final `/trip/:id` ⇒ the notice + **ONE** action, asserted as a count, to `/plans/:tripId`); `journey-1`'s case clicked a final-aware My Plans tile and waited for `/trip/`, which a PRE-final plan never reaches, and now addresses `/trip/:id` directly. **Orphan count unchanged, 30 → 30.** **An ARMED spec asserting D8 does exist and is neither of the two D8 names:** `playwright/tests/slip-rail-actions.spec.ts` **A12**, on the blocking `slip-rail-actions-gate.yml` (PR + push). Original: **CLAUDE.md Locked Decision 42 D8 says the `journey-1` and `finalize-booking-modal` specs are "armed" and assert the pre-final Trip Card notice — neither appears in ANY workflow** [verified 2026-09-14 by `scripts/check-test-files-wired.cjs`; `finalize-booking-modal` is one of the 30 orphans classified `blocked` in ledger `2026-09-14-test-files-wired-orphans`]. The D8 wording therefore rests on specs CI never runs. | Either wire both (the orphan lane's classification names what blocks `finalize-booking-modal`) or amend D8's wording — a CLAUDE.md change, so the decision-maker's, not a lane's. Do not delete either spec. |
| ~~D-18~~ | ✅ **ANSWERED 2026-09-15 (option A — the marker), AND THE CODE LANDED THE SAME DAY** — decision-maker ruling, ledger `2026-09-15-d18-announced-marker`, **migration 297**. `ready_made_purchases.notified_at`: additive nullable timestamp, **NO DEFAULT, NO CHECK, no backfill**, **declared in `shared/schema.ts`** (publish-trap rule). **NULL = NO RECORD OF AN ANNOUNCEMENT** (§13) — a stamped default would claim a fact nobody has. **ONE WRITER (§18 rule 1):** `notifyBuyerOfReadyMadeDelivery`, the one shared sender, stamping through an atomic conditional (`storage.markReadyMadePurchaseNotified`, `WHERE id = ? AND notified_at IS NULL` — §15, the statement is the guard, so a second stamp is a truthful no-op). It stamps on the buyer's notification row EXISTING rather than on this call having inserted it, because the already-inserted case IS the half-finished state the column records; the EMAIL stays gated on the insert, so nothing is re-sent. **§17:** the drift job's new **R6** finds a `cloned` purchase with a NULL marker past a **CONFIG** grace (`server/config/ready-made-announce.config.ts`, envDays posture — no literal) and HANDS IT BACK to that same sender (the one narrow exception, `promotePaidCheckout`'s standing one rail over); it composes no message, sends nothing itself and **writes the column never**. A successful hand-off records **nothing** — the pass fixed it — and only a hand-off that still cannot produce an announcement appends **`rm_delivery_not_announced`** (`warning`, dedupe-keyed; the kind column carries no CHECK, so **no migration**). **§13 in both directions:** a `paid` row is never announced (it was never delivered), a row inside the grace is untouched, and a buyer who DELETED their own clone trip is not indicted for their own housekeeping. **§19:** omitted from `insertReadyMadePurchaseSchema`, re-admitted by no pick. Proven by `server/__tests__/ready-made-announce-marker.test.ts` (A1–A7, pure) and `reconciliation-detection.db.test.ts` (N27a–N27e). Original question: **Does a delivered ready-made purchase get a durable "announced" marker, so an unsent notification can be retried?** Lane `2026-09-14-readymade-notifications` (PR #900) hangs the buyer's bell + email on the winner of the atomic `paid → cloned` claim and makes them exactly-once through `notifications.dedupe_key`; but a crash BETWEEN the claim and the send leaves a delivered purchase unannounced and nothing retries it, because no column records that the announcement happened — `email_outbox` has no dedupe column either, so the email's once-ness rides the notification insert. [verified 2026-09-14 by that lane; stopped rather than write a migration] | Add `ready_made_purchases.notified_at` (additive nullable, NO CHECK, declared in `shared/schema.ts` — publish-trap rule; NULL = never announced, §13), stamped by the same claim-winner path, and let the EXISTING §17 reconciliation job hand a `cloned`-and-NULL row back to the notifier as recovery-layer-2-arriving-late (detect, then the one shared sender — never a second sender). Alternative: accept the gap and say so on the purchase row. |
| ~~D-19~~ | ✅ **ANSWERED 2026-09-15 (option (b)), AND THE STORE LANDED THE SAME DAY** — decision-maker ruling, ledger `2026-09-15-d19-plan-proposals`, **migration 299**. **`plan_proposals`** is a NEW child table of `trips` on the `dmo_extracted_places` / `service_route_points` pattern: FK **ON DELETE CASCADE**, additive, **NO DB CHECK and NO DEFAULT on `status`** (publish-trap posture), table **and** index **declared in `shared/schema.ts`** (deploy-push durability rule). **THE EXPERT `trip_suggestions` RAIL IS UNTOUCHED** — its NOT NULL `expert_id`, its gate and its hardcoded `origin:'expert'` all stand, and **P3** pins that no source anywhere mints an AI sentinel author on it. A **LOG, not an ordered list**: no `position`, no UNIQUE; ordering is `created_at`. `conversation_id` is a nullable FK → `conversations` (LD 45 (1), migration 290) **ON DELETE SET NULL** — deleting a thread never deletes the proposals it produced. §13: NULL `conversation_id` names no thread, NULL `question` is not recorded, NULL `model_tier` is unrecorded and is a **cost record only** (LD 41 (c), never a product claim), and an empty log means the plan has never been asked anything. `applied_item_ids` is the record of what an apply created — **LD 42 D18: a record, never an undo**. Value set stated ONCE in `shared/plan-proposals.ts` (§18 rule 1); admission is **pick-based** with **no `insertPlanProposalSchema` at all** and reaches no request body (§19); ONE service, whose `discardPlanProposal` is an **atomic conditional** (§15/§18b) so two concurrent discards produce one write and an applied row is never discardable. Two routes — `GET /api/trips/:tripId/proposals` and `POST /api/trips/:tripId/proposals/:id/discard` — gated by the SAME `authorizeTripLogistics(..., { requireWriteAccess: true })` predicate item mutations use (owner ‖ §12 WRITE-status advisor, never `pending`), owner from the session (§14), and **one 404 for every refusal, never a 403** (the probing posture). **NOT BUILT AND NAMED:** no apply (the charge point — **D-20**/**D-21**, which add their own columns, which is why this table carries no payment/charge/claim column at all), no create route and no client UI — the L16 Ask-AI drawer is the consumer that follows. Pinned by `server/__tests__/plan-proposals.db.test.ts` **P1–P7** (own Postgres job in `build.yml`). Original question: ~~**Where does an AI proposal live before the traveler applies it?**~~ CLAUDE.md Locked Decision 45 (3) rules that every Ask-AI answer is a PROPOSAL staged beside the plan, and the L16 brief's first version said it would reuse the expert suggestions rail. It cannot: `trip_suggestions.expert_id` is **NOT NULL, FK → `users.id`** (`shared/schema.ts`), `POST /api/trips/:id/suggestions` refuses anyone who is not `isExpertAssignedToTrip`, and the approve path **hardcodes `origin: 'expert'`** on the item it creates (`server/routes/booking-actions.ts`) — the false attribution LD 42 **D4**/**D23** forbid by name. So the rail is expert-shaped in its column, its gate and its output, and there is nowhere honest for an AI proposal to sit. **[verified 2026-09-15 against `main` @ `319de1fd3`; blocks all of L16]** | Two options, both schema, so neither is a lane's to take. **(a)** Widen the existing rail: nullable `expert_id` plus an explicit author column the approve path **derives** instead of hardcoding, so one review surface serves both authors. **(b)** A new `plan_proposals` child table of `trips` on the `dmo_extracted_places` / `service_route_points` pattern (CASCADE, ordered, additive-nullable, **NO DB CHECK** — publish-trap posture, declared in `shared/schema.ts`), leaving the expert rail untouched. **Recommend (b):** an expert suggestion and a machine proposal have different authors, different review consequences and different protection rules (D3), and widening a NOT NULL identity column is how one rail starts answering two questions. |
| ~~D-20~~ | ✅ **ANSWERED 2026-09-15 (option A — FLAT, from `fee_bands`) AND LANDED THE SAME DAY** — decision-maker ruling, ledger `2026-09-15-d20-d21-proposal-charge`, **migration 300**. `resolveAiTaskChargeCents` (`server/services/proposal-charge.service.ts`) reads the `concierge:ai_task` band through the EXISTING fail-loud `requireFlatCentsBand`: **no literal, no fallback default** — a charge path that cannot price itself REFUSES (§8/§13). **`optimization_fees` IS UNTOUCHED** (a second tiered fee table for a second AI product is how the platform gets two fee homes nobody can reconcile; folding them is an older question and its own lane). The band's **`owner` entry in `RESOLVER_FEE_BAND_REQUIREMENTS` is AMENDED** to name the charge path beside `pricing.routes`, so V-5's deactivation warning says what actually breaks (a traveler cannot apply a proposal) instead of naming only the pricing page. Pinned by `plan-proposal-charge.db.test.ts` **C7** — the assertion is the EQUALITY against the `fee_bands` row and it follows an admin edit, never a number typed into a test. Original question: ~~**Is the AI task one flat price, or tiered like the optimizer?**~~ The band exists and is admin-editable — `concierge:ai_task`, `flat_cents`, seeded 299 by `server/migrations/258_plans_reconcile.sql`, `required: true` with a fail-loud fallback (`server/services/fee-band-requirements.ts`) — but it is **display-only**: only `GET /api/pricing` reads it. The optimizer, the nearest comparable charge, prices from a **different home**: `getFee` over `optimization_fees`, keyed on complexity tier and event type (`server/services/optimization-fee.service.ts`). A task costing the same on a three-day city break and a fourteen-day multi-stop wedding may be right or may be the wrong shape, and the answer decides which resolver L16 wires. **[verified 2026-09-15]** | **Flat, from `fee_bands`** — LD 45 (3) already names the band, §8 forbids a literal anywhere else, and a second tiered fee table for a second AI product is how the platform ends up with two fee homes nobody can reconcile. When the charge point lands, amend that band's `owner` entry in `RESOLVER_FEE_BAND_REQUIREMENTS` so V-5's deactivation warning names the real consequence instead of only the pricing page. Do **not** fold `optimization_fees` into `fee_bands` as a side effect — that is an older question and its own lane. |
| ~~D-21~~ | ✅ **ANSWERED 2026-09-15 (option A — ONE CHARGE PER DISTINCT PROPOSAL APPLIED) AND LANDED THE SAME DAY** — decision-maker ruling, ledger `2026-09-15-d20-d21-proposal-charge`, **migration 300**. The §15b CLAIM is ONE atomic conditional on the `plan_proposals` row taken BEFORE the Stripe call (`charge_claimed_at IS NULL`), the Stripe idempotency key is **`ai-apply-<proposalId>`** — the proposal id and nothing else, unlike the optimizer's per-target-per-DAY key — and a double-click, a retry or a resumed sheet is ONE charge (**C1** fires two concurrent claims and proves exactly one lands). Asking, reading and discarding stay free. A Trip Pass takes **no claim and creates no PaymentIntent** (**C2**), records `charge_basis='trip_pass'` with `charged_amount_cents` **NULL, never 0**, and writes **no ledger row**. A second apply is 409 with no second items (**C5**). Discard after a claim: **allowed while no PaymentIntent exists** (that row is a pay attempt whose Stripe call never landed, and discard is its recovery), **refused once one does** (**C6**) — with the stated liveness limit that nothing releases a claim (§15b). Copy is "charged only when you apply a proposal", never "per question". Original question: ~~**Is `ai_task` consumed per ASK or per APPLY, and what is one "task"?**~~ LD 45 (3) says charged only on apply; it does not say whether a second proposal applied from the same question charges again, nor whether re-applying one proposal after an edit charges twice. Today nothing calls `coversAction(tripId, "ai_task")` at all (LD 41 (f)), so the question has never had to be answered — and because a Trip Pass covers the action **unlimited** (`server/services/trip-entitlement.service.ts`), the answer bites only uncovered travellers and only at the charge, never as a counter to race. **[verified 2026-09-15]** | **One charge per DISTINCT PROPOSAL APPLIED**, idempotent on that proposal's id: the §15b claim sits on the proposal row, the Stripe idempotency key derives from it, and a double-click or a retry is one charge. Asking is free however many times, which is what makes the review-first shape honest (D18 — there is no undo, so the traveller must be able to read and discard without paying). State it on screen as "charged only when you apply", never "per question". |
| ~~D-22~~ | ✅ **ANSWERED 2026-09-15 = YES, and LANDED (ledger `2026-09-15-d22-dates-confirmed`, migration 302): `trips.dates_confirmed_at` — additive nullable timestamp, NO DEFAULT, NO CHECK, declared in `shared/schema.ts`, NO BACKFILL. NULL = NOT CONFIRMED ⇒ every reader labels the window a PLACEHOLDER (never "no dates" — the columns are NOT NULL), the `.ics` makes no pinned DTSTART claim off it and the countdown is withheld. SERVER-SIDE and OPT-IN (§19): `insertTripSchema` omits it, `storage.createTrip` takes the mint site's own `datesChosenByTraveler` (silence = no claim), `storage.updateTrip` stamps on any date change. Ten mint sites decided and annotated — the ready-made clone, both expert authoring builds, the cart convert-to-itinerary mint and the Plus occasion draft leave NULL.** ~~**May a plan carry `trips.dates_confirmed_at` (additive nullable timestamp, NO DEFAULT, NO CHECK, declared in `shared/schema.ts`) so a ready-made clone's placeholder window renders AS a placeholder until the buyer answers?** Under D-1 = B the clone is editable, but `start_date`/`end_date` are NOT NULL with no marker, so the slip cannot distinguish the platform's `new Date()` window from a chosen one (lane `2026-09-14-clone-date-revalidation`, step 2). NULL = not confirmed ⇒ every reader shows the dates as placeholders and the .ics/countdown surfaces treat them as not captured (§13); set by the ONE re-date write (the owner-gated `PATCH /api/trips/:id`, which the client never calls today — that rail is R-4's lane). Ordinary mints may stamp it at create since the traveler chose those dates.~~ | Ruled yes and built in one lane with R-4. |
| — | *(**D-23** was never opened — the D-4 lane closed it without filing a column; the number is retired, not lost, and is never reused.)* | — |
| D-24 | **Does a booking carry `service_bookings.accepted_at`, and is the acceptance deadline STORED or DERIVED?** D-6 rules that an artifact completes on acceptance, and nothing on the row records that a traveler accepted — `completed_at` records the money event, not the answer that caused it. Same question for the deadline: a stored `acceptance_window_ends_at` or a value derived from the delivery instant plus the config window. *(from D-6; `docs/design/EXPERT_ACCEPTANCE_BRIEF.md` §4)* | **`accepted_at` yes; the deadline DERIVED, not stored.** A stored end date is a second authority that disagrees with the config the moment the config moves, and it would have to be re-stamped on every re-delivery — `resolveCompletionEligibility` already derives `eligibleAt` this way. Additive nullable, **NO DEFAULT and NO DB CHECK** (publish-trap posture), declared in `shared/schema.ts`, no backfill; NULL = never accepted and the row is OMITTED, never rendered as "not accepted" on a booking that is not an artifact (§13). The two new `service_bookings.status` values (`awaiting_acceptance`, `revision_requested`) need **no** migration — the column is `varchar(30)` with no CHECK (`shared/schema.ts:1426`), the LD 44(e) posture. |
| D-25 | **Is a revision a counter (`revisions_used` integer) or a child table, and does a `revision_status` varchar mirror the ready-made shape?** The allowance is `provider_services.revisions_included` and nothing on the service rail reads it today; the nearest precedent is `ready_made_purchases.revision_status` / `revision_request_note` / `revision_requested_at` (migration 252, app-enforced vocabulary, no CHECK, claimed by one atomic conditional). *(from D-6)* | **A CHILD TABLE** (`booking_revision_requests`) on the `service_route_points` / `dmo_extracted_places` pattern — FK → `service_bookings` ON DELETE CASCADE, `UNIQUE (booking_id, "position")`, `requested_at`, `note`, `resolved_at`; the count is DERIVED from the rows and never stored beside them (§18 rule 1). A counter answers *how many* and nothing else: it cannot carry the traveler's words, which are the evidence admin review needs, nor when a revision was asked for or answered. **No `revision_status` mirror:** ready-made needs one because its entitlement is exactly one and lives on the purchase row; here the allowance is N and the child rows say the same thing more precisely. |
| D-26 | **Is there a PER-BOOKING deliverable?** Today the artifact is `provider_services.service_file` — a LISTING-level file, stamped by `deliverable_uploaded_at` and served to every buyer by `GET /api/service-bookings/:id/deliverable` — so a revision delivered to one traveler **rewrites the file every other buyer downloads**. *(from D-6; this is why the revision half of the ruling cannot be built truthfully without an answer)* | **Yes — a per-booking artifact pointer**, additive nullable, no CHECK, declared in `shared/schema.ts`, with the listing file as the honest fallback when it is NULL (§13 — a booking with no per-booking artifact reads the listing's, and says so). Without it, "re-deliver" either corrupts other buyers' purchases or silently does nothing. |
| D-27 | **Does `artifact_timer` RETIRE as a completion rule, what is the escalation period, and who reviews?** D-6 forbids a silent timeout completing in the seller's favour, and `artifact_timer` is exactly that: `TIMER_DRIVEN_COMPLETION_RULES` contains it, and `server/jobs/bookingAutoCompletion.ts` flips the booking to `completed`, minting held earnings. *(from D-6)* | **Retire it as a COMPLETION rule and keep it as an ACCEPTANCE-PROMPT rule:** it leaves `TIMER_DRIVEN_COMPLETION_RULES`, the `auto_complete_pdf` actor goes, and the job's artifact arm targets `awaiting_acceptance` and then `admin_review` instead. **Escalation lands in the EXISTING admin dispute queue** with its own reason — never a second queue. The period is a config value on the `completion-windows.config.ts` pattern; **the decision-maker sets the number.** Existing rows completed under the old timer are NOT rewritten — a booking that was completed was completed (LD 44(e), no backfill). |
| D-28 | **Is a custom quote a CHILD TABLE or columns on a request row?** No quote table and no quote columns exist anywhere (grepped at `4d1c0c4`); `booking_requests.counter_price` is a dormant legacy table with one reader and no writer, and `provider_booking_requests.counter_offer` is the expert's vendor-wrangling rail that no checkout reads. *(from D-8)* | **A child table, `service_quotes`** — FK → `provider_services` and the requesting traveler, `amount_cents`, `currency`, `expires_at`, `quoted_by`, `accepted_at`, `superseded_by` (self-FK), `note`. Additive, NO CHECK, declared in `shared/schema.ts`, no backfill, §19 allowlist writes. Columns on a request row cannot hold a **sequence** of offers, and re-quoting is the ruling's own required behaviour — a row edited in place destroys the record of what expired. Amount in **CENTS**. Do not extend either legacy table: a third meaning for "a price a provider proposed" is the drift class §18 rule 1 names. |
| D-29 | **Where does the quote validity window live, and may a provider choose one?** The ruling says config or a provider choice under a configured ceiling, never a literal (§8). *(from D-8)* | **A config default** (`QUOTE_VALIDITY_DAYS`, the `server/config/completion-windows.config.ts` `envDays` shape — a day count is not a fee, so §8's `fee_bands` rule does not bind it but the no-literal half does) **plus an optional per-quote provider choice clamped to a configured ceiling**, refused with the ceiling stated rather than silently clamped. The ceiling is **one platform value, not per-listing** — a per-listing ceiling is a second authority for the same rule. **The decision-maker sets the numbers.** |
| D-30 | **What does a quote REQUEST create, and does acceptance mint through the existing checkout?** Today the buy resolver lands a quote listing on `store: "booking_request"` and `service-detail.tsx:1774` says out loud that no such rail exists — the button opens the LD 40 conversation and writes nothing. But `POST /api/expert-booking-requests` **does** write: `Number(service.price ?? 0)` → a real `service_bookings` row at `totalAmount: "0"` (`server/routes.ts:1779,1855`), with a commission split computed off zero, while `POST /api/bookings`, the second add rail, `GET /api/cart` and checkout all carry `PRICELESS_LISTING_REFUSAL`. *(from D-8)* | **The request creates NO booking row** — a `service_quotes` row in `requested` state plus the existing LD 40 conversation for the words. **Acceptance mints through the EXISTING checkout spine** (CLAIM → AUTHORIZE → PROMOTE, §15b) with the quoted amount as the line total; `stripePaymentIntentId` keeps its sole writers (§19a). **This row also owns the $0 hole:** `POST /api/expert-booking-requests` must either carry the same refusal its four siblings carry or route into this lane. Leaving it is the one thing the brief says must not happen. |
| D-31 | **Does an accepted quote's `chargeMode` become `deposit_balance`?** `chargeModeFor` tests `commitment === "quote_approve"` **before** `depositEnabled === true` (`server/services/offering-commerce-contract.ts:424-425`), so a custom-quote listing with deposits switched on resolves `after_quote` and the deposit fact never reaches the contract — which is then snapshotted onto the booking (`offering_contract_snapshot`, `shared/schema.ts:1470`). *(from D-8)* | **Yes**, and the resolver's ordering changes so `depositEnabled` is consulted for an ACCEPTED quote. Until it is ruled, a quote-born booking that actually took a deposit carries a contract snapshot saying `after_quote` — a snapshot that misdescribes its own money schedule. No amount, rate or fee band moves either way. |
| D-32 | **Is per-component bundle state a CHILD TABLE or the existing jsonb?** Per-component COMPLETION already lives on `booking_details.componentCompletions` — a `{componentId: ISO}` map merged by `recordBundleComponentCompletion` (`server/services/booking-completion.service.ts:571-624`) — but there is no per-component FAILED, REFUNDED or ACCEPTED state anywhere. *(from D-9)* | **A child table, `booking_component_states`** — FK → `service_bookings` ON DELETE CASCADE, `component_service_id`, `UNIQUE (booking_id, component_service_id)`, `state` varchar(20) app-enforced, `delivered_at`, `accepted_at`, `failed_at`, `failure_reason`, `refunded_at`, `refund_amount`, `stripe_refund_id`. Additive, NO CHECK, declared in `shared/schema.ts`, no backfill. **A jsonb key cannot be the target of an atomic conditional** the way a row can, so every §15 claim would become a read-modify-write on the whole document. The old map stays readable for historical rows and is written no more. |
| D-33 | **Does the bundle snapshot carry a PRICE?** Checkout snapshots each component as `{ id, serviceName }` and nothing else (`server/routes/payments.routes.ts:1074`), so the ruling's "refunded at its own SNAPSHOTTED price" has nothing to read. *(from D-9; this is why the refund half of the ruling cannot be built truthfully without an answer)* | **Yes** — extend the checkout snapshot to carry each component's price **as it was at purchase**. The only alternatives are both §13 lies: refund an equal share of an unequally-priced bundle, or refund the component's price as it stands today. Old rows keep no price and their refund goes to a human with the reason stated. **The component prices need NOT sum to the bundle price** (bundles are routinely discounted): the refund is the component's **pro-rata share of what was actually charged**, derived server-side. |
| D-34 | **Is `partially_completed` a `service_bookings.status` value, and which lists learn it?** The column is `varchar(30)` with NO DB CHECK (`shared/schema.ts:1426`), so a new value is a code change and not a publish trap (LD 44(e)) — but `PAID_EQUIVALENT_STATUSES` and `TERMINAL_STATUSES` (`server/jobs/stripeReconciliation.ts:152-158`), `COMPLETION_ALLOWED_FROM_STATUSES` (`booking-completion.service.ts:80`) and the hard-coded SQL of `paid-service-bookings-have-payment-intent` (`scripts/invariants.mjs:149`) would each not know it. *(from D-9)* | **Yes**, app-enforced, no CHECK, no backfill. It **joins** `PAID_EQUIVALENT_STATUSES`, the invariant's SQL and `COMPLETION_ALLOWED_FROM_STATUSES` (so a late-delivered component can still complete the parent); it **does not join** `TERMINAL_STATUSES`. Each is one line **and a deliberate decision** — a status nobody's list knows is a paid state with no money-integrity oversight. |
| D-35 | **How do earnings release per component?** `mintCompletionEarningsForBooking` inserts one `platform_revenue`, one `provider_earnings` and one `expert_earnings` row with `onConflictDoNothing` on `sourceId`/`referenceId` (`server/storage.ts:3354` ff., migration 203's partial unique indexes), so minting a second set per booking is refused by the index. *(from D-9)* | **One mint at `partially_completed` over the reduced figures**, keeping the one-row-per-booking indexes intact. Per-component earning rows are the alternative and are a schema change; per-component **release** does not need them, because release is driven by `available_at` and `dispute_state` on the earning row, not by row count. `reverseEarningsForBooking` stays restricted to 100% refunds — a partial reversal would wrongly zero the retained share, which is why the cancel path already restricts it. |
| D-36 | **Does a booking carry `service_bookings.completion_declared_at`, and is the dispute deadline STORED or DERIVED?** D-7 rules that the seller DECLARES and the traveler then has a window, and nothing on the row records the declaration — `completed_at` records the money event, which under this ruling happens after the window, not at the declaration. *(from D-7; `docs/design/EXPERT_ACCEPTANCE_BRIEF.md` Part II §13)* | **`completion_declared_at` yes; the deadline DERIVED, not stored** — the same answer D-24 gives one table over, for the same reason: a stored end date is a second authority that disagrees with the config the moment the config moves. Additive nullable timestamp, **NO DEFAULT and NO DB CHECK** (publish-trap posture), declared in `shared/schema.ts`, no backfill; NULL = never declared and the row is OMITTED, never rendered as "not declared" on a booking whose rule is a timer (§13). The new `service_bookings.status` value `completion_declared` needs **no** migration — `varchar(30)` with no CHECK (`shared/schema.ts:1425`), the LD 44(e) posture. |
| D-37 | **Where does the earnings MINT sit relative to the declared window?** Today `completeBooking` flips `confirmed → completed` and `storage.updateServiceBookingStatus` mints the held earning **inside that transaction**, with `availableAt = now + holdWindowDays('service_booking')`. Inserting a declared state ahead of it moves either the mint or the anchor, and getting it wrong is a silent payout delay nobody would see in a test. *(from D-7; the ONE money question in Part II)* | **Mint at the WINDOW'S CLOSE, with `availableAt` anchored to the DECLARATION instant** (`availableAtFor`'s existing `from` parameter). Keeps ONE mint trigger (`completed`, unchanged), ONE dispute window (`completion-windows.config.ts`'s own "do not add a parallel constant" rule), and today's payout timing to within a scheduler pass. **Rejected: minting at the close with the default `now` anchor** — it serves the window twice (7 declared + 7 held) and doubles every seller's wait, a money-timing change this ruling did not authorize. **Also rejected: minting at the declaration** — it grows a second mint-triggering status and splits `completed_at` from the earning's birth. |
| D-38 | **Is a pre-completion dispute the SAME row as today's post-completion one?** `GET /api/admin/disputes` selects `service_bookings WHERE status = 'disputed'` and reads `booking_metadata->>'disputeReason'`; there is **no disputes table** — a dispute is the booking's status plus that jsonb key plus `{provider,expert}_earnings.dispute_state`. *(from D-7)* | **Yes — REUSE it.** `status='disputed'` + the same jsonb reason + the same queue; **do not add an `admin_review` status the queue's own predicate cannot see, and do not create a disputes table.** If the queue must tell the two apart, derive it from `completion_declared_at` and the absence of `completed_at` — never a second status (§18 rule 1). **§13 sub-point that must not be lost:** under D-37's recommendation no earning exists yet when a declared-window dispute arrives, so `setBookingEarningsDispute` flags **zero rows**, and zero must never be read as *cleared* — the block is the status itself, because the booking never reaches `completed` and so nothing mints. |
| D-39 | **Coordination: who declares, what does the window gate, and is a coordinator ever paid an earning?** `coordination_states.status` is `varchar(30)` with **no CHECK** (a new value is a code change), advanced by `PATCH /api/coordination-states/:id/status`; the fee is captured up front (`fee_payment_status` unpaid→pending→paid→refunded, which **HAS** a DB CHECK — a new value *there* is a publish trap) and recorded as `platform_revenue`, so **no coordinator earning is ever minted** and `completed` on an engagement moves no money at all. *(from D-7)* | **The assigned coordinator (`assigned_expert_id`) declares**, the traveler gets the same config window, and — because there is nothing to release — the window gates the **admin refund** (`POST /api/coordination-states/:id/refund`), never a payout that would be a claim nobody can keep (§13). **Whether a coordinator is ever paid an earning out of the captured fee is a SEPARATE, unruled money question** (owner = the memberships/engagement lane) and must not be invented as a side effect of a completion state. Same row: the traveler arm of that PATCH today has **no ordering check at all** (Part II §15 F3) and needs a rule before a declared state means anything there. |
| D-40 | **Build the expense object — and may a `hybrid` listing declare an artifact deliverable of its own?** D-7 rules expenses **option B — not yet**: no table, no column, no quote, no approval, no evidence store, no refund route, and **no surface may promise one** (pinned by `client/src/lib/__tests__/no-expense-reimbursement.test.ts`). The target spec is recorded verbatim in Part II §16, with the columns such an object would need. *(from D-7)* | **Not yet — and when it is built, only in the ruled shape:** quoted and approved by the traveler BEFORE incurred or not reimbursable, ever; charged through platform rails (§14/§15); evidence attached to the approved quote; refund by the same route; never reconciled after the fact from receipts. Until then a seller who must spend to deliver **prices it into the listing** — the rail that already exists, whose amount is server-derived and whose refund story is written. **Sub-question in the same row:** whether a `hybrid` listing may DECLARE an artifact deliverable that takes D-6 acceptance on its own while the booking keeps D-7 completion — the honest alternative to reclassifying the whole booking (Part II §10). |
| ~~D-41~~ | ✅ **ANSWERED 2026-09-15 (yes), AND THE CODE LANDED THE SAME DAY** — decision-maker ruling, ledger `2026-09-15-d41-item-quantity`, **migration 298**. `itinerary_items.quantity`: integer, additive nullable, **NO DEFAULT, NO CHECK, no backfill**, **declared in `shared/schema.ts`** (publish-trap rule). **NULL = ONE UNIT** — the item model's own historical shape and the only reading that leaves every existing row saying exactly what it already said (§13): never 0, never unknown, never a guess. **ONE COPY-DOWN, BOTH WAYS (§18 rule 1):** `server/services/cart-projection.service.ts` and nothing else — `syncItemProjection` writes `quantity: item.quantity ?? 1` onto the cart row, and the materialize and convert rails carry the line's count onto the item through the ONE shared `buildPlanItemValues`. **D-16 (a) IS LIFTED:** the `quantity_gt_one` refusal is DELETED (§18c — not left unreachable), because the round trip that is the module's whole admission test is now faithful at a count above one. **§13:** a single-unit line is carried as NULL, not as 1 — `cart_items.quantity` is `DEFAULT 1` and every units-pinned archetype is pinned there by rule, so writing 1 would turn "never asked" into "they answered one". **§19:** omitted from `insertItineraryItemSchema` and stripped in storage — **there is ONE admission rule for units and it is D-14's**: a traveler sets them on the CART LINE, where `archetypeAsks` validates the question against the listing's archetype, and the item receives the count by PROJECTION. **CHECKOUT MATH UNTOUCHED** (the money path still prices `rate × quantity` off the CART row; no money path reads the plan's column) and **NO PARTY COLUMN WAS ADDED** — a plan item carrying a party size is a separate decision nobody has made. **A READY-MADE CLONE DOES NOT CARRY IT** (`CLONE_EXCLUDED_FIELDS`): a plan item's unit count is the BUYER's own cart-line answer projected onto it, and the author's count is not an answer the buyer gave — §13, the clone carries NULL, which means ONE UNIT. Proven by `server/__tests__/plan-item-units.test.ts` (U1–U7, pure), `guest-cart-becomes-plan.http.test.ts` (G6 repaired, G7 and G13 now assert the LIFT and the round trip at 3 units) and `ready-made-clone-fields.db.test.ts` (C7 exhaustiveness). Original question: **Does an `itinerary_items` row get a unit count?** D-14 is answered — `cart_items.quantity` is UNITS of the listing — but the PLAN has nowhere to hold one. `itinerary_items` carries `min_participants`/`max_participants` (the item's own capacity BOUNDS, from templates) and no count of what was bought **[verified 2026-09-15 against `shared/schema.ts` and every migration]**. So `syncItemProjection` writes `quantity: 1` unconditionally, and `materializeCartLinesAsItems` REFUSES a multi-unit cart line rather than silently reducing what the traveler is charged (D-16(a)). Three of a listing's seats therefore cannot become a plan item at all. *(This lane could not decide it: adding a column is a schema change, and the lane brief forbids one.)* | **`itinerary_items.quantity` integer, additive nullable, NO DB CHECK** (the publish-trap posture — migrations 181/195/273/275/276/277/279/280/281/282/284/287), **declared in `shared/schema.ts`** per the deploy-push durability rule, and **NO BACKFILL**. **NULL = ONE UNIT, which is the item model's own historical shape** and the only reading that leaves every existing row saying exactly what it already said (§13) — never 0, and never a guessed count. Then `syncItemProjection` carries it BOTH ways (item → cart row, and the materializer cart row → item) through the ONE copy-down (LD 39, §18 rule 1), and D-16(a)'s refusal LIFTS with the `guest-cart-becomes-plan` suite extended to prove the round trip is faithful at a count above one. **It is a UNIT count, not a party count:** the party lives on `cart_items.party_size` (ruling 83) and would need its own, separate decision before a plan item could carry one — do not conflate them in one column. |
| ~~D-42~~ | ✅ **ANSWERED 2026-09-15 (option A), AND THE CODE LANDED THE SAME DAY** — decision-maker ruling, ledger `2026-09-15-d42-reconciliation-tallies`, **migration 301**. `reconciliation_runs` grows the ready-made rail's two per-pass tallies — **`checked_ready_made_purchases`** and **`ready_made_announce_hand_offs`** (the first is named for the `ReconciliationResult` field it records, not for the `scanned_*` siblings) — written on EVERY pass, completed, skipped and failed alike, by the EXISTING run-row writer `closeRun` (§17 rule 2; §18 rule 1 — one writer, no new insert path). Both are additive INTEGER, **NULLABLE, NO DEFAULT, NO CHECK** (the publish-trap posture) and **declared in `shared/schema.ts`** (deploy-push durability rule), with **no backfill** and no index. **The ruling amends this row's own sketch of option A on one point:** not `NOT NULL DEFAULT 0` but nullable, because **NULL = NOT TALLIED** is the only reading a pre-migration run can bear and a stamped `0` would claim a pass examined zero ready-made purchases — a fact nobody has (§13). A pass that looked and found none records a real `0`, a different fact, and the two are pinned apart. Both `/api/admin/reconciliation/*` SELECTs carry the columns and `/admin/reconciliation` says “ready-made rail: not tallied on this run” rather than printing 0. No detection predicate, exception kind, dedupe key or the hand-off itself was touched. New suite `server/__tests__/reconciliation-run-tallies.db.test.ts` (T1–T4, 7/7) in the existing `reconciliation-detection` job; that suite still 47/47. No CHECK added or changed, so `preflight-prod-constraints.cjs` needs no manifest entry. |
| ~~D-44~~ | ✅ **ANSWERED 2026-09-15 (the ratchet), AND THE CODE LANDED THE SAME DAY** — decision-maker ruling, ledger `2026-09-15-orphan-ratchet`; **no schema, no migration, no TypeScript**. Two clauses. **(1) `e2e/` STAYS OUT OF `TEST_ROOTS`** — the limit `2026-09-15-test-guard-prose-echo` left STATED rather than silently closed now STANDS by ruling; only the ruling and its date were added beside it, plus the note that an `e2e/` spec is invisible to the ratchet too. **(2) THE INVENTORY RATCHETS.** `scripts/test-orphan-baseline.txt` records the orphan set EXACTLY as the repaired guard computes it on `origin/main` @ `42e5213ef` — **233 paths** (203 `server/`, 30 `playwright/`) against **278/511 reachable** — and the default scan, which still prints the whole inventory, now **EXITS 1** on either direction of drift: a **NEW ORPHAN** not in the baseline, or a **STALE BASELINE ENTRY** that is now reachable or no longer exists. **THE FILE MAY ONLY SHRINK**, and trimming a line for a still-orphan suite fails at once as a NEW ORPHAN, so it cannot be edited into agreement with anything but the tree. **IT IS RECORDED DEBT, NOT AN ALLOWLIST** — its own header says so — and every listed suite is still owed repair-or-delete under `2026-09-14-test-files-wired-orphans`; **a green run means the debt did not GROW, never that the 233 are acceptable**. The pure comparison is proven by 5 new `--self-test` fixtures (7 → **12**), and the job is renamed `test-file-reachability (ratchet - new orphans fail)` and added to `.github/branch-protection.json` + CI_GATES Tier 1 — **which a human must apply by hand**, because `enforce-branch-protection.yml` has no `BRANCH_PROTECTION_PAT`. Original question: **the reachability inventory prints 233 orphans and exits 0 — what stops the 234th?** Nothing did. An advisory guard is where a defect survives longest because nothing ever goes red (`2026-09-15-test-guard-prose-echo` proved it over eight days), and making the scan blocking outright would fail `main` on 233 rows the moment it landed. | **Record the debt, fail on growth, and say what the record does not excuse.** The baseline is never an allowlist: it is the list of suites that already owe repair-or-delete, and it may only shrink. |
| D-45 | **Is the Ask-AI drawer a THREAD, or a stateless ask over the proposal log?** `plan_proposals.conversation_id` exists (migration 299, nullable, ON DELETE SET NULL) and `conversations.trip_id` exists (L15, migration 290), so a thread is possible — but the conversation rail is structurally human↔human (`buildConversationId` concatenates two `users.id`, LD 40), which is the same open question **LD 44** records for the booking copilot. *(from `docs/design/ASK_AI_DRAWER_BRIEF.md` §7; blocks the L16 create rail)* | **Stateless for L16.** The `question` column plus the proposal log IS the thread; `conversation_id` stays NULL and the drawer says so (§13). Binding to `conversations` is a later lane needing the same answer LD 44's seventh open question needs. **Whichever wins, the AI's words are attributed to the AI** — the false-attribution line LD 42 D4 drew for `expert_note`. |
| D-46 | **What throttles a free ask, and is CREATE idempotent?** Asking is free by ruling (D-21 = A) and the expensive half is the model call, not the row. The only user-keyed limiter is `checkMessageRateLimit` (`server/infrastructure/message-rate-limiter.ts:128`) and it REQUIRES a `recipientId`; an AI ask has no recipient, and passing a fabricated one would be an invented identity on an identity key. Nothing today makes a create idempotent. *(from the L16 brief §3.4/§7; blocks the create rail)* | **One more NAMED limit in that SAME fixed-window module** — `checkAiAskRateLimit`, keyed on `senderId + tripId`, sharing the existing store, cleanup and loopback bypass — never a second limiter implementation (§18 rule 1) and never a fake recipient. For idempotency, an **in-flight marker keyed on (tripId, session user)** that refuses a second ask while one runs and answers 409 with the in-flight proposal's id — no schema, no column, no UNIQUE index. **Stated limit:** the store is in-memory per process, so a multi-instance deployment throttles per instance — the same limit the messaging limiter already states for itself; closing it is a shared-store lane. |
| D-47 | **What model tier does a PAID AI task run on, and what three values does its `ai_cost_tracking` row carry?** `resolveAiDraftModel` (`server/services/ai-draft-model.ts:41`) is the FREE draft's cost knob and its own header forbids the optimizer reading it; nothing names a tier for a paid task. The `sourceType`/`userId` half is the question **LD 44** left open for the copilot, asked here first. `ai_cost_tracking` IS declared in `shared/schema.ts:8543`, so LD 44 (f)'s prerequisite is met. *(from the L16 brief §3.3/§7)* | **A sibling module `ai-task-model.ts` with its own env knob (`AI_TASK_MODEL`), defaulting to the optimizer's Sonnet-class tier** — the traveler pays for this one, so the free lane's cost knob must not reach it (LD 41 (c)). Cost row: **`sourceType: "ai_task"`** (the field's type is open and the table has no CHECK), **`userId` = the ASKER from the session** (spend attributed to whoever caused it, never to the plan's owner, which would misattribute an advisor's asks), **`requestId` = the proposal id** (the link back to the plan with no new column). A failed ask that burned tokens writes the row too. **The tier is a COST record and never a product claim** (LD 41 (c)) — no surface may read `model_tier`, in either direction. |
| D-48 | **May a §12 WRITE advisor ASK, PAY and APPLY on the traveler's plan — and whose card is charged?** The four landed proposal rails all gate on `authorizeTripLogistics(..., { requireWriteAccess: true })`, which admits `accepted`/`assigned` advisors; **LD 42 D16** says the slip's edit controls are the OWNER's; and `POST /api/trips/:tripId/proposals/:id/pay` builds the PaymentIntent from `getOrCreateCustomer(<session user>)`, so **an advisor who pays pays with their own card**. Separately the only coverage read, `GET /api/trips/:tripId/trip-pass` (`server/routes/trip-pass.routes.ts:47`), is owner-only and 403s an advisor, so an advisor-viewed drawer cannot honestly say whether the plan is covered. *(from the L16 brief §4.2/§7; blocks the drawer)* | **Ask and read for both; PAY for the OWNER only** — narrow the pay route in the same lane, because an earner funding a traveler's purchase is the rail **LD 44 D19** refuses by name, reached from the other direction. **APPLY stays at the write tier** so a Trip-Pass-covered apply by an accepted advisor still works (it moves no money). For coverage: **extend the EXISTING `GET /api/trips/:tripId/proposals` response with a server-resolved `aiTask: { coveredByTripPass, priceCents }`** using the same `coversAction` call and the same band resolver behind the gate that route already runs — **no second entitlement rail and no second fee read** (§18 rule 1). |
| D-49 | **Does applying a proposal on a FINALIZED plan advance the Trip Card's version?** `applyPlanProposal` (`server/services/proposal-charge.service.ts:296`) writes items and does **not** call `reFinalizeIfCurrentlyFinal` (`server/services/trip-finalize.service.ts:119`) — which the expert-suggestion accept path DOES call, precisely so an accepted change is not invisible behind a frozen snapshot (its four callers verified). **LD 45 (3)** puts the drawer post-final on the Trip Card, so this is reachable the day the drawer mounts there. *(from the L16 brief §1.2(g)/§4.1/§7)* | **Yes — one more caller of the existing helper**, best-effort and AFTER the apply commits (§15b: an ancillary effect may not break the operation that authorizes it), exactly as the suggestion path does it. **Until it is ruled the drawer does not mount post-final** — an omitted control is honest (§13); an apply that silently rewrites a frozen card is not. |
| D-50 | **What does the create rail send the model, and may a proposal name a live catalog listing?** `PlanProposalAddition.providerServiceId` exists (`shared/plan-proposals.ts:107`) and the apply writes it onto the item. **LD 41 (c)** says the FREE draft has no live catalog pricing and flags external fills as such; it says nothing about a PAID task. `loadOptimizerCatalog` (`server/services/optimizer-baseline.service.ts:262`) is the existing reader for exactly this. *(from the L16 brief §7; blocks the create rail)* | **Yes for a paid task, through `loadOptimizerCatalog` and no second catalog read** (§18 rule 1). The plan is sent as: the trip row's own fields, the ordered items **with the protected set marked as CONSTRAINTS** (the one existing predicate — `itineraryItemIsExpertWork` + `itineraryItemIsMoneyCommitted`, never a second test), the plan's events and stops, **and nothing else** — no other traveler's data, no other plan, no `users` row beyond the session's own (§14's read clause). `estimatedCost` is filled ONLY from a catalog row's real price or a source that stated one; otherwise omitted (§13). **Sub-question in the same row:** a staleness window for a catalog price carried into a proposal applied days later — **config, never a literal**, and the same answer LD 44's brief leaves open for the copilot's snapshot. |

## 2 · Verified defects, ready to fix

> **V-21..V-26 were found by the 2026-09-15 section-2 re-check** and filed as-of `46d383a25bdf84c9c4ceb659cea30fbca127ce9f` (`git rev-parse origin/main`) by the verify-and-file lane `task-punchlist-v21-v26` — each row was opened on that sha, quoted at `file:line`, and mapped to its governing rule. V-22 and V-25(a) name an owner rather than a fix; nothing was changed in this lane.

| # | Defect | Size |
|---|---|---|
| V-1 | ~~**A transport option is stamped `confirmed` the moment the Stripe Checkout session is created, before any payment succeeds.**~~ **ALREADY CLOSED before this row was re-read — by ledger `2026-09-08-transport-confirm-timing`, not by the confirm-honesty lane of 2026-09-12.** The stamp was removed and nothing replaced it: the option keeps the status it has (`available` — true while a checkout is in flight), no fifth value was invented for "awaiting payment", and the promotion belongs to the payment signal. Pinned by `confirm-honesty.test.ts` A1–A4, and **A1/A2 were REPAIRED rather than deleted** on 2026-09-12 when the self-report rail stopped using the storage helper their old call-site count keyed on — they now assert the invariant directly against the initiation handler's own body. | closed |
| ~~V-2~~ | ✅ **CLOSED — was already fixed on 2026-09-08 by ledger `2026-09-08-confirmation-code-is-the-server-s`; the row went stale because a lane closes only the rows it OWNS.** `BookingFlowModal.tsx:299` reads `statuses[b.id]?.confirmationCode ?? null`, and a booking whose code the server has not issued carries NONE (§13). **The `TRV`/`Math.random()` text at :291 is the ANNOTATION recording the removal** — grepping a defect's own vocabulary matches the comment that records its death, which is why this row survived two rankings. Original citation `:257` had drifted to `:299`. | — |
| V-3 | ~~**`ready_made_purchases` is absent from scheduled Stripe reconciliation.**~~ **CLOSED 2026-09-12 (ledger `2026-09-12-readymade-reconciliation-rail`).** The diagnosis stood exactly as written, and it was §17's own founding failure one table over: a ready-made PaymentIntent carries `metadata.type='ready_made_purchase'` and no `bookingIds`, its row is not a `service_bookings` row, so every cart-rail query matched zero rows and errored on nothing. `ready_made` is now a THIRD `ReconciliationRail` in the shape the existing two take — one scan function, the same append-only exception rows, the same run row — with five kinds: `rm_pi_succeeded_no_purchase`, `rm_purchase_paid_not_cloned` (this row's named case), `rm_amount_mismatch`, `rm_purchase_pi_not_succeeded`, `rm_refund_not_reversed`. **NO REPAIR, not even the cart rail's one narrow exception** — that one exists because `promotePaidCheckout` is a ratified recovery layer arriving late, and **this rail has no recovery layer at all**: only the buyer's own `/purchase/confirm` writes the row, and `handlePaymentSucceeded` keys on `bookingIds`, so the webhook no-ops on a ready-made payment. `fulfillReadyMadePurchase` is idempotent and is deliberately NOT called; **the missing recovery layer is now a named finding, not a gap the detector quietly fills** (it is the successor to this row and belongs to a decision, not a lane). §13 rulings that keep it honest: a 15-minute fulfilment grace (a liveness window, not a rate — §8), a buyer-deleted clone is not a failed delivery, the expected amount is the row's own `price_paid_cents` and never the listing's price today, and an unseen PaymentIntent is never judged. Found while building: the cart rail's `pi_succeeded_no_booking` would have indicted every store purchase, and now skips a ready-made intent exactly as it skips a legacy one. **No schema change, no migration, no new write on the purchase path.** Proven by `reconciliation-detection.db.test.ts` N23a–N23i — and that suite, which had NO CI job since ruling 40, now has one. **TWO LIMITS, STATED:** the admin exceptions endpoint's `?rail=` filter still admits only `cart\|legacy` (the rows list fine, they just do not rail-filter), and `reconciliation_runs` has no `scanned_ready_made_purchases` column, so the per-rail tally lives in the manual-run response and the log line. Both need `server/routes/admin.routes.ts`, which a sibling lane owned this day; they are one small follow-up. | closed |
| V-3b | ~~**The ready-made purchase rail has no recovery layer at all.**~~ **CLOSED 2026-09-12 (decision-maker ruling; ledger `2026-09-12-readymade-recovery-path`).** V-3's successor, exactly as that row named it. The `ready_made_purchases` row was written ONLY by the buyer's own browser calling `POST /api/ready-made/:id/purchase/confirm`, and `handlePaymentSucceeded` keyed on `metadata.bookingIds` — which a ready-made PaymentIntent never carries — so the webhook silently no-opped: a closed tab between capture and confirm meant money taken, no purchase row, no clone, no author earning, nothing in any log, while the cart rail had three recovery layers. Fixed in **§15c's shape one table over**: `fulfillReadyMadePurchase` is THE fulfilment and **took its second caller unchanged**; the webhook recognises `metadata.type='ready_made_purchase'` and drives it through the new shared `recordAndFulfilReadyMadePurchase`, which the confirm route now calls too (one author of the purchase INSERT, §18 rule 1). **No new write on the purchase path** (§17 rule 4) — the linkage is the metadata the purchase route already stamps, so **PaymentIntents that already exist are recoverable**. §15c's authorization clause is unweakened: `SERVER_VERIFIED_ACTORS={webhook}`, so only a signature-verified delivery resolves a buyer from metadata alone and a client-supplied PaymentIntent fulfils nothing. Idempotency stays structural (UNIQUE `stripe_payment_intent_id` + the atomic `paid→cloned` claim): one row, one clone, one author earning, one platform-revenue row. §13: an unresolvable PaymentIntent — deleted listing, missing buyer — creates NOTHING and is left for the detector. **§17 untouched: the JOB still only detects**; whether it may also fulfil is a separate decision nobody has made. Expect `rm_pi_succeeded_no_purchase` to fire far less; if it still fires, that is information, not noise. **Found and NOT fixed:** there is no purchase-confirmation email anywhere on this rail (so the fourth exactly-once fact is the revenue row), and a fulfil dying between the `cloned` claim and `createExpertEarning` still leaves a delivered purchase with no earning and no retry — pre-existing, not widened. **THAT SECOND FINDING IS NOW ITS OWN ROW AND IS CLOSED: V-3c** (ledger `2026-09-12-readymade-earning-retry`, migration 294). Proven by `reconciliation-detection.db.test.ts` **N24a–N24h** (detection 34/34). | closed |
| V-3c | ~~**A ready-made fulfilment interrupted after the `cloned` claim leaves a delivered purchase with no author earning and no retry.**~~ **CLOSED 2026-09-12 (decision-maker ruling; ledger `2026-09-12-readymade-earning-retry`; migration 294).** V-3b's own "found and NOT fixed" clause, promoted to its own row. `storage.createExpertEarning` was a plain INSERT reached only by the winner of the atomic `paid → cloned` claim — safe under CONCURRENCY, **unreachable after a CRASH**: the clone trip and its items commit, the purchase is `cloned`, the author is never credited, and every later fulfil returns at `status === 'cloned'`. No exception, no log line, no detector. **THE FIX IS TWO HALVES AND THE SECOND IS THE ACTUAL BUG.** (a) Migration 294 gives the credit migration-203's guard one `reference_type` over — a PARTIAL unique index, `expert_earnings (reference_id) WHERE reference_type = 'ready_made_purchase' AND amount >= 0`; **partial is load-bearing**, since five other rails write this table and several legitimately repeat a `reference_id` (N25c). (b) A unique index prevents a double credit, it does not create one that never happened — so an already-`cloned` purchase now **ENSURES** the money leg through the ONE shared `ensureReadyMadeAuthorCredit` (§15c's "money leg only", both rows uniqueness-guarded: 294 for the earning, migration 244's `paymentIntentId` index for the revenue). **The CLONE stays strictly once-only** — it is not guarded and a second one is a real second trip (N25b). **No amount moved** (§14/§8: same expression, same `fee_bands` take) and **every other `createExpertEarning` caller is unchanged** (`insertExpertEarningOnce` is a new sibling this rail alone uses). **§13:** a missing listing, a vanished author or no payable share writes NOTHING, reports the reason on `FulfillResult.authorCredit` and logs at ERROR — never thrown, because the buyer's clone is already committed and a throw would make a webhook retry forever; a refunded purchase runs no money leg at all, so the refund's reversal stands (N25d). **PUBLISH TRAP — OPERATOR STEP:** the index is declared in `shared/schema.ts` (publish-trap rule), and because the deploy push creates it BEFORE `runMigrations`, run this READ-ONLY check against production first — `SELECT reference_id, count(*) FROM expert_earnings WHERE reference_type = 'ready_made_purchase' AND amount >= 0 GROUP BY reference_id HAVING count(*) > 1;` — **a non-zero result must be resolved by hand before publishing**, or the deploy fails mid-push and offers the destructive "copy dev over production". `preflight-prod-constraints.cjs` needs no entry (its manifest is CHECKs; this is an index). **Checked and reported, not touched:** the `provider_earnings` twin does not have this gap — it is not written by the ready-made rail, and on the completion rail `mintCompletionEarningsForBooking` is already built to be re-callable after a crash. Proven by `reconciliation-detection.db.test.ts` **N25a–N25e**; N25a fails on the pre-fix code (detection 39/39). | closed |
| V-4 | ~~**The fee-band admin panel cannot edit the cap.**~~ **CLOSED 2026-09-12 (ledger `2026-09-12-fee-band-admin-gaps`).** `max_amount` — the DOLLAR ceiling the resolver applies, never `max_rate`, which bounds the RATE — is selected by `GET /api/admin/fee-bands` and accepted by the PATCH's allowlist. Present-but-invalid is REFUSED, not silently ignored (a string cap, a negative cap, NaN, Infinity), matching the posture the rate fields already had; blank means UNCAPPED, which is a different setting from a cap of 0 and is stored as such. And a band whose reader REQUIRES a cap — `traveler_service_fee`, since `GET /api/pricing` throws without it and the deployment gate fails a band missing it — cannot have its cap CLEARED: a 409 naming the reader, which is the same fact the gate enforces, enforced one step earlier at the only surface that can produce it. | closed |
| V-5 | ~~**Nothing stops an operator deactivating a band a resolver requires.**~~ **CLOSED 2026-09-12 (same ledger row), and the fix is NOT a blanket refusal.** §8 records that some readers have a deliberate SAFE failure mode (`resolveCoordinationFee` keeps charging its ratified floor when the row is gone) while others are fail-loud; that split decides the answer. A band with **no** fallback is refused (409, audit-logged under its own `fee_band_deactivation_refused` action so the log distinguishes an attempt from an edit, and the switch is disabled on the page with the reason permanently visible); a band **with** one is allowed, and the operator is told in advance which resolver takes over and **at what value**. **THE SPLIT IS READ FROM THE ONE MANIFEST** (`RESOLVER_FEE_BAND_REQUIREMENTS`), never restated: it is DECLARED rather than computed because three band keys arrive from database columns, and the derivable direction — every fail-loud read with a static key must be declared required — is the CI pin `fee-band-admin-guards` D5, derived from the file SET with its blind spot asserted. **§13:** an undeclared band is allowed off and NOTHING is claimed about the consequence. **Recorded, not fixed:** four seeded bands (`concierge:booking_pct`, `concierge:booking_cap_cents`, `plans:plus_task_allowance`, `ready_made:platform_band`) have **no reader in code at all** — the panel now says so instead of implying they are safe. | closed |
| V-7 | ~~**Any signed-in account can stamp any transport option `confirmed`, with any reference.**~~ **CLOSED 2026-09-12 (ledger `2026-09-12-transport-status-self-report-gate`).** The diagnosis stood exactly as written. `PATCH /api/transport-booking-options/:optionId/status` now: resolves the option, derives its owning scope (`variant_id`, or the leg's variant → comparison) and authorizes it through the SAME `authorizeTransportScope` the hub read uses, with `requireWriteAccess` since a self-report is a mutation (LD 12); parses a **pick-based `.strict()` allowlist** whose value set is `available \| booked \| cancelled` — **`confirmed` is not in it**, because that value is the payment's word and a traveller may not type themselves into a machine state (LD 44 (e)); answers **ONE 404** for an absent row, an unownable row and a refused caller alike, so the rail cannot be used to probe which options exist (LD 40's `conversations/start` posture); **refuses a PLATFORM option outright**, since its status follows the money and a hand-written `booked` would reopen V-1 through a second door; and writes with a from-state guard IN the statement so a self-report can never overwrite a payment-written `confirmed`. The reply reports the PERSISTED row, never an echo. **SUPERSEDED THE SAME DAY — THE RAIL IS GONE (ledger `2026-09-12-delete-dead-transport-status`).** The gating lane's own report established the endpoint had **ZERO callers** anywhere in `client/` or `e2e/`, which makes it **§18c's case exactly — no consumer plus a state-bearing effect ⇒ DELETE, don't gate**: a gated dead rail is still a rail, and §18c exists because a state-bearing writer nobody calls is an unaudited way to mutate state. The decision-maker ruled deletion on 2026-09-12. Both halves are gone — the route with its pick-based schema, its `TRAVELER_SELF_REPORT_STATUSES` value set and its `resolveOptionScope` helper, and the orphaned, ungated `storage.updateTransportBookingOptionStatus` that pin C9 had only been fencing. **The hole is closed because the door is closed, not because it is locked.** Pins C1–C5 described the gate's shape and went with their subject (a pin that passes because its subject no longer exists is worse than no pin); the **NEW C9** asserts neither half is re-added, derived from the file SET with comments stripped, and **C10** keeps `authorizeTransportScope`'s `requireWriteAccess` passthrough — deliberately KEPT as a general LD 12 capability — exercised. **§13, recorded not worked around:** with the rail gone nothing writes `booked`, `cancelled` or `confirmation_ref` on `transport_booking_options`; the payment signal writes only `confirmed`, and the column is otherwise born `available`. `TransportBookingCard`'s cancelled branch and its bare-`booked` badge, and `traveler-profile.service.ts`'s `booked` arm, are therefore unreachable — named here rather than deleted, since which of those states the platform should be able to reach is a decision, not a cleanup. | closed |
| V-8 | ~~**A late paid signal promotes a cancelled transport option.**~~ **CLOSED 2026-09-12 (same ledger row).** Both writes in `handleStripePaymentSuccess` (`server/services/stripe.service.ts` — **not** `stripe-payment.service.ts`, which only routes the webhook to it) are now single atomic conditionals carrying their own from-state list: `service_bookings` promotes only from `pending` (`payment_pending` is deliberately absent — §18b keeps the claim machine its sole author) and `transport_booking_options` only from NULL/`available`/`booked`. A refused promotion is **logged, never swallowed** (§17: detect, do not repair), and the traveler-service-fee ledger write now sits inside the confirmed gate, so a fee event is never recorded against a booking this handler refused to confirm. Pinned by C6–C8. | closed |
| V-6 | ~~**The AI-task price is invisible in that panel.**~~ **CLOSED 2026-09-12 (same ledger row), and it was three rate_types, not one.** The page kept two hand-written filters (`rate_type === "percent"` and `=== "flat"`) while migration 258 widened the DB CHECK to five, so `flat_cents`, `count` and `rule` bands all rendered nowhere — `concierge:ai_task` among them. Groups are now built from `shared/fee-band-display.ts`, **pinned to migration 258's own CHECK** so a sixth type turns a CI pin red rather than quietly disappearing, and each group states what its stored number MEANS (cents are not dollars; a count is not money). A rate_type this build has no label for still RENDERS, in its own honestly-labelled group — a band the page cannot categorise must not vanish from it, which is the whole defect. | closed |
| V-9 | ~~**Every live listing's booking mode is a fabricated `false`.**~~ **CLOSED 2026-09-11 by lane OC-A0b (`2026-09-11-booking-mode-provenance`), and the fix is NOT the one this row proposed.** The diagnosis stands: `server/services/buy-action-payload.ts` guards on `row.bookingMode || row.ownerUserId`, `ownerUserId` is NOT NULL, so the honest-absence branch is unreachable and an owner with no `service_provider_forms` row resolves to `request` as if they had chosen it — **64 of 67 production listings [verified]**. The proposed remedy ("test whether the OWNER FLAG is known") was **wrong**: it would make the mode `undefined` for those 64 rows, and `resolveBuyAction` gives a modeless row no booking verb — i.e. it would strip the buy button from the entire live catalogue. `request` is the safe default (the traveler asks, the seller accepts, nothing is charged without an acceptance) and is deliberately kept. What was actually missing was the PROVENANCE: `resolveBookingModeWithProvenance` (`shared/schema.ts`) now says whether the answer was `listing_declared`, `account_declared` or `platform_default`, the payload stops coercing a NULL flag to `false`, and the misleading comment is corrected. | closed |
| V-10 | ~~**`booking_details` is client-supplied on `POST /api/bookings`, and the traveler-charge ERA DISCRIMINATOR lives inside it.**~~ **CLOSED 2026-09-12 by lane `2026-09-12-booking-birth-holes`.** The diagnosis below stands verbatim. Fix = the §19 two-layer strip over a NAMED family of server-authored keys (`shared/booking-details-admission.ts`): layer 1 on `createBookingRequestSchema` (the one place a request body becomes these two jsonb fields), layer 2 in `storage.createServiceBookingAtomic` (this rail's writer, and its only caller). **Deliberately NOT in `createServiceBooking`** — that is the CHECKOUT CLAIM's writer and the claim COMPOSES `travelerCharge` server-side on every real purchase, so a blanket strip there would erase a genuine money fact and silently re-read the whole platform as pre-A3; B8 is the assertion that keeps it that way. `TRAVELER_CHARGE_SNAPSHOT_KEY` moved to `shared/` and is re-exported, so the strip and the composition name one string. Proven by B7/B8 beside B1–B6. **TWO FOLLOW-UPS FILED, NOT BUILT (§19b/§13):** (a) **rows already on disk** — a planted `travelerCharge` is indistinguishable from a composed one by inspection, so no detector can call it forged; a CANDIDATE predicate exists and is cheap (`booking_details ? 'travelerCharge' AND idempotency_key IS NULL` — the claim site writes both in the same INSERT, a born row can write neither), and a new `reconciliation_exceptions` kind belongs to the drift job's own lane with its own negative fixtures (§17), so it is recorded here rather than added under cover of this one; (b) the strip is a **DENYLIST inside a free-form jsonb** — an allowlist needs a ratified `booking_details` shape, which nobody has decided, and until then the key list is extended by hand and pinned by B7. | `createBookingRequestSchema` `.pick()`s `bookingDetails` (`shared/schema.ts:2747`) **[verified]**, so a crafted body can plant `booking_details.travelerCharge.conciergeFee` at BIRTH. `travelerChargeBasis` (`server/services/traveler-charge.ts`) reads that key's PRESENCE — nothing else — to decide whether a row was priced under the A3 composition or the pre-A3 one, and `travelerChargeForRow` then computes a different amount on each branch. Three live readers act on it **[verified]**: the cancellation quote (`cancellation-policy.service.ts:172`), the REFUND CEILING clamp (`stripe-payment.service.ts:954`) and the checkout re-drive's charged-amount reconstruction (`checkout-claim.service.ts:318`). Flipping a row to `a3_snapshot` moves its refund ceiling and changes what a re-drive charges. Pre-existing §19-class hole (a privileged fact reachable through a client-writable jsonb), found by OC-B1 and **reported, not fixed** — the pick is a genuine allowlist, so the fix is a key-level strip inside `bookingDetails`, and a strip CHANGES BEHAVIOUR for any row that already carries a planted key, so it wants its own negative proof beside `booking-birth-provenance.db.test.ts` B1–B6 rather than riding another lane. | small-to-medium, and it is a money-path hole |
| V-11 | ~~**A listing with no price books at `0.00`.**~~ **CLOSED 2026-09-12 by lane `2026-09-12-booking-birth-holes`.** The diagnosis below stands verbatim. The rule was NOT re-decided: `resolveBuyAction` stays the sole author (ruling 9) and its row 11 already says a priceless listing can only ever be REQUESTED, so the handler now consults the ONE translation of the price column into that resolver's own `hasPrice` fact — `hasPublishedPrice`, exported from `buy-action-payload.ts` — and refuses with its `no_published_price` vocabulary before any amount is derived. A REFUSAL and not a row-with-no-amount because `service_bookings.total_amount` is NOT NULL: "not quoted yet" is not a state this table can hold, and committing anyway makes the platform state a price nobody set. A PRICED listing is byte-identical (it passes the gate, and `Number(service.price) || 0` is `Number(service.price)` for every row that does). Proven by B9; `archetype-fixtures` N1, which committed P5 at `0.00` to record the break, is REPAIRED to assert the refusal. **STILL BROKEN, recorded not fixed:** the quote rail a `server_quote` listing needs (`chargeMode: after_quote`) does not exist, so P5 is publishable and unsellable — an honest refusal instead of a silent $0 sale, and still a hole in the product (D-8 is its decision). | `POST /api/bookings` sets `totalAmount = Number(service.price) || 0` (`server/routes.ts:6571`). The §14 comment above it is satisfied — the amount comes from the catalog, not the body — but **NULL means "no price stated" and `|| 0` turns it into "free"** (§13). `resolveBuyAction` row 11 already rules that a priceless listing can only ever be REQUESTED, never charged; this endpoint does not consult it. Reaches every custom-quote listing, which by design carries no price — so §9.2's "do not send a quote through generic checkout" has **no enforcement behind it**. **[verified]** | small, and it is a money-path hole |
| V-12 | ~~**Four of the five expert archetypes publish while unclassifiable, and nobody is told.**~~ **CLOSED 2026-09-12 (ledger `2026-09-12-listing-expert-offering-key`, migration 292)** — on the condition this row set for itself, and on both halves. RESOLUTION: `provider_services.expert_offering_type_key` exists, `loadOfferingListingInput` reads it, and **E2/E3/E4/E6 flip to working end to end in `archetype-fixtures`**. AUTHORING (added to the same lane by the decision-maker, so the harm closes rather than half-closes): the shared `ServiceForm`'s picker is now "What you sell", rendered for every owner — required for experts exactly as before, optional and clearable otherwise, never pre-selected (§13) — reading the catalog live and restating no names. **N2 STANDS as the cost of an UNSET key:** there is no backfill, so a pre-292 expert listing still resolves through `custom_other` to P1 until its owner names an offering; that is asserted, not hidden. Original text: **Four of the five expert archetypes publish while unclassifiable, and nobody is told.** `provider_services` carries **no offering-type key** — migration 107's two FKs are on the APPLICATION forms (`local_expert_forms` → `expert_offering_types`, `service_provider_forms` → `service_offering_types`), not on the listing **[verified, `shared/schema.ts:619,673`]**. So E2/E3/E4/E6 resolve `catalog_keys_unrecognised`, which is deliberately warn-not-block, and the listing goes live, bookable, with its `offering_contract_snapshot` recording a refusal. **Worse than a refusal at the gate.** And the catch-all is no escape: an E6 authored under `custom_other` — the only category an expert can honestly pick — resolves to **P1**, a provider's scheduled place service, with P1's required context and completion rule (pinned by `archetype-fixtures` N2). A wrong archetype that looks like an answer. **D-13 is now ANSWERED** (ledger `2026-09-12-listing-names-its-expert-offering`) and the FK lane is queued; this row closes when E2/E3/E4/E6 flip to working in the `archetype-fixtures` suite. **[verified]** | medium, unblocked |
| V-13 | ~~**The main listing page authors its own buy button and draws the same three CTAs for every archetype.**~~ **CLOSED 2026-09-13 (ledger `2026-09-13-service-detail-buy-action`).** The diagnosis below stands verbatim, including its own estimate: the answer was already on the payload, so the lane RENDERED it and DELETED the local authoring — **no server change**. Each control appears only when the shipped descriptor names its kind and wears the descriptor's label (`serviceDetailBuyRender`, `client/src/lib/service-buy-action.ts`); the refusal reason is printed as a SENTENCE keyed on the resolver's own `BuyRefusalReason` union (§13 — a disabled Book the server would refuse is still a promise); an absent descriptor offers nothing and says so rather than falling back to the three fixed buttons. `request_to_book` opens the LD 40 conversation rail (`{ serviceId }`) and writes nothing, because row 11 lands on `booking_request` — there is no platform quote rail on `main` and none was invented. Contact is NOT a buy button (LD 40) and was not moved; vacation mode and a room's stay availability stay the page's own disabling, because the descriptor is built with `isLive: true` and cannot say "away". The `content.routes.ts` comment this row calls FALSE is now true and says precisely what the page does with `landing` (obeys it, never prints it). The three OTHER `ld23-buy-action-gap` authors keep their markers and are untouched. Original diagnosis: `client/src/pages/service-detail.tsx` renders "Book on Traveloure" / "Add to Cart" / "Contact Provider" unconditionally (lines ~1686, ~1725, ~1767) and **never references `resolveBuyAction`** — grep it. Ruling 9 made that resolver the SOLE author of the buy button and the landing rule, and the page bypasses it. Worse than the three known card gaps, because the answer is **already on the payload it reads**: `GET /api/services/:id` ships `buyAction` (`server/routes/content.routes.ts:2412,2641`) and its own comment there claims "the page renders `buyAction` and never re-derives" — which is FALSE. Two concrete wrong labels, observed in a browser across P1/P3/P5/P6/E1 against the seeded archetype fixtures **[verified]**: (a) the custom-quote `archetype-fixture-p5`, whose `price` is NULL, is offered **"Book on Traveloure" and "Add to Cart"** where row 11 rules a priceless listing can only ever be REQUESTED; (b) a P1 with no published availability is offered **"Book"** where row 11 rules instant-without-availability is a REQUEST. It carries **none** of the `ld23-buy-action-gap` markers the three known authors carry (`OfferingCard.tsx:65`, `catalog-preview-presentation.ts:70`, `storefront.tsx:271`), so until now it was an **unrecorded** gap on the most important surface. **Ledger `2026-09-13-cart-priceless-gap` closed the SERVER half of (a)** — the cart rail now refuses a priceless listing with `no_published_price`, so that button no longer produces a $0 cart line; it now produces an error the traveler was invited to trigger, which is honest and is still the wrong button. Conforming this page to ruling 9 is its own lane and was deliberately NOT done there. **A `ld23-buy-action-gap` marker beside each CTA is the MINIMUM if the fix is deferred** — the three known authors are recorded and this one is not, which is the only reason it survived five sweeps. **[verified]** | medium — the page already receives `buyAction`, so the work is rendering the resolver's answer (primary/secondary/refusal) instead of three fixed buttons, plus the refusal sentence §13 requires |
| V-14 | ~~**A ready-made clone's explicit routing-status override is INERT, so a buyer's plan inherits the AUTHOR's item routing state.**~~ **CLOSED 2026-09-13 (ledger `2026-09-13-clone-carries-content-not-state`).** The diagnosis below stands verbatim. The fix is not a corrected key — that would leave the DENYLIST that caused V-15 — but the §19 ALLOWLIST: `server/services/itinerary-item-clone.ts` names the 40 CONTENT columns a clone carries, `buildClonedItineraryItem` copies nothing else, and `routingStatus` is set explicitly in the key drizzle reads. The `as any` is gone, so the next misspelled key is a compile error. Proven by C1 (`server/__tests__/ready-made-clone-fields.db.test.ts`), which reads the buyer's row back out of Postgres in the DATABASE's column names and FAILS on the pre-fix service. **Recorded, because it is the reason this survived:** the unit suite that existed to guard exactly this rule (`server/services/__tests__/ready-made-clone-routing.test.ts`) MIRRORED the production spread into its own fixture and asserted on its own snake_case key — green for the defect's whole life — and it ran in **no workflow at all**. It now calls the one implementation and is wired into `build.yml`. Original diagnosis: `server/services/ready-made-purchase.service.ts:274-280` writes the key **`routing_status`** (snake_case) while the drizzle column key is **`routingStatus`** (`shared/schema.ts:5314`). Drizzle reads values by COLUMN KEY, so the override is silently dropped — no error, because the `as any` cast removes the type check that would have caught it — and `...rest` carries the source row's own `routingStatus` through. **Why it matters:** under LD 39 the cart IS this table's `ready_for_checkout` projection, so an author item left in `ready_for_checkout` lands in the BUYER's cart on purchase. Invisible so far only because the column's default is `in_planning`. **[verified 2026-09-13]** | small |
| V-15 | ~~**The same clone spread copies the AUTHOR's booking linkage onto the buyer's items.**~~ **CLOSED 2026-09-13 (same ledger row, same five lines).** The diagnosis below stands verbatim, and the sweep it invited found more of the same class than it named: besides the four, `...rest` also carried `bookingReference`, `slotId`, `vendorContractId` (booking state), `userExperienceId` — an event on the AUTHOR's trip, which LD 29's pairing rule makes both live write rails REFUSE — `participantIds`, `conflictsWith`, `backupPlanId` (ids of rows in the author's own trip), `scheduledDate`/`checkIn`/`checkOut` (the author's calendar), `status` (whose value set includes `booked`) and `privateNotes` (the SELLER's organizer-only notes, the item-level twin of the `trips.expertNotes` this same fulfilment already declines to carry). All stay behind; `estimatedCost` and `expertNote` travel, because a published estimate and a 'from your expert' note are the CONTENT the buyer paid for. C2/C3/C4/C6 are the proofs and C2/C3/C4 fail on the pre-fix service. Original diagnosis: Same map at `ready-made-purchase.service.ts:274-280` strips only `id`, `tripId`, `createdAt`, `updatedAt` — so `...rest` carries `bookingId` (`shared/schema.ts:5190`, FK → `service_bookings`), `bookingStatus` (`:5185`), `confirmationNumber` (`:5191`) and `actualCost` (`:5195`) from the author's row onto a **different user's** plan item. A buyer can hold a plan item pointing at the author's booking and showing the author's confirmation number. Adjacent to R-4 but a DIFFERENT claim — R-4 is stale dates, this is copied cross-user booking state. Same five lines as V-14, so one fix. **[verified 2026-09-13]** | small, and it crosses a user boundary |
| ~~V-16~~ | ✅ **CLOSED 2026-09-13 — VACUOUS, measured against PRODUCTION, not assumed.** Every count is **0**: `clone_trips` **0**, `clone_items` **0**, and therefore zero cross-user booking links, zero inherited `ready_for_checkout` rows, zero seller `private_notes`, zero copied confirmations, actual costs, event links or `booked` statuses on a buyer's plan. The unscoped probe is **0** too — `xuser_booking_link_all` finds no item anywhere on the table whose `booking_id` resolves to a booking held by someone other than the trip owner — so the pattern has no other source either. **NO ready-made purchase has ever been cloned in production**, which is why the defect never fired: `2026-09-13-clone-carries-content-not-state` fixed it before the rail carried a real buyer. **No migration was written and none is owed** — a repair would have edited travelers' plans, and there are none to edit. *Point-in-time, 2026-09-13: it closes because the population is empty, not because a repair ran.* | — |
| ~~V-17~~ | ✅ **CLOSED 2026-09-13 — DELETED, not gated (§18c).** `storage.bulkInsertItineraryItems` had **zero callers** anywhere in `server/`, `client/`, `shared/`, `e2e/`, `playwright/` or `scripts/` — only its own `IStorage` declaration and one guard pattern. It spread a caller-supplied `any` straight into `db.insert(itineraryItems)`, so it was a second, unaudited way to write plan items beside the named-field sites, which is exactly what §18c refuses. Method and interface line both removed. **`scripts/check-linkage-preservation.cjs` KEEPS its pattern, annotated as a FENCE** — it matches nothing today and that is the point: re-adding the method puts its call sites back under the linkage guard immediately rather than silently. Guard still green (764 files, 39 write sites). | — |
| ~~V-18~~ | ~~**`.github/branch-protection.json` requires two status contexts no job ever reports, so `main` blocks every PR while all its checks are green.**~~ ✅ **CLOSED 2026-09-14 (ledger `2026-09-14-required-check-names`).** Filed and closed in the same lane — the row did not exist on `main` when the lane opened, and is written here so the register carries the defect, not only its fix. The JSON required `hardcoded-links-check (static analysis)` and `navbar-links-smoke (Playwright DOM gate)`; both jobs in `navbar-links-gate.yml` are named without the suffix, and each carries a comment saying the name must equal the required context and take none. Verified against PR #892's 122 check runs: the unsuffixed names report, the suffixed ones do not. The JSON and the `CI_GATES.md` Tier 1 table were corrected; no job was renamed and no allowlist added. The other seven required contexts were checked one by one against every workflow `name:` — all seven match. | closed |
| ~~V-19~~ | ~~**`/services/:id` labels a `per_person` listing "per service", and four pages each derive that label for themselves.**~~ ✅ **CLOSED 2026-09-14 (ledger `2026-09-14-price-unit-one-derivation`).** Filed and closed in the same commit — the row did not exist on `main` when the lane opened, and is written here so the register carries the defect, not only its fix (V-18's precedent). Diagnosis, from production QA 2026-09-13 (commit `a1dfc7f`, Napa Valley Wine Experience, `priceType: "per_person"`, $195): `service-detail.tsx`'s `priceLabel`/`priceSubLabel` branched on `pricingUnit === "per_night"`, then `hourly`/`package_tiers`/`per_event`/`variable`, and caught everything else at `"per service"` — there was no `per_person` branch on either. The comment above the chain already recorded the SAME fall-through being found wrong once for `per_night` and patched one branch at a time, which is the §18 rule 1 shape: `catalog-preview-presentation.ts`, `storefront.tsx` and `provider/distribute.tsx` each carried their own third, fourth and second answer to the same question, and only the listing page's had drifted. Fix = ONE derivation (`client/src/lib/price-unit.ts`), read by all four; every pre-existing label is byte-identical (P6-P9 pin them), `null` = not derivable and is never printed (§13), and no amount, server route or `shared/buy-action.ts` was touched. **Found and NOT fixed, recorded here rather than silently:** `client/src/pages/discover.tsx:494` renders a HARDCODED "per service" with no derivation at all — a fifth instance, outside this lane's named scope. | closed |
| ~~V-20~~ | ~~**`/services/:id` asserts "Payment is processed securely through Traveloure" directly beneath its own note that nothing is charged.**~~ ✅ **CLOSED 2026-09-14 (ledger `2026-09-14-direct-booking-panel-conditioned`).** Filed and closed in the same commit — the row did not exist on `main` when the lane opened, and is written here so the register carries the defect, not only its fix (V-18/V-19 precedent). Diagnosis, from production QA 2026-09-13: `text-buy-request-note` says "A request opens a message with the provider about dates and price. Nothing is booked and nothing is charged here" and `section-direct-booking`, ~40 lines below it, says "Direct Booking — You're booking directly with the provider. Payment is processed securely through Traveloure." The second panel is transcribed mock copy (`docs/design/marketplace-experts-earn-grammar-mock.html:565`) that predates the V-13 resolved-buy-action work, renders UNCONDITIONALLY, and never reads `buyAction`; V-13's ledger row scoped itself to the buy CONTROLS and did not name it. All 67 production listings take the request branch, so the claim was false on every one. Fix = the claim follows the descriptor's own landing store. | closed |
| ~~V-21~~ | ~~**The Terms of Service — the document every account must accept — sells a RETIRED credits system and publishes four fee rates as literals.**~~ ✅ **CLOSED 2026-09-15 (decision-maker ruling = option A; ledger `2026-09-15-v21-terms-copy`).** The diagnosis below stands verbatim, both halves. **(a)** §7.2 "Platform Credit System" and the 16.3 forfeiture clause are DELETED; 7.3/7.4/7.5 renumbered 7.2/7.3/7.4 with no "reserved" hole. **(b)** All four published rates are replaced by sentences that name no number and point at Traveloure's published fee schedule in effect at the time of the booking; because all four are about EARNER earnings, each says the amount is shown in the earner's console on each earning and payout record — and deliberately NOT that a platform fee is shown to the traveler before they pay, which ledger `2026-09-08-cart-fee-line` makes false (the commission is a deduction from the payout, never a line on the buyer's summary). No tier scheme, no "always", no "lowest". **The gate's stated negative space was RECORDED, not repaired (§18d):** `phase2-fee-gate.sh`'s three predicates cannot see a rate in prose and are untouched — widening a fee grep to read prose would flag every refund percentage in the repo. The layer is a committed pin, `client/src/lib/__tests__/terms-copy-honesty.test.ts` (T0-T7, 8/8, CI-wired by the whole-directory `unit-suite-client-lib` job), which pins no percentage RANGE and no commission/share stated as a percentage — NOT a blanket "no `\d+%`", because Stripe's `2.9% + $0.30` pass-through rate and the cancellation policy's `50%`/`100%` refund percentages are real content this ruling forbids touching, and T7 asserts all three survive. ORIGINAL DIAGNOSIS: [verified 2026-09-15 at `46d383a2`] `client/src/pages/terms.tsx` is live at `/terms` (`client/src/App.tsx:569`) and is the gate every signed-in user passes: `App.tsx:247-248` redirects a user with no `termsAcceptedAt` to `/accept-terms`. **(a) The retired product.** §7.2 "Platform Credit System" (`terms.tsx:293-300`) promises "Users purchase platform credits in advance", "Credits are used to pay for platform fees and Expert consultation services" and "Unused credits do not expire but may be subject to inactivity fees after 24 months"; `terms.tsx:516` adds "unused platform credits may be forfeited". The system is RETIRED (**LD 43(b)**): all four wallet/credit endpoints answer **410 Gone** (`server/routes/payments.routes.ts:251,255,261,265`, whose own docblock at `:241-247` records that `deductCredits` has no callers and `POST /api/wallet/add-credits` was a free-credits hole). The Terms state a contract the platform cannot perform in either direction — a §13 claim on the one page whose entire job is to be true. **(b) The rate literals.** `terms.tsx:304` "Experts receive 75-85% of consultation fees; Traveloure retains 15-25% as platform fee. Exact split determined by Expert tier, performance, and volume"; `:307` "Standard commission of 4-12% of booking value"; `:252` "Commission Rate: 15-20% of service fees"; `:208` "Referral Commission: 5-15% of booking value". §8 forbids a rate literal outside `fee_bands`/config, and these are the same numbers the resolver owns — the seeded band is ONE 75/25 split (`server/services/commission.ts:81`, annotated `fee-literal-ok` precisely because it is a documented last-resort default mirroring the `expert_standard` seed), not a published tier-based range. An admin band edit moves the charge and can never move this page. **Why the gate did not catch it, stated because it is the reusable half (§18d):** `client` IS in `scripts/phase2-fee-gate.sh`'s `ROOTS`, so this is **not** a root gap — Pass A/C match a FIXED value list (`5.99\|9.99\|…`, `599\|1999\|…`) these numbers are not on, and Pass B needs the number on the SAME LINE as a fee-ish identifier followed by `:` or `=`, which prose never is. All three predicates return zero hits on this file, re-run at this sha. Green-within-stated-bounds, exactly as that clause says to read it. Fix = delete §7.2 and the `:516` clause, and replace each published rate with a sentence that names no number and points at the platform's own fee schedule. | ✅ DONE — the decision-maker supplied the wording call (option A); ledger `2026-09-15-v21-terms-copy` |
| V-22 | **`POST /api/expert-booking-requests` births a real `service_bookings` row at `totalAmount: "0"` for a priceless listing, with a commission split computed off zero — the one add rail its four siblings' refusal never reached.** [verified 2026-09-15 at `46d383a2`] `server/routes.ts:1817` — `const totalAmount = Number(service.price ?? 0);` — with no `hasPublishedPrice` gate anywhere in the handler (`routes.ts:1788-1960`), and `:1893-1908` commits it: `storage.createServiceBooking({ … status: "pending", totalAmount: String(totalAmount), platformFee: platformFeeAmt, providerEarnings: providerEarningsAmt })`, where both split amounts are `totalAmount × rate` off a zero base (`:1885-1886`). The four siblings all refuse with the shared `PRICELESS_LISTING_REFUSAL`: `POST /api/bookings` (`routes.ts:6777`), `POST /api/cart/items` (`:6608`), `POST /api/cart` (`:8622`) and `POST /api/checkout` (`payments.routes.ts:1011`). `routes.ts:6768-6770` states the governing reason in the register's own voice — `service_bookings.total_amount` is NOT NULL, so "not quoted yet" is not a state this table can hold, and committing the row anyway makes the platform state a price nobody set (§13/§14). **OWNED BY D-30** (the D-28..D-31 custom-quote lane, ruled A on 2026-09-15): the request creates **no booking row** — a `service_quotes` row in `requested` state plus the LD 40 conversation — and D-30's own text names this hole as its own ("this row also owns the $0 hole… leaving it is the one thing the brief says must not happen"). Filed here so the register carries the defect and not only its owner; **no second fix is proposed** (a refusal bolted on beside D-30's quote rail would be the §18 rule 1 drift that lane exists to avoid). | owned by D-30 — do not fix in isolation |
| ~~V-23~~ | ~~**`POST /api/bookings/:id/dispute` flips a booking to `disputed` with NO from-state list, so the transition is not the guard (§18b).** [verified 2026-09-15 at `46d383a2`] `server/routes/bookings.ts:801` — `await storage.updateServiceBookingStatus(bookingId, 'disputed', reason);` — is a THREE-argument call, and the FOURTH parameter is the atomic conditional: with `expectedFromStatuses` absent the guard falls back to `eq(serviceBookings.id, id)` (`server/storage.ts:3255-3257`), an unconditional `UPDATE … WHERE id = ?`. The route mounts at `app.use("/api/bookings", bookingsRoutes)` (`server/routes.ts:1052`). Two consequences. **(a) Any status is disputable, including a provisional claim.** `status='payment_pending' AND stripe_payment_intent_id IS NULL` is an unauthorized claim by construction (§15b) whose sole author is `checkout-claim.service.ts`; flipping it to `disputed` is the SD-1 shape one rail over — `voidClaim` and `promotePaidCheckout` both key on `payment_pending`, so both then match zero rows and the claimed `vendor_availability_slots.booked_count` is stranded with no code path in the repo to return it. The `OWNER_BOOKING_TRANSITIONS` docblock (`routes.ts:6838-6847`) names that exact failure, which is why `disputed` needs the same treatment `confirmed`/`cancelled` already got there. `cancelled`, `refunded` and an already-`disputed` row are equally reachable. **(b) The window cutoff is a read-then-write.** `bookings.ts:772-786` SELECTs `completedAt`, computes the `holdWindowDays('service_booking')` deadline and 409s — then writes at `:791-801`; nothing pins the row in between, so a dispute and a concurrent completion, refund or cancel can both pass their own checks. Sibling precedent in the SAME file: the completion rail 63 lines up already passes its list (`bookings.ts:738`, `['confirmed']`). Named as **F1** in `docs/design/EXPERT_ACCEPTANCE_BRIEF.md:610-614` and verified here. Fix = the fourth argument plus a named from-state constant beside `OWNER_BOOKING_TRANSITIONS`, and checking the return value the call currently discards (an `undefined` is a lost race, not a success).~~ ✅ **CLOSED 2026-09-15 (ledger `2026-09-15-v23-v25-from-state-guards`).** Fixed as filed: `DISPUTABLE_FROM_STATUSES` (`confirmed`/`deposit_paid`/`completed`) is the writer's 4th argument, declared beside `OWNER_BOOKING_TRANSITIONS` in the new one home `server/utils/booking-from-states.ts`, and an `undefined` return is answered **409**, never `success: true`. The window cutoff stays the error message. Pinned by `server/__tests__/from-state-guards.db.test.ts` F1/F1b/F1c/F2 (CI job `from-state-guards`). | closed |
| ~~V-24~~ | ~~**`POST /api/admin/disputes/:bookingId/reject` flips ANY booking to `completed` with no from-state list — and that flip MINTS earnings and re-stamps `completed_at`.** [verified 2026-09-15 at `46d383a2`] `server/routes/admin.routes.ts:1364` — `await storage.updateServiceBookingStatus(bookingId, "completed");` — a TWO-argument call, so the guard is again `eq(id)` alone (`server/storage.ts:3255-3257`). Three effects ride on the target status inside that one transaction: `status === "completed"` stamps `updates.completedAt = new Date()` (`storage.ts:3249`) and calls `mintCompletionEarningsForBooking` (`storage.ts:3279`). So rejecting a dispute on a row that has since been `refunded`, `cancelled` or swept MINTS held provider/expert earnings against it, and the re-stamped `completed_at` restarts the traveler's own dispute deadline, which `bookings.ts:777-780` anchors on that very column, and the payout anchor `availableAt` with it. The route reads no status at all before writing — there is no 404 for a booking that does not exist — and the return value is DISCARDED, so `res.json({ success: true, … })` at `:1378-1383` reports success for a write that changed nothing. The mint is idempotent under conflict (partial unique indexes + `ON CONFLICT DO NOTHING`, `storage.ts:3272-3278`), which bounds a DOUBLE mint but says nothing about a mint from the wrong state. §18b names this class in its own words: the pre-check is only the error message, the transition itself is the guard. Named as **F2** in `docs/design/EXPERT_ACCEPTANCE_BRIEF.md:615-619` ("the single riskiest write named in Part II") and verified here. Fix = `expectedFromStatuses: ["disputed"]`, the returned row checked (409 on a lost race, 404 on a missing one), and no `completed_at` re-stamp on a re-completion.~~ ✅ **CLOSED 2026-09-15 (ledger `2026-09-15-v23-v25-from-state-guards`).** Fixed as filed, plus the third effect the row named: `DISPUTE_REJECT_FROM_STATUSES` (`["disputed"]`) is the 4th argument, the handler reads for its own **404**, a lost race is **409**, and `updateServiceBookingStatus` now stamps `completed_at` as `COALESCE(completed_at, NOW())` — so a re-completion cannot restart the traveler's dispute window or the payout anchor. Pinned by F3/F3b/F4/F4b (a refused reject mints ZERO rows; a legal one still mints, which is what makes the zero mean something). | closed |
| V-25 (a) | **`PATCH /api/coordination-states/:id/status` has no from-state guard on EITHER arm, and its writer read-modify-writes the audit history.** [verified 2026-09-15 at `46d383a2`] `server/routes.ts:9898-9931`. **(a) The traveler arm has no ordering rule at all.** The `FORWARD_ORDER` check at `:9914-9924` sits inside `if (isCoordinator && !isTraveler)`, so the traveler (`state.userId === userId`, `:9904`) may set any string in any direction — `completed` on a fresh intake, or an engagement walked backwards — and `coordination_states.status` is `varchar(30)` with no DB CHECK, so a value outside `FORWARD_ORDER` is accepted too. *(The ordering RULE for that arm is **OWNED by D-36..D-39**, ruled A 2026-09-15 — brief §15 F3, `docs/design/EXPERT_ACCEPTANCE_BRIEF.md:620-625`, lane 5. This row proposes none.)* **(b) BOTH arms are check-then-update, and that half is THIS row.** `state` is read at `:9900`, the coordinator's ordering compared against `state.status` at `:9920`, and the write issued at `:9926` through `storage.updateCoordinationStatus`, whose UPDATE is `.where(eq(coordinationStates.id, id))` (`server/storage.ts:4570-4573`) — unconditional. Two concurrent coordinator requests both read `vendor_discovery`, both pass the forward check, and the later write wins; §15 says a check-then-update is the TOCTOU bug, **not** a guard. **(c) The history loses an entry in the same race.** `updateCoordinationStatus` SELECTs `stateHistory`, appends in JS and writes the whole array back (`storage.ts:4557-4567`), so the losing writer's transition is silently erased from the one record of who advanced what — and `completedAt` is re-stamped unconditionally on every `completed` write (`storage.ts:4568`). Fix = the `expectedFromStatuses` shape `updateServiceBookingStatus` already carries (§18 rule 1 — the same guard delegated, not a second invention) plus a SQL `stateHistory \|\| $entry::jsonb` append so the trail cannot be clobbered. **(b) IS CLOSED 2026-09-15 (ledger `2026-09-15-v23-v25-from-state-guards`):** `storage.updateCoordinationStatus` takes the same `expectedFromStatuses` shape in the same 4th position (§18 rule 1), BOTH arms pass the status they read through ONE call site, a lost race is **409**, the `state_history` append is one SQL `COALESCE(state_history,'[]'::jsonb) || <entry>::jsonb` inside the guarded UPDATE, and `completed_at` is stamped only on the transition INTO completed. Pinned by `from-state-guards.db.test.ts` F5/F5b/F5c/F6/F6b. **(a) STAYS OPEN and its OWNER is D-39** (`docs/design/EXPERT_ACCEPTANCE_BRIEF.md` F3) — the traveler arm still has no ordering rule, and this lane deliberately added none; F6b pins that the omission stays named in the code with its owner, and that there is still exactly ONE `FORWARD_ORDER` list, on the coordinator arm only. | (a) owned by D-39; (b) closed |
| ~~V-26~~ | ~~**`storage.bookSlot` claims ONE unit of `vendor_availability_slots.booked_count` per cart line whatever the line's unit count, so three seats consume one seat of capacity — and `releaseSlot` has the same 1-unit shape.** [verified 2026-09-15 at `46d383a2`] `server/storage.ts:4469-4485`: `bookSlot(id)` takes no count, sets `booked_count = COALESCE(booked_count, 0) + 1` and guards `COALESCE(booked_count, 0) < COALESCE(capacity, 1)` — so it refuses only a FULL slot, never an over-subscribing line. The checkout claim calls it once per line (`server/routes/payments.routes.ts:1279`, `const claimed = await storage.bookSlot(itemSlotId);`) while the MONEY on that same line is `rate * (item?.quantity \|\| 1)` (`payments.routes.ts:402`, `resolveItemBaseAmount`) and the booking row records what was bought: `bookingDetails.quantity: item.quantity \|\| 1` (`payments.routes.ts:1698`). The traveler is charged for N, the provider's calendar is debited 1, and the discrepancy is durable and visible on the row. **D-14's landing made this reachable through the SUPPORTED flow, not a crafted body:** ledger `2026-09-15-d14-quantity-is-units` derives `quantity = party_size` server-side for exactly the seat-shaped `in_person`/`hybrid` archetype that is slot-bound (`shared/cart-quantity.ts:233-245`), so a listing that ASKS for seats books three and claims one. D-14's own answer records the gap in passing — "slot capacity is still `storage.bookSlot`, which claims exactly ONE unit per slot-bound line and reads neither column — recorded, not changed" — and this row promotes it to a verified defect. **`releaseSlot` mirrors it exactly** (`storage.ts:4489-4501`, `GREATEST(COALESCE(booked_count,0) - 1, 0)`), so the two AGREE today and nothing drifts — which is precisely why both halves must ship together: widening only the claim would leak a slot's capacity permanently on every cancel/refund, which releases through that same writer (`storage.ts:3286-3300`, `deriveClaimedSlotIds`) and through `stripe-payment.service.ts:1171`. The STAY branch is NOT affected — a room pins `quantity` to 1 and claims one slot per NIGHT (`payments.routes.ts:1257`). Fix = a unit-count parameter on both writers with the arithmetic AND the capacity guard inside the same single statement (`booked_count + units <= capacity` — the statement stays the guard, §15/C3), the checkout passing the line's own count (the ONE number `resolveItemBaseAmount` multiplies by, §18 rule 1), and the release deriving it from the `bookingDetails.quantity` the row already carries.~~ **CLOSED 2026-09-15 (ledger `2026-09-15-v26-slot-units`).** Both writers now take a `units` count, with the arithmetic AND the capacity guard inside the SAME single statement (`+ units <= capacity` — the statement stays the guard, §15/C3; refused, never clamped, and a non-positive or fractional count is refused BY NAME rather than defaulting to 1). The checkout passes the line's own count through the ONE `resolveItemUnitCount` that `resolveItemBaseAmount` multiplies by (§18 rule 1), and the STAY branch passes `1` explicitly per NIGHT with the reason beside it. **THE RELEASE READS WHAT THE CLAIM RECORDED, NOT WHAT THE LINE WAS PRICED AT (§13):** `bookingDetails.quantity` is the priced unit count and a PRE-FIX row carries it while holding exactly ONE unit, so the claim now stamps `claimedSlotUnits` — the RELEASE-ALL-NIGHTS precedent for WHICH slots, applied to HOW MANY — and absent means one. ONE decider (`deriveClaimedSlotUnits`), three release callers; the key is server-authored and joined `SERVER_AUTHORED_BOOKING_DETAIL_KEYS` (§19d), widening B7's pinned set. **No §15b/§15c predicate, idempotency key or atomic conditional moved** — only the size of the inventory movement. No schema change: `capacity`/`booked_count` are already `integer`. Proven by `slot-units.db.test.ts` S1–S8 (8 proofs, own CI job); S1/S3/S4/S6 fail on main's storage. **STATED LIMIT, recorded 2026-09-15 by the section-3 re-sweep (not a defect):** `deriveClaimedSlotUnits` (`server/services/checkout-claim.service.ts:687-693`) returns ONE number for the whole booking and every release path applies it UNIFORMLY across the slot set `deriveClaimedSlotIds` returns. That is correct for every rail on `main` — a cart line claims N units of one slot, a stay pins `quantity` to 1 and claims one slot PER NIGHT stamping `1` — so no booking today holds mixed per-slot unit counts. It is a PROPERTY OF THE CURRENT RAILS, not of the data model: `booking_details` could hold a per-slot map and nothing refuses one. A rail that ever claims different counts on different slots of one booking must change this decider, not work around it. | closed |
| ~~V-27~~ | ~~**The Privacy Policy — the SECOND document every account must accept — still says the platform collects and manages RETIRED credit balances; V-21 deleted the same claim from the Terms and never reached this page.**~~ ✅ **CLOSED 2026-09-15 (ledger `2026-09-15-v27-v28-privacy-kind-labels`).** Both list items are DELETED from `client/src/pages/privacy.tsx` — §2.2's "Platform credit balances and usage" and §3.1's "Manage platform credits and process payments" — with no replacement sentence, as the row prescribed: the platform does not collect the thing. The card copy the row put OUT of scope is untouched and is now ASSERTED, so the sweep can never be satisfied by deleting it. The layer is the row's own suggestion, built as ONE predicate rather than a sibling copy (§18 rule 1): `terms-copy-honesty.test.ts`'s retired-product rule (T2) reads a LIST of consent documents, card copy is neutralised once instead of exempted per-rule, T6 gained the two removed sentences as non-vacuity fixtures plus the reverse fixture, and a new T8 pins the card sentences and the surviving bullets of both sections. 9/9, already CI-wired by the whole-directory `unit-suite-client-lib` job. Diagnosis below stands verbatim. [verified 2026-09-15 at `5d5f7ccc3`] `client/src/pages/privacy.tsx:78` lists **"Platform credit balances and usage"** among the payment information Traveloure COLLECTS, and `:138` lists **"Manage platform credits and process payments"** among what it uses that data FOR. The credits/wallet system is RETIRED (**LD 43(b)**): all four endpoints answer **410 Gone** — `server/routes/payments.routes.ts:253,257,261,267`, whose docblock at `:244-251` records that `deductCredits` has no callers and `POST /api/wallet/add-credits` was a free-credits hole. No surface anywhere else in `client/src` mentions a credit balance (swept: `platform credit`, `credit balance`, `add credits`, `your credits`, `wallet balance`, `unused credits` — these two lines are the only hits). This page is a GATE, not a footer link: `client/src/App.tsx:247` redirects any signed-in user with no `privacyAcceptedAt` **or** no `termsAcceptedAt` to `/accept-terms`, so both documents are consent-of-record, and `App.tsx:566` serves `/privacy`. A privacy policy that names a data category the platform cannot hold is the §13 shape on the one page whose entire job is to be true — the same defect V-21 closed next door, and the reason it survived is that the V-21 lane's scope was the Terms file. **Fix = delete both bullets** (no replacement sentence is owed: the platform does not collect the thing). **NOT in scope:** "credit/debit cards" at `:76` and "complete credit card numbers" at `:72` are card copy, not platform credits. | trivial — two line deletions; the layer is a copy pin beside `client/src/lib/__tests__/terms-copy-honesty.test.ts` |
| ~~V-28~~ | ~~**The admin reconciliation page has no human-readable label for SEVEN of the sixteen drift kinds the job emits — every `rm_*` ready-made kind and the trip-booking kind — so §17's own output renders as a raw identifier twice over.**~~ ✅ **CLOSED 2026-09-15 (ledger `2026-09-15-v27-v28-privacy-kind-labels`).** Built as the row's own **durable** fix rather than the seven entries: the canonical list MOVED (not copied) out of `shared/schema.ts` into the new dependency-free `shared/reconciliation-kinds.ts`, which schema.ts RE-EXPORTS — so it is still ONE list, the job and every other importer are untouched, and a browser page can read the vocabulary without the ORM schema. The labels are declared BESIDE it as `Record<ReconciliationExceptionKind, string>`, so **tsc** now fails the day a kind arrives without one, and `reconciliationKindLabel` is the ONE reader. The sweep found a THIRD copy the row did not name — `RECONCILIATION_LABELS` in `server/services/email.service.ts`, whose own comment said it "mirrors KIND_LABELS on the admin page", with the same seven gaps — and it reads the shared helper too. §13 held in both directions: an unknown kind still renders as ITSELF (never a guess, never "Unknown drift"), and the reader uses `hasOwnProperty` rather than a plain index because a kind arriving from an API response would otherwise answer `toString` out of `Object.prototype`. New pure pin `client/src/lib/__tests__/reconciliation-kind-labels.test.ts` K0–K9 (10/10), wired by the whole-directory `unit-suite-client-lib` job. The JOB IS UNCHANGED. Diagnosis below stands verbatim. [verified 2026-09-15 at `5d5f7ccc3`] `server/jobs/stripeReconciliation.ts` emits sixteen kinds; `KIND_LABELS` in `client/src/pages/admin/reconciliation.tsx:94-105` names ten. Missing: `rm_pi_succeeded_no_purchase` (`:1123`), `rm_amount_mismatch` (`:1158`), `rm_purchase_paid_not_cloned` (`:1201`), `rm_purchase_pi_not_succeeded` (`:1235`), `rm_delivery_not_announced` (`:1344`), `rm_refund_not_reversed` (`:1393`) and `trip_booking_without_item` (`:879`). The render is `{KIND_LABELS[e.kind] ?? e.kind}` at `:872` with the raw kind already printed beneath it at `:874`, so an unlabelled row shows the identifier on both lines and the sentence that says what drifted is simply absent. This is the §13 direction that matters here: the fallback does not LIE, it just fails to inform — and the reader is the operator deciding whether money moved. The six `rm_*` kinds are V-3's whole rail (ledger `2026-09-12-readymade-reconciliation-rail`) and `trip_booking_without_item` is D-11's; both landed detection without landing its label. **Fix = seven entries in that map**, written from the kinds the job emits. **Stated negative space:** nothing derives this map from the job's own kind union, so it will fall behind again on the next kind — whether to derive it from the canonical vocabulary in `shared/schema.ts` (the comment at `:8310` calls it "the canonical drift vocabulary") is the durable fix and is worth one line of the same lane. | trivial |
| ~~V-29~~ | ~~**TWO resolvers answer "may this person rewrite the plan", they disagree about the OWNER, and the item PATCH/DELETE rails use the one that can refuse a plan's own owner.** [verified 2026-09-15 at `5d5f7ccc3`] LD 42 **D17** rules ONE predicate for that question — "a second 'may this person rewrite the plan?' test is the drift class §18 rule 1 names". There are two. **(A)** `getTripWriteRole` + `canMutateTrip` (`server/utils/trip-role.ts:69-86` and `:93-95`) resolves the owner **only** through a `trip_collaborators` row — it never reads `trips.user_id` — and includes neither the trip author nor an admin, so every caller bolts `isTripAuthor` on beside it as a parallel branch (`server/routes/trips.routes.ts:2979`, `:3086`; `server/routes.ts:8990`, `:9350`, `:12133`). **(B)** `authorizeTripLogistics(..., { requireWriteAccess: true })` (`server/utils/trip-logistics-auth.ts:35-71`) resolves the owner through `verifyTripOwnership` at `:44` (the `trips.user_id` column), then the same write-status advisor predicate at `:51-54`, then the author at `:58` and an audit-logged admin at `:62-68`. **The two therefore differ on three principals — owner, author, admin — while sharing the §12 advisor half.** Live split: `PATCH /api/trips/:tripId/itinerary-items/:itemId` (`trips.routes.ts:2969`, gate at `:2976-2981`) and its DELETE twin (gate at `:3084-3087`) take **A**; `POST /api/trips/:tripId/itinerary/reorder` (`server/routes.ts:12095`), `PATCH /api/trips/:tripId/expert-traveler-note` (`trips.routes.ts:3144`) and the D-19 proposal rails (`trips.routes.ts:3361`, `:3399`) take **B** — whose own comment at `:3335-3341` calls `requireWriteAccess` "exactly the §12 narrowing D17 asks for". **The owner disagreement is real, not theoretical:** under **A** a trip owner with no `trip_collaborators` row resolves `null` and is 403'd from editing their own plan. It does not bite today only because a DATA invariant holds — `storage.createTrip` writes the owner row in the same operation (`server/storage.ts:1518-1523`), the raw-SQL and clone mints each remember it (`server/services/booking.service.ts:133`, `:1216`; `ready-made-purchase.service.ts:264`), and `server/seeds/trip-ownership.seed.ts` backfills at boot (`server/index.ts:479-481`). An invariant every future mint site must remember is not the same as a predicate that reads the column. **Fix shape (D17's own): ONE resolver, the other a caller or deleted.** **B** is the one that reads ownership from the row and already carries the author and admin branches the **A** call sites hand-roll, so the cheap direction is to move the five **A** call sites onto **B** and delete **A**'s write arm — but that GRANTS admin and author where **A** refused them, which is a behaviour change on five rails and needs to be stated in the lane's ledger row, not slipped in.~~ **CLOSED 2026-09-15 — ruled option B by the decision-maker; ledger `2026-09-15-v29-one-trip-write-resolver`.** Every write-gating caller moved onto `authorizeTripLogistics(tripId, userId, route, { requireWriteAccess: true })` — the item PATCH and DELETE, `POST /api/itinerary-items/:id/backup`, the comparison CREATE and GENERATE, `optimize-order`, `apply-to-trip` and `PATCH /api/transport-legs/:legId/status`; the last two were not in this row's list because they gated a WRITE through the READ resolver `getTripRole`. `getTripWriteRole` and `canMutateTrip` are DELETED (§18c); `getTripRole` survives as the READ resolver, so the `trip_collaborators` owner-row invariant and its notes were deliberately kept. **THE DELTA IS STATED, NOT SLIPPED IN:** (a) an owner with no `trip_collaborators` row is no longer 403'd on their own plan; (b) the trip AUTHOR and an AUDIT-LOGGED ADMIN gain write on all eight, as they already had on reorder / expert-traveler-note / the proposal rails; (c) on the two READ-resolver rails only, a NARROWING — a `pending` advisor can no longer set a backup plan or confirm a transport leg (§12 applied where it was missing). Statuses and each router's body shape are unchanged, and **B itself was not widened**. Proven by `one-trip-write-resolver.db.test.ts` W1–W6 (10 proofs, own CI job); `optimizer-run-predicate` R1–R3 and `expert-work-protected` E5 repaired to the new invariant. **STATED AND NOT FIXED:** `getTripRole` keeps the collaborator-only owner resolution, so a raw-insert plan still 403s its owner on the plancard READ — a real pre-existing gap this ruling does not reach. | closed |
| ~~V-30~~ | ~~**`scripts/check-test-files-wired.cjs` reports "477/507 reachable; 30 orphan(s)" and the true figure is 233 orphans: one PROSE `echo` line inside a workflow makes every test under `server/` look wired.** [verified 2026-09-15 at `5d5f7ccc3`; R-11 promoted] `.github/workflows/publish-gate-and-fundamentals-gate.yml:272` is a failure-summary `echo` whose text contains `` `Run solo: npx tsx --test server/__tests__/<file>` ``. The guard extracts every `run:` block, splits it on whitespace (`shellWords`, `:87-93`), sees `tsx` + `--test`, and takes **every following word** as a selector (`runnerSelectors`); the sentence's later words include the bare word **`server`**, and `selectorMatches` at `:126` treats a non-glob selector as a directory prefix (`file.startsWith(clean + "/")`). So `server` matches every test file in the tree. Measured by re-running `inventory()` with that one line removed: **477 → 274 reachable, 30 → 233 orphans, 203 suites reachable through nothing but that echo**, among them `server/__tests__/availability-model.db.test.ts`, `s11-stay-booking.db.test.ts`, `balance-payer-atomicity.db.test.ts`, `deposit-checkout.db.test.ts` and `mutation-auth/*`. The job the echo lives in runs exactly five named suites (`:200-233`). **The line predates the orphan ledger** — introduced 2026-09-07 in `2457c7404`, while `2026-09-14-test-files-wired-orphans` reported 88 → 30 on 2026-09-14 — so **both** of that row's counts were computed against the poisoned predicate and neither is the true orphan set. The guard exits 0 by design (advisory), so nothing went red; that is exactly why it went unseen. **Fix = the selector must come from a segment whose FIRST command is the runner**, not from any word following `--test` anywhere in a sentence — plus a committed `--self-test` fixture carrying a prose `echo` that names a runner (§18d: a predicate change ships with fixtures), and the unscanned-root limit R-11 also names (`TEST_ROOTS` at `:34` excludes `e2e/`, so no `e2e/` spec is counted at all) added to the script's own **CANNOT DETECT** block at `:16-22`. Re-run the orphan classification afterwards: the ledger's 30-orphan list is not the real one.~~ ✅ **CLOSED 2026-09-15 by lane `2026-09-15-test-guard-prose-echo` (ledger row of that id).** The predicate now reads selectors ONLY from a command segment whose LEADING command is a runner: the `run:` block is read line by line, heredoc bodies and `#` comments are dropped, each line is split at top-level `&&`/`||`/`;`/`|`/`<`/`>`/`>>`/`(`/`)` **with quotes respected** (the `;` inside the sentence is what cut off the leading `echo`), and the leading command is found by skipping `NAME=value` assignments and wrapper commands (`npx`, `env`, `time`, …). A bare directory-prefix selector must also name a directory that EXISTS — stated in the script as a SECOND filter, not the fix, because `server` is a real directory and the position rule is what removes it. **Measured: 477/507 reachable / 30 orphans → 274/507 / 233, exactly the figure the re-sweep predicted; all 30 previously-reported orphans are still orphans, and of the 203 newly revealed only eight are named anywhere in CI or `package.json` — all eight correctly orphans (one in a COMMENT explaining its exclusion, seven in npm scripts no workflow calls).** Seven committed `--self-test` fixtures (prose echo; real invocation + trailing prose echo; `::error::`/comment lines; heredoc body; non-existent directory prefix; redirect target; plus the two originals) run in CI before the scan, unchanged (§18d). The script's `CANNOT DETECT` block now names BOTH limits this row required — the prose blind spot WITH the new assumption it replaces it with, and that `TEST_ROOTS` excludes `e2e/` — and **the roots were deliberately NOT widened**: adding `e2e` moves numerator and denominator and changes what the inventory measures, which is a ruling. **Advisory exit 0 is UNCHANGED** and no gate turns red (the inventory is read by no gate). The true 233-row orphan list, grouped by directory, is published in `docs/lane-reports/2026-09-15-test-guard-prose-echo.md`; **re-classifying those 203 suites is the follow-up lane and is NOT closed here.** | small — one parser rule plus fixtures; the re-classification of 203 suites is the real work and is its own lane |
| ~~V-31~~ | ~~**`playwright/utils/auth.ts` `loginAs` asserts nothing, so every spec that calls it runs ANONYMOUS and its assertions describe a signed-out site.**~~ ✅ **CLOSED 2026-09-15 (ledger `2026-09-15-v31-loginas-verifies`).** `loginAs` is ONE implementation that posts the app's own `/api/auth/login` through the caller's own cookie jar and then PROVES the session — `connect.sid` present AND `GET /api/auth/user` 200 AND the principal it names IS the account asked for — and THROWS with the evidence otherwise; it takes a `Page` or an `APIRequestContext` and returns the authenticated user. The second helper of the same name (`playwright/tests/personas/_persona-helpers.ts`) is now a CALLER of it, not a second implementation (§18 rule 1). `logout` was the mirror defect and now proves the 401 (through `POST /api/auth/logout`, not the Replit-only `GET /api/logout`, which was measured 404 here with the session surviving). `isLoggedIn`/`waitForAuth`, two more silent-pass helpers with zero consumers, are DELETED (§18c). Proven BEFORE/AFTER on a database built from empty: `seam-cross-console`'s five seams stopped reporting `[API BROKEN] … 401` about four healthy endpoints and now reach real ground (59 assertions were unreachable); `phase-3` went 10.2m→2.2m with four tests now naming their unseeded account in a second instead of timing out anonymously; `phase-4-7` 4f/2p→3f/3p; `stripe-init-deferral` is 2/2. **WHAT THIS ROW DOES NOT CLOSE, and it is deliberate:** the four suites are STILL ORPHANS (233 unchanged) — arming needs a `spec-green` Actions run URL and the lane's egress blocked every Playwright browser host, so every run was local; `stripe-init-deferral` is the one ready to arm. The seeded-accounts question stays R-12/R-13's. Two product defects the real sessions surfaced are filed below as **V-32** and **V-33**. The original row follows. ORIGINAL: [verified 2026-09-15 at `5d5f7ccc3`; R-10 promoted, first found by lane R-8] `loginAs` (`playwright/utils/auth.ts:6-27`) fills `/login`, clicks submit, and waits only for the URL to stop containing `/login`; it never checks that a session cookie was set and never reads `GET /api/auth/user`. Lane R-8 reproduced the consequence in this harness — zero cookies, `GET /api/auth/user` → 401 — so `playwright/tests/seam-cross-console.spec.ts` (16 `loginAs` calls, `:88`…`:693`, across traveler, expert, provider and admin personas) exercises the anonymous site while reading as a cross-console authorization suite. Two more suites import the same helper: `phase-3-traveler-flows.spec.ts` (5 calls) and `phase-4-7-advanced-flows.spec.ts` (7 calls); `stripe-init-deferral.spec.ts` uses it once. A sibling, request-scoped `loginAs` exists and does return an actor (`playwright/tests/personas/journey-traveler.spec.ts:47,76`) — two helpers of the same name with different contracts is the §18 rule 1 shape underneath this. **The silent-pass is the defect, not the missing session:** a login helper that cannot fail turns every downstream assertion into a claim about the wrong principal. **Fix = assert the session inside the helper** (a cookie AND a 200 from `/api/auth/user`, failing loudly otherwise), and decide separately whether these four specs get seeded accounts or are retired — all four are also ORPHANS (no workflow runs them; see V-30, which is why nobody saw them pass wrongly). **AMENDED 2026-09-15 by lane `2026-09-15-test-guard-prose-echo` — THIS ROW STAYS OPEN, and the lane brief's instruction to strike it CLOSED was declined as a §13 falsehood.** That lane repaired V-30 and published the true orphan list; it did NOT touch `playwright/utils/auth.ts`. `loginAs` still asserts nothing about the session, and the second helper of the same name with a different contract still exists at `playwright/tests/personas/journey-traveler.spec.ts:47` — **the silent-pass defect this row names is unfixed.** What the repair DID settle is this row's reachability clause, and it settled it by CONFIRMING rather than changing it: all four suites (`seam-cross-console`, `phase-3-traveler-flows`, `phase-4-7-advanced-flows`, `stripe-init-deferral`) live under `playwright/`, not `server/`, so the poisoned `server` selector never hid them — they were already on the 30-orphan list and they are on the true 233-row list unchanged. Marking the row closed would record a fix nobody made. | small for the helper; the seeded-accounts question is R-12/R-13's lane |
| ~~V-32~~ | ~~**`POST /api/leads/route` has a comment and no handler — the smart-lead-routing door has answered 404 since the June 2026 route defragmentation.**~~ ✅ **CLOSED 2026-09-15 (ledger `2026-09-15-v32-v33-leads-door-item-read-gate`) — RULED RETIRED, NOT RESTORED.** The row left the restore-or-supersede question open; it is answered. A door whose job is “score experts and auto-assign” would be a SECOND AUTHOR of the advisor row — Locked Decision 42 D7 rules ONE advisor-add rail (`POST /api/trips/:tripId/advisors` → `upsertTripAdvisorRow`), Locked Decision 32 routes a LEAD through `POST /api/expert-requests` (which calls the very same `lead-routing.service.ts`), and `scripts/check-advisor-row-author.cjs` would refuse the insert anyway. The comment is replaced by a retirement note stating why and saying “do not re-add”; the scoring SERVICE is untouched and keeps its live importers (L1b). The one caller, `seam-cross-console.spec.ts` Seam 1, is REWRITTEN onto `POST /api/expert-requests` rather than deleted — no other step in that suite exercises the lead → routing-queue seam. Ratcheted by `leads-door-and-trip-read-gate.db.test.ts` **L1**, which fails on BOTH shapes of the defect: a restored registration AND the route-header comment that WAS the defect. **WHAT THIS DOES NOT CLOSE:** the suite is STILL AN ORPHAN — nothing was armed here and the 233-row orphan count is unchanged. ORIGINAL: **`POST /api/leads/route` has a comment and no handler — the smart-lead-routing door has answered 404 since the June 2026 route defragmentation.** [verified 2026-09-15 at `42e5213ef`, found by the V-31 lane once its sessions were real] `server/routes/payments.routes.ts:2867` is the line `// POST /api/leads/route  — score experts and auto-assign`, and the very next non-blank line is `export default router;` (`:2869`). No `/leads/route` registration exists anywhere under `server/` (`grep -rn '"/leads'` and `grep -rn 'api/leads'` return that comment and nothing else), so the endpoint answers `404 {"code":"NOT_FOUND"}` — reproduced against a local production build. The handler WAS live at `server/routes.ts:18777` (`app.post("/api/leads/route", isAuthenticated, …)`, importing `./services/lead-routing.service`) until commit `a8d7a8bf2` "Route defragmentation complete: routes.ts 20,741 → 430 lines" (Jun 6 2026), which carried the COMMENT into the new router file and left the body behind. `server/services/lead-routing.service.ts` is still live and still imported (`content-matching.service.ts:40`), so what is missing is the HTTP door, not the logic. No client file calls it either, which is why nothing surfaced it for three months — the only caller left is `seam-cross-console.spec.ts`, an orphan suite that could not reach the call until this lane fixed `loginAs`. **What is NOT established here:** whether the rail should be restored or whether it was superseded (the slip's `POST /api/expert-requests` auto-route and Locked Decision 42 D7's `POST /api/trips/:tripId/advisors` both exist). That is a decision; this row records that the code and its own comment disagree. | small to decide, then either restore the handler or delete the comment and the spec's step |
| ~~V-33~~ | ~~**The read gate grants experts on `trips.expertId`, a column NOTHING writes, so an advisor assigned through the ratified rail is refused 403 and logged as an IDOR attempt.**~~ ✅ **CLOSED 2026-09-15 (ledger `2026-09-15-v32-v33-leads-door-item-read-gate`).** The broken arm now asks the CANONICAL §12 READ predicate `isTripAdvisor` (`server/utils/trip-advisor.ts`), imported and never re-derived (§18 rule 1); `pending` PASSES per Locked Decision 12, `rejected` still denies, and the EA-managed and share-token arms are untouched. The `[IDOR ATTEMPT]` line no longer fires for a legitimately assigned advisor and still fires for a real stranger. The second site reasoning about the same dead column — the generate-itinerary stopgap in `server/routes.ts` — loses its `isTripColumnExpert` arm, which narrows nothing (§18c: dead code on an authorization path reads as a live grant). The COLUMN is KEPT and ANNOTATED in `shared/schema.ts` as written-by-nothing (dropping it is an unratified schema change). §21 is NOT widened: `canSeePrivateExpertNotes` was already effectively EA-only (its `isExpert` term was the same dead column) and stays so — the private build notes keep their own WRITE-status rail. Proven by `leads-door-and-trip-read-gate.db.test.ts` A0–A5/R1/S1–S2 (11 proofs, wired; 4 fail against the unfixed gate). **CORRECTION THIS ROW OWES ITS READER, and it is why the headline above is re-worded:** the defect is in **`GET /api/trips/:id`**, not in the itinerary-item readers. This row's own line reference (`server/routes/trips.routes.ts:359-371` at `42e5213ef`) and its `[IDOR ATTEMPT]` repro both point at the trip GET — that handler is the only source of that log line — while BOTH `GET /api/trips/:tripId/itinerary-items` readers (`trips.routes.ts`, `server/routes.ts`) were ALREADY consulting `trip_expert_advisors` through `storage.isExpertAssignedToTrip` on `main`. Nothing was changed there, because nothing was broken there. **The pending-vs-accepted question this row deferred is answered by ruling, not by this lane:** Locked Decision 12 names the trip GET among the read surfaces that grant `pending`. ORIGINAL: **The itinerary-item read gate grants experts on `trips.expertId`, a column NOTHING writes, so an advisor assigned through the ratified rail is refused 403 and logged as an IDOR attempt.** [verified 2026-09-15 at `42e5213ef`, reproduced end to end] `server/routes/trips.routes.ts:359-371` decides `GET /api/trips/:tripId/itinerary-items` on `isOwner || trip.expertId === userId || managedByEaId === userId || share-token` and **never consults `trip_expert_advisors`** — the table Locked Decision 32's `upsertTripAdvisorRow` is the one author of, and whose §12 statuses every other advisor rail reads. `trips.expertId` exists (`shared/schema.ts:147`) and **no code writes it**: a grep over drizzle `.set({ expertId … })` and raw `expert_id =` across `server/` finds only `affiliate_booking_requests`, and in a database built from empty 0 of 6 trips carried one. Reproduced: the traveler created a trip and assigned the Kyoto expert through `POST /api/trips/:id/expert-advisor` (200; the `trip_expert_advisors` row was written, status `pending`), then the expert's read answered **403 `{"message":"Access denied"}`** while the server logged `[IDOR ATTEMPT] User … tried to access resource owned by …` about a legitimately assigned advisor — a §13 falsehood in the log as well as a refused read. `server/routes.ts:1513-1533` carries a second reader reasoning about the same dead column ("EA-managed and expertId-linked trips keep working"), so this is a grant condition that two readers rely on and nothing can satisfy. **Related but distinct from V-29**, which is about two WRITE resolvers disagreeing about the owner; this is a READ grant with no writer. **Deliberately not decided here:** whether a `pending` advisor should read items (Locked Decision 12 grants `pending` on the read surfaces it names — assigned-trips, trip GET, plancard — and does not name this one). The verified defect is narrower and holds either way: the expert arm of this gate can never be true. | small for the gate; the pending-vs-accepted question is a ruling |
| ~~V-34~~ | ~~**`storage.updateDailyRevenueSummary` is a CHECK-THEN-INSERT on a UNIQUE date, so two first-writers on one day race to `daily_revenue_summary_date_unique`.**~~ ✅ **CLOSED 2026-09-16 (ledger `2026-09-16-ci-main-red-repairs`), filed and struck in the same PR — a §15 defect found by a whole-directory run.** [verified 2026-09-16 at `0de20e07e`] `server/storage.ts` `updateDailyRevenueSummary`: `const existing = await this.getDailyRevenueSummary(date); if (existing) { UPDATE … } else { INSERT … }` — the shape §15 names as the TOCTOU bug, not a guard. Every genuine `insertPlatformRevenueOnce` (the canonical `platform_revenue` writer, `recordPlatformRevenue` delegates to it) rolls TODAY's summary row up through it, and the coordination-refund handler in `server/routes.ts` fires it per compensating row. It surfaced in CI, not production: `suite-server-routes-migrations` runs the three revenue-dedup vitest suites in PARALLEL worker processes, each mints its first `platform_revenue` row within milliseconds of the others on a fresh database with no row for today, and the loser of the INSERT race threw the unique violation up through the suite. Production creates a day's row once and then updates it, which is why nothing was ever seen there — and why a §15 defect can sit in an analytics writer for a year. **Fix landed:** ONE atomic `INSERT … ON CONFLICT (date) DO UPDATE SET total_* = COALESCE(total_*,0) + excluded.total_*, transaction_count = COALESCE(transaction_count,0) + 1` — the same shape `confirmCartBookingRevenue`'s rollup already used in the same file (§18 rule 1: two rollups, one shape), against the UNIQUE constraint that already exists (no schema change). Proven by `server/migrations/__tests__/daily-revenue-summary-upsert.test.ts` U1–U3, which is deterministic (eight first-writers on a per-run date) where the CI race was not (it did not reproduce in three local runs of the job's exact line — stated, not hidden), fails on the pre-fix writer with that exact constraint name, and runs in the SAME parallel vitest line that exposed the race. **Still open and NOT this row:** the MONEY_MAP F-4 divergence — the raw cart-confirm `INSERT INTO platform_revenue` in `booking.service.ts` does not call this writer at all — is pre-existing, annotated in both files, and untouched. | small; closed |

## 3 · Reported by audit, NOT verified here — open the file before acting

- ~~**R-1**~~ ✅ **CLOSED 2026-09-14 (ledger `2026-09-14-transport-confirm-stamps-pi`).** ~~Transport confirmation stores the Checkout Session id as the confirmation code and never stamps `stripe_payment_intent_id`, so the cancellation endpoint (which only refunds when a PaymentIntent exists) can go status-only even when a refund is owed, and reconciliation may raise a misleading "confirmed booking has no PaymentIntent" exception.~~ Re-verified on `6b84e501`: the row is a `service_bookings` row (`bookingDetails.bookingType='transport'`, `service_id` NULL — the documented transport-commerce exception), born `pending`; `stripe.service.ts:287-292` was the promotion (`confirmationCode: session.id` at `:291`), `routes.ts:7329` the cancel gate, `stripeReconciliation.ts:636-650` the detector. The id is now recorded from the session this server RE-RETRIEVES from Stripe (§17b), BEFORE the status flip, through `stampTransportPaymentIntent` in `checkout-claim.service.ts` — §19a's SOLE writer of that column, one more caller, never a second UPDATE site; it could not reuse `stampAuthorization`, whose from-state is the cart claim's `payment_pending`, a state a transport row never enters. The cancel path needed NO change: its amount is server-derived, its Stripe call carries an idempotency key and its status transition is an atomic conditional — it was starving on the missing input, and T4 proves both sides of that. **Old rows keep DETECTION and get no backfill (§19b):** a hosted session puts its metadata on the SESSION, so their PaymentIntents carry no `bookingIds` and no server-verified path leads back to them; `booking_confirmed_no_pi` stands, annotated. **Found and NOT fixed:** the `confirmationCode: session.id` write is **INERT** — `service_bookings` has no `confirmation_code` column and the `as any` makes drizzle drop the key (the V-14 shape) — left in place with a pin on the column's absence, because what a transport confirmation code should be is a ruling; and the cancel's non-refund branch calls `updateServiceBookingStatus` with no `expectedFromStatuses` behind a check-then-update (§18b shape, pre-existing and not transport-specific).
- ~~**R-2**~~ ✅ **CLOSED 2026-09-14 (ledger `2026-09-14-transport-card-cancel`).** ~~`TransportBookingCard.tsx:136-137` — `if (isCancelled || isBooked || isConfirmed) return null;` is the FIRST line of `actionButton()`, and no cancel/refund control exists in the file.~~ Re-verified on `f0ee79fb` at `:137` (the file still opens `actionButton()` that way, correctly — that renderer answers "can this be BOOKED"). The real blocker was that the card had **no ADDRESS**: a platform transport purchase mints an ordinary `service_bookings` row whose only link back is `booking_details.optionId` (`transport_booking_options.booking_id` is an unrelated integer platform options never populate), so nothing on the hub payload could name the id `POST /api/bookings/:id/cancel` takes. The hub DTO now resolves the **SESSION viewer's own** booking id + status (`transport-viewer-booking.service.ts`) — an expert/author/admin reading the same hub gets nothing, which is the answer the cancel route itself gives (§14 on a read). Which statuses show the control is the **shared `@shared/booking-cancellation` list the route's own 400 reads**, collapsing four copies (route, `cancel-preview`, `my-bookings`, and the new surface) into one; the confirmation dialog is `my-bookings`' own, **EXTRACTED not copied** (§18 rule 1), so every figure and sentence on it is the server's `cancel-preview` quote. **Server, narrowly:** R-1's parting finding is closed — the cancel route's NON-refund branch's flip now carries `expectedFromStatuses` (§18b: the transition is the guard), and a refused transition answers **409** rather than writing a notification for a cancellation that did not happen. **§13:** the card computes no refund, names no policy or percentage, and after a cancel renders the server's returned status plus a refund **only** when the response says one was issued — never inferred from a status flip (LD 44 (e)). **Found and NOT changed:** cancelling the `service_bookings` row does **not** flip `transport_booking_options.booking_status`, so a refetched hub still calls the option booked — which is exactly why the card holds the server's own cancel response rather than re-reading the option; what that column should say after a cancellation is a ruling, not a cleanup. Refund policy, eligibility and timing remain entirely server-side.
- ~~**R-3**~~ [CLOSED 2026-09-15 under D-1 = B (ledger `2026-09-15-d1-ready-made-editable-template`): the marker is not owed; expert-row protection landed via `2026-09-14-clone-items-are-expert-work`]** the clone is minted as an ordinary traveler trip (`ready-made-purchase.service.ts:236`, `authorId: null`), no `do_not_optimize` column exists, and `optimizer-run-authorization.ts:58-75`'s whole `authorized:false` union is payment reasons only. The cloned ready-made trip carries no durable "authored plan, do not optimize" marker, so the ordinary optimize path stays available on a professionally authored itinerary. **AMENDED 2026-09-14:** the clone's items are now stamped `origin='expert'` server-side (`clonedItemOrigin`; `'ai'` preserved verbatim), so Locked Decision 42 D3 protects them — the optimizer reads them as fixed constraints and no rebuild or apply may delete them. That protects the ROWS and refuses NO run: the marker, and whether a purchased plan should refuse Optimize at all, is **D-1** and stays open.
- **R-4 — ✅ CLOSED 2026-09-15 (ledger `2026-09-15-d22-dates-confirmed`, migration 302). THE MISSING MOMENT NOW EXISTS.**
  Everything the diagnosis below states about `main` was TRUE and is now FIXED in its two load-bearing clauses.
  **(a) A fact now distinguishes a placeholder window from a chosen one:** `trips.dates_confirmed_at` (D-22, ruled yes
  — additive nullable, NO DEFAULT, NO CHECK, declared in `shared/schema.ts`, NO BACKFILL). NULL = NOT CONFIRMED ⇒ the
  slip labels the window a PLACEHOLDER, the `.ics` makes no pinned DTSTART claim off it and the countdown is withheld
  (§13; Locked Decisions 30 and 45 (6)). The ready-made clone, both expert authoring builds, the cart
  convert-to-itinerary mint and the Plus occasion draft leave it NULL; the traveler-stated mints stamp it.
  **(b) A client rail now re-dates an existing trip row:** `SetPlanDates` on the slip header (OWNER ONLY, Locked
  Decision 42 D16) calls `useUpdateTrip` — which had ZERO call sites — against the owner-gated `PATCH /api/trips/:id`,
  which writes through `storage.updateTrip` so Locked Decision 30's `timezone` and Locked Decision 34 / 42 D12's
  `market_slug` derivations re-run on the same write, and which STAMPS the confirmation SERVER-SIDE (§19: the column is
  `.omit()`ed from `insertTripSchema`, re-admitted by no pick, and stripped in the storage writer — a client may change
  its dates and may never certify them).
  **(c) STILL OPEN, AND IT IS THE THIRD LANE THIS ROW ALWAYS NAMED: availability revalidation.** The re-date rail
  triggers NOTHING, because there is nothing to call — the lane that was to build it (`2026-09-14-clone-date-revalidation`)
  landed VERIFICATION ONLY, and the read half is unchanged: `resolveBuyAction` is still date-blind
  (`hasPublishedAvailability` means "a slot dated today or later", explicitly not "free on these dates"), and the only
  date-scoped read is still one month-at-a-time query per service that no slip surface makes. The absence is annotated
  at the route rather than faked with a call that cannot answer the question (§13). **What HAS changed for that lane:
  its input now exists** — a re-date is a recordable, dated event, and `datesConfirmedAt` tells it which plans were
  never answered at all.
  Proofs: `server/__tests__/dates-confirmed.db.test.ts` K1–K7 (K3/K7 fail on `main`);
  `client/src/lib/__tests__/plan-dates.test.ts` P1–P7. Original diagnosis, kept verbatim:
  R-4 [RE-SCOPED 2026-09-15 under D-1 = B: build lane = the missing re-date write rail (`PATCH /api/trips/:id` has zero client callers) rendering placeholder dates honestly; needs D-22 for the marker; availability revalidation stays a third lane]** placeholder window at `ready-made-purchase.service.ts:219-221` + `:258-259` (line numbers repaired; the old `:217-219`/`:255-256` predate R-3/R-5) — `new Date()` + `durationDays-1`, comment "the buyer re-dates it in their own planner"; no revalidation path exists anywhere (the repo's only `revalidat*` is the unrelated one-pager recruitment job). **THE EXACT BLOCKER, and it is upstream of the availability question: THERE IS NO MOMENT AT WHICH THE BUYER PICKS REAL DATES.** (a) **No fact distinguishes a placeholder window from a chosen one** — `trips.start_date`/`end_date` are NOT NULL `date` with no confirmed/provisional/tentative marker, and `status:"draft"` is every plan's mint default (`shared/schema.ts` `trips`), so a marker would be a COLUMN and is refused here. (b) **No client rail re-dates an existing trip row.** The one plan modal's step 3 collects dates and, for an existing `tripId`, writes them ONLY to the `trip_contexts` jsonb (`plan-modal.tsx:880-891`); its `PATCH /api/trips/:tripId/occasion` body carries `eventType`/`adults`/`kids`/`budgetApprover*`/`accessibilityNote` and **no dates** (`trips.routes.ts:3163-3179`); the owner-gated `PATCH /api/trips/:id` (`server/routes.ts:1357`) WOULD accept them via `insertTripSchema.partial()`, but its only client shape `useUpdateTrip` (`client/src/hooks/use-trips.ts:95`) has **zero call sites**. Dates reach the row at MINT and never again. So any availability status derived against `trips.start_date` today would be checking the fulfilment job's own `new Date()` and presenting it as the traveler's answer (§13), and every item's displayed day date (`dayDateIso(startDate, dayNum)` at `trip-plan.service.ts:881`, with `startDate` read off the trip row at `:571`) is already that guess. (c) **The read-derived half is separately unbuildable from ratified readers:** `resolveBuyAction` is DATE-BLIND — `hasPublishedAvailability` means "at least one slot dated today or later", explicitly not "free on these dates" (`server/services/buy-action-payload.ts:188-203`) — the only date-scoped read (`GET /api/services/:id/availability?month=`, `content.routes.ts:2662`) is one query per service per month that the slip does not make, and the plancard activity DTO carries **no `providerServiceId`** (`trip-plan.service.ts:774-860`), so an item row cannot even name the service it references. The pill family (`RoutingBadge`/`OriginBadge`, `ActivitiesSection.tsx:56,130`) is where such a chip would mount. **NOT broken:** the row-level facts survive the clone — `providerServiceId` is in `CLONE_CARRIED_FIELDS` while `slotId`/`bookingId`/`scheduledDate`/`checkIn`/`checkOut` are excluded with reasons (`itinerary-item-clone.ts`) — so the data to revalidate exists on disk and nowhere on the wire. **AWAITS D-1:** whether a purchased ready-made plan is a finished plan or an editable template decides whether the buyer is ASKED for dates at delivery, which is the missing moment; a "dates not yet chosen" marker is a column and a re-date prompt is a new surface, so neither is a lane's to add. **RE-VERIFIED 2026-09-15 at `5d5f7ccc3` by the section-3 re-sweep — STILL STANDS, with citations repaired and ONE CLAUSE NOW STALE.** Repairs: the placeholder window is `ready-made-purchase.service.ts:219-221` + `:258-259` (unchanged); the occasion PATCH's pick-based body is `trips.routes.ts:3215-3231` (was cited `:3163-3179` — the schema moved, the fact did not: it admits `eventType`/`adults`/`kids`/`budgetApprover*`/`accessibilityNote` and **no dates**); the owner-gated `PATCH /api/trips/:id` is `server/routes.ts:1423` (was `:1357`) and still parses `insertTripSchema.partial()` (`shared/routes.ts:78-88`), which WOULD accept dates; `useUpdateTrip` is `client/src/hooks/use-trips.ts:98` (was `:95`) and still has **zero call sites** in `client/`, `e2e/` or `playwright/`. The plan modal still writes step 3's dates only into the context jsonb (`client/src/components/trip/plan-modal.tsx:881-891`). `resolveBuyAction` is still date-blind (`buy-action-payload.ts:188`) and the only date-scoped read is still one month-at-a-time query per service (`content.routes.ts:2671`). **STALE — half of clause (c) is now FIXED:** the plancard activity DTO **does** carry `providerServiceId` (and `affiliateProductId`), added by today's D-4 lane (`server/services/trip-plan.service.ts:866-871`, present-only-when-set, read-only pass-through), so an item row CAN now name the service it references and that particular blocker on a revalidation chip is gone. Everything else in (a)/(b)/(c) is unchanged, and the LANE is unchanged: the missing moment at which a buyer picks real dates is still the blocker, and it is still upstream of availability.
- ~~**R-5**~~ ✅ **CLOSED 2026-09-14 (ledger `2026-09-14-readymade-notifications`).** ~~the ONLY notification on the whole rail is an ADMIN dispute row (`ready-made.routes.ts:1469`). No buyer notification for delivery or fulfilment, no expert notification for a revision request (it only stores a message on the advisor row, `:1628-1633`), and no purchase-confirmation email at all.~~ Re-verified on `6d0d2ff7`: the admin dispute row is `ready-made.routes.ts:1467-1474` and the revision request stored its note and stopped at `:1625-1633`. **TWO sends now exist, not three:** promotion and delivery are the SAME event on this rail — the clone and its items commit BEFORE the atomic `paid → cloned` claim, so the claim is simultaneously the promotion and the delivery and there is no later transition to hang a second message on. (a) the BUYER gets one bell row + one outbox email from the claim winner inside `fulfillReadyMadePurchase` (never from the browser's confirm call); (c) the SELLING EXPERT gets one bell row + one email where the revision note is stored, after that route's own `revision_status IS NULL` claim. Both go through the EXISTING writers — `storage.createNotificationOnce` (new sibling of `createNotification`, migration 209's dedupe index) and the LD 26 outbox `enqueueEmail` — with **no new sender, no schema change and no migration**. The email is gated on the notification insert, so the dedupe index is the exactly-once marker for both. §13 on the copy: a PLAN on PLACEHOLDER dates, nothing booked; it never says "finished trip" and the amount is the purchase row's own `price_paid_cents`. **Stated limit:** a crash between the claim and the send leaves a delivered purchase unannounced — there is no retry and no `notified_at` column, and adding one is a migration nobody has ratified.
- ~~**R-6**~~ ✅ **CLOSED 2026-09-13 — both halves already satisfied.** Ready-made refunds ARE implemented distinctly (`ready-made-purchase.service.ts:594` `refundReadyMadePurchaseLedger` — its own escrow window, its own earning reversal, its own platform-revenue reversal at `:687`, its own clone soft-revoke), and the rule IS written down: ledger `2026-08-22-concierge-p3` (paid-out refusal, always soft-revoke, 90-day admin bound, full-refund-only) and `2026-08-23-concierge-q2-softrevoke-fix`, restated in the service header `:580-593` and `docs/MONEY_MAP.md:93-99`. *If the original intent was "restate it in CLAUDE.md", that is a doc lane, not a defect.*
- **R-7** `provider_services.service_type` carries a **second, category-shaped vocabulary** beside the declared six: `storage.getProviderServices` filters category with `ilike` on it (owner-scoped) and `content-matching.service.ts` does `inArray(serviceType, rule.serviceTypes)` for placement rules. Production's 61 demo listings hold values like `florist` and `av-equipment` for that reason, so **correcting them would break those readers**. Ruled a finding for lane **OC-A2**, not a data cleanup (`2026-09-11-oc-a1-ratified`). **[code half verified 2026-09-13:** `storage.ts:2016` `ilike(serviceType, '%'+category+'%')` (owner-scoped) and `content-matching.service.ts:339,344` `inArray(serviceType, rule.serviceTypes)`; the declared six are `shared/schema.ts:768` and the column carries NO DB CHECK. **CANNOT TELL FROM THE REPO:** the "61 demo rows hold `florist`/`av-equipment`" count needs the production database, and the reader inventory is still not exhaustive.**]** **RE-VERIFIED 2026-09-15 at `5d5f7ccc3` — STILL STANDS, citations repaired, VERDICT UNCHANGED (stays a section-3 row: the half that would promote it needs the production database, which no lane can open from the repo).** The two readers are `server/storage.ts:2092` (`ilike(providerServices.serviceType, '%' + filters.category + '%')`, owner-scoped — was cited `:2016`) and `server/services/content-matching.service.ts:339` and `:344` (`inArray(providerServices.serviceType, rule.serviceTypes)`, unchanged), whose own rule table at `:48-94` enumerates only the declared six. The six are `shared/schema.ts:886` (`serviceTypeEnum`; was cited `:768`) and the column is `varchar("service_type", { length: 50 }).default("planning")` at `:1042` with **no DB CHECK** — so a category-shaped value is accepted by the database and read as a category by one reader and as a type by the other. The "61 demo rows hold `florist`/`av-equipment`" count remains a PRODUCTION fact.
- ~~**R-8**~~ ✅ **CLOSED 2026-09-14 [verified 2026-09-14] (ledger `2026-09-14-trips-mint-400`) — the SPEC was stale, not a door.** The field is `budget`: `trips.budget` is `decimal("budget", { precision: 10, scale: 2 })` (`shared/schema.ts:120`), so `insertTripSchema` — what `POST /api/trips` parses (`shared/routes.ts` `api.trips.create.input`, `server/routes.ts:1300`) — declares it a **string**, and the route reads it as one (`parseFloat(sanitizedInput.budget)`, `server/routes.ts:1321`). `seam-cross-console.spec.ts` sent `budget: 3000` (number). Reproduced live on a CI-shaped database: number ⇒ `400 {"message":"Expected string, received number"}`, string ⇒ `201` with `"budget":"3000.00"` echoed back. **Every live client door already conforms** — `buildTripMintBody` (the ONE mint door, Locked Decision 32 rule 4) sends no budget at all, and `quick-start-itinerary.tsx`, the only other `POST /api/trips` caller in `client/`, sends none either; both parse clean. So the spec's payload moved, the schema did not (Locked Decision 42 D12 — no coercion added). Its stripped-in-silence `guestCount` (a `user_experiences` column, not a `trips` one) is stated on `numberOfTravelers`. **The spec is NOT wired** and stays orphaned as ledger `2026-09-14-test-files-wired-orphans` classified it: with the 400 gone the mint row lands, but `playwright/utils/auth.ts` `loginAs` establishes NO session in this harness (zero cookies; `GET /api/auth/user` → 401), so the spec runs anonymous and takes the guest branch of the mint — which writes the row and then never answers (no response in ≥100s, by curl and by the 120s spec timeout). Both are recorded as findings, neither fixed here.
- ~~**R-9**~~ ✅ **CLOSED 2026-09-14 (ledger `2026-09-14-guest-trip-mint-responds`).** ~~the anonymous/guest branch of `POST /api/trips` writes the trip row and then never responds — the request hangs ≥100 s on a CI-shaped database (reproduced by curl and by a 120 s spec timeout).~~ Re-reproduced on `36c13bc39` against a CI-shaped Postgres 16: the row COMMITS, curl gets NO response in 60 s, and the server logs `Unhandled promise rejection` at that instant. The line is `server/routes.ts:1339`, `crypto.randomBytes(32)` on the BARE GLOBAL — that file imports only `randomBytes` from `node:crypto`, so `crypto` is the Web Crypto object (probed in a real module: `crypto.constructor.name === "Crypto"`, `crypto.randomBytes === undefined`; only `node -e` fakes a working one) and the call is `TypeError: crypto.randomBytes is not a function`; the handler's `catch` re-throws it out of an async Express 4 handler, a rejection Express never sees, so NO RESPONSE IS EVER SENT. The serving copy is the `server/routes.ts` monolith (it registers first; `tripsRoutes` is `app.use`d last), and the `trips.routes.ts` twin — which imports `crypto` as a namespace and so would NOT have hung — is mount-order dead. **The token line was NOT repaired.** `2026-09-13-guest-cart-becomes-plan` states that nothing lets an anonymous principal own a `trips` row (G2 HELD) and **D-15** routes the guest through the cart, so making it work would have shipped guest trips as a side effect of fixing a hang. An anonymous caller now gets **401 `Sign in to create a plan`**, decided before the parse and before any write (LD 42 D5) — no id, no token and **no row**, since an orphan nobody can address is worse than a refusal (§13) — and the now-unreachable shareToken branch is deleted rather than disabled (§18c). The owner path is untouched (201, with `market_slug`/`timezone`/`tracking_number`/owner collaborator row); no schema, migration, route or backfill. Proven by `server/__tests__/guest-trip-mint-responds.http.test.ts` (R1–R5) in its own gate; the negative was run — 4 of 5 fail on the pre-fix handler. **Recorded, not changed:** `IntakePanel` still offers the mint to a signed-out visitor (the server refusal is the enforcement, §14 posture); NULL-owner rows already on disk are not backfilled; and `useCreateTrip`'s `onSuccess` guest branch is now unreachable — it is the ONLY caller of `addGuestTrip`, so deleting it orphans `GuestTripContext`'s add half and `use-claim-guest-trips` with it, which is a guest-trip-client retirement lane, not this one. The hook's error path DID change, minimally and for §13: a 401 now surfaces the server's own sentence instead of the catch-all "Failed to create trip", and nothing on the signed-in path moves. **FOUND, NOT FIXED — the CLASS survives the instance:** the handler's `catch` still re-throws every non-Zod error, this app registers NO Express error-handling middleware, and Express 4 never sees an async rejection, so any other throw in this handler still hangs the request instead of answering 500. R-9 removed the one thing that threw; giving the re-throw an answer is a behaviour change on the OWNER path that no ruling covers, and it is annotated at the `catch` rather than changed.
- ~~**R-10**~~ ➜ **PROMOTED 2026-09-15 to verified defect V-31 by the section-3 re-sweep; this row stays struck as the pointer.** ~~`playwright/tests/seam-cross-console.spec.ts`'s `loginAs` (`playwright/utils/auth.ts`) establishes no session in this harness — zero cookies and `GET /api/auth/user` → 401 — so the spec runs anonymous throughout.~~ Re-verified at `5d5f7ccc3`: the helper is `playwright/utils/auth.ts:6-27` and asserts nothing about the session, and the sweep widened the blast radius — three more suites import the same helper (28 `loginAs` calls in total) and a SECOND helper of the same name with a different contract exists at `playwright/tests/personas/journey-traveler.spec.ts:47`. Full evidence and fix shape are in **V-31**.
- ~~**R-11**~~ ➜ **CONFIRMED AND PROMOTED 2026-09-15 to verified defect V-30 by the section-3 re-sweep; this row stays struck as the pointer.** ~~`scripts/check-test-files-wired.cjs` counted `server/__tests__/checkout-payment-promotion.db.test.ts` as REACHABLE while no workflow on `main` named it (L25 wired it as job `plan-work-one-rail`); its "reachable" answer is not "a job runs it" — the predicate has a false-positive class worth a self-test fixture. **[AMENDED 2026-09-15 by lane D-17 — a SECOND blind spot, and it is not the same one.]** `journey-1` was neither reported reachable nor reported orphan: the inventory's `TEST_ROOTS` are `server`, `shared`, `client`, `playwright`, so **every spec under `e2e/` is invisible to it** — `e2e/specs/journey-1.spec.ts` was never counted, which is why the orphan ledger could say “neither name appears in any workflow” while the file has a real (schedule-only, staging-secret-gated) CI reach through `playwright.e2e.config.ts`'s `testDir`. That limit is NOT in the script's own **CANNOT DETECT** list, so §18d's “a guard states its negative space” is unmet for it. R-11's false-positive class is UNCONFIRMED here — this is an unscanned-root miss, not a false positive. **Guard untouched by that lane, by its brief.**~~ **BOTH halves verified at `5d5f7ccc3`, and the false-positive class is not a class — it is one line.** The mechanism: `.github/workflows/publish-gate-and-fundamentals-gate.yml:272` is a failure-summary `echo` containing the prose `` `Run solo: npx tsx --test server/__tests__/<file>` ``; the guard's whitespace split (`scripts/check-test-files-wired.cjs:87-93`) hands every following word to `runnerSelectors`, the bare word `server` is among them, and `selectorMatches:126` treats a non-glob selector as a directory prefix — so **every** test under `server/` is reported reachable, `checkout-payment-promotion.db.test.ts` included. Measured: removing that one line takes the inventory from **477/507 reachable, 30 orphans** to **274 reachable, 233 orphans**. The amendment's unscanned-root half stands verbatim: `TEST_ROOTS` at `:34` is `server`/`shared`/`client`/`playwright`, so no `e2e/` spec is counted, and the script's **CANNOT DETECT** block at `:16-22` names neither limit. Fix shape in **V-30**.
- ~~**R-12**~~ ✅ **CLOSED 2026-09-15 (ledger `2026-09-15-orphans-t4-t7-red-suites`).** Wired into `suite-ai-draft-and-availability` in `build.yml` — a DB-backed job that also boots the built app, which is what the HTTP half always needed — and repaired at the cause triage §6 named: `server/__tests__/fixtures/registered-actor.ts` reads the registered actor back on the TEST process's own connection before any dependent fixture row is inserted, and asserts the role UPDATE matched exactly one row. Real SELECT round-trips, **never a `setTimeout`** (§6 measured a bare 50 ms sleep as 13/13 on one run and 0/13 on another; one extra round-trip was 13/13). **Not one assertion was changed, relaxed or skipped** — 13/13 on four consecutive runs, one of them against a database built from empty. Option (c), the pure split, was NOT taken and is not owed: the suite is now RUN, which is how a suite leaves the orphan list. Its baseline line is removed in the same PR. ORIGINAL:  at `5d5f7ccc3`; the RED-against-a-bare-DB half is reported by the V-26 lane and was NOT re-run here]** `server/__tests__/availability-model.db.test.ts` (S7 availability model, 13 proofs) is run by **no workflow and no npm script**. It is absent from the guard's 30-orphan list only because of **V-30** — `node scripts/check-test-files-wired.cjs` calls it reachable, and re-running the inventory with the poisoning `echo` line removed puts it straight back on the orphan list. Nothing names it: `grep -rn 'availability-model' .github/workflows package.json` returns zero. It also cannot run headless — its `before()` registers actors over HTTP against a live server (`:77-92`, `POST /api/auth/register` at `JOURNEY_BASE_URL`, default `http://127.0.0.1:5000`) and then inserts a `provider_services` fixture directly (`:103-105`), so it needs a running app AND a disposable database. Per ledger `2026-09-14-test-files-wired-orphans` a suite leaves the orphan list **by being run or by being gone, never by allowlist**. **Options: (a) REPAIR** — wire it into a workflow job that stands up the app and a throwaway Postgres, alongside the other live-server `*.db.test.ts` suites, and fix whatever the first green run reports; **(b) DELETE** — the ruling it proves (ledger row 102, migration 210) has no other pin, so deleting removes the only proof that the materializer is ADD-ONLY and that a blackout never cancels a booked slot; **(c) SPLIT** — extract the pure half (the weekday-occurrence derivation at `:111-120` and the blackout arithmetic) into a unit suite that runs in CI today, and retire the HTTP half. **Recommendation: (c) then (a)** — the pure half costs nothing and stops the ruling being unpinned while the harness question is answered. Do not allowlist. **CONFIRMED 2026-09-15 by lane `2026-09-15-test-guard-prose-echo`, and STILL OPEN:** with V-30 repaired the guard now prints this file as an ORPHAN on its own — it is row `server/__tests__/availability-model.db.test.ts` of the true 233-row list published in `docs/lane-reports/2026-09-15-test-guard-prose-echo.md`. Nothing about the three options or the recommendation changes; that lane deliberately wired, deleted and allowlisted nothing.
- ~~**R-13**~~ ✅ **CLOSED 2026-09-15 (ledger `2026-09-15-orphans-t4-t7-red-suites`).** Same lane, same one repair, same job: `s11-stay-booking.db.test.ts` calls the SAME `registerActorWithReadBack` fixture R-12 does — one implementation, two callers (§18 rule 1), which is the whole reason these were one lane. 8/8 on four consecutive runs, no assertion touched, so the multi-night stay claim shape is pinned by something that actually runs. Its baseline line is removed in the same PR. ORIGINAL:  at `5d5f7ccc3`; the RED-against-a-bare-DB half is reported by the V-26 lane and was NOT re-run here]** `server/__tests__/s11-stay-booking.db.test.ts` (S11 stay booking, 8 proofs) is the same shape and the same verdict as **R-12**: named by no workflow and no npm script, hidden from the orphan list by **V-30**, and dependent on a live server plus a disposable database — its fixture inserts a `property_room` listing directly (`:126-128`, `pricing_unit = 'per_night'`). It is the ONLY suite that pins the multi-night claim shape that the V-26 slot-units lane had to reason about explicitly ("a stay claims one slot PER NIGHT and stamps `1`"), which makes its silence expensive: the stay branch of the claim machine is asserted nowhere that runs. **Same three options, same recommendation (c) then (a)**, and the two suites are ONE lane — they share a harness problem, not two. Do not allowlist. **CONFIRMED 2026-09-15 by lane `2026-09-15-test-guard-prose-echo`, and STILL OPEN:** with V-30 repaired the guard now prints this file as an ORPHAN on its own — it is row `server/__tests__/s11-stay-booking.db.test.ts` of the true 233-row list published in `docs/lane-reports/2026-09-15-test-guard-prose-echo.md`. Nothing about the three options or the recommendation changes; that lane deliberately wired, deleted and allowlisted nothing.

## 4 · Lanes unblocked and ready to build

> **LANE LIST AS OF 2026-09-15** (re-checked at `5d5f7ccc3292a967de26f02292ef30197051151d` by the
> section-3 re-sweep). **AMENDED 2026-09-15 (later): six new decision rows,
> D-45..D-50, are open — see item 8 below.** Section 1 is fully ruled and section 2 is closed apart from the two rows struck
> open with their owners named (V-22, V-25(a)) plus V-27..V-31 (V-27 and V-28 CLOSED 2026-09-15,
> ledger `2026-09-15-v27-v28-privacy-kind-labels`), so what is left to BUILD is the
> decision lanes, in this order:
>
> 1. **D-20 / D-21** — the AI-task charge (flat vs tiered; what one "task" is). Their store landed
>    (`plan_proposals`, migration 299); these two add the payment columns in their own migration.
> 2. ~~**D-22 + R-4** — the placeholder-dates marker and the missing re-date write rail.~~
>    ✅ **LANDED 2026-09-15** (ledger `2026-09-15-d22-dates-confirmed`, migration 302): the column
>    exists, the ten mint sites are decided and annotated, and the re-date rail has its first client
>    caller. **Availability revalidation is still the third lane and is still unbuilt** — the read
>    half (`resolveBuyAction` is date-blind; one month-at-a-time query per service) is unchanged.
> 3. **D-24..D-26 + D-40** — one HYBRID lane: acceptance timestamp and deadline, revisions,
>    per-booking deliverable, and whether a `hybrid` listing declares an artifact of its own.
> 4. **D-27** — does `artifact_timer` retire as a completion rule, and who reviews an escalation.
> 5. **D-28..D-31** — custom quotes: child table vs columns, the validity window, what a request
>    creates, and whether an accepted quote becomes `deposit_balance`.
> 6. **D-32..D-35** — bundles: per-component state, the snapshot price, `partially_completed`, and
>    per-component earnings release.
> 7. **D-36..D-39** — completion and dispute: `completion_declared_at`, where the mint sits, whether
>    a pre-completion dispute is the same row, and coordination.
> 8. **D-45..D-50** — the L16 Ask-AI drawer. Its design brief is WRITTEN
>    (`docs/design/ASK_AI_DRAWER_BRIEF.md`, ledger `2026-09-15-l16-ask-ai-drawer-brief`) and the
>    STORE, DISCARD, PAY and APPLY are already BUILT (D-19 migration 299; D-20/D-21 migration 300).
>    What is missing is the **CREATE rail** (the proposal writer on the AI rail — needs D-45, D-46,
>    D-47, D-50) and the **drawer UI** (needs D-48 for visibility and the coverage line; D-49 for the
>    post-final mount). Five lanes, sequenced in the brief's §8; no migration unless D-46 is ruled
>    toward a durable idempotency key.
> 9. **HELD — the offering-key id drop (PR #907)**, below. It is BUILT and green and is blocked on a
>    human running the read-only production preview; it is not waiting on a lane.
>
> **D-42** (persisting the ready-made rail's per-pass tallies on `reconciliation_runs`) is the one
> question this re-sweep opened and is unscheduled. **V-30** implies a follow-on that is NOT on this
> list and should be: once the guard's predicate is repaired, the 30-orphan classification in ledger
> `2026-09-14-test-files-wired-orphans` has to be redone against the true set (233 today), and
> **R-12/R-13** are two members of it.

- ~~**OC-B1 next**~~ ✅ **LANDED 2026-09-12** — `service_bookings.offering_contract_snapshot`, migration 291,
  declared and registered (ledger `2026-09-12-offering-contract-snapshot`). **What still stands from this bullet:**
  **OC-B2 is HELD** until a real seller catalog exists (production carries 6 non-demo listings), and **OC-B3's
  D-14 blocker is CLEARED** (ledger `2026-09-15-d14-quantity-is-units`): `quantity` is UNITS of the listing,
  `party_size` is the party, and `archetypeAsks` names which an archetype collects — which is the required-context
  vocabulary OC-B3 needs. D-16(a) is now CLOSED on the plan side too — D-41 landed `itinerary_items.quantity` (migration 298, NULL = one unit).
  The pre-publish
  `DATABASE_URL=<prod> npx tsx scripts/audit-offering-classification.ts` step remains a PRODUCTION fact and is
  unverifiable from the repo.
- **LANE 2 of `2026-09-12-offering-key-is-canonical` — DROP `provider_services.expert_offering_type_id`.**
  **[PREPARED 2026-09-15 — PR #907 open; HELD on the production preview; `2026-09-15-offering-key-id-drop`]**
  Built and green: migration **296** (DDL only), `scripts/preview-offering-key-id-drop.cjs` (read-only; the
  operator runs `DATABASE_URL=<prod> node scripts/preview-offering-key-id-drop.cjs` per `docs/RELEASE.md`
  step 3 and it exits non-zero on a non-zero blocking count), the `shared/schema.ts` declaration and the
  `lane2-removal-target` arm removed, K7 repaired to an EMPTY reader set. **Not merged, not un-drafted —
  the coordinator lands it once a human has run the query below against production and read the output.**
  Lane 1 landed the backfill (migration 293), repointed every reader to the canonical
  `expert_offering_type_key`, and made the legacy uuid unwritable on every rail. Lane 2 removes the
  column: delete its declaration from `shared/schema.ts` (**that** is what makes the Replit deploy
  push drop it), add the `DROP COLUMN` migration and register it, delete the `lane2-removal-target`
  fallback arm in `server/services/booking-concierge.service.ts` (and `storage.getExpertOfferingTypeKeysByIds`
  with its last caller), and repair K7 to assert the reader set is now EMPTY rather than deleting it.
  **PRECONDITION — run this read-only query against PRODUCTION first and read the output; the drop
  is refused unless it returns 0** (the `preview-category-key-repair.cjs` posture, `docs/RELEASE.md`
  step 3): a drop is unrecoverable, and a stamped migration never re-runs, so a row still answering
  only through the uuid loses its offering permanently — and on the money path that silently stops a
  Booking Concierge line being charged its facilitation fee.
  `SELECT count(*) FROM provider_services WHERE expert_offering_type_id IS NOT NULL AND expert_offering_type_key IS NULL;`
  Migration 293 must have applied on that database first (a non-zero count usually means it has not).
  **Also read, but NOT blocking** — the rows where the two columns already disagree, which the
  backfill deliberately never touched and where the key is canonical by ruling; a human should see
  them before the id is gone:
  `SELECT ps.id, ps.expert_offering_type_key, eot.offering_type_key AS legacy_key FROM provider_services ps JOIN expert_offering_types eot ON eot.id = ps.expert_offering_type_id WHERE ps.expert_offering_type_key IS NOT NULL AND ps.expert_offering_type_key <> eot.offering_type_key;`

- ~~**L25 plan-work rail**~~ ✅ **LANDED 2026-09-15 (ledger `2026-09-15-plan-work-one-rail`).** ~~ruling 11 (ratified 2026-09-08): a planning-tier listing bought at checkout writes the advisor row on authorization, inside the booking's transaction, through the existing single author. One more caller, never a new insert site.~~ `grantPlanWorkAdvisorAccess` runs inside `stampAuthorization`'s own transaction and calls `upsertTripAdvisorRow` with the booking's trip, its `provider_id` and status `accepted`; the class is `impactClassFor`'s, read off the booking's committed `offering_contract_snapshot` rather than the live listing. Ruling 12 needed no code — a consult grants nobody, asserted. A plan-work line with no plan is refused at the checkout CLAIM (409 `plan_work_requires_a_plan`), before any Stripe call. **REMAINS:** `resolveBuyAction` still reads no impact class (`BuyActionRow` carries neither catalog key, and the ruling makes no CTA statement) — its stale "NOT ratified" note is corrected in place.
- ~~**Booking-agent claim lane**~~ ✅ **LANDED 2026-09-15 (ledger `2026-09-15-booking-agent-claim`).** ~~ruling `2026-09-08-assignment-is-claimed`: retire auto-assignment, requests are claimed from the pooled queue, matching only orders the queue, existing assignees keep theirs.~~ Both create rails pin `expertId` to null and `getExpertUserIds` is DELETED (§18c — it had no other caller); ONE claim rail `POST /api/affiliate-booking-requests/:id/claim` (actor from the session, `.strict()` empty body, a single `WHERE expert_id IS NULL` conditional, 409 to the loser, idempotent for the holder); the PATCH rail's body-sourced `expertId`/`"self"` retired in both layers. Queue ORDERING is untouched — the pooled reader's `status, created_at` order is kept as found, and no category/city sort was added, because a sort key nobody ruled is a matching claim. The traveler read `GET /api/affiliate-booking-requests/user` still has zero client callers (LD 44) and was not touched.
- **L16 Ask AI drawer** — **[brief v2 landed 2026-09-15 via `2026-09-15-ask-ai-drawer-brief-v2`; **D-19, D-20 and D-21 are ALL ANSWERED and their server rails LANDED 2026-09-15** — the store (ledger `2026-09-15-d19-plan-proposals`, migration 299: `plan_proposals`, its one service, its read/discard routes) and the CHARGE POINT (ledger `2026-09-15-d20-d21-proposal-charge`, migration 300: flat from the `concierge:ai_task` band, one charge per distinct proposal applied, the §15b claim on the row, the pay and apply rails). **NO RULING BLOCKS THIS LANE ANY LONGER.** What is still unbuilt is the lane itself: the DRAWER UI, the CREATE rail (nothing produces a proposal yet) and the model call behind it]**. The blocker named in this bullet (the conversation trip link, migration 290) HAS landed; the brief (`docs/design/ASK_AI_DRAWER_BRIEF.md`) was re-read against `main` and found three of its own claims false — chief among them that the expert suggestions rail could carry an AI proposal, which it cannot (`trip_suggestions.expert_id` is NOT NULL and the approve path hardcodes `origin:'expert'`). Everything else in the lane is buildable on ratified ground once D-19 answers.
- **The cards that still author their own buy label** — ~~three~~ **TWO, verified 2026-09-15 (ledger `2026-09-15-buy-label-cards`)**. ~~`OfferingCard` (no importers)~~ **DELETED** — zero importers in the root app (both consumers had re-forked their own card), which is the §18c shape: no consumer plus a standing authorship claim is removed, not gated. The live storefront card **[verified 2026-09-15; blocker holds: a seller-away fact on `BuyActionRow` (plus its own `BuyRefusalReason` member — `not_available` would misdescribe it) and a descriptor for the ready-made lane, which gets none]**. The catalog preview **[verified 2026-09-15; blocker holds: `GET /api/provider/services` ships no `buyAction`, and §14 binds the buyer to the session, which there is the OWNER]**. Both carry a grep-able `ld23-buy-action-gap` note naming what is missing; the S3 pin now asserts that set EXACTLY by walking `client/src`, so a third author cannot appear undeclared. Widening the descriptor is ruling 9's author's call.
- Orphan-spec follow-up — **T-1/T-2/T-3 of the triage plan are STRUCK, landed 2026-09-15 (ledger `2026-09-15-orphans-t1-t3-green-directories`): 39 suites wired, inventory 279/512 → **318/512 reachable**, baseline 233 → **194**.** Three jobs in `build.yml` (`unit-suite-server-units`, `suite-server-services`, `suite-server-routes-migrations`) now run `server/utils`, `server/seeds`, `server/services/travelpayouts`, `server/services/trend-engine`, `server/services` and `server/routes` + `server/migrations` `__tests__`; the directory's one red (`server/routes/__tests__/booking-idor-guard`) was repaired in its FIXTURE, not its assertions. ~~**WHAT REMAINS on this row: T-4..T-10 (194 orphans)**~~ **UPDATED 2026-09-15: T-4..T-7 landed** (`2026-09-15-orphans-t4-t7-red-suites`, 318/512 → 328/511, baseline 194 → 183), **and T-8/T-9 have now landed too** (`2026-09-15-orphans-t8-t9-server-tests-class`): **inventory 328/511 → 481/514 reachable, baseline 183 → 33**, and `server/__tests__` goes from **152 recorded orphans to 2**. One job, `suite-server-tests`, runs **233 of that directory's 239 suites**; 13 suites were repaired, grouped by SIX causes rather than by file. **T-9 IS LANDED BUT NOT COMPLETE — two suites stay ORPHANED and each needs a DECISION, not effort:** `server/__tests__/deposit-cancel.db.test.ts` (5 tests, 5 SKIPPED without a real Stripe test key — wiring it would be the green-but-vacuous shape) and `server/__tests__/deliverable-protected-rail.http.test.ts` (needs `OBJECT_STORAGE_DRIVER=memory`, which `server/infrastructure/object-storage.ts` refuses under `NODE_ENV=production`, so a production-bundle job cannot carry it by construction). **Four more suites are deliberately not in that job and are ALREADY WIRED elsewhere:** the `@traveloure.test` purge suite (its subject destroys the shared `ci-*` accounts three suites log in as), the two clean-platform reconciliation suites, and the Stripe-keyed one-click suite. **WHAT REMAINS on this row after T-8/T-9: T-10 (the 30 Playwright specs) plus the 3 non-Playwright rows in the 33-row baseline** — `content-matching` (below), `deposit-cancel` and `deliverable-protected-rail`. The row also carries ONE named item from the T-1..T-3 lane — `server/services/__tests__/content-matching.test.ts` fetches `http://localhost:5000` and is therefore an HTTP suite the triage binned as DB-GREEN; it stays orphaned until an app-backed job carries it, and it is the reason `suite-server-services` names its files instead of globbing the directory. A second finding with the same shape: **five files under these roots are `vitest` suites and `tsx --test` cannot load them at all**, so a "whole-directory glob" is only available where a directory has ONE runner — T-9's shape must account for that. **T-9 CONFIRMED that finding: seven vitest files in `server/__tests__` need their own step, and a whole-directory GLOB is additionally unavailable there because six suites must be excluded and the reachability guard cannot follow a shell substitution.**
- ~~Orphan-spec follow-up (2026-09-14 wave)~~ — ledger `2026-09-14-test-files-wired-orphans` took the guard from 88 orphans to 30 (47 unit suites wired by directory; 11 Playwright specs wired in `unwired-spec-gate.yml`). The 30 left are classified per spec in that row and the PR body: 9 need fixture/seed data a fresh CI DB never creates, 11 hold a stale expectation against a surface that still exists, 5 are rotted legacy `phase-*`/`seam-*` suites, 5 are staging-only (`crossbrowser/smoke`, `tier4/*`). None is dead — every surface still resolves in `App.tsx` — so each is repaired-to-the-invariant or given its fixture, never allowlisted; the count only moves by wiring.

## 5 · Landed in this program (context, not work)

> **THE 233 ORPHANED TEST SUITES ARE NOW CLASSIFIED — `docs/lane-reports/2026-09-15-orphan-triage.md`**
> (ledger `2026-09-15-orphan-triage`, advisory, docs only). `2026-09-15-test-guard-prose-echo`
> published the true orphan list and said re-classifying it was unscheduled; that triage has run.
> **203 of the 233 were each executed** against a Postgres built from empty by
> `migrate-entry.ts` plus `ci-db-setup`'s other two steps, and — for the HTTP suites — the
> production bundle on `:5000`; the 30 Playwright specs were classified statically, because the
> 2026-09-14 lane already ran all 41. **Result: 85 PURE-GREEN · 72 DB-GREEN · 7 HTTP-GREEN ·
> 30 RED-FIXTURE · 7 RED-ASSERTION · 2 DEAD · 30 PLAYWRIGHT.** Four out of five of the node suites
> pass today with no repair at all; the report proposes ten ordered, sized lanes (T-1..T-10) and
> recommends **wiring the green 164 before repairing the red 39**, since the two are independent.
> **Nothing was wired, deleted, skipped, allowlisted or repaired and no assertion was edited**, per
> `2026-09-14-test-files-wired-orphans`' rule that a suite leaves the list by being run or by being
> gone. **No new R-row is filed**, for reasons the report states in full:
>
> - **R-12 / R-13 have their cause.** Both fail in their own fixture helper before any assertion on
>   `provider_services_user_id_users_id_fk` — the V-26 lane's guess, **confirmed** — and the cause
>   under it is a **race**, not a gap: the actor `POST /api/auth/register` creates in the server
>   process is intermittently invisible to the test process's own connection when it inserts the
>   fixture row, and `before()` asserts only the 201. Measured **13/13 and 8/8 when the race does
>   not bite**, and 13/13 with one extra round-trip inserted. Their rows are otherwise unchanged:
>   the recommendation stays "(c) split the pure half, then (a) wire", and they stay ONE lane.
> - **V-29 now has a live reproduction.** `server/__tests__/item-event-link.db.test.ts:222` expects
>   Locked Decision 29's cross-trip `400` and gets **403**, because its trip is born by a direct
>   `db.insert(trips)` with no `trip_collaborators` row — V-29's mechanism exactly. Evidence for the
>   open row, not a new one.
> - ~~**One candidate, named and not filed:**~~ ✅ **SETTLED 2026-09-15 (ledger
>   `2026-09-15-orphans-t4-t7-red-suites`) — THE RULING WAS ALREADY IMPLEMENTED AND NO CODE
>   CHANGED.** The decision-maker ruled that every `/api/admin/*` route answers 403 to an
>   authenticated non-admin and 401 only when unauthenticated. Both guards already do exactly
>   that: `adminApiGuard` (`server/routes.ts`, the §2 blanket `app.use("/api/admin", …)`) 401s
>   only on `!req.isAuthenticated()` and 403s off a DB role lookup, and `isEA`
>   (`server/middleware/ea-rbac.ts`) has the identical shape. All three named rails were probed
>   directly as an authenticated ordinary user and answered **403**, and the suite is
>   **143/143 green on three consecutive runs** plus a fourth against a database built from empty.
>   **THE DIVERGENCE DID NOT REPRODUCE AND NO MECHANISM IS CLAIMED FOR IT (§13):** an 8-round
>   tight register→login→probe loop produced 0 of 48 401s, and no commit between the triage's base
>   and this head touches either guard. All three rails sit at inventory indices 9, 10 and 17 of
>   143, which is consistent with the triage fixture's session not yet being live for its first
>   probes — a shape, not a mechanism, and it is left recorded rather than dressed up as a fix.
>   The suite now runs in CI (`suite-mutation-auth`), so a real recurrence will be visible.
>   ORIGINAL: with `MUTATION_AUTH_AUDIT_OK=1`,
>   `mutation-auth/admin-mutation-auth.test.ts` runs 143 real probes and finds three answering
>   **401 where its contract says 403** for an authenticated ordinary user
>   (`DELETE /api/admin/service-offering-types/:key`, `DELETE /api/admin/slow-queries`,
>   `DELETE /api/ea/executives/:id`). All three still refuse, so it is a refusal-vocabulary
>   divergence on 3 rails of 143 and picking the right side is a ruling, not a measurement.
> - **One near-miss, recorded because avoiding it was the point.**
>   `city-case-match.db.test.ts` fails `27 !== 30` and reads like a case-sensitivity leak in the
>   city feed; querying the canonical and mis-cased URLs directly returns **27 and 27**. The suite
>   is flaky, not the product.
>
> ~~Two suites are **DEAD** and named~~ **CORRECTED 2026-09-15 by ledger
> `2026-09-15-orphans-t4-t7-red-suites`: ONE was dead, not two.** `text-sanitization.test.ts`
> imports four symbols `server/utils/text-sanitizer.ts` does not export, cannot load, and its
> first assertion expects an encoding that module's own canonical-policy header says it
> deliberately does not do — **DELETED**, with every real behaviour already pinned by the wired
> `server/utils/__tests__/text-sanitizer.test.ts` (22 tests). `mutation-auth/non-admin-payments-…`
> is **NOT dead and is NOT deleted**: it names no retired rail anywhere in its source — it drives
> every probe off `generated/security/mutation-auth-manifest.json`, a GENERATED inventory nothing
> in CI checked, which had drifted far enough to carry the four retired `/api/expert/templates*`
> rails and to miss 43 live ones. Regenerating it fixed the suite (220 real probes, green), the
> freshness check is now a CI step, and the file survives as the only UNAUTHENTICATED audit of the
> non-admin payment/user-data surface. ORIGINAL: two suites are DEAD and named
> (`text-sanitization.test.ts` imports a removed export;
> `mutation-auth/non-admin-payments-…` asserts a rail retired by
> `2026-09-03-expert-templates-consumer-sunset`). **Three are GREEN-BUT-VACUOUS and must not be
> wired as-is** — `deposit-cancel.db` (5 of 5 skipped, needs a real Stripe key),
> `admin-mutation-auth` (124 skipped without its flag) and `admin-query-role-changes` (skips itself
> without `DATABASE_URL`). A job that runs a suite it cannot execute satisfies the inventory and
> protects nothing, which is the shape of V-30 itself.


Twelve lanes plus the decisions record: the console grammar, home honesty, my-plans rows, trip-card
honesty, the start-with-AI door, the concierge door, the Trip Card one page, Home's time axis, the
client pen scope, the expert-request review sheet, the impact-class lookup, the buy-action resolver,
the my-events fold, inbox context, the first Home browser gate, Discover in the shell, doors passing
the trip id, the extraction date anchor, bookings by plan, the orphan sweep, the all-advisors reader,
the concierge ownership check, the shadowed-twin sweep, the agent status vocabulary, the conversation
trip link, and **the fee correction on both money rails** (`2026-09-08-cart-fee-line`,
`2026-09-08-legacy-rail-fee`) — travelers are no longer billed the commission that is also withheld
from the provider's payout.
