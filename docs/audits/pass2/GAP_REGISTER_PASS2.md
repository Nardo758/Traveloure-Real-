# Gap register: Supply → Demand content journey — Pass 2

**Base SHA:** `280931ddd88b75f01969608a6f73b83f4d176e40` (`git merge-base HEAD origin/main`).
**Build SHA:** `3304c1dc03df95b575b1291c84920d19194dfea3` (`dist/build-info.json`, built 2026-09-25T17:36:44.000Z, bundle `DGjrKh7Q`).
**Final run id:** `nu4fb2` — 12 passed, 0 failed, 4 skipped (`HELD:stripe`); teardown whole-database row-count diff is empty (`test-results/teardown-rowcount-diff.txt`).
**Environment:** local disposable PostgreSQL (`postgresql://postgres:***@localhost:5432/traveloure`, `inet_server_addr()=127.0.0.1`), Stripe secret key is a 29-char CI stub (`sk_test_ci_stub…` → `GET /v1/account` returns 401), no Stripe publishable key set, AI (`x.ai`/Grok) key is a stub, Unsplash key is absent. Full harness rules: `e2e/supply-demand/` (`BRIEF.md` R-1…R-4), `.github/workflows/supply-demand-e2e.yml` (non-blocking, path-filtered, nightly `17 3 * * *`).

**A note on ids.** Pass 2 ran the same build (`3304c1d`) across several harness runs while the specs and DB helpers were still being hardened (probe runs, run `rbvvcu`, then the final `nu4fb2`). The finding id `P2-<journey>-<n>` was **reused for different content between drafts** as rows were renumbered during curation — e.g. `P2-S1-1` named a `return_to` hijack in an early raw run and a different (verification-gate) observation in the curated 38-row set the lead reviewed (`$P2/findings.final.jsonl`, run `rbvvcu`). This register resolves the collision by carrying the curated `rbvvcu` ids as filed, adding two lead-discussed raw-run findings under new slugged ids (`P2-S1-RTN`, `P2-S1-ADMINCARD`) rather than reusing `P2-S1-1`/`P2-S1-2` a third time, and layering the final `nu4fb2` run's own findings (`test-results/supply-demand-findings.jsonl`, ids ending differently, e.g. `P2-S1-8`, `P2-D3-6`, `P2-D6b-15/17`) on top as the current-state confirmation. `$P2/LEAD_VERDICTS.md` is authoritative over all of it.

**Severity**
- **P1:** the user loses work, a canonical Trip/booking record is never created, or money is at risk.
- **P2:** wrong or stale display.
- **P3:** cosmetic, redundant, or a documented environment/fixture limit.

**Evidence**
- **Behavioural:** a journey step with its screenshot, network log and/or DB diff, run against the local server. Cited with a screenshot path or DB query result.
- **Static:** a `file:line` reading only, not exercised this run.

**Gap classes** (per `$P2/BRIEF.md`): `INVISIBLE_RESULT`, `DEAD_TRIGGER`, `SPEC_DIVERGENCE`, `FALSE_PROMISE`, `SILENT_SUCCESS`, `STALE_CACHE`, `ORPHAN_READ`, `WRONG_TARGET`, `LOST_STATE`.

---

## A. Root causes

| # | Root cause | Symptoms | Where |
|---|---|---|---|
| **RC2-A** | **Identity/business verification is Stripe-only with no admin override.** `ServiceForm.tsx`'s `verificationGateBlocked` disables Publish until both `service_provider_forms.identity_verification_status` and `business_verification_status` read `"verified"`, and those columns flip only via Stripe Identity/Connect webhooks. With the CI Stripe stub, no listing (provider or expert) can reach "live" without the harness seeding the columns directly (R-1 exception, one class of write). | `client/src/components/ServiceForm.tsx:2082-2085`; `server/utils/earner-verification.ts:37`; expert side `local_expert_forms.identity_verification_status` |
| **RC2-B** | **Born-`submitted` admits an admin race against an in-progress draft.** Migration 111 makes `provider_services.approval_status` default `"submitted"` the moment a provider Saves Draft — before Publish, before the category-specific required fields are filled — so an admin can approve a row mid-wizard and it goes live carrying the pre-wizard snapshot (`location='Unknown'`, missing category fields). One write (`createMutation`'s PATCH/POST) is read by the admin queue as "ready for review" with no "still editing" signal. | `shared/schema.ts` (`provider_services.approval_status` DEFAULT `'submitted'`, migration 111); `client/src/pages/admin/service-approvals.tsx`; `client/src/components/ServiceForm.tsx` (Save Draft and "submit for review" are the same click) |
| **RC2-C** | **A plan item never inherits its listing's coordinates (COORDS).** All ten Kyoto fixtures have `latitude`/`longitude`; every plan item added from them has NULL, disabling the slip map ("No stops are located yet"). Reproduced on every content-filled plan this pass. | server item-add write path (file:line not isolated this pass — filed as `P2-D1-COORDS`); FOLLOWUP: fix must resolve the listing's location at READ time, never copy coordinates onto the item |
| **RC2-D** | **No neighbourhood ⇒ `location='Unknown'`, invisible everywhere, and the wizard never says so.** A listing that never opens the neighbourhood picker publishes with `location="Unknown"`, `city=NULL`, which fails every location-scoped surface (city page, location browse, paid optimizer) with no warning at Save/Submit time. | `client/src/components/ServiceForm.tsx:1440` (location composer); `server/services/location-view.service.ts:80-85` (`cityScopePredicate`); `server/storage.ts:3934` (`listingLocationMatches`); `shared/schema.ts:1170` (`location` DEFAULT `'Unknown'`) |
| **RC2-E** | **Cross-surface visibility after publish is inconsistent within a single 10s poll, and the pattern does not fit "pagination alone."** The lead withdrew an earlier finding (`P2-S1-7`) as `/services` pagination (a listing sitting on page 2 of 20), but the final `nu4fb2` run reproduces the same symptom on `/discover/location/kyoto`, a surface the Phase 0 contract states has **no limit** (§3(a)) — so pagination cannot be the whole explanation there. Filed fresh as `P2-S1-8`, not silently folded into the withdrawn row. | see §3(a) of `PHASE0_SUPPLY_DEMAND.md` for the governing filter per surface; `test-results/visibility.jsonl` (nu4fb2 rows) |
| **RC2-F** | **Two commission resolvers disagree for an expert- or provider-owned listing charged through the quote path, and one fails silently to `platform_fee=0`.** Static, P1-candidate money-path finding — see `P2-RES-1` below. | `server/services/fee-resolution.service.ts:178`; `server/services/direct-charge-rate.service.ts:92`; `server/services/commission.ts:575,668-696`; `server/services/service-quotes.service.ts:747-780`; `server/services/quote-charge.service.ts:264-340` |

---

## B. Open gaps (behavioural unless marked static)

| id | class | sev | KNOWN | title | expected | actual | where | evidence |
|---|---|---|---|---|---|---|---|---|
| `P2-S1-RTN` | WRONG_TARGET | P2 | — | A stale `sessionStorage.traveloure_return_to` hijacks the next full navigation after signup | After signup, an explicit `page.goto` to a chosen URL (e.g. `/become-provider`) lands there | `Signup.tsx` routes to `/dashboard` before the auth query re-resolves; `ProtectedRoute`'s guard fires once, stores `traveloure_return_to="/dashboard"`, bounces to `/`; `AuthReturnToRestorer` replays that value on the session's *next* full navigation regardless of where it was headed. Reproduced deterministically outside Playwright and again in the final run. | `client/src/App.tsx` (`ProtectedRoute` effect + `AuthReturnToRestorer`, ~218-233, 307-323) | shot `shots/S1-providerA-01-post-signup.png` |
| `P2-S1-3` | FALSE_PROMISE | P2 | — | A listing is born `approval_status='submitted'`, so admin approval can race an in-progress wizard | F2 (migration 111) intends "born submitted" to mean a legitimate review candidate, not that approval is safe to race against an unfinished edit | Save-Draft's row is immediately `submitted`/`draft`, eligible for `/admin/service-approvals` before cover photo, availability, neighbourhood or category fields are set. Behavioural proof from an earlier probe: Provider C's row was admin-approved and went `active` while `button-publish-service` had never become enabled (blocked on unfilled category fields) — it went live carrying `location='Unknown'`, `neighborhood=NULL`, the pre-wizard snapshot. Same root as walkthrough F-3. | see RC2-B | build `3304c1d` |
| `P2-S1-8` | INVISIBLE_RESULT | **NOT PROVEN** | — | Some approved Kyoto listings read hidden on location surfaces within a 10 s poll (final run) | Visible once approved + neighbourhood + handle | Run `nu4fb2` rows contradict each other per item: `Arashiyama…` reads hidden then visible on `/discover/location/kyoto`; `Traditional Tea…` reads visible at 20 702 ms, and its later hidden row follows D7 PAUSING that listing (correct); `/services` shows 20 of 30 by rank. The lead's direct API check in run `lead3` returned A and B on `/api/discover?location=Kyoto` and `/api/discover/location/kyoto`. Not a defect until a poll longer than 10 s, taken before D7 runs, still shows a hidden approved listing. | `location-view.service.ts:80-85` | `test-results/visibility.jsonl` | behavioural, inconclusive |
| `P2-S2-1` | SPEC_DIVERGENCE (re-graded from FALSE_PROMISE per lead) | P3 | — | Admin's approval queue says "approved" for a row not yet live to travelers | The expert listing-home tells the expert the offering is not-live/pending verification — it does, confirmed | `provider_services` row: `approval_status=approved`, `status=draft` (held by the identity gate, RC2-A). Expert listing-home text does carry not-live/pending-verification wording; the gap that remains is the admin queue's own copy, which does not say the same thing. | `client/src/pages/admin/service-approvals.tsx` | shot `shots/S2-expertE-06c-…png` (screenshot 06b in the lead's notes) |
| `P2-S3-1` | INVISIBLE_RESULT | P2 | — | An approved listing with no neighbourhood picked stores `location='Unknown'`, invisible on every location-scoped surface, with no wizard warning | Either a neighbourhood is required before publish, or an unset location is surfaced to the seller as a visibility-affecting gap | `provider_services.location="Unknown"`, `city=NULL` for a listing that walked Logistics without ever calling `option-neighborhood-<slug>` — nothing flagged it before submit | see RC2-D | shot `shots/S3-02-provider-pending-view.png` |
| `P2-D1-COORDS` (merges `P2-D1-2`+`P2-D1-3`) | INVISIBLE_RESULT | P2 | — | A plan item added from a located listing carries no lat/lng | A located listing's item is expected to carry coordinates so the slip map can render it | Both "Traditional Tea Ceremony" and "Arashiyama Bamboo Grove…" items carry no lat/lng after add-to-plan, disabling the slip map. Reproduced again on the final run (nu4fb2 evidence tagged the same way). | server item-add write path (COORDS) | build `3304c1d`; FOLLOWUP filed below |
| `P2-D5-2` | LOST_STATE | P2 | `KNOWN:RC-8` | The item a guest tried to add did not survive into the account created moments later | RC-8: guest state is expected to be dropped on auth | `itinerary_items` match=0, `cart_items` match=0 after signup | `client/src/pages/Signup.tsx`; `client/src/components/SignInModal.tsx` | shot `shots/D5-05-post-signup-service-detail.png` |
| `P2-D7-4` | ORPHAN_READ / STALE_CACHE (re-graded from P1 to P2 per lead) | P2 | — | The slip still lists a provider's item with no unavailable notice after that provider's listing is paused | The item is either removed with a reason, or shown with an honest "no longer available" flag — never silently still bookable | Provider A's listing `status='paused'`; slip text includes the item title, includes no unavailable-style word. A traveler learns nothing until checkout. | `client/src/components/plancard/SlipView.tsx` | shot `shots/D7-05-slip-after-changes.png` |
| `P2-D6b-JOIN` (evidence: `P2-D6b-1`/`-15`, `P2-D6b-3`/`-17`) | — | **PASS** | — | T-auth's `/my-bookings` renders the confirmed booking's join link | `join-link-<bookingId>`/`button-join-session-<bookingId>` render as an `<a>` with `href="https://meet.example.test/e2e-<run>"` and `target="_blank"` | Both `rbvvcu` and `nu4fb2` runs' *automated* attribute read reported `href=null`/`anchorVisible=false` when read immediately after the toggle click — a read-before-render race, not a product defect. The lead's manual re-check on `rbvvcu` (`JOIN_LINK.png`) shows the anchor rendering correctly with the exact href. `provider_services.join_link` persists byte-for-byte on both runs (`P2-D6b-1`/`-15`). Recorded PASS; see Withdrawn table. | `client/src/pages/my-bookings.tsx` (~857-870) | `docs/audits/pass2/img/JOIN_LINK.jpg`; `docs/audits/pass2/img/D6b-05-tauth-my-bookings-confirmed.jpg` |
| `P2-D3-RC11` (evidence: `P2-D3-6`) | — | **PASS** | — | First message to an expert from a freshly opened conversation succeeds (RC-11 regression check) | `POST /api/conversations/start` 201 → `POST /api/chats` 201, `user_and_expert_chats` row written | Final run (`nu4fb2`): `conversations/start` 201, `POST /api/chats` statuses `[201]`, `user_and_expert_chats` row found. The one 404 anywhere in the pass's net logs is from a stale probe run (`4z373j`) against a stale bundle, not `3304c1d`. | `server/services/messages.service.ts` | `net/D3-nu4fb2.jsonl`; shot `shots/D3-03-after-send.png` (superseded, see Withdrawn) |

**P2-STORE-1** (P2, DEAD_TRIGGER/SPEC_DIVERGENCE, static) — added by decision-maker request: **the expert storefront has no booking action.** Each offering card is a plain link to `/services/:id` (`client/src/pages/storefront.tsx:1081`) with a generic CTA string (:1069-1071); page buttons are Message (:867, :1170), Share (:880) and owner Edit (:858). A traveler cannot "book a consulting session" from the storefront — every booking starts on the service page, whose button is server-resolved (`shared/buy-action.ts:253-426`). Proposed design (documented, not built): the storefront listing payload carries each listing's server-resolved `buyAction` (never re-derived on the card — §18 rule 1); new server work needed is (a) `buyAction` on the storefront listing query, (b) a public per-listing next-availability read (today's only one, `/api/me/next-availability`, is owner-scoped, `expert-console.routes.ts:408`). Full design detail: `$P2/LEAD_VERDICTS.md` "Added by decision-maker request" section. **This gap is currently being built on branch `claude/storefront-booking-actions`.**

**P2-RES-1** (P1 candidate, money path, **static — behavioural leg HELD**) — two rate resolvers disagree for an expert- or provider-owned listing priced through the quote path, and one fails silently: the authoritative `resolveProviderRate` (category band) is never reached for these owners; `resolveServiceOwnerShareRate` returns `null` when it is handed the **raw** category slug (e.g. `events-celebrations`) instead of the normalized fee category the cart checkout computes, so the quote-born booking is stamped with **no rate at all**. It is born with `platform_fee=0`, charged with that `0`, and minted at completion with `0` — the platform takes nothing and the earnings column is NULL. Full resolver-by-resolver table and the `expert_new`/`beta_flat` doc-drift note: `$P2/D5_RESOLVER.md`. **Behavioural leg (accept a quote on an expert listing and a provider listing, read `service_bookings.platform_fee`/`provider_earnings`) was not run this pass** — `P2-D6-1` records that no `custom_quote`-priced listing existed among this run's fixtures; it needs no Stripe, only a fixture change, so it is listed as HELD-by-fixture-gap rather than HELD:stripe.

---

## C. Seeded steps (R-1)

Every DB write the harness made outside test-account creation is itself a finding under R-1 ("no UI path exists for this write, or Stripe blocks the UI path"). None of these represents a product code change.

| step | write | why seeded | HELD reason |
|---|---|---|---|
| Provider identity/business verification | `UPDATE service_provider_forms SET identity_verification_status='verified', business_verification_status='verified' WHERE user_id=<e2e account>` | No admin control sets either field — only Stripe Identity/Connect webhooks do | `HELD:stripe` (RC2-A) |
| Expert identity verification | `UPDATE local_expert_forms SET identity_verification_status='verified', identity_verified_at=NOW() WHERE user_id=<e2e account>` | Same class as above, expert side; reproduces `activateVerificationHeldListings`'s production auto-activation sweep verbatim | `HELD:stripe` |
| Meeting pin (lat/lng/meeting point) | `UPDATE provider_services SET latitude=35.0116, longitude=135.7681, meeting_point=<text> WHERE id=<row>` | Map click-to-place is not headless-reliable in Playwright | Not Stripe-blocked — a harness reliability fallback, applied once per account and shared across specs (`lib/dedupe.ts`) |
| Confirmed booking (D6b join-link fixture) | `INSERT INTO service_bookings (…, status='confirmed', stripe_payment_intent_id=NULL, platform_fee/provider_earnings computed from the live `fee_bands` "moderate" row at test time, rate=0.08)` | A real checkout needs a live Stripe PaymentIntent | `HELD:stripe`. Per §19b, this exact row would be flagged `payment_provenance_unverified` by the production drift job on its next pass — the honest state for a harness-seeded row, never a fabricated PaymentIntent id (§19a). |

---

## D. Withdrawn after verification

| id | original class/sev | reason withdrawn |
|---|---|---|
| `P2-D3-1`/`P2-D3-RC11` region | DEAD_TRIGGER, P1 | **RC-11 RESOLVED on the rebuilt bundle.** The only 404 anywhere in the net logs is from run `4z373j` against a stale bundle. Every rebuilt run (`rbvvcu`, `nu4fb2`) shows `POST /api/conversations/start` 201 then `POST /api/chats` 201. Root cause of the original false finding was the HARNESS: the net log is appended across runs and the spec scanned all of it; it also checked a `chat_messages` table instead of the actual chat store (`userAndExpertChats`). Recorded as a PASS above. |
| `P2-S1-7` | INVISIBLE_RESULT, P2 | The API returns both listings for `location=Kyoto` and on the city page; `/services` shows 20 of 30 results by rank (pagination), so a test listing can sit on page 2. Not a filter defect for the rows this originally covered. **Caveat:** the final run reproduces a similar-looking symptom on a different surface set — filed fresh as `P2-S1-8` rather than folded into this withdrawal, because `/discover/location/kyoto` has no page limit per Phase 0 §3(a). |
| `P2-D6b-3` | INVISIBLE_RESULT, P1 | Lead re-check as T-auth: `GET /api/service-bookings` returns `service.joinLink=https://meet.example.test/e2e-rbvvcu` for the confirmed booking; `button-join-session-<id>` is an `<a>` with that exact href and `target=_blank` (`JOIN_LINK.png`). The row's `href=null` came from reading the attribute before render. Record D6b as PASS. |
| `P2-S1-2`/`P2-S1-ADMINCARD` (raw-run "admin applications card not found") | DEAD_TRIGGER, P1 | Same class the supply agent already proved was a fetch race, fixed by polling — not a product defect. |

---

## E. Superseded

| id | ruling superseded | superseded by |
|---|---|---|
| D-2 | `2026-09-24-ai-first-draft-waiver` | `2026-09-25-planning-tolls` (#1093). No divergence row filed; today's behaviour was tested against the replacement ruling directly (free draft on an empty plan writes no fee row — confirmed, `P2-D4-1`; paid AI task charged on apply from `concierge:ai_task`; every toll = one `ai_concierge_fee` row + one `fee_waiver` with `metadata.covered_by` when covered). |

---

## F. Not proven

See `docs/audits/pass2/NOT_PROVEN.md` for the full list (Stripe-held journeys, AI/Unsplash stubs, static-only claims, and the harness's own stated limits). Two items surfaced by the lead's review that specifically belong here rather than in the open-gaps table:

- **`P2-D3-4`** (trip_suggestions not created, filed SILENT_SUCCESS/P1): **now PROVEN PASSING in run `nu4fb2`.** The harness gap was the missing accept step; with it, the round trip completed and the DB shows it: `user_and_expert_chats` row → `trip_expert_advisors` `pending` → expert accepts (`accepted`) → `trip_suggestions` row created → traveler approves on the slip (`status='approved'`). Frames `D3-06c`, `D3-08`, `D3-10`, `D3-11`.
- **`P2-S2-3`** (ready-made not on `/admin/template-approvals`): **now PROVEN PASSING in run `nu4fb2`.** The row had stayed `draft` because a page reload reset the workspace to its default tab and hid the Submit control (harness). With the fix it was submitted and admin-approved (`ready_made_trips.status='approved'`) and D2 opened it on `/ready-made` and `/ready-made/:id`. The PURCHASE/clone itself is HELD:stripe, so "items resolve to A, B, C after cloning" stays NOT PROVEN; the build-time check that the ready-made's items reference live `provider_services` ids passed.

## G. Notes carried forward, not re-derived

- **Trip Card expert-note fallback (P3, static):** `server/services/trip-plan.service.ts:802` falls back `expertNote: item.expertNote || item.notes`; `itinerary_items` has no `notes` column today, so it is dead, but it would attribute a traveler note to the expert if one were added (LD 42 D4).
- **Stripe environment proof:** the Stripe connector shows account `Traveloure` (`acct_1OL2SOJZ5fFY5Q8L`) in TEST mode with Connect in use (Express + Standard connected accounts exist) — Connect-test-mode is PROVEN at the account level; the app's own keys were a CI stub for this pass, so all Stripe journeys stayed HELD regardless. See FOLLOWUPS (a).
