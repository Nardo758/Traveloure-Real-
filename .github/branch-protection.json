{
  "_comment": "Machine-readable declaration of required branch-protection status checks for `main`. Applied automatically by enforce-branch-protection.yml on every push to main. To add a new required check: (1) add it to CI_GATES.md Tier 1 table, (2) add the exact status context string here, (3) add the required-check comment block to the workflow YAML.",
  "branch": "main",
  "required_status_checks": {
    "strict": false,
    "contexts": [
      "build (vite + esbuild bundle)",
      "test-file-reachability (ratchet - new orphans fail)",
      "money-endpoint-guard (CLAUDE.md §14)",
      "claims-only-lookup-guard (session-user extraction)",
      "navbar-links-smoke",
      "hardcoded-links-check",
      "app-routes-smoke (Playwright DOM gate)",
      "auth-routes-smoke (Playwright DOM gate)",
      "verify-selection-controls (logic gate)",
      "verify-service-offering-types (HTTP count gate)",
      "earn-page-smoke (Playwright DOM gate)",
      "ci-db-setup-lint (no inline migration steps)",
      "relevance-dominance (pure unit)",
      "suite-mutation-auth (server/__tests__/mutation-auth — live authorization probes, DB + app)"
    ]
  }
}
